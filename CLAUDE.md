# yamaguchi-yusho-data

UDC2026（アーバンデータチャレンジ2026）応募作品①のリポジトリ。
中国運輸局が PDF で公表している「自家用有償旅客運送者の登録簿」を機械可読化し、
山口県の市町別に福祉輸送の供給量を可視化する。

## 計画の所在（goal-to-done スキル使用時の plan location 指定）

**このリポジトリでは `SPEC.md` が実行契約であり、DAG の代わりである。**
新しい run record ファイルを作らない。新しい契約や DAG を自分で組み立てない。

- ゴール・終了条件 → `SPEC.md` §0 と §5
- スコープと除外 → `SPEC.md` §1
- 置き場所・環境の制約 → `SPEC.md` §1.5
- 入力と利用規約 → `SPEC.md` §2
- 出力スキーマ・制度上の定義 → `SPEC.md` §3
- 実行手順 → `SPEC.md` §4
- 完了条件・検証 → `SPEC.md` §5
- 未確認事項 → `SPEC.md` §9
- 進捗の記録先 → `PROGRESS.md`

全体計画（複数作品にまたがる DAG、ゲート、提出戦略）はこのリポジトリの外にあり、
チャット側が保持している。ここでその一部を再構築しない。

## スコープは SPEC.md が限定している

goal-to-done は「最初に動くものができても止まるな」と指示するが、
**この指示より `SPEC.md` §1 のスコープ限定が優先する。**

具体的には、まず `000271730.pdf`（山口県・福祉有償運送・NPO等）1本を
`SPEC.md` §5 の完了条件 1〜8 まで通しきる。
そこで止まって報告する。残り3ファイルおよび他県への展開は、指示があるまで着手しない。

## 絶対に守ること

- **代表者の氏名は抽出も出力もしない。** パース段階で捨てる。
  `tests/test_verify.py` に、出力ファイルに氏名列が存在しないことを確認するテストを置く。
- **「動いた」で完了にしない。** `SPEC.md` §5 の完了条件を満たし、
  実行結果を `verification.md` に書くまでは未完了。
- **推定で先に進まない。** `SPEC.md` に「未確認」と書かれた箇所（§9 の U1〜U6）は、
  原本と照合して確定させてから次に進む。確定できないなら止まって報告する。
- **`SPEC.md` を書き換えない。** 誤りや原本との食い違いを見つけたら、
  `PROGRESS.md` に「何が食い違ったか」「原本ではどうだったか」を書いて止まる。
  修正はチャット側で行う。
- **push と GitHub Pages 有効化は承認を待つ。** 指示があるまで実行しない。

## ファイルの書き手

このリポジトリは書き手を分けている。

| ファイル | 書き手 |
|---|---|
| `CLAUDE.md` `SPEC.md` `.claude/settings.json` `.claude/hooks/*` `tools/spec_coverage.py` | チャット側のみ |
| `PROGRESS.md` `verification.md` `src/` `data/` `raw/` `tests/` `evidence/` | Claude Code |
| `run_record.md`（リポジトリ外） | チャット側のみ。**読まない** |

**これは実行時に強制されている。**`.claude/settings.json` の `permissions.deny` と
`.claude/hooks/guard_protected_files.py`（PreToolUse フック）が、
チャット側所有のファイルへの書き込みをブロックする。
ブロックされたときは `[guard] blocked write` を含むメッセージが返る。

**回避しようとしないこと。**Bash 経由のファイル操作はガードの対象外だが、
`copy` や `echo >` で保護ファイルを書き換えてはならない。
直したい点があれば `PROGRESS.md` に「どのファイルの何行目を、なぜ、どう直すべきか」を書いて止まる。

## 証拠の残し方

**Web 取得・コマンド実行・サブエージェントの調査結果を、事実として使うときは、
要約ではなく生の出力を `evidence/` に保存する。**

```
evidence/YYYYMMDD_<短い名前>.txt
```

先頭3行に必ずこれを書く。

```
取得日時: 2026-08-07 14:30 JST
取得元  : https://example.go.jp/foo.pdf   （またはコマンド全文）
取得方法: curl / WebFetch / pdfplumber / サブエージェント名
```

そのうえで、**事実を主張する文には保存したファイル名を添える。**

- ファイルが無い事柄は主張しない。「〜のはずです」と言い換えるのではなく、**未確認と書く**か、確認してから書く
- **サブエージェントの報告をそのまま使う場合も同じ。**生の出力を `evidence/` に落とし、ファイル名を添える
- **自分が確認していないことを「確認しました」の体で書かない**

これは書き方の心得ではなく、ファイルの有無で検査できる形にするための運用である。

## 調査で守ること

- **`WebFetch` の要約を一次情報として使わない。**生バイトを取得して自前でデコードし、照合する。
  過去に一覧ページの要約が、存在しない表の内容を返し、山口県の行として別県のファイル名を提示した実績がある
- **原因の断定を急がない。**観測したのが「A が動かなかった」だけなら、書いてよいのは「A が動かなかった」まで。
  「A は壊れている」「A はこの用途に向かない」は別の主張である
- **探索が目的から離れたら3手で打ち切る。**離れた時点で `PROGRESS.md` に書いて止まる
- **定義で決まることをデータから推測しない。**行政文書には通達・記載要領が付く。
  法令と様式に無ければ、次は通達を見る

## 作業ログ

`PROGRESS.md` に追記する。1回の作業の終わりに以下を書く。

- やったこと
- 確認できた事実（数字を含める。根拠は `evidence/` のファイル名で示す）
- 詰まったこと、`SPEC.md` と食い違ったこと
- 次にやること

このファイルはチャット側に貼って共有されるので、
「後で自分が読めば分かる」ではなく「第三者が読んで分かる」書き方にする。

## 技術メモ

- PDF は `pdfplumber` で読む。`pypdf` / `PyPDF2` は表構造を壊すので使わない。
- **pandas は使わない。** 標準ライブラリの `csv` と `json` で書く。依存は `pdfplumber` のみ。
- 一覧ページ `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html` は Shift-JIS（cp932）。
- 対象 PDF はテキスト埋め込み済み。OCR は不要。
- 空欄セルはテキスト抽出で消えるため、`extract_text()` では列の対応が取れない。
  `extract_words()` で座標を取り、ラベル語の位置を基準に読む。
- **車両欄の列境界はページごとに異なる。**縦罫線から毎回導出する（`SPEC.md` §4.1）。
  テンプレートを1つ作って使い回さない。
- **括弧付きの数字は軽自動車の内数。**通達で確定済み（`SPEC.md` §3.4(a)）。
  したがって `count_kei ≤ count` は必ず成立する。破れたら抽出誤り。
- **素の `python` コマンドを使わない。**0バイトの Microsoft Store エイリアスに解決され、
  非対話シェルから呼ぶとハングする。`C:\Users\user\dev\udc2026\.venv\Scripts\python.exe` の
  フルパスか `uv` を使う。
- git の commit email は `--local` で noreply アドレスを設定する。`--global` は触らない。

## 公開

`docs/` フォルダが GitHub Pages の公開ルート。ビルドは挟まない。
出典表記とライセンスは `SPEC.md` §8 に従う。
