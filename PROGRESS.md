# PROGRESS

## 2026-08-07 リポジトリセットアップ

### やったこと

1. `C:\Users\user\dev\udc2026\yamaguchi-yusho-data` が OneDrive / iCloud Drive の同期対象外であることを確認
2. `C:\Users\user\dev\udc2026\.venv` を作成（Python 環境まわりで詰まった。詳細は下記）
3. `.gitignore` を作成（`raw/` は除外していない。git init より前に作成）
4. `git init`（デフォルトブランチ `main`）
5. `src/` `raw/` `data/` `tests/` `docs/` を作成し各々に `.gitkeep` を配置
6. `requirements.txt` に `pdfplumber` の1行のみ記載し、`.venv` にインストール
7. `README.md` の骨子を作成（出典・ライセンス（準備中）・UDC2026応募作品である旨）
8. `docs/index.html` にタイトルのみのプレースホルダを配置
9. `git config --local user.email` を `183986175+yyy-yuichi@users.noreply.github.com` に設定（`--global` は未変更、`yuichi@example.com` のまま）
10. `git status` を確認後、`git add -A` → コミット（`.claude/` はユーザー確認のうえ含める判断）
11. `gh repo create yamaguchi-yusho-data --public` でリポジトリ作成
12. `git log --format='%ae'` で全コミットのメールアドレスが noreply アドレスのみであることを確認
13. `git push -u origin main` → GitHub Pages を `main` ブランチ `/docs` で有効化

### 確認できた事実（数字を含める）

- `.venv` の Python: **3.13.12**（後述の経緯により `uv` 管理下のビルドを使用）
- `.venv` へのインストール: `pdfplumber==0.11.10` を含む8パッケージ。`import pdfplumber` の動作確認済み（バージョン `0.11.10` を表示）。pandas は未インストール
- 初回コミット: `9974a3f`、14ファイル、559行追加
- リモートリポジトリ: `https://github.com/yyy-yuichi/yamaguchi-yusho-data`（public）
- コミット者メールアドレス: 全1コミットとも `183986175+yyy-yuichi@users.noreply.github.com` のみ（実メールアドレスの混入なし）
- GitHub Pages: `main` ブランチ `/docs` で有効化。`status: "built"` を確認
- **公開URL: <https://yyy-yuichi.github.io/yamaguchi-yusho-data/>**（ブラウザで表示確認済み。タイトル・本文とも想定どおり）

### 詰まったこと、SPEC.md と食い違ったこと

- **SPEC.md との食い違いではないが、環境起因で大きく詰まった点として記録する。**
  `python` / `python3` コマンドが `%LOCALAPPDATA%\Microsoft\WindowsApps\` の
  App Execution Alias スタブ（0バイト）にしか解決せず、`python -m venv` や
  `python --version` がハングした。調査の結果、実体は
  `C:\Program Files\WindowsApps\PythonSoftwareFoundation.Python.3.13_...\python.exe`
  （Microsoft Store 版、171,744バイト）だが、保護フォルダのため直接実行不可（Access Denied）。
  通常のインストーラー版 Python・Anaconda・pyenv 等はこのマシンに存在しなかった
  （レジストリ・優先パス・`C:\` 全体の再帰検索のいずれでも未検出）。
  → アプリ実行エイリアスの修復を試みる前に、既に `C:\Users\user\.local\bin\uv.exe`
  （`uv` 0.10.0）が導入済みであることが判明したため、方針を切替。
  `uv python install 3.13` で Python 3.13.12 を導入し、
  `uv venv .venv --python 3.13` で仮想環境を作成した。
  **今後このマシンで Python が絡む作業をする際は、素の `python` コマンドではなく
  `uv` 経由（または `.venv\Scripts\python.exe` のフルパス）を使うこと。**
  なお `uv python list`（引数なし）はシステムの `python.exe` を検出しようとして
  同様にハングする。`uv python list --managed-python --only-installed` を使えば回避できる。
- 上記以外、`SPEC.md` の内容そのものとの食い違いは無し。

### 次にやること

- `SPEC.md` §2「最初にやること」— 中国運輸局サイトの利用規約確認（二次利用条件の控え）
- 一覧ページ（`https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`、cp932）を取得し、
  山口県の行と列の対応が `SPEC.md` の表と一致するか確認
- 上記2点が終わるまで、`parse.py` の実装には着手しない（CLAUDE.md の指示どおり）

---

## 2026-08-07 一覧ページ照合 と 000271730.pdf の原本検証

`parse.py` には着手していない。原本の読み取り検証のみ。

### やったこと

1. 一覧ページを `curl` で生バイト取得し `iconv -f CP932 -t UTF-8` でデコードして表を全件読んだ
2. `000271730.pdf` を `curl` で `raw/` に取得（生バイト）
3. `pdfplumber` の `extract_words()` と `page.edges`（罫線）で、
   全4ページの車両欄を座標付きで読み、列見出し・数字の列対応を確定させた
4. `SPEC.md` §3.4 の記載と1団体目を項目ごとに突き合わせた
5. 括弧付き数字が軽自動車の内数か外数かを判定するため、
   「内数なら『括弧 ≦ 素』が常に成立し、『括弧のみ・素なし』は起こり得ない」
   という反証テストを山口県4ファイルに対して実施した
6. 様式の定義側から確定させるため、指示された関東運輸局の XLS を取得して中身を確認した

### 確認できた事実（数字を含める）

#### 一覧ページの照合

- ページは `<meta charset="Shift_JIS">`。HTTP 200、7,716バイト。「2026年8月7日 更新」
- 行ラベルは上から **広島県 → 鳥取県 → 島根県 → 山口県** の4行。岡山県の行は無い
- ヘッダ行は「（空セル）／交通空白地有償運送（colspan=2）／福祉有償運送（colspan=2）」。
  **ヘッダ行に実施主体の列ラベルは無く**、実施主体は各セルのリンクテキスト内
  （「（自治体）」「（ＮＰＯ）」）にのみ書かれている
- **山口県の4ファイルは `SPEC.md` §2 の表と完全一致した**

  | SPEC.md の記載 | 原本の列 | ファイル名 | 一致 |
  |---|---|---|---|
  | 交通空白地有償運送 × 市町村営 | 山口県・列1（自治体） | `000359215.pdf` | 一致 |
  | 交通空白地有償運送 × NPO等 | 山口県・列2（ＮＰＯ） | `000268896.pdf` | 一致 |
  | 福祉有償運送 × 市町村営 | 山口県・列3（自治体） | `000230003.pdf` | 一致 |
  | 福祉有償運送 × NPO等 | 山口県・列4（ＮＰＯ） | `000271730.pdf` | 一致 |

  `SPEC.md` §2 が「推定であり未確認」としていた他3ファイルの種別対応は、推定どおりで正しかった
- 島根県の列1 は `<a>` タグが2つに割れている（`交通空白地有償運送（自治体` と `）` が別リンク）。
  リンク先はどちらも `000285361.pdf` で同一。**リンク抽出時は重複除去が要る**
- 用語の表記差: 原本は「自治体」「ＮＰＯ」（全角）、`SPEC.md` §3.1 の例は「NPO等」。
  出力値は `SPEC.md` の例に従う方針とし、`SPEC.md` の書き換えは不要と判断した

#### PDF の取得情報

- 完全パス: `C:\Users\user\dev\udc2026\yamaguchi-yusho-data\raw\000271730.pdf`
- MD5: `c49543729e3122adbd935fa79260b3b7`
- 136,476バイト / HTTP 200 / `application/pdf` / PDF 1.6 / **4ページ**
- テキスト埋め込みあり（`images=0`、各ページ 110〜119語）。OCR 不要であることを実測で確認

#### このPDFの団体数 = 4件（1ページ1団体）

| ページ | 登録番号（原文ママ） | 名称 |
|---|---|---|
| 1 | 中山福第**１**号 | 特定非営利活動法人 豆たん |
| 2 | 中山福第**3**号 | 特定非営利活動法人 らいと |
| 3 | 中山福第**４**号 | 社会福祉法人 菊水会 |
| 4 | 中山福第**６**号 | 社会福祉法人 大島白壽會 |

**2号と5号は欠番。**登録番号の連番から件数を推測してはいけない。

#### 車両欄の列見出し（座標で確定・全4ページ共通）

左から7列。各セル内に `(軽)` が同居する形になっている。

| # | 抽出された語（そのまま） |
|---|---|
| 1 | `寝台車` + `(軽)` |
| 2 | `車いす` + `車(軽)` |
| 3 | `兼用車` + `(軽)` |
| 4 | `回転` `シート` + `車(軽)` |
| 5 | `セダン` + `等(軽)` |
| 6 | `バス` ← **`(軽)` が無い** |
| 7 | `合計` + `(軽)` |

**バス列にのみ `(軽)` が無い。**このため括弧付きの数字は1行あたり最大6個
（寝台車・車いす車・兼用車・回転シート車・セダン等・合計）になる。
`SPEC.md` §3.4 が示す `( ) (1) ( ) ( ) (1) (2)` の6個と整合する。

#### 列境界がページごとに異なる（重要）

車両欄の左端 x 座標:

| ページ | 寝台車列の左端 | 1列あたりの幅 |
|---|---|---|
| p1 | 294.0 | 約 39.8–40.8 |
| p2 | 285.0 | 約 39.5–40.4 |
| p3 | 286.0 | 約 38.4–39.3 |
| p4 | 286.3 | 約 38.3–39.2 |

**固定座標でパースすると誤読する。**列境界は各ページの縦罫線
（`page.edges` の `orientation == "v"`、近接する線を2pt未満で1本に畳む）から
ページごとに導出すること。

#### `SPEC.md` §3.4 との突合（1団体目・中山福第１号）

一致した項目:

| 項目 | 原本 | SPEC.md | |
|---|---|---|---|
| org_name | 特定非営利活動法人 豆たん | 同じ | 一致 |
| org_address | 下関市古屋町２丁目４－８ | 同じ | 一致 |
| service_area | 下関市全域（離島を除く） | 同じ | 一致 |
| valid_from / valid_to | 令和6年4月24日～令和9年4月23日 | 2024-04-24 / 2027-04-23 | 一致 |
| office_name | 豆たん介護センター | 同じ | 一致 |
| office_location | 下関市内日下1027-3 | 同じ | 一致 |
| vehicles_total | 合計欄の素の数字 = 3 | 3 | 一致 |
| `extract_text()` の見え方 | `2 1 3` / `( ) (1) ( ) ( ) (1) (2)` | 同じ | 一致 |

座標で確定した列の対応:

- 素の数字: `2` → 車いす車、`1` → セダン等、`3` → 合計
- 括弧付き: `(1)` → 車いす車、`(1)` → セダン等、`(2)` → 合計
- 持込行は素の数字なし・括弧すべて空 → 0台。`SPEC.md` の「持込は0台」と一致

**旅客範囲の推定(b)は確定に格上げできる。**

- 凡例は `イ．身体障害者 ロ．精神障害者 ハ．知的障害者 ニ．要介護認定
  ホ．要支援認定 ヘ．基本チェックリスト ト その他` で `SPEC.md` と完全一致
- 1団体目の該当記号は `イ ニ ホ ヘ`
- 凡例テキストは x ≦ 412.80 で終わり、該当記号は x = 431.76 / 472.56 / 513.36 / 554.16 と
  **右側の別セル**にあることを座標で確認した。凡例の文字を誤って拾ったものではない

相違した項目:

- **全角・半角が混在している。**`registration_no` の原本は全角「中山福第**１**号」だが、
  `SPEC.md` §3.4 の期待値は半角「中山福第1号」。他ページも 3号（半角）・４号（全角）・
  ６号（全角）とバラバラ。`office_location` も、上部の事務所欄は全角
  「下関市内日下１０２７－３」、車両欄は半角「下関市内日下1027-3」で表記が違う
- これは `SPEC.md` の誤りではなく、**正規化仕様が `SPEC.md` に書かれていない**。
  §4 に「全角数字・全角ハイフンを正規化する」とはあるが、
  どの列を正規化し、どの列を原文保持するかの対応が `registration_no`・`office_location`
  について未定義。`SPEC.md` は書き換えていない。チャット側で決める必要がある

#### 他ファイルを見て分かった構造差

- **市町村営ファイル（`000230003.pdf`）には「所有区分」列そのものが無い。**
  NPO等ファイルにある「所有 / 持込」の行分けが存在しない。
  `SPEC.md` §3.2 の `ownership` 列は山口県内でも常に取れるとは限らない
- 「持込」行に `( )` ではなく素の `0` が書かれているページがある（`000271730.pdf` の p2・p3・p4）
- `000271730.pdf` の p4 では全角括弧 `（` `）` が縦に分離して出現する
  （セダン等列、top=592.28 と 603.32）。半角括弧と混在するのでパーサは両方を扱う必要がある

#### 原本側のデータ異常（`SPEC.md` に記載なし）

`000271730.pdf` の p4（中山福第６号 社会福祉法人 大島白壽會）で、
**上部の登録事項と車両欄の事務所情報が食い違っている。**

- 上部: 名称 = 社会福祉法人 大島白壽會 / 住所 = 山口県大島郡周防大島町大字西方1623番地の３ /
  事務所の名称及び位置 = 大島白壽會・同住所 / 運送の区域 = 周防大島町（旧東和町に限る）
- 車両欄: 事務所の名称 = **桑陽病院腎友会** / 事務所の位置 = **防府市車塚町3番２０号**

周防大島町と防府市は別の市町で、組織名も別。原本側の誤りの可能性が高いが断定はしない。
`SPEC.md` §3.1 は `office_name` / `office_location` を1組しか持たないが、
原本には2箇所に別々の値が入っており、どちらを採るかの規定が無い。

### 詰まったこと、`SPEC.md` と食い違ったこと

#### 未確定: 括弧付き数字が軽自動車の内数か外数か（`SPEC.md` §3.4 の推定(a)）

**決着していない。推測で埋めずに止めた。**

- **算術では判別できない。**4団体すべてで「素の内訳和 = 素の合計欄」と
  「括弧の内訳和 = 括弧の合計欄」が独立に成立する。
  この関係は内数でも外数でも成立するため、識別力が無い

  | 登録番号 | 素の数字 | 括弧付き |
  |---|---|---|
  | 中山福第１号 | 車いす車2 + セダン等1 = 合計3 | 車いす車1 + セダン等1 = 合計2 |
  | 中山福第3号 | 車いす車9 + セダン等1 = 合計10 | 車いす車6 + セダン等1 = 合計7 |
  | 中山福第４号 | 車いす車1 + セダン等2 = 合計3 | セダン等2 = 合計2 |
  | 中山福第６号 | 車いす車1 = 合計1 | 車いす車1 = 合計1 |

- **反証テストを実施した。**内数なら「括弧 ≦ 素」が常に成立し、
  「括弧のみあって素の数字が無い」列は起こり得ない。
  山口県4ファイル・判定できた23ページ・括弧セル26件を走査して**反証0件**。
  内数と整合するが、**外数を排除する証拠にはならない**
  （サンプル内にたまたま軽自動車のみ保有の団体が無かった可能性を排除できない）
- **対象PDF内に「軽」の凡例・備考は無い**（全4ページの全文を確認済み）
- **様式定義側からの確定を試みたが失敗した。**
  `https://wwwtb.mlit.go.jp/kanto/content/000228273.xls` を取得したが
  （152,576バイト、MD5 `f7c6249db99c40a48cb0cd29b7b233f6`、MS Excel 97-2003、code page 932）、
  **これは目的の様式ではなかった。**中身は回送運行許可（ディーラーナンバー）関係の様式集で、
  シート「第２号様式」は「運転者等に対する法令関係研修の実施状況」。
  道路運送法施行規則第51条の5 の第2号様式（自家用有償旅客運送者登録簿）は含まれていない。
  シートは第２号〜第３２号様式の計20枚あるが、いずれも回送運行関係

→ **未確定のまま。`vehicles.csv` の設計はこれが決まるまで確定できない。**
内数なら中山福第１号は3台（うち軽2）、外数なら5台になる。

#### WebFetch がページを誤読した（作業手順への教訓）

最初に一覧ページを WebFetch で読ませたところ、
**「貨物自動車運送事業者登録状況の表」「一般貨物／特積貨物」「実績値版／見込値版」という
存在しない内容**を返し、山口県の行として**鳥取県のファイル名**
（`000361492` 他4本）を提示した。cp932 の生デコードと突き合わせて初めて誤りと判明した。

`SPEC.md` §3.4 の実例も WebFetch 経由で書かれており同じ経路を通っているため、
今回はゼロから読み直して検証した（結果は上記のとおり、§3.4 の実例自体は正しかった）。

→ **このプロジェクトでは WebFetch の要約を一次情報として使わない。**
cp932 のページも PDF も、必ず生バイトを取得して自前でデコード・照合する。
`fetch.py` もこの方針で書く。

#### `raw/000271730.pdf` をコミットしていない理由

`SPEC.md` §2 に「国交省サイトは政府標準利用規約準拠であることが多いが、
**確認せずに再配布しない**」とある。利用規約の確認は未了のため、
public リポジトリへの PDF のコミットは再配布に当たると判断して見送った。
ファイルはローカルの `raw/` に存在する（上記の完全パスと MD5）。
規約を確認したうえで改めてコミットする。

### 次にやること

1. **括弧付き数字の内数／外数を確定させる。**推測で進めない。
   - 第一手: e-Gov 法令検索で道路運送法施行規則（昭和26年運輸省令第75号）の
     第2号様式の本文・別記様式を取得し、`(軽)` の定義を読む
   - 決まらなければ中国運輸局 自動車交通部（旅客担当）に直接照会する。
     ここは推定で通してはいけない箇所で、外すと全ファイルの `vehicles.csv` が作り直しになる
2. `SPEC.md` §2 の利用規約確認（`raw/` のコミット可否と README のライセンス欄がこれ待ち）
3. チャット側で決めてもらう必要がある事項（`SPEC.md` に規定が無い）
   - `registration_no` と `office_location` の全角・半角の正規化方針
   - p4 のように上部と車両欄で事務所情報が食い違う場合、どちらを採るか
   - 市町村営ファイルで `ownership` が取れない場合の既定値
4. 上記1が確定するまで `parse.py` には着手しない

### 再現用メモ

- Python は `C:\Users\user\dev\udc2026\.venv\Scripts\python.exe` を使う
  （素の `python` はハングする。前回の記録を参照）
- 一覧ページのデコード: `curl` で生取得 → `iconv -f CP932 -t UTF-8`
- 検証用スクリプトは scratchpad に置いており、リポジトリには含めていない。
  他3ファイル（`000359215` `000268896` `000230003`）も反証テストのため
  scratchpad にのみダウンロードした。`raw/` には入れていない

## 2026-08-07 e-Gov法令検索での軽自動車内数/外数の定義確認

「次にやること」1番（括弧付き数字の内数/外数の確定）について、e-Gov法令検索の一次情報
（curl生バイト取得）を様式定義側から確認した。

### やったこと

1. e-Gov法令API (`https://laws.e-gov.go.jp/api/2/laws?law_title=...`) で
   道路運送法施行規則（昭和26年運輸省令第75号）の法令IDを特定した
2. 法令全文XML (`https://laws.e-gov.go.jp/api/2/law_data/326M50000800075?response_format=xml`)
   をcurlで取得し、第五十一条の五の条文本文と別記様式の参照箇所を確認した
3. 第2号様式の実体（画像PDF）を取得するため `https://laws.e-gov.go.jp/api/1/lawdata/326M50000800075`
   (Version 1 API) をcurlで取得した。`<ImageData>` 要素にZIP形式・Base64エンコードで
   添付図表一式が格納されていることを確認し、デコード・展開して
   `2FH00000044553.pdf`（第2号様式本体、74,274バイト）を復元した
4. 復元したPDFを `pdftoppm` で300dpiにレンダリングし、目視確認した
5. WebFetchは一切使用せず、法令API・XML・PDFすべてをcurl生バイト取得のみで確認した

### 確認できた事実（数字を含める）

- 道路運送法施行規則の e-Gov 法令ID: `326M50000800075`
- 条文本文（第五十一条の五、`ArticleCaption`＝「自家用有償旅客運送者登録簿」）に
  「軽自動車」の文字列は0件（法令XML全文検索で確認）。
  「登録簿は、第二号様式によるものとする」との参照があるのみ
- 第2号様式のタイトル: 「第２号様式（第51条の5関係）（平18国交令86・追加、
  平26国交令7・旧第1号様式繰下、平27国交令21・令2国交令93・一部改正）」、
  様式名「自家用有償旅客運送者登録簿」
- 様式下段の「自家用有償旅客運送自動車の数」欄の表頭は、寝台車・車いす車・兼用車・
  回転シート車・セダン等・合計の6列に「（軽自動車）」という表記があり、
  **バス列のみこの表記が無い**
  - `000271730.pdf`（山口県・中山福第１号ほか）の車両欄の列構成（(軽)が5列+合計にあり、
    バスのみ無し）と完全に一致する。様式の`(軽)`＝「軽自動車」であることの裏付けにはなる
- 各欄の記入欄は空欄括弧`（　）`のみが印字されており、**「内数」「うち」「外数」等、
  この括弧内数値の集計方法を明示する文言は様式内のどこにも存在しない**（表の下は余白のみ）
- 使用した実URL:
  - `https://laws.e-gov.go.jp/api/2/laws?law_title=...`（法令ID特定）
  - `https://laws.e-gov.go.jp/api/2/law_data/326M50000800075?response_format=xml`（条文本文）
  - `https://laws.e-gov.go.jp/api/1/lawdata/326M50000800075`（様式PDF本体、ImageData経由）

### 詰まったこと、`SPEC.md` と食い違ったこと

**未確定: 様式の定義側からも確定できなかった。**

道路運送法施行規則 第51条の5・第2号様式には、括弧書きの数字が軽自動車の「内数」であることを
明示的に定義する文言は存在しない。様式は各車種欄に「（軽自動車）」という見出しキャプションを
付しているのみで、それが総数に含まれる内数か、別途計上する外数かを言葉で説明する注記・脚注は
本文にも様式にも無い。

（参考・確定情報ではない: 括弧欄が本数欄と同一列内に配置されている構造は一般的な官公庁様式の
「内数」表現の典型パターンと一致するが、これは様式の慣習からの推測であり、本文・様式に
明記された定義ではないため確定情報として採用しない）

### 次にやること

1. 中国運輸局 自動車交通部（旅客担当）に、下記の照会文（下書き）を確認・補記のうえ送付する
2. 回答が得られるまで `vehicles.csv` の設計・`parse.py` の実装には着手しない
3. （既存・未着手）`SPEC.md` §2 の利用規約確認（`raw/` のコミット可否）
4. （既存・未着手）チャット側で決めてもらう必要がある事項
   （`registration_no`/`office_location` の全角半角正規化方針、事務所情報の食い違い時の採否、
   `ownership` 欠落時の既定値）

### 中国運輸局 自動車交通部（旅客担当）への照会文（下書き）

（送付前に宛先の正式な課名・差出人氏名・連絡先・日付を確認・補記すること）

---

件名: 自家用有償旅客運送者登録簿（第2号様式）の車両数記載に関する照会

中国運輸局 自動車交通部 旅客担当 御中

平素より大変お世話になっております。
[差出人氏名／所属] と申します。貴局が道路運送法第79条の3第1項に基づき公表しておられる
「自家用有償旅客運送者登録簿」（道路運送法施行規則第51条の5・第2号様式）の記載内容について、
1点確認させていただきたく、ご連絡いたしました。

同様式の「自家用有償旅客運送自動車の数」欄には、寝台車・車いす車・兼用車・回転シート車・
セダン等・合計の各欄に、括弧書きで「（軽自動車）」の数字が付記されております。
この括弧内の数字は、括弧の外に記載されている当該車種の総数（本数）に**含まれる内数**
（例: 車いす車の総数が2台のうち1台が軽自動車）でしょうか。
それとも、本数とは別に**外数として計上**されているもの（例: 通常車1台＋軽自動車1台で
実質2台とは別に軽自動車の記載がある）でしょうか。

具体例で恐縮ですが、貴局公表の山口県分登録簿（中山福第１号、下関市）では、
車いす車欄が「2 (1)」、セダン等欄が「1 (1)」、合計欄が「3 (2)」と記載されております。
この場合、実際の保有車両台数は3台（うち軽自動車2台を含む）でしょうか、
それとも5台（通常車3台＋軽自動車2台）でしょうか。

様式の記入要領等、根拠となる資料がございましたら併せてご教示いただけますと幸いです。
お手数をおかけいたしますが、ご確認のほどよろしくお願い申し上げます。

[差出人氏名]
[連絡先（メールアドレス／電話番号）]
[日付]

---

## 2026-08-07 括弧付き数字の内数/外数を確定（国自旅第316号・第317号の記載要領）

中国運輸局への照会はせず、方針転換して(1) 国交省自動車局の通達、(2) 他運輸局の登録簿サーベイ、
の2方向で調査した。(1)で確定したため(2)は中断した。

### やったこと

1. 国交省自動車局の通達一覧ページ「自家用有償旅客運送に関係する通達について」
   （`https://www.mlit.go.jp/jidosha/jidosha_tk3_000044.html`）を curl で生取得した
2. 交通空白地有償運送・福祉有償運送それぞれの処理方針通達（国自旅第316号・第317号）と、
   対応する申請書等様式（Word）をcurlで生バイト取得した
3. PDFは pdfminer.six と PyMuPDF の2ツールで独立にテキスト抽出しクロスチェックした。
   Wordファイル（.doc）はOLEバイナリの WordDocument ストリームを UTF-16LE で解析した
4. WebFetchは一切使用しなかった
5. 並行して他運輸局サーベイ（九州8件＋東北青森29件、計37件、★1件で打ち切り／0件でも
   打ち切りという縮小スコープで承認済み）にも着手していたが、(1)で確定が得られたため、
   ユーザー判断により**中断・実施不要**とした。ダウンロード済みファイルがあっても
   scratchpad止まりで `raw/` には反映していない

### 確認できた事実（数字を含める）

- **結論: 括弧内の軽自動車数は内数。** 4つの独立文書で文言が一致（下記）
- 根拠1: 「交通空白地有償運送の登録に関する処理方針について」
  （令和2年11月27日 国自旅第316号）
  出典: `https://www.mlit.go.jp/jidosha/content/001766048.pdf`
  該当箇所（「２．登録の申請」⑦）原文引用:
  > 「事務所ごとに市町村又は特定非営利活動法人等が保有する自家用自動車、ボランティア個人
  > 及び株式会社等からの持込み自動車（運行委託先の事業者が保有する事業用自動車を含み、
  > 交通空白地有償運送を実施する間、申請者が使用権原を有するものに限る。）の別ごとに、
  > 以下に掲げる自動車の種類ごとの数（**軽自動車がある場合には、その数を内数として
  > 括弧書きで記載**。また、自動運行旅客運送（特定自動運行旅客運送を除く。）又は
  > 特定自動運行旅客運送の用に供する自動車がある場合には、その数を内数として該当欄に
  > 記載）を記載する。」
- 根拠2: 「福祉有償運送の登録に関する処理方針について」
  （令和2年11月27日 国自旅第317号）
  出典: `https://www.mlit.go.jp/jidosha/content/001766049.pdf`
  該当箇所（同項目⑦）原文引用:
  > 「…の別ごとに、以下に掲げる自動車の台数を記載（**軽自動車がある場合には､その数を
  > 内数として括弧書きで記載**。また、自動運行旅客運送（特定自動運行旅客運送を除く。）
  > 又は特定自動運行旅客運送の用に供する自動車がある場合には、その数を内数として
  > 該当欄に記載）するものとする。」
- 根拠3: 交通空白地有償運送 申請書等様式（Word）様式第１－１号
  「自家用有償旅客運送の登録の申請」（様式第１－４号・第１－５号変更届出書にも同一文言）
  出典: `https://www.mlit.go.jp/jidosha/content/001516252.doc`
  表「５．事務所ごとに配置する自家用有償旅客運送自動車の数及びその種類ごとの数」
  （バス／普通自動車（軽）／合計 の欄構成）直下の注記、原文引用:
  > 「（　）軽自動車については、（　）内に内数で記載すること
  > 事業用自動車については、※欄に記載すること」
- 根拠4: 福祉有償運送 申請書等様式（Word）様式第２－１号
  「自家用有償旅客運送の登録の申請」（様式第２－５号変更届出書にも同一文言）
  出典: `https://www.mlit.go.jp/jidosha/content/001516262.doc`
  表（寝台車（軽）／車いす車（軽）／兼用車（軽）／回転シート車（軽）／セダン等（軽）／合計）
  直下の注記、原文引用:
  > 「軽自動車については、（　）内に内数で記載すること
  > 事業用自動車については、※欄に記入すること」
- 4文書とも「内数として括弧書きで記載」「（　）内に内数で記載すること」という文言で一致。
  pdfminer.six / PyMuPDF の独立抽出でも同一文言を確認した
- 現行の国自旅第316号・第317号は令和2年11月27日付。旧処理準則
  （平成18年9月15日付 国自旅第141号・第142号）を廃止して新たに定められたもの
  （通達本文冒頭に記載）
- 一覧ページ: `https://www.mlit.go.jp/jidosha/jidosha_tk3_000044.html`
  （「自家用有償旅客運送に関係する通達について」、text/html UTF-8）
- 参考（今回の結論には未使用・未詳読）: 参考様式（xlsx）
  - `https://www.mlit.go.jp/jidosha/content/001516253.xlsx`（交通空白地有償運送）
  - `https://www.mlit.go.jp/jidosha/content/001516263.xlsx`（福祉有償運送）

### 結論

**確定: 括弧付きの数字は軽自動車の内数。** e-Gov法令検索の登録簿様式（第2号様式）自体には
内数/外数を定義する文言が無かったが、登録の元になる処理方針通達（国自旅第316号・第317号）と
申請書等様式の記載要領に明記されていた。登録簿は申請内容に基づき作成されるため、
この記載要領が登録簿の解釈根拠になる。

- 中山福第１号: 車いす車「2 (1)」→ 車いす車2台のうち軽1台、セダン等「1 (1)」→
  セダン等1台のうち軽1台、合計「3 (2)」→ **合計3台（うち軽自動車2台）**。5台ではない
- 中山福第3号・第4号・第6号（既存記録の素の数字・括弧数字）も同じ規則（括弧＝内数）で読む

他運輸局サーベイは上記により目的が消滅したため中断（未実施）。

### 次にやること

1. `SPEC.md` §3.4 の推定(a)「括弧付き数字は軽自動車の内数」を、今回確定した内容
   （出典・条項・原文引用つき）で正式な記載に改訂する
   （次回セッション、チャット側の指示で実施。今回は `SPEC.md` 未編集）
2. `vehicles.csv` の設計を「合計欄の素の数字＝総台数（軽自動車を含む）」で確定し、
   `parse.py` 着手に進める
3. （既存・未着手）`SPEC.md` §2 の利用規約確認（`raw/` のコミット可否）
4. （既存・未着手）チャット側で決めてもらう必要がある事項
   （`registration_no`/`office_location` の全角半角正規化方針、事務所情報の食い違い時の採否、
   `ownership` 欠落時の既定値）

## 2026-08-07 一覧ページ照合（evidence/ 運用に沿って再実施）

以前の一覧ページ照合（本ファイル上部「一覧ページ照合 と 000271730.pdf の原本検証」）は
生の出力を `evidence/` に残していなかったため、CLAUDE.md の証拠の残し方に従って
生バイト取得からやり直した。`parse.py` には着手していない。

### やったこと

1. `curl -sD raw/00001_00903_headers.txt -o raw/00001_00903.html` で一覧ページの生バイトを
   `raw/` に取得（リポジトリ外の `/tmp` 等は一切使用しなかった）
2. `iconv -f CP932 -t UTF-8 raw/00001_00903.html > raw/00001_00903_utf8.html` で cp932 → UTF-8 にデコード
3. デコード後の全文とレスポンスヘッダを `evidence/20260807_list_page_cp932_decode.txt` に保存
   （先頭3行に取得日時・取得元・取得方法を記載）
4. デコード後の HTML を目視し、山口県の行（4セル）を `SPEC.md` §2 の表と突き合わせた

### 確認できた事実（数字を含める）

- HTTP 200 / `Content-Type: text/html` / `Content-Length: 7716` バイト。実測サイズも 7,716 バイトで一致
  （`evidence/20260807_list_page_cp932_decode.txt`）
- `<meta charset="Shift_JIS">` を確認（同ファイル）
- テーブル行順は **広島県 → 鳥取県 → 島根県 → 山口県**。岡山県の行は無く、本文中に
  「中国地方（岡山県を除く）」と明記されている（同ファイル、`raw/00001_00903_utf8.html` 68行目）
- ヘッダ行（`raw/00001_00903_utf8.html` 75-79行目）は「（空セル）／交通空白地有償運送
  （colspan=2）／福祉有償運送（colspan=2）」。実施主体（自治体／ＮＰＯ）の列ラベルはヘッダ行になく、
  各セルのリンクテキスト内（「（自治体）」「（ＮＰＯ）」）にのみある
- **山口県の行（同ファイル 113-123行目）は `SPEC.md` §2 の表と完全一致した**:

  | SPEC.md §2 の記載 | 一覧ページのリンクテキスト | ファイル名 | 一致 |
  |---|---|---|---|
  | 交通空白地有償運送 × 市町村営 | 交通空白地有償運送（自治体） | `000359215.pdf` | 一致 |
  | 交通空白地有償運送 × NPO等 | 交通空白地有償運送（ＮＰＯ） | `000268896.pdf` | 一致 |
  | 福祉有償運送 × 市町村営 | 福祉有償運送（自治体） | `000230003.pdf` | 一致 |
  | 福祉有償運送 × NPO等 | 福祉有償運送（ＮＰＯ） | `000271730.pdf` | 一致 |

- 島根県の列1（交通空白地有償運送・自治体）は `<a>` が2つに割れている
  （`交通空白地有償運送（自治体` と `）` が別リンク。リンク先はどちらも `000285361.pdf`）ことを
  再確認した（`raw/00001_00903_utf8.html` 104-105行目）
- 用語差: 一覧ページは「自治体」「ＮＰＯ」（全角）、`SPEC.md` §2 の表は「市町村営」「NPO等」。
  前回照合時と同じ差分で、`SPEC.md` の書き換えを要する食い違いではないと判断した
  （出力値は `SPEC.md` の表記に従う）

### 詰まったこと、`SPEC.md` と食い違ったこと

- 食い違いなし。前回（証拠未保存）の照合結果と一致した。
- 作業手順上のミス: 途中で2回、確認用の中間ファイルを `/tmp` に書こうとしてユーザーに止められた。
  このリポジトリでは中間ファイルも含め一切をリポジトリ外に書かない方針（`raw/` または `evidence/` に置く）。

### 次にやること

- `SPEC.md` §2 の利用規約確認（`raw/` のPDFコミット可否。前回から未着手のまま）
- チャット側で決めてもらう必要がある事項（`registration_no`/`office_location` の全角半角正規化方針、
  事務所情報の食い違い時の採否、`ownership` 欠落時の既定値）
- 上記が済むまで `parse.py` には着手しない

## 2026-08-07 ガード動作確認

チャット側の指示により、`.claude/settings.json` の `permissions.deny` /
`.claude/hooks/guard_protected_files.py` の動作確認のみを行った。

### やったこと

1. `Edit` で `CLAUDE.md` 末尾に1行追記を試行（2回、同一結果）
2. Bash で `echo test > /tmp/guardcheck.txt` を実行

### 確認できた事実

- `CLAUDE.md` への `Edit` はブロックされた。返ってきたエラーはそのまま以下:

  ```
  File is in a directory that is denied by your permission settings.
  ```

  `CLAUDE.md` 本文が予告する文言 `[guard] blocked write` とは異なる文言だった。

- **Bash のリダイレクトによるリポジトリ外書き込みがブロックされなかった。原因は未特定。**

  実行したコマンド:

  ```
  echo test > /tmp/guardcheck.txt
  ```

  コマンドの出力:

  ```
  (出力なし・エラーなし)
  ```

  書き込まれたことの確認（`ls -la` / `cat`）:

  ```
  -rw-r--r-- 1 user 197121 5 Aug  7 20:13 /tmp/guardcheck.txt
  ---
  test
  ```

  `CLAUDE.md` は「リポジトリの外への書き込みは、`Edit` / `Write` でも Bash のリダイレクトでも
  フックがブロックし、`[guard] blocked write outside the repo` が返る」としているが、
  今回はブロックされずファイルが作成された。確認後、このテストファイルは削除した
  （`rm -f /tmp/guardcheck.txt`）。

## 2026-08-07 src/parse.py 実装（000271730.pdf 1本、SPEC.md §5 完了条件1〜8達成）

### やったこと

1. `SPEC.md` §4 の実装方針に従い `src/parse.py` を新規作成した。着手前に、
   `pdfplumber` の `extract_words()` / `page.edges`（縦罫線・横罫線）で全4ページを
   座標付きで実測し、`pdftoppm -r 200` で全4ページを画像化して目視確認した
   （生の出力は `evidence/20260807_explore_words_edges.txt`,
   `evidence/20260807_explore_hedges.txt`,
   `evidence/20260807_page_render-{1..4}.png` に保存）
2. 実装したパーサを実行したところ複数の例外・サイレントな誤りが見つかったため、
   Plan agent（read-only、実行はせず座標データを手計算でトレース）にコードレビューを依頼し、
   計6件のバグを修正した（詳細は次節）
3. 修正後、パーサが4ページとも例外なく完走することを確認し、`data/operators.csv` /
   `data/vehicles.csv` / `data/operators.json` / `data/vehicles.json` を生成した
4. `tests/test_verify.py` を `unittest`（`pytest` は依存に無いため）で新規作成し、
   SPEC.md §5-2, §5-3, §5-8 を自動テスト化した。特に代表者氏名の不出力確認は、
   氏名文字列をテストソースに残さないよう、PDFから都度動的に抽出して比較する方式にした
5. `verification.md` を新規作成し、SPEC.md §5 の完了条件1〜8全項目の実行結果を記録した

### 修正した6件のバグ（Plan agentのレビューで発見）

いずれも `evidence/20260807_explore_words_edges.txt` / `evidence/20260807_explore_hedges.txt`
の実測座標と突き合わせて特定した。実行順に1〜4は例外で停止するバグ、5〜6は例外を出さずに
出力が誤るサイレントなバグだった。

1. `extract_transport_type`: `join_words(row, gap_threshold=3.0)` が、実測で5.04ptある
   字間（「福」「祉」等の間）にスペースを挿入し「福 祉 有 償 運 送」になって完全一致チェックに
   失敗していた。`gap_threshold=100`（詰めて連結）に修正
2. `extract_office_block`: ミニ見出し「名称」「位置」を座標ボックスで拾っていたが、
   下限が緩く実データ行（事務所名・位置の値）まで巻き込んでいた。テキスト完全一致
   （`{"名","称","位","置"}`）で拾う方式に修正
3. 事務所名称/位置の列境界検出: 罫線が「次ラベル位置の直前まで届く」前提だったが、
   実測でp1は7.06pt・p2は6.31pt罫線が短く、条件を満たす罫線が0本になっていた。
   実データ行の実際の縦範囲とオーバーラップする罫線を探す方式に修正
   （さらに、修正の過程で表右端の外枠罫線(x=578.40)まで拾ってしまう問題も見つかり、
   x座標に上限500を追加して除外した）
4. `extract_vehicle_table` の所有区分ラベル判定: 「所有」「持込」の実測中心xが全4ページで
   264.48〜271.44あり、固定しきい値`x1=260`では拾えなかった。事務所名称/位置列と
   車両欄の間の縦罫線（p1=247.92, p2=243.84, p3=245.88, p4=246.36付近）から
   `owner_divider`を動的に導出する方式に修正（修正時に左右の対応を1度取り違え、
   ラベルが空になる別の例外を踏んだため、これも合わせて直した）
5. `office_mismatch`判定: 全角/半角・語間スペースを正規化せずに比較していたため、
   実際は一致している中山福第1号（上部"下関市内日下１０２７－３"全角 / 車両欄"1027-3"半角）と
   中山福第3号（`join_words`の語間ギャップの違いで空白挿入の有無が変わっただけ）に
   誤ってフラグが立っていた。比較の直前だけ全角/半角と語間スペースを正規化する処理を追加
   （保存する値自体はSPEC.md §4.3の「原文保持」どおり正規化していない）
6. `extract_scope_flags`（旅客の範囲マーク判定）: 「右隣の語との水平距離12pt未満なら
   凡例の断片とみなす」というヒューリスティックが、中山福第6号（凡例が3行に折り返される
   レイアウト）で破綻し、凡例末尾の「ト」が実マーク「ニ」との距離16.8pt(>12pt)で
   除外条件をすり抜けて`scope_to_other`が誤って1になっていた。水平距離ではなく、
   欄内に「その他」を含む語（または単独の「そ」）が存在するかどうかで「ト」を
   凡例の断片と判定する方式に修正（SPEC.md §3.1の固定凡例文言に基づく判定）

さらにコードレビュー外で気づいた設計上の誤りも修正した:
- `vehicles.csv` の `office_name`/`office_location`（車両欄側の値）を、所有行は名称のみ・
  持込行は位置のみで片方を空欄にする実装になっていたが、SPEC.md §3.2は「同一事務所の
  車両欄側の名称・位置」を指しており、所有/持込どちらの行にも両方の値を入れるべきだった。修正した
- `extract_vehicle_table` の kei（軽自動車内数）が、括弧自体が全く印字されないセル
  （持込行の合計列など）で `None` のまま残り、合計処理で `TypeError` になっていた。
  SPEC.md §3.4(a)の「空の括弧・括弧なし・素の0はいずれも0」を素直に適用し、
  (軽)欄を持つ列で値が見つからなければ無条件で0とする処理に修正
- `unicodedata` インポートと未使用の `parse_date` 関数（デッドコード）を削除
- `partner_operator_name`/`address` を無条件で空文字列にハードコードしていたが、
  抽出結果を無視する実装だったため、抽出した値が空でなければ例外を投げて止める
  （空欄前提を検証しつつ、想定外の値を推定で握りつぶさない）方式に変更

### 確認できた事実（数字を含める）

- `data/operators.csv`: 4行（4団体）。`data/vehicles.csv`: 11行
- 車両合計の照合: 4団体全件で `vehicles.csv` の `count`/`count_kei` 合計と
  `operators.csv` の `vehicles_total`/`vehicles_total_kei` が一致（不一致0件）。
  `count_kei <= count` は全11行で成立
- `service_type_code` は全件「福」、`authority_code` は全件「中山」で登録番号の整合を確認
- 旅客範囲の選択マーク（SPEC.md明示要求を超える追加確認）: 中山福第1号=イニホヘ、
  中山福第3号=イニ、中山福第4号=マーク無し、中山福第6号=ニホ。
  4ページ全件を `evidence/20260807_page_render-{1..4}.png` の目視と突合し一致を確認
- `office_mismatch` フラグ: 中山福第4号・中山福第6号の2件に立った
  （詳細は次節「詰まったこと」参照）
- `tests/test_verify.py`: `unittest` で6件全て成功（実行ログは `verification.md` に記載）
- 代表者氏名の不出力: 4ページ全件についてPDFから氏名を動的抽出し、
  `data/` 配下の全出力ファイルに含まれていないことを自動テストで確認
- `git rev-parse HEAD`: `7c21ce837716ee7a440facb49247b0b6cfaf0452`（作業開始時点）

### 詰まったこと、`SPEC.md` と食い違ったこと

#### 要確認事項: 中山福第4号の事務所位置表記差（`SPEC.md` に規定なし、判断せず保留）

`office_mismatch` フラグが中山福第6号（`SPEC.md` §3.6 で既知の別組織異常）だけでなく
中山福第4号にも立った。中山福第4号は事務所名称は一致する（「まんてんの星」）が、
事務所位置が上部「下関市菊川町大字田部５３６番地１」、車両欄「下関市菊川町大字田部５３６－１」
で異なる。これは全角/半角の違いではなく「番地」という語の有無・「－」表記という
**記載慣行の差**であり、`office_mismatch` の正規化（全角/半角・語間スペースのみ）では解消しない。

これが中山福第6号のような「原本側の別組織」を示す異常と同種に扱うべきか、
単なる記載慣行差でフラグ対象外とすべきかは `SPEC.md` に規定が無い。
**コード側では判断せず、`office_mismatch` を立てたままにしている。**
`verification.md` §5-7 に事実として記録した。`SPEC.md` rev.3 での規定を待つ
（ユーザーからの指示: 「p3の扱いは判断しない。PROGRESS.mdに要確認事項として書くだけにする」）。

#### `SPEC.md` §3.5 の期待値と §4.3 の正規化方針が矛盾する箇所（未解決のまま実装、書き換えはしていない）

`SPEC.md` §4.3 は `office_location` を「原文保持。正規化しない」と明記しているが（rev.2で追加）、
§3.5 の「期待される出力」例は `office_location: 下関市内日下1027-3`（半角）としている。
実物のページ上部の記載は全角「下関市内日下１０２７－３」であり（`evidence/20260807_explore_words_edges.txt`
PAGE 1 で確認）、§4.3どおり原文保持で実装すると§3.5の例と一致しない。
§4.3はrev.2で「正規化仕様が未定義」という以前の食い違いを解消するために新設された、より具体的な
規定と判断し、**今回は§4.3を優先して実装した**（`operators.csv`のoffice_locationは全角のまま）。
`SPEC.md`は書き換えていない。

#### その他

- 事業者協力型自家用有償運送の氏名又は名称/住所は、山口県4ファイル中このファイルの4団体は
  全件空欄だった（`evidence/20260807_page_render-{1..4}.png` で目視確認）。
  パーサは空欄前提を検証する実装にしており、想定外の値があれば例外で止まる
- `unicodedata`・`parse_date`など、実装過程で書いたが使わなかったコードは削除した

### 次にやること

- SPEC.md §5 の完了条件1〜8を満たしたため、CLAUDE.mdの指示どおりここで止めて報告する
- 残り3ファイル（`000230003.pdf` `000359215.pdf` `000268896.pdf`）・他県展開は指示があるまで着手しない
- チャット側での判断待ち事項:
  1. ~~中山福第4号の事務所位置表記差（上記）を `office_mismatch` の対象とすべきか~~
     → `SPEC.md` rev.3 で解決済み（次節参照）
  2. `SPEC.md` §3.5 と §4.3 の `office_location` 正規化方針の矛盾（上記、未解決のまま）
  3. `raw/000271730.pdf` のコミット可否（`SPEC.md` §2 で利用規約はPDL1.0準拠と確認済みだが、
     `wwwtb.mlit.go.jp/chugoku` への適用はU6として未確認のまま。今回のセッションでは
     この点の追加調査は行っていない）

## 2026-08-08 SPEC.md rev.3 反映（事務所情報フラグを2種類に分割）

### やったこと

1. `git pull` して `SPEC.md` が rev.3 になっていることを確認した
2. rev.3 §3.6（および §5-7, §10.2）を読み、上記の要確認事項1が
   `office_mismatch` / `office_notation_diff` の2種類のフラグとして規定されたことを確認した
3. `src/parse.py` の事務所情報比較ロジックを rev.3 の規定どおりに修正した:
   - 比較の前処理は「全角→半角」「空白（全角・半角）を全て除去」の2段のみ
     （既存の `normalize_for_compare` は元々この2段のみだったため、前処理自体は変更不要だった）
   - `office_name` が不一致 → `office_mismatch`
   - `office_name` は一致し `office_location` のみ不一致 → `office_notation_diff`
   - 両方一致 → フラグなし
4. `src/parse.py` を再実行し、`data/operators.csv` の `flags` 列が
   `SPEC.md` §3.6 の実例（§5-7の期待値）と一致することを確認した
5. `tests/test_verify.py` に `OfficeFlagsTest`（4団体の `flags` が期待値と一致することを
   確認する自動テスト）を追加した
6. `verification.md` §5-7 を rev.3 の規定に合わせて書き直した

### 確認できた事実（数字を含める）

- `data/operators.csv` の `flags` 列: 中山福第1号=空、中山福第3号=空、
  中山福第4号=`office_notation_diff`、中山福第6号=`office_mismatch`。
  `SPEC.md` §3.6 の実例表と完全一致
- `tests/test_verify.py`: `unittest` で7件全て成功（既存6件 + 今回追加の
  `OfficeFlagsTest` 1件）

### 詰まったこと、`SPEC.md` と食い違ったこと

食い違いなし。前回セッションで保留していた要確認事項がそのまま rev.3 で規定された。

### 次にやること

- SPEC.md §5 の完了条件1〜8を引き続き満たしている（flags列の変更のみで他項目に影響なし）
- 残り3ファイル・他県展開は指示があるまで着手しない
- 未解決のまま残っている事項: `SPEC.md` §3.5 と §4.3 の `office_location` 正規化方針の
  矛盾（rev.3でも未言及。今回は§4.3を優先する実装を継続）

## 2026-08-09 000230003.pdf（福祉有償運送・市町村営）の原本取得と第1ページ構造確認

`parse.py` には着手していない。SPEC.md §7 の「残り3ファイルへの展開」に向けた最初の1ファイルの
原本取得と目視確認のみ。開始前に作業ツリーが clean であること、`raw/000230003.pdf` が
未取得であることを確認済み。

### やったこと

1. `curl -sS -L -D raw/000230003_headers.txt -o raw/000230003.pdf` で原本PDFとレスポンス
   ヘッダを `raw/` に取得（リダイレクトなし、生バイト取得）
2. `%PDF` シグネチャ・HTTPステータス・Content-Type・バイト数・MD5・SHA256を確認
3. `C:\Users\user\dev\udc2026\.venv\Scripts\python.exe` の `pdfplumber` でページ数・
   ページごとの単語数・画像数・登録番号らしき文字列（正規表現）・`extract_words()`座標を確認
4. `pdftoppm -f 1 -singlefile -r 200 -png` で第1ページを直接
   `evidence/20260809_000230003_page1.png` にレンダリングした（外部一時ファイルなし）
5. 第1ページを目視し、登録番号・運送種別・実施主体・車両欄の列見出し・
   所有/持込区分列の有無を確認した
6. 生の実行コマンド・ヘッダ・ハッシュ・ページ全文（代表者氏名は `[氏名-非出力]` に置換）・
   目視結果・未確認事項を `evidence/20260809_000230003_inspection.txt` に記録した

### 確認できた事実（数字を含める。根拠は evidence/ のファイル名で示す）

- HTTP 200 / `Content-Type: application/pdf` / `Content-Length: 19003` バイト、実測サイズも
  19,003バイトで一致。`%PDF-1.4` シグネチャを確認
- MD5: `0f50b2ed90744aca1be2829299b1b4a1`（レスポンスヘッダの ETag と一致）
- SHA256: `d215cada64ce8d174ec22cb521379734da185b7616f29b27166dfccf701af1ab`
- ページ数: **3**（`000271730.pdf` の4ページとは異なる）
- 全ページ `images=0`・`word_count>0`・`text_len>0` でテキスト埋め込みを確認（OCR不要）
- 登録番号は各ページ1件ずつ、計3件。**第２号は欠番**
  （`000271730.pdf` の第２号・第５号欠番と同様のパターン）
  - page1: 「中山市福第１号」（実施主体: 山口市）
  - page2: 「中山福第３号」（実施主体: 阿武町）
  - page3: 「中山市福第４号」（実施主体: 下関市外出支援サービス事業）
  - いずれも `extract_words()` の座標（登録番号ラベル直近の1トークン）で確認済み
- 第1ページ目視結果（`evidence/20260809_000230003_page1.png`）:
  - 登録番号「中山市福第１号」、運送の種別「福祉有償運送」、名称「山口市」
    （実施主体は市＝市町村営。ファイル種別と整合）
  - 車両欄の列見出しは7列: 寝台車(軽)／車いす車(軽)／兼用車(軽)／回転シート車(軽)／
    セダン等(軽)／バス／合計(軽)。`000271730.pdf` と同一の列構成
  - **所有/持込区分列は無いことを画像上で確認した。** 事務所ごとに数値行が1組
    （合計欄の括弧外・括弧内の2行）のみで、`000271730.pdf` にあった「所有」「持込」の
    2行分割構造が無い。「所有」「持込」の文字列自体も本文に存在しない。
    SPEC.md §7 の想定（「所有区分の列が無い。`ownership` は空になる」）と実物で整合を確認した
  - ページ2・3の抽出テキストでも同様に、各事務所につき数値行が1組のみであることを確認した

### 詰まったこと、SPEC.md と食い違ったこと

- **登録番号の表記ゆれ（未確認のまま。推定でSPECを補っていない）。**
  SPEC.md §3.4(b) は山口県分の登録番号を「中山福第N号」の1パターンのみ想定しており、
  `000271730.pdf` の実測（全件「中山福第N号」）でもこれに合致していた。
  しかし `000230003.pdf` では「中山」と「福」の間に「市」が入る表記（中山市福第１号・
  中山市福第４号）と、入らない表記（中山福第３号）が同一ファイル内に混在していた。
  座標抽出で3件とも確認済みであり、抽出誤りではなく原本の印字。
  実施主体が「◯◯市」の2件（page1, page3）でのみ「市」が入り、「◯◯町」の1件（page2）では
  入らない、という並びが観測されたが、**この対応が意図的な表記規則か偶然かは未確認。**
  詳細は `evidence/20260809_000230003_inspection.txt` §8 に記録した。
  SPEC.md §3.4(b) の登録番号構造の定義（`authority_code` の切り出し方）に影響しうるため、
  パーサ実装前にチャット側での確認・SPEC.md改訂が必要と考える。今回はここで止め、
  推定で先には進んでいない。
- ページ数が `000271730.pdf`（4ページ）と異なり3ページだった。1ページ1団体・団体数3件
  （登録番号の出現数と一致）で、これ自体はSPEC.md §4「団体の区切りは登録番号の出現で判定」
  と矛盾しない
- 車両欄の列境界（縦罫線のx座標）の導出は今回のタスク範囲外のため行っていない
- 上記以外、SPEC.md §7 の想定（所有区分列が無い）と食い違う点は無かった

### 次にやること

- 登録番号の「中山市福」/「中山福」表記ゆれをチャット側で確認し、SPEC.md §3.4(b) の
  改訂要否を判断してもらう（パーサの `authority_code`/`service_type_code` 抽出に影響しうる）
- 上記が確定するまで `000230003.pdf` のパーサ実装には着手しない
- 残る2ファイル（`000359215.pdf` `000268896.pdf`、交通空白地有償運送）の原本取得・
  第1ページ構造確認は指示があるまで着手しない

## 2026-08-09 000230003.pdf パーサ実装・2PDF統合検証

`000230003.pdf`（福祉有償運送・市町村営、3ページ=3団体）のパーサ実装と、
`000271730.pdf` との2PDF統合検証を行った。**これはClaude Codeによる記録更新であり、
受入判定はCodexが実ファイル・差分・テスト・証拠を読み戻して行う。**

### やったこと

1. `src/parse.py` に `000230003.pdf` の登録番号ブロック抽出・複数事務所展開・
   `ownership` 空欄処理を実装し、`000271730.pdf` と統合して `data/operators.csv` /
   `data/vehicles.json` 等4ファイルを再生成した
2. `tests/test_verify.py` に2PDF統合向けのテスト（複合キー一意性、車両合計、
   複数事務所展開、`office_flags` など）を追加した
3. `tests/test_verify.py` の `RepresentativeNameNotOutputTest` の動的検査
   （`discover_name_scan_targets()`）を `data/` `raw/text/` に加え
   `src/**/*.py` `tests/**/*.py` `verification.md` `PROGRESS.md` `README.md` `SPEC.md`
   `CLAUDE.md` `evidence/**/*.txt` `evidence/**/*.md` まで拡張した
4. 拡張後の初回実行（13:43 JST）で `evidence/20260807_explore_words_edges.txt`
   （2026-08-07作成、rev.4.4の伏字化規定より前の座標ダンプ）に代表者氏名の伏字化漏れ
   （8行）を検出。編集許可を得たうえで伏字化し、行数798行・役職語・座標・行構造は
   変えずに実氏名の値のみ `[氏名-非出力]` に置換した
5. 再検証（14:00 JST）で `unittest discover -s tests` を再実行し、
   `verification.md` §5-1〜§5-8 と `evidence/20260809_000230003_parser_verification.txt`
   を実結果に合わせて更新した

### 確認できた事実（数字を含める。根拠は evidence/ のファイル名で示す）

- unittest: **21件中21件成功、exit 0**（初回13:43 JSTは20/21・exit 1、伏字化修正後の
  再検証14:00 JSTで21/21・exit 0。`evidence/20260809_000230003_parser_verification.txt` §2, §2.1）
- `git diff --check`: exit 0（同ファイル §7）
- `discover_name_scan_targets()`（自動テスト対象）: **25ファイル**、代表者実氏名候補の
  漏えい0件（同ファイル §3）
- Codex独立スキャン: 自動テスト対象25件に `run_record.md` を加えた**26ファイル**、
  漏えい0件（対象範囲がClaude Code側の自動テストと異なる点に注意）
- `raw/text/*.txt` 7ファイル: 各ファイルに `[氏名-非出力]` がちょうど1件
- `evidence/20260807_explore_words_edges.txt`: 変更8行、全8行が実氏名値から
  `[氏名-非出力]` への置換。全798行の行数、役職語、座標、構造は維持
- `operators.csv`: 7行（`000271730.pdf`=4行、`000230003.pdf`=3行）
- `vehicles.csv`: 23行（`000271730.pdf`=11行、`000230003.pdf`=12行）
- 車両実数合計23台、うち軽16台。`000230003.pdf` 単体は6台、うち軽4台
- `(source_pdf, registration_no)` は7件とも一意
- `registration_no` 単独の重複群は1つ（「中山福第3号」が `000271730.pdf`・
  `000230003.pdf` に1件ずつ、正常な重複）
- 既存 `000271730.pdf` 由来の `operators`/`vehicles` 行はHEAD版から不変
- CSV/JSONは文字列表現を正規化すると完全一致
- `000230003.pdf` の `ownership` は全行空、中山市福第4号（4事務所）の `office_seq` は
  1,2,3,4
- `flags`: 空4件、`office_mismatch` 2件、`office_notation_diff` 1件
- SPEC.md §5 の完了条件1〜8はすべて達成

### 遭遇して解決した不整合

- **旧市町村番号の表記ゆれ（前回セッションで未確認としていた事項）。** `000230003.pdf`
  の登録番号は「中山市福第N号」（実施主体が市の2件）と「中山福第N号」（実施主体が町の
  1件）が同一ファイル内に混在していた。パーサでは `authority_code` を「中山」で統一し、
  「市」の有無は登録番号文字列の表記差として保持する扱いとした
- **複合キーの必要性。** `registration_no` 単独では `000271730.pdf` と `000230003.pdf` に
  それぞれ「中山福第3号」が存在し重複するため、`(source_pdf, registration_no)` を
  複合キーとして一意性を担保する実装・テストとした
- **複数事務所の展開。** `000230003.pdf` の中山市福第4号は1団体4事務所で、
  `office_seq` 1..4として `vehicles.csv` に4行展開する必要があった。展開後も
  `office_name`/`office_location` の要素数・順序が上部表記と一致することをテストで確認した
- **既存証拠ファイルの氏名伏字化漏れと役職語の誤検知。** `evidence/20260807_explore_words_edges.txt`
  がrev.4.4（伏字化規定）より前に作成されており未伏字化だった点は上記のとおり修正した。
  あわせて、動的検査が「代表者」「理事長」等の役職語自体を氏名候補と誤検知しないよう、
  役職語を除いた氏名候補のみを検査する実装であることを確認した。役職語の出現は
  漏えいとして扱わない

### 未確認事項

- **この作業で新たに発生した未確認事項はない。** ただし、SPEC.md §9 の既存U1〜U6は
  引き続き未確認のまま残っており、この作業をもって解決済み扱いにするものではない
- 前回セッションで挙げていた登録番号表記ゆれ（「中山市福」/「中山福」）は、
  `000230003.pdf` 1件についてはパーサの `authority_code` 統一方針として上記のとおり
  対応した。ただし、これは `000230003.pdf` 単体で確認した扱いであり、
  未取得の `000359215.pdf` / `000268896.pdf` に一般化できるかは未確認である
- `000359215.pdf` / `000268896.pdf` は未取得のままであり、ページ数・ハッシュ・
  列構成（車両欄の列境界を含む）はいずれも未確認である

### 次にやること（安全な作業）

- **次の安全な作業は `000359215.pdf`（交通空白地有償運送）の原本取得・ハッシュ確認・
  ページ構造確認であり、パーサ展開とは同時に行わない。** Codexの次の一指示があるまで
  未着手とする
- 他県への展開・`run_record.md` の参照はいずれも指示があるまで着手しない

## 2026-08-09 I-3 公開状況ページの現況同期

### 訂正したこと

- `docs/status.html` の1/4 PDF、4団体・17台の県全体表示、残り3本未着手、U6未解決、
  旧「次にやること」を現在の受入済み事実へ更新した
- 4/4 PDF、23団体、90車種行、実車両136台、軽20台、I-1・I-2受入済みを表示した
- PDF処理と市町別ビューの受入はT1〜T4の達成ではないこと、I-3は状況ページ訂正までで
  止めること、push・外部提出を行わないことを明記した
- 現行SPEC §9に残る未確認事項をU2・U3・U7の3件へ同期した
- 市町別ビュー、データの限界、出典・利用条件へのリンクを追加した

### 検証結果

- 状況ページ用の自動テスト2件を追加し、全41テスト成功
- デスクトップ表示と390×844のモバイル表示を実機確認し、横方向のページはみ出しを0へ修正
- 状況ページから市町別ビューを実際にクリックし、19市町の新画面へ到達することを確認
- クエリなしのリンクでブラウザが旧「準備中」画面をキャッシュから返したため、
  `index.html?v=20260809`へ変更して再確認した
- ブラウザコンソールのerrorは0件
- 旧`docs/status.html`現物との`tools/spec_coverage.py`は合格。ただしHTMLは検査トークン0件のため、
  旧版現物、差分、自動テスト、リンク実操作、デスクトップ・モバイル目視を主証拠とした

### 区切り

I-3の受入とローカルコミットで今回の作業を止める。GTFS-JP統合、地図、経路検索、応募、push、
外部提出には着手していない。

## 2026-08-09 I-2 市町別の登録供給ビュー

### 実装したこと

- `docs/index.html` を「準備中」画面から、山口県19市町を選んで登録供給を確認できる静的画面へ更新した
- `src/build_site_data.py` を追加し、検証済みの `data/operators.json` と `data/vehicles.json` を
  公開用 `docs/data/` へ同一バイトで複製し、市町別集計 `municipal_supply.json` を決定論的に生成した
- 福祉有償運送と交通空白地有償運送を分離し、団体数、登録車両、軽自動車の内数、登録番号、
  実施主体、有効期間、原本PDFとページを追跡できるようにした
- 0件市町は「登録簿上の該当記録は0件」と表示し、交通手段や移動支援の不存在を意味しない旨を明記した
- 複数市町を運送区域に含む団体は各市町に再掲されるため、市町値を県計へ合算できない旨を明記した
- 山口県公式の19市町一覧を `evidence/20260809_yamaguchi_municipalities.txt` に保存した
- 公開画面も代表者氏名の自動検査対象へ追加した

### 検証結果

- 生成結果: 19市町、登録簿記録あり15市町、23団体。県全体の一意集計は136台、軽20台
- 0件市町は宇部市、防府市、山陽小野田市、平生町の4市町
- 独立固定値の下関市は5団体・49台・軽13台、山口市は2団体・7台・軽2台で一致した
- `python -m unittest discover -s tests -v`: 39件成功（既存30件とI-2の9件）
- ブラウザ実操作で山口市、宇部市、下関市を切り替え、件数・0件注記・種別内訳・原本リンクを確認した
- 390×844のモバイル表示で横方向のページはみ出しなし。実行時コンソールエラー0件
- 旧 `index.html` 現物との `tools/spec_coverage.py` は合格。ただしHTML旧版の検査トークンは0件のため、
  画面差分、集計テスト、ブラウザ実操作を主な受入証拠とした

### ゴールとの関係と次の安全な作業

I-2は、作品①を第三者が検証できる公開アプリへ進める中間成果であり、UDC2026の応募完了や受賞を
意味しない。`docs/status.html` には1/4 PDFなどの古い表示が残るため、次はこの公開済み画面を現状へ
同期させる。外部提出・pushは行っていない。

## 2026-08-09 `000268896.pdf` の実装・4PDF統合検証

### やったこと

Claude Codeが月次利用上限だったため、ユーザー了承の例外としてCodexが実装と記録更新を行った。
開始時点はSPEC.md rev.4.9のコミット`23ae6c2`で、仕様を先に固定した後、次を一作業として実施した。

1. `src/parse.py`へ`000268896.pdf`（交通空白地有償運送・NPO等）を追加した
2. 原本の登録種別コード`過`を保持し、車種は分析用`普通自動車`と原本見出し
   `普通自動車(軽）`を分離して保存した
3. 見出しが空の未使用車種列を値が無いことを確認して除外した
4. 4ページ目で`pdfplumber`が欠落させる合計セルを、同じ行・列の単語座標から復元した
5. 複数行の事務所セルは、上部表の名称・位置と正規化照合して境界を決めた
6. 市名を印字しない`向津具地区`だけ、国土地理院住所検索で確認済みの固定対応
   `長門市`を適用した。住所からの一般推測には広げていない
7. 4PDFを再処理し、CSV/JSON、伏字済みraw textを再生成した
8. `tests/test_verify.py`を4PDF・23登録へ拡張し、今回固有の座標復元、複数行事務所、
   原本車種見出し、登録コード、日付、市町対応を回帰テスト化した

### 実ファイルから確認した結果

- `000268896.pdf`: 4団体、4事務所、8行、実車両7台、軽2台
- 4PDF統合: **23団体、90行、実車両136台、軽20台**
- ファイル別団体数: 4 / 3 / 12 / 4、車両行数: 11 / 12 / 59 / 8
- 4ページ目の合計は2台・軽1台として復元され、普通自動車の明細2台・軽1台と一致した
- 今回4団体の上部表と車両欄の事務所差分は0件で、`flags`も全件空だった
- `raw/text/000268896_p1.txt`〜`p4.txt`を生成し、`[氏名-非出力]`は各1件だった
- 4PDF・23登録の氏名値全体を51対象ファイルへ動的照合し、一致0件だった
- `(source_pdf, registration_no)`は23件すべて一意。登録番号単独の重複は既知の
  `中山福第3号`だけで、異なる2ファイルに各1件存在する
- CSV/JSONはoperators 23行、vehicles 90行の全行・全列が一致した
- 全30テスト成功、`git diff --check`成功

### 完了条件1〜8の受入結果

SPEC.md §5の完了条件1〜8は4PDF統合でも全て合格した。団体数、車両内訳と団体合計、
`count_kei <= count`、登録番号、全件突合、日付順序、事務所差分、代表者氏名不出力を、
Codexが生成物と独立集計を読み戻して確認した。パーサの成功表示だけでは受け入れていない。

### 次にやること（安全な一作業）

- `README.md`の利用条件・処理状況を、原本の現物と4PDF統合値に基づき4/4へ訂正する
- README訂正の受入後、データ処理を目的化せず、作品①の利用者が何を判断できるかを定義して
  GTFS等との統合・静的アプリ設計へ進む
- 外部提出・pushは本人承認前に行わない

## 2026-08-09 `000359215.pdf` パーサ実装・3PDF統合検証

### やったこと

1. Codexが `raw/000359215.pdf`、全20ページ画像、取得・構造検査記録を読み戻して受け入れた
2. 国土交通省の公式通達に基づき、交通空白地有償運送の「セダン等」欄を
   `vehicle_type=普通自動車`、`vehicle_type_label=セダン等` とする方針をSPEC.md rev.4.5へ記載した
3. 上部表と車両欄を全26事務所で比較し、差がある8事務所を原本画像で再確認して
   SPEC.md rev.4.6へ列挙した。26−8の一致数を19とした算術誤記はrev.4.7で18へ訂正した
4. 既存2PDFの座標パーサを残し、`000359215.pdf` 専用の罫線表・複数ページ団体パーサを
   `src/parse.py` に追加した。登録番号があるページから次の登録番号直前までを1団体に束ね、
   ページ20の付録は原文テキストだけ保存してレコード生成から除外した
5. `tests/test_verify.py` に、12登録の開始・終了ページ、26事務所、59行、車両総数、
   車種対応、福祉対象フラグ0、付録除外、事務所差分8件、CSV/JSON一致を検証するテストを追加した
6. Claude Codeが利用上限のため、ユーザー了承の例外としてCodexが実装と記録更新を行った。
   Codexは実ファイル・差分・生成物・全テストを別々に読み戻して受入判定した

### 確認できた事実（実ファイルからの再集計）

- 今回分: **12団体、26事務所、59行、実車両106台、軽2台**
- 車種別の実車両: `普通自動車` 59台、`バス` 47台。各事務所の`合計`行26行も
  106台・軽2台となり、明細と一致した（合計行は実車両数へ二重加算しない）
- 3PDF統合: **19団体、82行、実車両129台、軽18台**
- 登録開始ページは1・2・3・8・9・11・12・13・14・17・18・19。
  複数ページ団体の最終ページは7・10・16を含め、車両行の`source_page`へ保持した
- 事務所差分はSPEC.md §3.6の**8件だけ**。団体フラグは
  `office_mismatch` 4団体、`office_notation_diff` 2団体、残る6団体は空で一致した
- `raw/text/000359215_p1.txt`〜`p20.txt`の20ファイルを生成した。登録開始12ページは
  `[氏名-非出力]` が各1件、継続7ページと付録1ページは0件で、代表者欄の無いページへ
  偽の伏字を追加していない
- 代表者氏名は3PDF・19登録から役職語を除いた**氏名値全体**を動的取得し、空白を正規化して
  46対象ファイルを照合した結果、実氏名値の一致は0件だった。初回テストの1件失敗は、
  姓または名だけの2文字が地名等に偶然一致した誤検知で、氏名全体の一致は0件と診断した。
  仕様の「氏名値」を検査するよう修正し、失敗時にも実名を表示しない性質を維持した
- 全25テスト成功（Python 3.12.13、pdfplumber 0.11.9）。`git diff --check` も成功した
- SPEC rev.4.7は旧版rev.4.6の現物と`tools/spec_coverage.py`で比較し、見出し40/40、
  識別子178/178、表の1列目105/105、コードブロック行51/51、説明なし欠落0で合格した

### 解決した確認事項

- Q1: 交通空白地側の車種は公式通達と原本見出しを併記して確定した。推測扱いではない
- Q2: 上部・車両欄の名称と位置を別々に原文保持し、原本順に対応させて差分フラグを付けた
- Q3: 有効期間の一般則は推定せず、各登録簿に印字された日付だけを抽出した
- Q4: 民間事業者に関する原文は専用欄へ保持し、事務所との制度的関係は推定していない

### 次にやること（安全な作業）

- 次の一作業は、未処理の `000268896.pdf` を公式一覧から取得し、ハッシュ・ページ数・全ページ画像・
  登録単位・表構造を確認してevidenceへ記録すること。パーサ実装はその受入後の別作業とする
- README.mdの処理状況は、今回の実装コミットを受け入れた後に3/4へ更新する。
  外部提出・pushは本人承認前に行わない

## 2026-08-09 `000268896.pdf`（交通空白地有償運送・NPO等）の取得・全ページ構造検証

### やったこと

1. 中国運輸局の公式一覧をShift_JISとして読み、山口県行の4リンク順から
   `000268896.pdf` が交通空白地有償運送・NPO等の原本であることを確認した
2. 公式URLから `raw/000268896.pdf` を取得し、HTTPヘッダー、サイズ、MD5、SHA256、
   PDF先頭、暗号化有無、ページ数を確認した
3. 全4ページを200dpi PNGへレンダーして原寸目視し、4ページ目の車両合計欄は400dpiでも再確認した
4. 罫線表、語座標、上部表と車両欄の事務所文字列を機械比較した。代表者氏名の値は
   テキスト証拠・標準出力・コードへ転記していない
5. 数字と構造差を `evidence/20260809_000268896_inspection.txt` に記録し、
   全4ページ画像を `evidence/20260809_000268896_page1.png`〜`page4.png` に保存した

### 確認できた事実

- HTTP 200、Content-Type `application/pdf`、149313バイト、PDF 1.6、暗号化なし、4ページ
- MD5 `83a60f022276cb26c6967bc85b947f67` はETagと一致、
  SHA256 `ece80864a99fc392dec0c279fe92f3609a43c11ce32178ced0cd60839c9b6177`
- 全4ページが各1登録・1事務所。継続ページと付録はない
- 登録番号は `中山過第1号`、`中山過第2号`、`中山交第2号`、`中山交第4号`。
  前2件の`過`は原本印字であり、確認なしに`交`へ置換しない
- 車両は全件`普通自動車`で2+2+1+2=**7台**、軽内数は1+0+0+1=**2台**。
  実装後の今回分期待値は4団体、4事務所、明細4行+合計4行=8行
- 3PDF統合値に加えた4PDF期待値は**23団体、90行、実車両136台、軽20台**
- 上部表の事務所名称+位置と車両欄の結合文字列は、空白・全半角を吸収すると4ページ全件一致。
  4ページ目だけ名称が2行に折り返されるため、先頭1行を名称とする既存ロジックでは偽差分になる
- 4ページ目は印字上の合計2・軽1を400dpiで確認したが、右罫線形状のため`table.extract()`では
  合計セルだけ空になる。座標上の合計2はx0=513.48に存在し、明細2・軽1とも一致する
- 2ページ目の運送区域は地区名だけで市名を印字しない。団体住所・事務所位置は長門市を明記するが、
  `service_area_municipalities`への反映は個別根拠をSPECへ書くまで推定で埋めない

### 実装前にSPECへ固定する構造差

- 登録番号の`service_type_code=過`を原文どおり受け付ける
- 車種見出し`普通自動車`をそのまま採用し、見出しが空の未使用列を無視する
- 4ページ目の合計セルを座標フォールバックまたは明細導出+印字照合で7台・軽2台へ確定する
- 複数行事務所名を上部表の名称接頭辞で分割し、原本上の事務所差分0件を期待値にする
- 2ページ目の市町対応を個別規則にするか空欄にするかを、推測と確認済み事実を分けて決定する

### 次にやること（安全な作業）

- 次の一作業は、CodexがSPEC.md rev.4.7の物理ファイルを旧版として保存し、今回の期待値と
  構造差を新revへ追記すること。`tools/spec_coverage.py`の合格出力を提示する
- パーサ実装、READMEの4/4更新、pushはそれぞれ後続の別作業とする

## 2026-08-09: `000359215.pdf`（交通空白地有償運送・市町村営）の原本取得と全ページ構造検証

### やったこと

- 一覧HTML `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html` で確認済みのリンク
  `https://wwwtb.mlit.go.jp/chugoku/content/000359215.pdf` から `curl` で原本を取得し、
  `raw/000359215.pdf` にバイナリのまま保存した（リダイレクトなし、初回応答で200）
- ファイル実在・先頭バイト・サイズ・MD5・SHA256・PDFバージョン・暗号化有無・ページ数を確認した
- `pdfplumber` で全20ページの `extract_text()` を取得し、`pdftoppm` で全20ページを150dpiで
  PNGにレンダリングして目視確認した。周南市エントリーの1マスのみ400dpiに拡大して再確認した
- パーサ実装は行っていない（今回のスコープ外）
- 証拠は `evidence/20260809_000359215_inspection.txt` に、代表者氏名を伏せて記録した
  （ファイル内に実氏名が含まれていないことを `grep` で確認済み）

### 確認できた事実（数字は `evidence/20260809_000359215_inspection.txt` に同じ）

- 取得: HTTP 200、Content-Type: application/pdf、Content-Length: 393922、
  Last-Modified: Fri, 07 Aug 2026 04:43:12 GMT、ETag: "d1f464e5e8fd89f6501c7e037c369912"
- ファイル: 393922バイト、先頭 `%PDF-1.7`、MD5 `d1f464e5e8fd89f6501c7e037c369912`
  （ETagと一致）、SHA256 `3107e8555aefe86527d482422977c050b4514cb06b0f077187cdec4f38a2c66b`、
  暗号化なし、20ページ
- 登録簿エントリーは12件（ページ1〜19）、ページ20のみ登録簿ではない付録表
  （輸送人員実績報告提出状況、R4〜R7年度・市町別）
- 車両欄の列構成は全ページ共通で「寝台車(軽)/車いす車(軽)/兼用車(軽)/回転シート車(軽)/
  セダン等(軽)/バス/合計(軽)」の7列（`extract_text()` の行分割順とは一致しないことを画像で確認済み）
- 車両合計を明示行またはページ内訳の手集計で足し上げると: 上関町2、周防大島町3、岩国市33、
  和木町3、下関市29、光市2、山口市6、阿武町4、周南市8（明示合計行なし・手集計）、下松市2、
  美祢市11（明示合計行なし・手集計）、田布施町3。12件合計は106、軽内数は2
  （山口市1・周南市1。軽は内数なので106へ加算しない）
- 登録番号の接頭辞は「中山市交第◯号」（10件）と「中山交第◯号」（美祢市・田布施町の2件、
  いずれもR5.10.1以降の新しい登録）の2種類が混在している
- **12登録がページ1〜19を使い、1ページ1団体ではない。** 岩国市はページ3〜7、下関市は
  ページ9〜10、周南市はページ14〜16に改ページし、ページ20は登録簿ではない付録である

### 確認済みの構造差と、残る未確認事項

- 旧見出し「SPECとの不整合または新規未確認事項（U1〜U8として`evidence`ファイル§5に記載）」は、
  既存SPECで解決済みの表記規則まで未確認へ戻していたため、確認済み事項と真に残る4件へ再分類した
- **登録番号の2体系は既存SPEC.md §3.4(b)で規定済み。** `中山市交` は旧市町村運営番号、
  `中山交` は現行番号として、いずれも `authority_code=中山`、`service_type_code=交` とする。
  原文は保持し、全角数字・空白だけを正規化して旧体系の `市` は削除しない。ページ9の全角数字も
  意味の未確認事項ではなく既定の表記正規化で扱う
- **改ページを前提にする。** 次パーサは登録番号があるページから次の登録番号の直前までを1団体として
  束ね、ページ20付録を除外する。既存の1ページ1団体ロジックを流用しない
- **光市の2種別欄併記、周南市の括弧なし0、周南市・美祢市の合計行欠落は確認済みの構造差。**
  光市は○印と値のある交通空白地側を座標で選び、括弧なし0は値0として扱う。合計行が無い団体は
  事務所内訳から合計を導出し、明示合計がある団体だけ照合する。理由の解明を実装ゲートにしない
- **事務所対応の不一致（下関市、Q2）。**
  見出し表（事務所の名称及び位置）と車両欄表（事務所の名称／事務所の位置）とで、同じ事務所の
  はずが課名（「地域政策課」対「地域振興課」）・住所（「下岡枝1480番地1」対「田部734番地1」）
  が食い違う。400dpiで2回確認しており抽出誤りではなく原本の記載そのものが2表で異なる
- 今回の実装・作品解釈に残る未確認事項は**4件**。Q1=`普通自動車` と `セダン等` の対応
  （既存SPEC §9 U5、公式通達確認が必要）、Q2=下関市の上部・車両欄の両事務所情報をどう保持するか
  （CodexのSPEC方針が必要）、Q3=新規登録の有効期間の一般則（田布施町の約2年例は確認済みだが
  一般化根拠が未確認）、Q4=美祢市の民間タクシー会社2社を事務所として記載する制度上の意味
  （原文保持は可能でパーサ停止条件ではないが、作品の説明・分類に影響）
- 今回の4件とは別に、未取得 `000268896.pdf` のページ数・ハッシュ・列構成、SPEC §9 U2の
  事務所一覧自体の改ページ例、U4の自動運行・特定自動運行、U6の利用規約適用範囲は未確認のまま残る
- `SPEC.md` は今回書き換えていない。Q1〜Q4のうち実装に先立つ仕様判断はCodexが別作業で行う

### 次にやること（安全な作業）

- 次はCodexが `raw/000359215.pdf` と `evidence/20260809_000359215_inspection.txt`、
  全20ページのPNGを読み戻して受入判定すること。Q1〜Q4について、公式通達を確認するか、
  原文を損なわない保持方針をSPECに定めるかの判断もCodex側で行う
- パーサ実装・既存データへの統合は、この受入判定の後の別作業とする。Claude Codeは
  次の一指示があるまで着手しない
- 他県への展開・`run_record.md` の参照はいずれも指示があるまで着手しない

## 2026-08-09 GTFS-1 山口県内GTFS/GTFS-JPの公式一次資料と対象範囲の棚卸し

### やったこと

- 作業開始前に `git status --short --branch` と `git log -1 --oneline` を確認し、
  main / HEAD `fae504a` / origin/main `b281540` から24コミット先行 / 作業ツリーclean の
  期待状態と一致することを確認した
- `CLAUDE.md` 全文、`SPEC.md` §0・§1・§1.5・§2〜§7（横断的に）・§8・§9を読んだ
- `run_record.md` は読み書きしていない
- 山口県19市町（`evidence/20260809_yamaguchi_municipalities.txt`）を母集団に、
  GTFS/GTFS-JPの公式一次資料を市町別に確認した。GTFSのダウンロード・統合・可視化は行っていない
- 候補発見にはWebSearch、および第三者まとめサイト bustime.jp・bus-routes.net の生ページを
  使い、確認根拠には使っていない。gtfs-data.jp はReactのSPAでサーバ側HTMLに一覧が含まれず、
  robots.txt・sitemap.xmlも手掛かりがなかったため3手で打ち切った
- 確認の一次資料は、山口県公式オープンデータカタログサイト（https://yamaguchi-opendata.jp/ ）と
  公共交通オープンデータセンター（ODPT, https://ckan.odpt.org/ ）、および個別の市町・事業者公式
  サイトに限定した。すべて `curl -sL` / `curl -sI` で生バイトを取得し、WebFetchの要約は
  候補発見にのみ使い確認根拠にしていない
- GTFS本体ZIPは `curl -sI` のHEADリクエストのみで到達確認し、本体はダウンロードしていない

### 確認できた事実（件数を含める）

- 公式一次資料で確認できたフィードは3件。内訳は山口県オープンデータカタログ2件
  （岩国市、光市）、ODPT 1件（船木鉄道株式会社、対象は宇部市・山陽小野田市・美祢市）
- フィードA（岩国市）・フィードB（光市）は、データセット説明文に公式のデータ基準日
  「令和８年４月１日時点」（2026年4月1日）が明記されている。これはカタログの最終更新日／
  HTTP Last-Modified（岩国市2026-04-10、光市2026-03-03）とも、GTFS内部の有効期間
  （ZIP未展開のため未確認）とも別の日付であり、3種類を混同していない
- フィードC（船木鉄道）は最新リソースに公式記載の有効期間「2025年11月17日〜2026年11月16日」
  がある。認証キーを含む公式ダウンロードURL雛形
  （https://api.odpt.org/api/v4/files/odpt/SentetsuBus/AllLines.zip?date=20251117&acl:consumerKey=[…]）
  は確認したが、開発者サイトでのアカウント登録・トークン取得を伴う認証済みダウンロードと
  ファイル本体そのものへの到達は未実施
- GTFS本体ZIPへのHEADリクエストによる到達確認は、フィードA・フィードBの2件のみで実施した。
  フィードCはURL雛形と有効期間の記載を確認したにとどまり、HEAD確認はしていない
- 公式GTFSを確認できた市は5市（岩国市・光市・宇部市・美祢市・山陽小野田市）。
  うち宇部市・美祢市・山陽小野田市の3市は、市自身の公式データセットには
  「データを保有していない」「公開していない」という趣旨の明記があり、市が自ら公開する
  GTFSではなく、市内で運行する船木鉄道（船鉄バス）が公式に公開するGTFSが対象地域として
  これら3市を明記している、という別々の事実として確認した
- 対象範囲未確認とした市町は0
- 今回確認した公式資料の範囲では確認できなかった市町は14
  （下関市・山口市・萩市・防府市・下松市・長門市・柳井市・周南市・周防大島町・和木町・
  上関町・田布施町・平生町・阿武町）
- 「見つからない」ではなく市町自身が公式に「データを保有していない」「事業を実施していない」と
  明記していた例が6件: 山口市（データセット`busdata`）、防府市（`352063-bus_information`）、
  柳井市（`352128_bus`）、美祢市（`352136_gtfs-jp`、GTFS-JPを名指しで非公開と明記）、
  田布施町（`33`、町営バス事業自体が無い旨）、平生町（`353442_gtfs-jp`、同）
- 根拠は `evidence/20260809_gtfs_yamaguchi_inventory.txt` にまとめ、個別の生出力69件を
  `evidence/20260809_gtfs_source_*.txt` として保存した

### 詰まったこと、SPEC.md と食い違ったこと

- 今回はSPEC.mdのスコープ外（I-1とは別のGTFS棚卸し作業）のため、SPEC.mdとの食い違いは無い
- gtfs-data.jp はSPA構成のためcurlの生バイト取得だけでは一覧を得られず、候補発見の経路として
  使えなかった。WebFetchでの要約取得も試したが同様に内容を得られなかった
  （`evidence/20260809_gtfs_source_gtfsdatajp_*.txt`）
- 作業中に一度、`curl -o` の出力先をリポジトリ外（`/tmp_check.html` 相当のパス）に向けて
  実行しかけた。実際にはサンドボックスの許可分類器に拒否され、書き込みは発生しなかった
  （該当パスに生成物が無いことを確認済み）。以後はすべて `evidence/` 配下への出力に限定した
- `curl -sI` のHEADレスポンスに `Set-cookie` ヘッダが含まれていたため、
  `evidence/20260809_gtfs_source_zip_head_check.txt` からCookie値を削除して保存し直した
  （CLAUDE.mdの「代表者氏名、認証情報、Cookieなどは保存しない」に基づく処置）

### 今回はGTFS統合や作品機能の実装をしていないこと

- GTFSデータの本体ダウンロード・パース・GTFS-JP検証・可視化はいずれも行っていない
- `docs/` `src/` `data/` `tests/` `SPEC.md` `CLAUDE.md` `run_record.md` はいずれも変更していない

### 次にやること

- 次はCodexが `evidence/20260809_gtfs_yamaguchi_inventory.txt` と個別の
  `evidence/20260809_gtfs_source_*.txt` を読み戻して受入判定すること
- 未確認事項（inventory §5のU-GTFS-1〜5）のうち、フィードCは認証キーを含む公式ダウンロードURL
  雛形までは確認済みだが、実際の認証済みダウンロード・ファイル本体への到達にはODPT開発者サイトでの
  アカウント登録・トークン取得が必要で、今回の作業指示（アカウント作成禁止）の範囲では対応できない。
  次の作業でどう扱うかはCodex側の判断が必要
- GTFSの整備状況・鮮度を作品①の指標にどう落とし込むかの仕様化は、この作業のスコープ外であり、
  次の仕様決定を待つ
- 他県への展開・`run_record.md` の参照はいずれも指示があるまで着手しない

## 2026-08-09 I-4 山口県19市町のGTFS/GTFS-JP公式状況表示

### やったこと

- 作業開始前に `pwd`・`git status --short --branch`・`git log -1 --oneline` を確認し、
  main / HEAD `fae504a` / origin/main `b281540` から24コミット先行、PROGRESS.mdと
  evidence/配下・Codex改訂のSPEC.md・run_record.mdが未コミットという期待状態と一致することを
  確認した。既存の未コミット変更（SPEC.md・run_record.md）は今回も読むだけで書き換えていない
- `CLAUDE.md` 全文、`SPEC.md` 全文（特に §12）、`evidence/20260809_gtfs_yamaguchi_inventory.txt`、
  現在の `docs/index.html`・`src/build_site_data.py`・`tests/test_site.py` を読んだ。
  `run_record.md` は読んでいない
- `SPEC.md` §12.4 の `municipality_code`（既存の公式19市町一覧の自治体コード）が
  リポジトリ内のどこにも定義されていなかったため、推定で埋めずに総務省
  「都道府県コード及び市区町村コード」（令和6年1月1日更新、PDF）を新たに一次資料として取得し、
  山口県19市町全件のコードを確認した。証拠は
  `evidence/20260809_i4_soumu_local_gov_code_list.pdf` と同 `_inspection.txt`
- `src/build_gtfs_status.py` を新設し、`data/gtfs_feeds.csv`・`data/municipality_gtfs.csv`と
  対応する `data/*.json`・`docs/data/*.json`（`data/`と同一バイト）を生成するようにした。
  値はすべて `evidence/20260809_gtfs_yamaguchi_inventory.txt` と総務省コード一覧からの
  手入力（転記）で、新規のGTFS調査・推測は行っていない
- `docs/index.html` を、GTFS-1-CORR-1完了報告の前に保存した現物
  （`evidence/20260809_i4_index_before.html`）から改訂した。既存の市町セレクタ・登録供給表示・
  出典・注意書きは変更せず、県全体のGTFS概要パネルと市町別のGTFS/GTFS-JP公式確認状況カード
  （軸A・B・C、フィード詳細、断定しない注記）を追加した
- `tests/test_site.py` に `GtfsStatusDataTest`（12件）と `IndexHtmlContractTest` への追加2件
  （禁止表現不在・GTFS要素の存在）を追加し、`setUpModule()` が `build_gtfs_status.main()` も
  呼ぶようにした
- `tools/spec_coverage.py` で `evidence/20260809_i4_index_before.html` と `docs/index.html` を
  比較し、合格出力を `evidence/20260809_i4_spec_coverage_index_before_vs_after.txt` に保存した
- ブラウザでの実操作検証は、`python -m http.server` で `docs/` を配信し、
  `msedge --headless=new` を Chrome DevTools Protocol 経由で Node.js から操作して行った。
  npmパッケージのインストールは行わず、Node組み込みの `WebSocket` のみを使った
  （リポジトリ外への書き込みを避けるため）。証拠は `evidence/20260809_i4_browser_verification.txt`、
  `evidence/20260809_i4_cdp_raw_output.json`、`evidence/20260809_i4_screenshot_*.png`
- `verification.md` に自動検証・spec_coverage出力・ブラウザ実操作結果・完了条件チェック表を追記した

### 確認できた事実（件数を含める）

- `data/gtfs_feeds.csv` は3行（岩国市・光市・船木鉄道株式会社）、`data/municipality_gtfs.csv` は
  公式順19行。`feed_id`・`municipality_code` はそれぞれ一意
- `availability_status` の内訳: `confirmed`=5（岩国市・光市・宇部市・美祢市・山陽小野田市）、
  `not_confirmed_in_checked_sources`=14、`unassessed`=0
- 3フィードと5市町の対応に重複はなく、船木鉄道1フィードが宇部市・美祢市・山陽小野田市の
  3市に関連付いている（3フィードとして二重計上していないことを自動テストで検査）
- `python -m unittest discover -s tests -v`: `Ran 55 tests`、`OK`
  （既存41件は退行なし、新規14件すべて成功）
- `tools/spec_coverage.py evidence/20260809_i4_index_before.html docs/index.html`:
  識別子12/12が新版に存在、欠落・説明なし0件、判定「合格」、終了コード0
- ブラウザ実操作: デスクトップ1280×900・モバイル390×844の2幅×7市町=14通り全てで、
  横方向はみ出し0（`scrollWidth - clientWidth = 0`）、コンソールのエラー・例外イベント0件
- `git status --short` で変更が確認できたのは `PROGRESS.md`・`verification.md`・
  `docs/index.html`・`tests/test_site.py`（いずれも今回の許可範囲）と、
  今回作成した `src/build_gtfs_status.py`・`data/gtfs_feeds.*`・`data/municipality_gtfs.*`・
  `docs/data/gtfs_feeds.json`・`docs/data/municipality_gtfs.json`・`evidence/20260809_i4_*`。
  `SPEC.md`・`run_record.md` の変更はCodex側の既存差分のままで、今回追加の変更はない
- `git diff --check`: 追跡対象への差分（今回変更したテキストファイル）で終了コード0

### 詰まったこと、SPEC.md と食い違ったこと

- ブラウザ自動操作ツール（chromium-cli等）がこの環境に無く、`npx playwright` は
  ブラウザ本体が未キャッシュのバージョンを要求した。npmパッケージをリポジトリ内外どちらに
  インストールしてもCLAUDE.mdの制約（リポジトリ外への書き込み禁止／今回変更可能なファイルの
  範囲限定）に抵触する可能性があったため、システムに既にあった `msedge.exe` を
  `--headless=new --remote-debugging-port` で直接起動し、Node.js組み込みの `WebSocket` で
  Chrome DevTools Protocolを直接叩く方式にした。npm installは一度も行っていない
- GTFS-1調査時に和木町の公式カタログ組織IDを`organization/35304`と根拠のない推測で試したところ
  404で、当時は「和木町という登録組織自体が見当たらない」と`evidence/20260809_gtfs_yamaguchi_inventory.txt`
  §4に記録していた。総務省の一次資料（`evidence/20260809_i4_soumu_local_gov_code_list.pdf`）で
  確認した和木町の6桁地方公共団体コードは353213であり、山口県オープンデータカタログサイトの
  正しいCKAN組織IDはその先頭5桁と一致する`35321`だった（35304は誤った候補URL）。
  この誤りは2026-08-10のGTFS-1-CORR-2作業でinventory §4・新設§7を訂正済みで、正しい組織
  `35321`には21件のデータセットが登録されており、名称・説明にGTFSの記載が無いことも
  `evidence/20260809_gtfs_corr2_waki_org_35321_p1.txt`・同`_p2.txt`で確認した
  （「今回確認した公式資料の範囲ではGTFS未確認」という結論自体は変えていない）。
  GTFS-1の生証拠 `evidence/20260809_gtfs_source_ydata_org_35304.txt`（誤った候補URLの404という
  取得時の現物）は今回も意図的に変更していない。今回新規に作った `data/municipality_gtfs.csv` の
  和木町の行は、当初から総務省一次資料の正しいコード353213を使っており、誤ったコードを
  引き継いでいない
- SPEC.mdとの食い違いは無い

### 次にやること

- Codexが `src/build_gtfs_status.py`・`data/gtfs_feeds.*`・`data/municipality_gtfs.*`・
  `docs/index.html`・`tests/test_site.py`・`verification.md` の実ファイルと差分、
  `evidence/20260809_i4_*` の証拠、テスト結果、画面を読み戻して受入判定すること
- 船木鉄道のGTFS本体への認証済みアクセス（ODPT開発者登録・トークン取得）をどう扱うかは
  Codex側の判断が必要
- 他県への展開・`run_record.md` の参照はいずれも指示があるまで着手しない
- commit・push・GitHub Pages公開・外部提出はいずれも行っていない

## 2026-08-10 I-5 READMEと状況ページの現況説明同期

### やったこと

- `SPEC.md` §13、`run_record.md` §0.5・§4のI-5部分を読み、完了条件・変更可能範囲を確認した
- `README.md`と`docs/status.html`の現物を、それぞれ`evidence/20260810_i5_readme_before.md`・
  `evidence/20260810_i5_status_before.html`へ保存してから改訂を開始した
- `README.md`を改訂: 「自動テスト: 30件成功」の固定値を除き、「現在の受入状況（I-1〜I-4）」節と
  「公式GTFS/GTFS-JPの確認状況」節を新設。4PDF・23団体・90行・136台・軽20台は保持したまま、
  GTFS公式フィード3件・対象市町名確認5/19・未確認14/19を追加し、「収録データ」節を
  「収録データ・利用画面」に改めて`data/gtfs_feeds.*`・`data/municipality_gtfs.*`・
  `docs/index.html`・`docs/status.html`を案内した。「公開」節にこのリポジトリ・ローカル実装を
  UDC応募・公開・受賞の完了として扱わない旨とT1〜T4未達を明記した
- `docs/status.html`を改訂: 「I-1 / I-2」「41テスト」「7工程中4工程」「I-3受入後に一つだけ
  再評価」「I-3はここまでで止める」を除いた。ロードマップにGTFS確認状況の工程を追加して
  8工程中5工程受入済みへ更新し、I-1〜I-4それぞれの受入済み成果をチェックリストで表示。
  4PDF集計とは別集計であることを明記した新設「公式GTFS/GTFS-JPの確認状況」セクションで
  3件・5/19・14/19を、率や達成度の指標と呼ばず、GTFS不存在とも断定せずに表示した。
  T1〜T3（自力終了条件）とT4（成果条件）を分離し、4件とも「未達」バッジで表示する新設
  セクションを追加し、締切日は主表示ではなくセクション末尾の参考情報に留めた。
  `docs/index.html`・公開JSON5点（operators/vehicles/municipal_supply/gtfs_feeds/
  municipality_gtfs）・国土交通省利用ルールへの実在リンクを設置した
- `tests/test_site.py`にI-5用テスト12件を追加: `StatusHtmlContractTest`6件、
  `ReadmeContractTest`6件（新設クラス）。既存の`test_current_status_scope_and_main_view_link_are_present`は
  新しい現況表示に合わせて内容を更新し、`test_stale_progress_claims_are_absent`に
  I-3受入時点の古い表示5点を追加した
- `verification.md`にI-5検証結果の節を追記した（自動検証・spec_coverage出力・
  ブラウザ実操作・完了条件チェック表）
- 旧新版比較として`tools/spec_coverage.py`をREADME.md・docs/status.htmlそれぞれに実行し、
  全出力を`evidence/20260810_i5_spec_coverage_readme.txt`・
  `evidence/20260810_i5_spec_coverage_status.txt`へ保存した
- ブラウザでの実操作検証は、`python -m http.server 8793`で`docs/`を配信し、
  `msedge --headless=new`をChrome DevTools Protocol経由でNode.jsから操作して行った。
  npmパッケージのインストールは行わず、Node組み込みの`WebSocket`のみを使った。
  証拠は`evidence/20260810_i5_browser_verification.txt`、
  `evidence/20260810_i5_cdp_raw_output.json`、`evidence/20260810_i5_screenshot_*.png`

### 確認できた事実（件数を含める）

- `python -m unittest discover -s tests -v`: `Ran 67 tests`、`OK`
  （既存55件は退行なし、新規12件すべて成功）
- `tools/spec_coverage.py evidence/20260810_i5_readme_before.md README.md`:
  見出し10/10・識別子20/20・コードブロック行1/1が新版に存在、欠落・説明なし0件、
  判定「合格」、終了コード0
- `tools/spec_coverage.py evidence/20260810_i5_status_before.html docs/status.html`:
  検査トークン数0件（HTML本文にmarkdown見出し・バックティック識別子が無いため）、
  欠落・説明なし0件、判定「合格」、終了コード0。I-3受入検証時と同じ理由で、この出力単独は
  十分な証拠とせず、67テスト・ブラウザ実操作・目視差分を併用した
- ブラウザ実操作: デスクトップ1280×900・モバイル390×844の画面表示2通りと、
  `docs/index.html`・公開JSON4点への実クリック遷移5通り、計7通り全てで横方向はみ出し0
  （`scrollWidth - clientWidth = 0`）、コンソールのエラー・例外イベント0件。
  クリック遷移先のJSON配列長は`gtfs_feeds.json`=3、`municipality_gtfs.json`=19、
  `operators.json`=23、`vehicles.json`=90で、いずれもビルド済みデータの実測値と一致した
- `git status --short`で変更が確認できたのは`README.md`・`docs/status.html`・
  `tests/test_site.py`・`PROGRESS.md`・`verification.md`（いずれも今回の許可範囲）と、
  今回新規作成した`evidence/20260810_i5_*`のみ。`SPEC.md`・`run_record.md`・`docs/index.html`は
  今回のI-5作業では変更していない（既存差分はCodex側または前回I-4作業のもの）
- `git diff --check`: 終了コード0

### 詰まったこと、SPEC.md と食い違ったこと

- `tools/spec_coverage.py`の標準出力をそのまま`>`でファイルへリダイレクトしたところ、
  Windowsのコンソール既定符号化（cp932）で出力され、日本語部分が文字化けした状態で
  保存されてしまった。`PYTHONUTF8=1 PYTHONIOENCODING=utf-8`を指定して再実行し、
  UTF-8で読める状態の`evidence/20260810_i5_spec_coverage_readme.txt`・
  `evidence/20260810_i5_spec_coverage_status.txt`に保存し直した
- README.mdの初稿で「5 / 19は整備率・網羅率・交通カバー率ではない」という否定文で
  禁止語をそのまま使ってしまい、自作のテスト（禁止語の完全不在を検査）に失敗した。
  `docs/index.html`が実際に採用している言い回し（「交通の充足度や達成度を測る指標では
  ない」等、禁止語自体を使わない表現）に合わせて書き直し、解消した
- SPEC.mdとの食い違いは無い

### 次にやること

- Codexが`README.md`・`docs/status.html`・`tests/test_site.py`・`verification.md`の
  実ファイルと差分、`evidence/20260810_i5_*`の証拠、67テストの結果、
  `tools/spec_coverage.py`の出力、PC・スマホ・リンク操作の結果を読み戻して受入判定すること
- 次の作品機能の選定・実装、GTFS本体取得・解析、地図・経路検索、登録簿4PDFの再処理・再集計、
  作品②、他県展開、UDC応募・外部公開はいずれも指示があるまで着手しない
- commit・push・GitHub Pages公開・外部提出はいずれも行っていない

## 2026-08-10 I-5-CORR-1 READMEの旧テスト件数残存とテスト漏れの訂正

### やったこと

- 編集前の現物を`evidence/20260810_i5_corr1_readme_before.md`・
  `evidence/20260810_i5_corr1_test_site_before.py`へ保存してから訂正を開始した
- `README.md`の「改訂点」節にあった「「自動テスト: 30件成功」という固定値を除き、
  I-5完了時点の実測値（67件成功）に置き換えた。30件はI-1（4PDF統合）時点の値で、
  I-2〜I-4で追加したテストを反映していなかった。」という記述を、旧件数「30」を再掲しない
  文章（「自動テスト件数の固定値をI-5完了時点の実測値へ同期した。旧版の値はI-1（4PDF統合）
  時点のものでしかなく、I-2〜I-4で追加したテストを反映していなかった。」）へ直した
  （README.md 154〜155行目）。I-5完了報告時の「30テスト」1語だけの検査では、
  この「自動テスト: 30件成功」「30件」という改訂点内の引用を見落としていた
- `tests/test_site.py`の`ReadmeContractTest.test_stale_fixed_test_count_is_absent`を強化し、
  「30テスト」「30テスト成功」「30件成功」「自動テスト: 30」の4パターンを個別に検査するよう
  拡張した。加えて、全角数字・空白を正規化（NFKC＋空白除去）したテキストに対しても
  「30(件|本|個)?のテスト」「テスト…30(件|本|個)」「自動テスト:30」の3パターンを
  正規表現で検査し、表記ゆれでの再発を防ぐようにした。テストメソッドは追加せず、
  既存メソッドの中身を強化した（テスト総数は67件のまま）
- `tools/spec_coverage.py`を、`evidence/20260810_i5_readme_before.md`（I-5開始前の現物）と
  最終`README.md`、`evidence/20260810_i5_corr1_readme_before.md`（今回の訂正前の現物）と
  最終`README.md`の2通りで実行し、それぞれの全出力を`evidence/20260810_i5_spec_coverage_readme.txt`・
  `evidence/20260810_i5_corr1_spec_coverage_readme.txt`へ保存した
- `verification.md`にI-5-CORR-1の検証結果を追記した

### 確認できた事実（件数を含める）

- README.md内の「30テスト」「30件成功」「自動テスト: 30」「30テスト成功」の検索件数は、
  訂正後いずれも0件（`grep -n`で確認）
- `python -m unittest discover -s tests -v`: `Ran 67 tests`、`OK`（テストメソッド数は
  `tests/test_site.py`37件＋`tests/test_verify.py`30件で訂正前と変わらず、67件のまま一致）
- `tools/spec_coverage.py evidence/20260810_i5_readme_before.md README.md`:
  見出し10/10・識別子20/20・コードブロック行1/1が新版に存在、欠落・説明なし0件、
  判定「合格」、終了コード0
- `tools/spec_coverage.py evidence/20260810_i5_corr1_readme_before.md README.md`:
  見出し12/12・識別子30/30・コードブロック行1/1が新版に存在、欠落・説明なし0件、
  判定「合格」、終了コード0
- `git diff --check`: 終了コード0
- `git status --short`で変更が確認できたのは`README.md`・`tests/test_site.py`・
  `PROGRESS.md`・`verification.md`（いずれも今回の許可範囲）と、今回新規作成した
  `evidence/20260810_i5_corr1_readme_before.md`・`evidence/20260810_i5_corr1_test_site_before.py`・
  `evidence/20260810_i5_corr1_spec_coverage_readme.txt`、更新した
  `evidence/20260810_i5_spec_coverage_readme.txt`のみ。`docs/status.html`・`docs/index.html`・
  `SPEC.md`・`run_record.md`・`tools/spec_coverage.py`・I-5以外の既存`evidence/`は変更していない

### 詰まったこと、SPEC.md と食い違ったこと

- SPEC.mdとの食い違いは無い

### 次にやること

- Codexが`README.md`・`tests/test_site.py`の実ファイルと差分、
  `evidence/20260810_i5_corr1_*`の証拠、67テストの結果、`tools/spec_coverage.py`2件の出力を
  読み戻して受入判定すること
- commit・push・GitHub Pages公開・外部提出はいずれも行っていない

## 2026-08-10 CKPT-CORR-1 GTFS証拠ファイル内の実JWT混入の訂正

I-5-CORR-1はCodex受入済み。今回は別系統のCKPT-AUDIT-1で見つかった問題への本訂正
（1作業限定）。`evidence/20260809_gtfs_source_official_hikari.txt`（curlで生バイト取得した
光市公式サイトのHTML）に、JWT形式の実トークン文字列が2個そのままコミットされていた
問題を、安全な決定論的プレースホルダーへ置換した。新機能・他ファイルの変更は行っていない。

### やったこと

1. 開始前に`git status --porcelain=v1 --untracked-files=all`が変更8件・未追跡124件、
   `HEAD=fae504a`・`origin/main=b281540`と一致することを確認した
2. `evidence/20260809_gtfs_source_official_hikari.txt`を正規表現
   `eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+`で走査し、JWT形式文字列2個を検出した
   （40行目`cms_api_token`のJS変数、1953行目`data-chatbot-token`のHTML属性値）
3. 修正前・修正後それぞれのファイル全体SHA256と、各トークンの文字数・SHA256のみを
   `evidence/20260810_ckpt_corr1_token_redaction.txt`に記録した（元のJWT値そのものは
   このファイルにも他のどのファイルにも一切書いていない）
4. 2箇所を`REDACTED_PUBLIC_CLIENT_TOKEN_{1,2}_SHA256_<hash>_LEN_<length>`という
   決定論的プレースホルダーへ置換した（Edit、HTML属性値内で1語）
5. `tests/test_site.py`の`GtfsStatusDataTest.test_no_real_access_token_is_recorded`を強化し、
   Hikari証拠ファイルにJWT形式の文字列が0件であること、上記プレースホルダーが2件存在する
   ことを検査する処理を追加した。新しいテストメソッドは増やしていない（総数は67件のまま）
6. `tests/test_site.py`・`PROGRESS.md`・`verification.md`について、編集前の現物を
   `evidence/20260810_ckpt_corr1_test_site_before.py`・
   `evidence/20260810_ckpt_corr1_progress_before.md`・
   `evidence/20260810_ckpt_corr1_verification_before.md`としてバイト同一コピーで保存した
   （`sha256sum`で編集前後の値が一致することを確認済み）
7. `run_record.md`の編集前現物コピーは**作成しなかった**。理由は次節「詰まったこと」参照
8. `evidence/20260809_gtfs_source_*.txt`（69件）から、ファイル名昇順・
   `basename + 半角空白 + 小文字SHA256`・CRLF連結・末尾改行なし・UTF-8という手順で
   新しいRAW_SET_SHA256を計算した
9. `python -m unittest discover -s tests -v`を実行し、67件成功を確認した
10. `tools/spec_coverage.py`を3組（`tests/test_site.py`・`PROGRESS.md`・`verification.md`の
    各編集前現物 vs 現行）で実行し、全出力を`evidence/20260810_ckpt_corr1_spec_coverage_*.txt`
    へ保存した

### 確認できた事実（数字を含める）

- Hikari証拠ファイルの修正前SHA256: `9fcfbe73a11de8cdba978b7851edc0c983fcb57ce55ba3c413ab872180a527a3`
- 同、修正後SHA256: `e0fb98166601d536bc1297599bed39b68af991faf652bf6a1a93cc488df72e6a`
- トークン1（`cms_api_token`）: 152文字、SHA256
  `0da4ffc05d3a9d356aee6553858dc46dcf645ab512b61a67787d85c3ef6764ac`
- トークン2（`data-chatbot-token`）: 157文字、SHA256
  `c2d424915c180bceed0996482d41ed2bd0e4115cdb975eb1a9a35f0b955507c1`
- 置換後の再走査でJWT形式文字列は0件、決定論的プレースホルダーは2件
  （`evidence/20260810_ckpt_corr1_token_redaction.txt`）
- `AKIA|sk-|AIza|ghp_|xox[baprs]-|consumerKey|consumer_key|secret|apikey|api_key|Bearer `の
  走査でHikari証拠ファイル内の他の強い秘密鍵・APIキープレフィックスは0件
- `consumerKey`／`consumer_key`はevidence/全体で4ファイルにのみ出現し、いずれも既存の
  文字列プレースホルダー`[アクセストークン/YOUR_ACCESS_TOKEN]`（一部`[…]`）であり実値ではない
- `evidence/20260809_gtfs_source_*.txt`: **69件のまま**（変化なし）
- RAW_FILE_COUNT=**69**、RAW_SET_SHA256=
  `57748e5bdae0d913f756cfc588c04585b95887f324dca7c94a7584a08efe4f5c`
  （`evidence/20260810_ckpt_corr1_token_redaction.txt`に手順・値とも記録）
- `python -m unittest discover -s tests -v`: `Ran 67 tests`、`OK`
  （`tests/test_site.py`のテストメソッド数37件、`tests/test_verify.py`30件で訂正前と変わらず）
- `tools/spec_coverage.py`の3比較（`evidence/20260810_ckpt_corr1_test_site_before.py`
  vs `tests/test_site.py`、`evidence/20260810_ckpt_corr1_progress_before.md` vs `PROGRESS.md`、
  `evidence/20260810_ckpt_corr1_verification_before.md` vs `verification.md`）はいずれも
  終了コード0・判定「合格」（全出力は`evidence/20260810_ckpt_corr1_spec_coverage_*.txt`）
- `git diff --check`: 終了コード0

### 詰まったこと、SPEC.md と食い違ったこと

- **作業指示は`evidence/20260810_ckpt_corr1_run_record_before.md`をバイト同一コピーとして
  作ることを求めていたが、実行できなかった。**
  `run_record.md`はReadツールでもBashツールでも、権限そのものでアクセスが拒否される
  （返ってきた文言はそれぞれ`File is in a directory that is denied by your permission
  settings.`と`Permission to use Bash with command ... has been denied.`で、
  `[guard] blocked write`という書き込みガードの文言とは別種のブロック）。
  `CLAUDE.md`の「`run_record.md`はClaude Codeが読み書きしない」というより厳格な既定と
  一致する方向のブロックだったため、迂回せず複製を断念し、この事実を
  `evidence/20260810_ckpt_corr1_token_redaction.txt`と本節に記録して報告する
- `SPEC.md`との食い違いは無い

### 次にやること

- Codexが`evidence/20260809_gtfs_source_official_hikari.txt`の差分、
  `evidence/20260810_ckpt_corr1_*`の証拠、67テストの結果、`tools/spec_coverage.py`3件の出力、
  新RAW_SET_SHA256を読み戻して受入判定すること
- Codex側で`run_record.md`の旧ハッシュ・関連記録を更新すること（本作業の対象外）
- commit・push・GitHub Pages公開・外部提出はいずれも行っていない

## 2026-08-10 CKPT-CORR-1 Codex受入

上記「次にやること」にあった受入待ちは、記録時点（2026-08-10 CKPT-CORR-1完了直後）では
未了だったが、**Codexが実ファイルを独立に読み戻し、以下を確認して受入済みとした。**

- `evidence/20260809_gtfs_source_official_hikari.txt`の修正後SHA256:
  `e0fb98166601d536bc1297599bed39b68af991faf652bf6a1a93cc488df72e6a`
- 同ファイルのJWT形式文字列0件、決定論的プレースホルダー2件
- 全dirtyテキストのJWT形式文字列0件、標準的な秘密鍵/APIキープレフィックス0件
- RAW_FILE_COUNT=69、RAW_SET_SHA256=
  `57748e5bdae0d913f756cfc588c04585b95887f324dca7c94a7584a08efe4f5c`
- `python -m unittest discover -s tests -v`は`Ran 67 tests`・`OK`
- `test_site`・`PROGRESS`・`verification`・`run_record`の旧版比較4件はいずれも
  終了コード0・判定合格
- `git diff --check`終了コード0、staged 0件
- HEAD `fae504a1dfba282335b107b5e2293a85c17f2505`、
  origin/main `b281540db03cb69ec4f7efd41d6c43abf75127e1`のまま
- `run_record.md`はCodex側でrev.28へ同期済み（Claude Codeは読み書きしていない）

**次の一作業はCKPT-CORR-1後のチェックポイント再監査であり、
git add・commit・push・公開・外部提出はまだ行わない。**

## 2026-08-10 CKPT-AUDIT-1 再監査完了

直前の節の「次はCKPT-CORR-1後のチェックポイント再監査」は記録時点のものであり、
**現在はCodexによる再監査が完了している。**

- CKPT-CORR-1後のCodex再監査と記録同期後の最終集合は151件。
  内訳はA（受入済み成果）15件、B（必要証拠）136件、C（削除候補）0件、D（未確認・停止要因）0件。
- 全dirtyテキストのJWT形式値0件、標準的な秘密鍵/APIキープレフィックス0件。
- Hikari証拠はJWT0件・決定論的プレースホルダー2件。
- RAW_FILE_COUNT=69、RAW_SET_SHA256=
  `57748e5bdae0d913f756cfc588c04585b95887f324dca7c94a7584a08efe4f5c`。
- 67テスト成功、旧版比較合格、`git diff --check`成功、staged 0件。
- HEAD `fae504a1dfba282335b107b5e2293a85c17f2505`、
  origin/main `b281540db03cb69ec4f7efd41d6c43abf75127e1`のまま。
- `run_record.md`はCodex側でrev.29へ同期済み（Claude Codeは読み書きしていない）。

**次の一作業は、本人が明示承認した場合に限り、確定151件をローカルチェックポイントとして
commitして停止することである。push・公開・外部提出は行わない。**

## 2026-08-10 GTFS-2 岩国市・光市GTFS ZIP取得・安全検査・実測

`SPEC.md` §14の完了条件1〜13を実行した。開始HEADは
`0a7ac36849b921026352ae27948932b4cb07f0da`、開始時`git status --porcelain=v1 --untracked-files=all`は
0件（クリーン）だった（コミット`74423df`・`0a7ac36`はこのセッション開始前にチャット側で
記録済み）。変更可能なのはSPEC.md §14.3の範囲のみで、船木鉄道は取得・認証していない。

### やったこと

1. 編集前の`README.md`・`docs/status.html`・`tests/test_site.py`・`PROGRESS.md`・`verification.md`を
   `evidence/20260810_gtfs2_*_before.*`へコピーし、SHA256一致でバイト同一を確認した
2. `data/gtfs_feeds.csv`から`iwakuni-gtfsjp`・`hikari-gtfs`の`download_url_or_template`を読み、
   curlで認証情報・Cookie・既存セッションを付けずHTTPS GETした
3. 取得直後にZIPマジックバイト（`PK\x03\x04`）・20 MiB以下・HTTP 200を確認してから
   `raw/gtfs/iwakuni_gtfsjp_20260401.zip`・`raw/gtfs/hikari_gtfs_20260401.zip`へ保存し、
   保存前後でSHA256が一致することを確認した
4. 公開可能ヘッダー（`Content-Type`/`Content-Length`/`Content-Disposition`/`Last-Modified`/
   `Cache-Control`/`ETag`/`Date`）だけを`evidence/20260810_gtfs2_fetch_{iwakuni,hikari}.txt`へ
   保存し、`Set-Cookie`は保存しなかった
5. `src/inspect_gtfs_archives.py`を新規実装した。`zipfile`のみを使い、`extract()`/
   `extractall()`は一切呼ばない。SPEC.md §14.5の安全検査（メンバー数・個別/合計サイズ・
   圧縮比・メンバー名の絶対パス/ドライブ名/`..`/バックスラッシュ/NUL/階層・Unicode正規化+
   大文字小文字無視の重複名・暗号化/シンボリックリンク/非通常ファイル・CRC）と、
   §14.6の実測（メンバー一覧、5コアファイルのヘッダー/行数/主ID統計、
   `stops.txt`の`location_type`別件数、`calendar.txt`/`calendar_dates.txt`/`feed_info.txt`の
   候補値、`trips`↔`calendar`系のservice_id差集合、8桁実在日付検証）を実装した
6. 両ZIPを実際に検査し、安全検査を通過すること・全実測値を`evidence/20260810_gtfs2_inspection_run_raw.txt`・
   `evidence/20260810_gtfs2_member_explore_raw.txt`・`evidence/20260810_gtfs2_content_probe.txt`へ記録した
7. `tests/test_gtfs_inspection.py`を新規実装した。検出可能な新規テストメソッドは
   `test_gtfs_archive_safety_and_measurement`の1件のみとし、32件の名前付き`subTest`で
   §14.7の1〜7の条件を分離した（8は全体のテスト実行結果で満たす）
8. `tests/test_site.py`の`TOTAL_TEST_COUNT`を67→68に、`README.md`・`docs/status.html`の
   テスト件数表記を67→68に、それぞれ数字だけ変更した
9. `python -m unittest discover -s tests -v`を実行し、68件成功・既存67件退行なしを確認した
10. `tools/spec_coverage.py`を5組（README/status/test_site/PROGRESS/verification、
    それぞれの編集前現物 vs 最終版）で実行し、全出力を`evidence/20260810_gtfs2_spec_coverage_*.txt`
    へ保存した
11. 全変更・新規ファイルをJWT形式・`AKIA`/`sk-`/`AIza`/`ghp_`/`xox[baprs]-`/`consumerKey`/
    `consumer_key`/`secret`/`apikey`/`api_key`/`Bearer `/`Authorization:`で走査し、
    実値が0件であることを確認した
12. `git diff --check`を実行した
13. 作業中に生成した探索用の一時スクリプト（`raw/gtfs/.check_magic.py`・`.explore.py`・
    `.explore2〜4.py`・`.run_inspection.py`・`.scratch_synth{,2}.py`・`.run_full_tests.py`）は
    すべて実行後にリポジトリから削除し、最終的に`raw/gtfs/`には2つのZIP以外を残していない

### 確認できた事実（数字を含める。根拠はevidence/のファイル名で示す）

- 開始HEAD: `0a7ac36849b921026352ae27948932b4cb07f0da`
- 岩国市: HTTP 200、719723バイト、SHA256
  `d236a58ff4a0edb4812a8bed543d4897670441164a1019e88d5e35ded5052de2`、12メンバー、
  安全検査`ok=True`。`agency.txt`1行・`stops.txt`800行（`stop_id`重複0）・`routes.txt`46行・
  `trips.txt`267行・`stop_times.txt`7362行（複合主キー`trip_id`+`stop_sequence`で空欄0・
  一意7362・重複0）。`calendar.txt`1行（`service_id`一意1、
  start_date最小20260327、end_date最大20270326、monday〜fridayは全列`{'1': 1}`、
  saturday・sundayは全列`{'0': 1}`、曜日`invalid_count`=0）。`calendar_dates.txt`1641行
  （`service_id`一意22、`exception_type`は全件`1`、追加日20260327〜20260930、削除日0件）。
  `feed_info.txt`の`feed_start_date`=20260327・`feed_end_date`=20270326
  （`evidence/20260810_gtfs2_fetch_iwakuni.txt`・`20260810_gtfs2_inspection_run_raw.txt`）
- 光市: HTTP 200、86273バイト、SHA256
  `f3403ebaf481805fff0e2316be3a986732f443a06a64eab5b579ea17191adde7`、14メンバー
  （標準GTFSファイルに加え非標準の`result.txt`を含むが、安全検査は同じ規則で違反なし）、
  安全検査`ok=True`。`agency.txt`1行・`stops.txt`172行（`stop_id`重複0）・`routes.txt`7行・
  `trips.txt`63行・`stop_times.txt`1344行（複合主キー`trip_id`+`stop_sequence`で空欄0・
  一意1344・重複0）。`calendar.txt`4行（`service_id`一意4、
  start_date最小20260401、end_date最大20270331、monday〜fridayは全列`{'0': 1, '1': 3}`、
  saturday・sundayは全列`{'0': 2, '1': 2}`、曜日`invalid_count`=0）。`calendar_dates.txt`61行
  （`service_id`一意3、`exception_type`は`1`が22・`2`が39、追加日20260429〜20270322、
  削除日20260429〜20270322）。`feed_info.txt`の`feed_start_date`=20260401・
  `feed_end_date`=20270331
  （`evidence/20260810_gtfs2_fetch_hikari.txt`・`20260810_gtfs2_inspection_run_raw.txt`）
- 公式ページ記載のデータ基準日（両方とも2026-04-01）とGTFS内部の`feed_start_date`は
  同一視していない。岩国市はGTFS内部`feed_start_date`=20260327で公式基準日と一致しない
  （事実として記録するのみで原因は断定しない）
- `python -m unittest discover -s tests -v`: `Ran 68 tests`・`OK`
  （既存67件は退行なし。新規は`test_gtfs_archive_safety_and_measurement`1件のみで、
  32件の名前付き`subTest`。`evidence/20260810_gtfs2_unittest_full_raw.txt`）
- `tools/spec_coverage.py`5組すべて終了コード0・判定「合格」
  （詳細は次節「詰まったこと」の下、および`evidence/20260810_gtfs2_spec_coverage_*.txt`）
- JWT形式文字列0件。強い秘密鍵/APIキープレフィックスは、PROGRESS.md・verification.mdの
  過去記録（CKPT-CORR-1）内にある走査コマンド自体の説明文・既存プレースホルダー`[…]`への
  言及のみで実値は0件
- `git diff --check`: 終了コード0（結果は次節参照）
- `git status --porcelain=v1 --untracked-files=all`で確認した変更・新規ファイルは
  `README.md`・`docs/status.html`・`tests/test_site.py`・`PROGRESS.md`・`verification.md`・
  `raw/gtfs/iwakuni_gtfsjp_20260401.zip`・`raw/gtfs/hikari_gtfs_20260401.zip`・
  `src/inspect_gtfs_archives.py`・`tests/test_gtfs_inspection.py`・`evidence/20260810_gtfs2_*`のみ

### 詰まったこと、SPEC.mdと食い違ったこと

- **SPEC.md §14.7「一時ディレクトリ内の合成ZIP」とCLAUDE.mdの「リポジトリの外に一切書かない。
  作業用の中間ファイルも例外ではない」が文字どおりには両立しない。**OS一時ディレクトリ
  （`tempfile`が使う既定の場所）はリポジトリ外にある。判断として、合成ZIPを
  `io.BytesIO`上に完全にメモリ上で構築し、ディスク（リポジトリ内外を問わず一時ファイルも）へは
  一切書き込まない実装にした。これはCLAUDE.mdの絶対条件をより厳格に満たしながら、
  SPEC.mdが求める「ネットワークに繋がずディスク上の実ファイルに依存しない合成ZIPでのテスト」
  という趣旨は満たせていると判断した。`SPEC.md`は書き換えていない。この判断が
  意図と違う場合は指示を待つ
- **`zipfile`標準ライブラリ自体が、NUL文字を含むメンバー名を読み込み時に切り詰め、
  単独のバックスラッシュを含むメンバー名をフォワードスラッシュへ正規化することを実測で確認した。**
  このため、実際のZIPバイト列を生成して読み込む統合テストでは、
  `ZipInfo.filename`にNULやバックスラッシュを含む値を再現できなかった
  （`evidence/20260810_gtfs2_synthetic_scratch2_raw.txt`のNUL/backslashの節）。
  該当2条件（`tests/test_gtfs_inspection.py`の`2h_nul_byte_in_name_pure_function`・
  `2i_backslash_only_name_pure_function`）は、判定ロジック`check_member_name()`を
  文字列引数で直接呼び出す形で検査した。SPEC.md §14.5が求める防御自体は
  `check_member_name()`に実装済みであり、加えて標準ライブラリ側にも多層防御があることを
  確認できた、という位置づけで報告する
- `zipfile.ZipInfo`のunixモードのファイル種別ビットが0（種別情報なし。DOS/Windows由来のZIPや
  `zipfile.writestr(name, data)`の既定動作で頻出）の場合、これを「通常ファイル以外」と
  誤検出しないよう`check_special_entry()`を実装した。誤検出したままだと、今回取得した
  岩国市の実ZIP（`unix_mode=0o0`のDOS由来メンバーのみ）が常に安全検査で弾かれてしまうため、
  実データで気づいて設計を直した
  （`evidence/20260810_gtfs2_member_explore_raw.txt`で両ZIPの`external_attr`/`unix_mode`を確認済み）
- SPEC.md本文との食い違いは無い

### 次にやること

- Codexが`raw/gtfs/*.zip`の実ハッシュ、`src/inspect_gtfs_archives.py`・
  `tests/test_gtfs_inspection.py`の実装、68テストの結果、`tools/spec_coverage.py`5件の出力、
  `evidence/20260810_gtfs2_*`の証拠を読み戻して受入判定すること
- SPEC.md §14.9に列挙されたもの（`data/gtfs_feeds.*`等の更新、内部運行期間の最終算出規則、
  単一鮮度スコア、地図・経路検索、船木鉄道の認証取得、登録簿4PDFの再処理、作品②、
  他県展開、UDC応募・外部公開）はいずれも指示があるまで着手しない

## 2026-08-10 SUPPLY-METRIC-2 岩国市・光市の輸送供給指標計算

SPEC.md §15（SUPPLY-METRIC-1の指標定義）を、GTFS-2で受入済みの岩国市・光市2 ZIPから
決定論的に計算した。`raw/gtfs/*.zip`は読み取り専用のまま、再取得・展開保存・ネットワーク接続は
一切行っていない。

### やったこと

1. `src/calculate_gtfs_supply_metrics.py`を新規実装した。低レベルのCSVデコード・GTFS日付検証・
   列統計（`decode_csv_bytes`/`read_csv_rows`/`is_valid_gtfs_date`/`compute_id_stats`/
   `inspect_safety`/`inspect_feed_info`/`inspect_calendar`/`inspect_calendar_dates`）は
   GTFS-2で受入済みの`src/inspect_gtfs_archives.py`をそのまま再利用し、同じロジックの
   重複実装を避けた
2. §15.4.2の3指標（`gtfs_agency_record_count`・`gtfs_route_id_count`・
   `gtfs_boarding_location_id_count`）と、§15.4.3の`scheduled_trip_count_by_date`
   （`calendar.txt`→`calendar_dates.txt`→`trips.txt`→`frequencies.txt`の順で適用、
   `exact_times=1`の定間隔便を式で数え、`0`/空欄が1件でもあれば当日を
   `not_exact_frequency_based`にする）を実装した
3. §15.5が岩国市・光市について既に確定した比較週2026-04-06〜2026-04-12を定数として使い、
   比較週の一般選定アルゴリズム（同§手順1〜7）は実装しなかった（今回のスコープ外）
4. `python -B src/calculate_gtfs_supply_metrics.py`を実行し、`data/gtfs_supply_metrics.json`
   （1レコード=1フィード、`metric_version`/`municipality_code`/`municipality`/`feed_id`/
   `source_zip_sha256`/`scope_note`/`official_reference_date`/`checked_at`/
   `comparison_week_start`・`_end`/`date_basis`/`metrics`/`scheduled_trip_count_by_date`を含む）
   を生成した
5. `src/calculate_gtfs_supply_metrics.py`とは別に手組みした一時検証スクリプト
   （`calendar.txt`/`calendar_dates.txt`/`trips.txt`を直接読み、`scheduled_trip_count_by_date`
   と3構造指標を独立に再計算するもの。実行後にリポジトリから削除）で全出力値を突き合わせ、
   完全一致を確認した
6. `tests/test_gtfs_supply_metrics.py`を新規実装した。合成GTFS（`io.BytesIO`上のみ、
   ディスク非経由）で通常calendar・calendar_dates追加/削除・0件・必須列欠落や重複主キー等の
   不正入力・`frequencies.txt`の`exact_times=1`（割り切れる場合と割り切れない場合）と
   `0`/空欄を検証し、岩国市・光市の実ZIPについてSHA256と全指標値を固定値で検証し、
   `data/gtfs_supply_metrics.json`の再生成がバイト一致することも検証した（29件の名前付き
   `subTest`、1テストメソッド）
7. `python -m unittest discover -s tests -v`を実行し、69件成功（既存68件は退行なし、新規は
   `test_supply_metrics_calculation`1件のみ）を確認した
8. `git diff --check`・`git diff --cached --name-only`（0件）・`git status --short --branch`を
   実行し、変更・新規ファイルがSUPPLY-METRIC-2の許可範囲（`src/`・`tests/`・`data/`・
   `evidence/`・本ファイル・`verification.md`）内であることを確認した
9. 作業中に作成した一時検証スクリプトはすべて実行後にリポジトリから削除し、最終的に
   リポジトリ直下や`raw/gtfs/`に余分なファイルを残していない

### 確認できた事実（数字を含める。根拠はevidence/のファイル名で示す）

- 岩国市（`iwakuni-gtfsjp`、`raw/gtfs/iwakuni_gtfsjp_20260401.zip`、SHA256
  `d236a58ff4a0edb4812a8bed543d4897670441164a1019e88d5e35ded5052de2`、GTFS-2受入値と一致）:
  `gtfs_agency_record_count=1`、`gtfs_route_id_count=46`、`gtfs_boarding_location_id_count=800`
  （すべて`measured`）。`scheduled_trip_count_by_date`は
  2026-04-06(月)=185、04-07(火)=186、04-08(水)=183、04-09(木)=161、04-10(金)=188、
  04-11(土)=146、04-12(日)=39（すべて`measured`）
- 光市（`hikari-gtfs`、`raw/gtfs/hikari_gtfs_20260401.zip`、SHA256
  `f3403ebaf481805fff0e2316be3a986732f443a06a64eab5b579ea17191adde7`、GTFS-2受入値と一致）:
  `gtfs_agency_record_count=1`、`gtfs_route_id_count=7`、`gtfs_boarding_location_id_count=172`
  （すべて`measured`）。`scheduled_trip_count_by_date`は
  2026-04-06(月)〜04-10(金)=各55、04-11(土)=41、04-12(日)=41（すべて`measured`）
- 岩国市・光市とも`frequencies.txt`が存在しないため、実データでは`frequencies.txt`関連の
  分岐（`exact_times`処理）を経由していない。この分岐は合成GTFSのテストでのみ検証している
  （`evidence/20260810_supply_metric2_crosscheck_raw.txt`）
- `python -m unittest discover -s tests -v`: `Ran 69 tests`・`OK`（既存68件は退行なし、新規は
  `test_supply_metrics_calculation`1件・29件の名前付き`subTest`。
  `evidence/20260810_supply_metric2_unittest_full_raw.txt`・
  `evidence/20260810_supply_metric2_focused_test_raw.txt`）
- `git diff --check`: 終了コード0、標準出力・標準エラーとも空
  （`evidence/20260810_supply_metric2_git_diff_check.txt`）
- `git diff --cached --name-only`: 0件（staged無し）
- `git status --short --branch`で確認した新規ファイルは、`data/gtfs_supply_metrics.json`・
  `src/calculate_gtfs_supply_metrics.py`・`tests/test_gtfs_supply_metrics.py`・
  `evidence/20260810_supply_metric2_*`のみ（GTFS-2由来の既存dirty差分・untrackedファイルは
  変更していない）

### 詰まったこと、SPEC.mdと食い違ったこと

- **`data/gtfs_supply_metrics.json`の書き込み方法。** `pathlib.Path.write_text()`は
  Windows既定でテキストモードの改行変換（`\n`→`\r\n`）が入り、実行環境によって出力バイト列が
  変わってしまう。この出力は「再生成してバイト一致することを検証する」という要件があるため、
  改行変換の無い`write_bytes()`でLFのまま書くよう実装した（他の既存生成スクリプト
  `src/build_gtfs_status.py`は`write_text()`を使っており出力はCRLFになっているが、
  今回はそちらへ合わせず明示的にLF固定にした）。SPEC.mdへの記載も既存コードの変更も
  行っていない、`calculate_gtfs_supply_metrics.py`内だけの実装判断
- **`checked_at`と`metric_computed_at`を分けた。** SPEC.md §15.4.1の表が`checked_at`の元データを
  `data/gtfs_feeds.csv`/`data/municipality_gtfs.csv`と定めているため、その値（2026-08-09、
  公式ページ側の確認日）をそのまま使い、今回の指標計算そのものを実行した日（2026-08-10）は
  §15.8の「最低限」の一覧に無い追加フィールド`metric_computed_at`として別に持たせた。
  壁時計を読まず固定値にして出力の決定論性を保っている
- **`scheduled_trip_count_by_date`の「必須列が無い」「参照関係が不正」「同一主キーが重複する」は
  週7日すべてを`invalid_input`にする設計にした。** SPEC.md §15.4.3本文が列挙するこれらの条件は
  いずれも特定の1日に限定されないGTFS構造上の不備と判断したため。`trips.txt`が丸ごと存在しない
  場合も同じ扱いにした
- **`frequencies.txt`の`exact_times=1`行の検証（§15.4.3手順5「時刻範囲が正で割り切れることを
  検証し」）を「end_time > start_timeかつheadway_secsが正の整数」と読んだ。** 続く文が
  `start_time + n * headway_secs < end_time`という不等式で数える設計になっており、余りが出る
  場合も扱えるため、厳密な整除を前提条件にすると数式と矛盾すると判断した。岩国市・光市の実ZIPは
  どちらも`frequencies.txt`を持たないため、この判断は実データの値には影響していない
- SPEC.md本文との食い違いは無い

### 次にやること

- Codexが`data/gtfs_supply_metrics.json`の実値、`src/calculate_gtfs_supply_metrics.py`の実装、
  `tests/test_gtfs_supply_metrics.py`の29件、`evidence/20260810_supply_metric2_*`の証拠を
  読み戻して受入判定すること
- SPEC.md §15.10が次段階として挙げるもの（比較週の一般選定アルゴリズム、他市町・船木鉄道への
  展開、地図・経路検索、UI表示、commit・push・公開）はいずれも指示があるまで着手しない

## 2026-08-10 SUPPLY-METRIC-2 岩国市・光市GTFS指標の計算

SPEC.md §15（SUPPLY-METRIC-1の定義）に沿って、岩国市・光市の受入済みGTFS ZIP 2件
（`raw/gtfs/iwakuni_gtfsjp_20260401.zip`・`raw/gtfs/hikari_gtfs_20260401.zip`）から
指標の実数を計算した。汎用フレームワークや完全なGTFS検証器は作っていない。
`raw/gtfs/`の2ZIPは変更・再取得・展開保存していない。ネットワークへは一切接続していない。

### やったこと

1. `src/calculate_gtfs_supply_metrics.py`を新規実装した。低レベルのCSVデコード・
   日付検証・列統計（`decode_csv_bytes`・`read_csv_rows`・`is_valid_gtfs_date`・
   `compute_id_stats`・`inspect_safety`・`inspect_feed_info`・`inspect_calendar`・
   `inspect_calendar_dates`・`sha256_of_file`）は、GTFS-2で受入済みの
   `src/inspect_gtfs_archives.py`をそのまま再利用し、重複実装していない
2. `gtfs_agency_record_count`・`gtfs_route_id_count`・`gtfs_boarding_location_id_count`
   （SPEC.md §15.4.2）と、`scheduled_trip_count_by_date`（同§15.4.3、
   `calendar.txt`→`calendar_dates.txt`の順で適用し、`frequencies.txt`の
   `exact_times=1`/`0`/空欄を分岐する計算）を実装した
3. `data/gtfs_supply_metrics.json`を生成した。フィールドはSPEC.md §15.8の最低限一覧
   （`metric_version`・`municipality_code`/`municipality`・`feed_id`・
   `source_zip_sha256`・`scope_note`・`official_reference_date`・`checked_at`・
   `comparison_week_start`/`comparison_week_end`・各指標の`value`/`metric_status`/`reason`・
   `feed_info`/`calendar`/`calendar_dates`の日付根拠）をすべて満たし、加えて
   このSUPPLY-METRIC-2実行日を`metric_computed_at`として追加した（詳細は次節）
4. `tests/test_gtfs_supply_metrics.py`を新規実装した。合成GTFS（`io.BytesIO`のみ、
   ディスクへは一切書かない）で通常calendar、calendar_dates追加/削除、0便、
   必須列欠落・重複主キー・不正日付・不正曜日値・不正exception_type・trips.txt欠落等の
   `invalid_input`、frequencies.txtの`exact_times=1`（割り切れる場合・余りが出る場合）・
   `0`・空欄を検証し、保存済み2ZIPのSHA256・全指標値・7実日付をすべて固定値で検証し、
   `data/gtfs_supply_metrics.json`の再生成がバイト一致することも検証した
5. `src/calculate_gtfs_supply_metrics.py`を`python -B`で直接実行し、実際に
   `data/gtfs_supply_metrics.json`を生成した。値の一部をSPEC.mdの計算規則から独立に
   手書きした簡易実装（`frequencies.txt`非対応・calendar/calendar_dates/tripsのみ）で
   再計算し、`scheduled_trip_count_by_date`の全14個の値（岩国市7日+光市7日）が
   一致することを確認した
6. `python -m unittest discover -s tests -v`で69件成功（既存68件は退行なし）を確認した
7. `git diff --check`・`git diff --cached --name-only`・`git status --short --branch`を
   実行し、空・空・想定どおりの変更/新規ファイルのみであることを確認した

### 確認できた事実（数字を含める。根拠はevidence/のファイル名で示す）

- 岩国市（`iwakuni-gtfsjp`、SHA256
  `d236a58ff4a0edb4812a8bed543d4897670441164a1019e88d5e35ded5052de2`）:
  `gtfs_agency_record_count=1`、`gtfs_route_id_count=46`、
  `gtfs_boarding_location_id_count=800`（いずれも`measured`）。
  `scheduled_trip_count_by_date`（すべて`measured`）:
  2026-04-06(月)=185、04-07(火)=186、04-08(水)=183、04-09(木)=161、
  04-10(金)=188、04-11(土)=146、04-12(日)=39
- 光市（`hikari-gtfs`、SHA256
  `f3403ebaf481805fff0e2316be3a986732f443a06a64eab5b579ea17191adde7`）:
  `gtfs_agency_record_count=1`、`gtfs_route_id_count=7`、
  `gtfs_boarding_location_id_count=172`（いずれも`measured`）。
  `scheduled_trip_count_by_date`（すべて`measured`）:
  04-06〜04-10は平日で各55、04-11(土)・04-12(日)は各41
- 岩国市の`calendar.txt`は`平日`（月〜金）1行だけだが、`trips.txt`のどのtripも
  `平日`を参照していない（`service_id_cross_check.calendar_union_only={'平日'}`、
  GTFS-2で既に確認済みの事実）。実際の運行日判定は`calendar_dates.txt`の
  1641行（すべて`exception_type=1`の追加のみ、削除0件）が担っており、
  22種類の曜日パターン別`service_id`（`月・水・金曜日運行`等）で構成されている。
  本実装はこれを推測で1つの「内部運行期間」へ丸めず、SPEC.md §15.4.3の手順どおり
  `calendar.txt`→`calendar_dates.txt`の順で適用して計算した
  （`evidence/20260810_supply_metric2_manual_crosscheck_raw.txt`で独立に再現）
- 岩国市・光市のどちらのZIPも`frequencies.txt`を持たない
  （`evidence/20260810_supply_metric2_manual_crosscheck_raw.txt`の
  `frequencies.txt present: False`）。したがって実データの`scheduled_trip_count_by_date`は
  すべてSPEC.md §15.4.3手順4（frequencies行が無いtripを1便と数える）だけで決まり、
  `exact_times`の分岐は実データの値には影響しない（テストでは合成GTFSで別途検証済み）
- `python -m unittest discover -s tests -v`: `Ran 69 tests`・`OK`
  （既存68件は退行なし。新規は`test_supply_metrics_calculation`1件のみで、
  内部を多数の名前付き`subTest`で分離。`evidence/20260810_supply_metric2_unittest_full_raw.txt`）
- `data/gtfs_supply_metrics.json`のSHA256は生成前後で同一
  （`26167df77efce48e6dbcacde757a08ff40f7229fe99b9928f25b541f3766db9b`、5904バイト）。
  `render_dataset_json(build_dataset())`を2回実行しても同じ文字列になることをテストで確認し、
  再生成のバイト決定論性を担保した（`evidence/20260810_supply_metric2_script_run_raw.txt`）
- `git diff --check`: 出力なし（異常なし）。`git diff --cached --name-only`: 出力なし
  （staged 0件）。`git status --short --branch`は新規ファイルが
  `data/gtfs_supply_metrics.json`・`src/calculate_gtfs_supply_metrics.py`・
  `tests/test_gtfs_supply_metrics.py`・`evidence/20260810_supply_metric2_*`のみで、
  作業開始時点で既に存在した差分（`PROGRESS.md`・`README.md`・`SPEC.md`・
  `docs/status.html`・`run_record.md`・`tests/test_site.py`・`verification.md`の
  変更、`raw/gtfs/`・`src/inspect_gtfs_archives.py`・`tests/test_gtfs_inspection.py`・
  `evidence/20260810_gtfs2_*`の新規）はそのまま・上書きせず残している
  （`evidence/20260810_supply_metric2_git_diff_check.txt`）

### 実装上の判断（詰まったこと・SPEC.mdの記載を推測で埋めた箇所）

SPEC.md本文を書き換える必要が生じるような食い違いは無かった。ただしSPEC.md §15は
「指標の定義」までで、実装の細部（後述の3点）はSUPPLY-METRIC-2側で判断する必要があった。
判断の根拠と実データへの影響は`src/calculate_gtfs_supply_metrics.py`のモジュール
docstring「実装上の判断」節にも同じ内容を書いた。

- **`checked_at`の意味を変えていない。** SPEC.md §15.4.1の表は`checked_at`の元データを
  `data/gtfs_feeds.csv`/`data/municipality_gtfs.csv`としており、これは「公式ページを
  確認した日」（2026-08-09）を指す。今回の指標計算そのものを実行した日（2026-08-10）は
  これとは別の日付なので、`checked_at`を上書きせず、SPEC.md §15.8が「最低限」と書く
  一覧に無い`metric_computed_at`フィールドを追加してそちらへ記録した
- **`location_type`列が丸ごと無い場合**は、GTFS Schedule Referenceの既定値（省略時は
  停留所/プラットフォームを意味する）に従い、全行を「空欄」として扱い乗降場所に数える。
  岩国市・光市の実ZIPはどちらも列が存在し全行`0`のため、この判断は実データの値には
  影響しない（合成GTFSのテストでのみ効く）
- **frequencies.txtの`exact_times=1`行の検証（§15.4.3手順5「時刻範囲が正で割り切れる
  ことを検証し」）は「end_time > start_time（範囲が正）かつheadway_secsが正の整数
  （除算が定義できる）」と読んだ。**続く文が`start_time + n * headway_secs < end_time`
  という不等式でnの個数を数えており、余りが出る場合も含めて数式自体が扱える設計になって
  いるため、「割り切れること」を別の前提条件（余り0を要求）にすると数式が無意味になる。
  岩国市・光市の実ZIPはどちらも`frequencies.txt`を持たないため、この判断は実データの
  値には影響しない
- **`data/gtfs_supply_metrics.json`の改行はLFに固定した。** `Path.write_text()`は
  Windowsの既定でテキストモードの改行変換（`\n`→`\r\n`）が入り、OSによって出力バイト列が
  変わる（既存の`data/gtfs_feeds.json`等はこの変換でCRLFになっている実測を確認した）。
  この出力は「再生成してバイト一致する」ことを自動テストで担保する決定論的な成果物なので、
  改行変換の無い`write_bytes()`でLFのまま書くことにした。既存ファイルの改行は変更していない
- SPEC.md §15.4.3「両方が無い、必須列が無い、日付・曜日値・例外値・参照関係が不正、
  同一主キーが重複する場合は…invalid_inputとする」は、`trips.txt`が丸ごと存在しない
  場合も同じ性質の構造的欠落と判断し、`invalid_input`に含めた（SPEC.md本文には
  `trips.txt`欠落の場合分けが明記されていないが、`calendar.txt`/`calendar_dates.txt`が
  両方無い場合と同じ扱いが自然だと判断した）

### 次にやること

- Codexが`data/gtfs_supply_metrics.json`の実値、`src/calculate_gtfs_supply_metrics.py`の
  実装、69テストの結果、`evidence/20260810_supply_metric2_*`の証拠を読み戻して受入判定すること
- SUPPLY-METRIC-2の対象外（比較週の一般選定アルゴリズム、他17市町への汎用化、追加指標、
  地図・経路検索、船木鉄道取得、commit・push・公開）はいずれも指示があるまで着手しない

## 2026-08-11 ENTRY-PAGE-1 作品①の応募説明ページ

### なぜ行うか

RELEASE-1で公開したデモは数値・日付・根拠を確認できるが、初見の審査員や交通担当者が、
「誰のどんな困りごとを、どう解決し、どこまで検証済みか」を一ページで把握できる入口がなかった。
既存成果を応募説明ページで結び、ロードマップ07を単なる準備作業で終わらせないため、
`SPEC.md` rev.5.6 §18のENTRY-PAGE-1として実装した。

### 公式要件の確認

- UDC2026公式募集要領の期日は、作品概要2026-11-15 23:59、本応募2026-12-27 23:59。
- 公式概要フォームの必須作品項目は、作品名、100字以内の作品概要、作品タイプ、作品テーマ等。
- 一次審査基準は実用度・完成度・挑戦度。作品タイプはアプリケーション、テーマは道路・交通とした。
- Googleフォーム内には2025年度の重点部門表記が残っていたため、2026年の期日・分類は公式募集要領を正とした。
- フォームへの入力・送信は行っていない。

### 実装

- `docs/entry.html`をJavaScript不要の静的ページとして新規作成した。
- 作品名「山口県 市町別の登録供給ビュー」、81字概要、対象者、解決したい問題、3段階の使い方、
  使用データ、実用度・完成度・挑戦度、分かること／分からないこと、公開成果へのリンクを掲載した。
- `docs/index.html`、`docs/status.html`、`README.md`から`entry.html`への導線を追加した。
- `tests/test_site.py`へ固定内容、81字／100字以内、必要節、リンク、禁止断定、レスポンシブ、
  既存ページ導線を直接検証する6テストを追加した。
- Claude Sonnet 5の直接実装はCLIから183.6秒後に`ConnectionRefused`で終了し、変更0件を確認した。
  既に承認されたGoal-to-doneを止めないためCodexが実装を継続し、独立性不足は自動テスト、差分検査、
  実Edge、スクリーンショットの証拠で補う。

### ローカル検証

- `python -B -m unittest tests.test_site -v`: 54件成功。
- `python -B -m unittest discover -s tests -v`: 86件成功。
- `tools/spec_coverage.py`: 旧rev.5.5の693トークン、欠落0、合格。
- Edge/CDP初回は29/30。desktopのスクロールバー15pxで文書幅がviewportより15px小さい
  `rawDifference=-15`を、検査側が0だけ合格としていたためFAIL。画面コードは変更していない。
- 検査を「正の超過だけをoverflow」とするよう補正し、新規prefixで30/30成功。
  1440×1600・390×844とも横overflow 0、HTTP 200、81字概要・6見出し・4導線一致、
  console error、Runtime例外、Log error、Network.loadingFailedはすべて0件。
- Codexが両幅の全ページPNGを読み、階層、可読性、折返し、主要導線を確認した。
- 初回FAILと補正後PASSは別prefixで保持する。

### 現在地

ローカル実装と実ブラウザ検証は完了し、Codex受入の残件は公開前最終検査、commit・push・Pages配備、
公開`entry.html`の再測定である。UDC概要フォーム、本応募、BODIK APPs登録は行っていない。

## 2026-08-11 RELEASE-1 GitHub Pages公開・公開URL受入

### なぜこの作業をしたか

- SUPPLY-VIEW-1がローカルで受入済みでも、公開トップが旧「準備中」のままでは、県・市町の
  交通担当者や事業者が2市の比較へ到達できず、Goal-to-doneの利用者成果を満たさないため。
- UDC本応募とは分離し、既に公開中のGitHub Pagesを受入済み現行デモへ更新する一作業とした。

### 公開前確認とrelease commit

- 生成成功、全unittest 80件成功、旧版網羅README 43 / status 0 / index 18 /
  run_record 236トークン・欠落0、差分検査、秘密情報0、JSON・2 ZIPのSHA256不変を再確認した。
- 未追跡証拠をstageして初めて見えた末尾空白7指摘は、数値・文言を変えず空白だけ正規化した。
- 66パスをcommit `18170b2a207940a889d527e29ef77f702c066b30`
  （`Publish GTFS inspection and supply comparison`）へまとめ、`origin/main`へpushした。

### GitHub Pagesと公開URLの確認

- GitHub Pages run `31449761225`はbuild・deployとも成功した。
- <https://yyy-yuichi.github.io/yamaguchi-yusho-data/> はHTTP 200、旧「準備中」を含まず、
  受入済みの登録供給、GTFS確認状況、岩国市・光市の供給比較を表示した。
- 公開`data/gtfs_supply_metrics.json`を入力として実Edge/CDP検証を再実行し、28/28条件が合格した。
  - desktop: 1440×1600、document横overflow 0
  - mobile: 390×844、document横overflow 0
  - 両幅とも20値・7実日付が公開JSONと一致、2市列を同時表示
  - console error、`Runtime.exceptionThrown`、Log error、`Network.loadingFailed`は全て0
- Codexが公開スクリーンショットを目視し、目的、2市比較、同じ7日、原本・日付・範囲の限界を
  PC・スマホで読めることを確認した。

公開証拠は`evidence/20260811_release1_browser_raw.json`、
`evidence/20260811_release1_browser_summary.txt`、
`evidence/20260811_release1_screenshot_1440.png`、
`evidence/20260811_release1_screenshot_390x844.png`。

### 現在地

- 公開状態の文書同期後、集中48件・全80件、旧版網羅7組、差分、秘密情報、公開JSON一致、
  Pages成功、公開ブラウザ28/28を再確認した。全結果は
  `evidence/20260811_release1_final_checks_raw.txt`に保持する。
- 状態同期を最終commitとして`main`へpushし、ローカルHEADと`origin/main`の一致を読み戻して
  RELEASE-1をCodex受入済みとする。
- 現行デモの公開はT1〜T4、UDC概要エントリー・本応募、外部提出、受賞の達成を意味しない。
- 次工程はロードマップ07「応募用の公開物一式」。地図、経路検索、船木鉄道取得、次機能は
  RELEASE-1へ含めていない。

## 2026-08-11 SUPPLY-VIEW-1 V4 実ブラウザ確認

### この確認を行った理由

- 静的テストの合格だけでなく、山口県・市町・事業者の担当者が実画面で、岩国市と光市の
  GTFS供給量、値の状態、対象範囲、公式ページ日・GTFS内部日・確認日・計算日、原本を
  混同せず、デスクトップとスマートフォンの双方で読めることを確認するため。
- 直前のSUPPLY-VIEW-1記録にあった「Browser runtimeなしのため未確認」という残件は、
  ユーザーの個別承認を受けたEdge headless + Chrome DevTools Protocol実測で解消した。

### 実測と補正履歴

1. `docs/`を専用localhost HTTP serverで配信し、専用一時user-data-dir・空きportで起動した
   EdgeをCDPで操作した。自分が起動したserver/Edgeだけを終了し、一時profileも削除した。
2. 初回は27/28条件合格。画面、JSON、DOM値、横幅には異常が無かったが、未指定の
   `/favicon.ico`をEdgeが要求して404となり、desktopの`Log error=2`だけが不合格だった。
   この結果は`evidence/20260810_supply_view1_browser_initial_fail_raw.json`と
   `evidence/20260810_supply_view1_browser_initial_fail_summary.txt`へ補正前証拠として保持した。
3. 404を証拠側で除外せず、`docs/index.html`の`head`に通信不要のinline data URI faviconを
   1行追加した。表示内容・入力JSON・計算値は変更していない。
4. 新しい空きport・新しい一時profileで再実行し28/28条件が合格した。
   - desktop: viewport 1440×1600、document横overflow 0
   - mobile: viewport 390×844、document横overflow 0
   - 両幅とも構造指標6値、2026-04-06〜12の7実日付・14便数が公開JSONと一致
   - 両幅とも構造表・日別表で岩国市と光市の2列が同時に可視
   - 測定済み、フィード全体値で市内だけ・全公共交通ではない限界、4種の日付、原本ZIP、
     完全SHA256をDOMで確認
   - `gtfs_supply_metrics.json`・`gtfs_feeds.json`はHTTP 200、供給比較と既存の市町別登録供給・
     GTFS確認状況はいずれも表示
   - 両幅ともconsole error 0、`Runtime.exceptionThrown` 0、Log error 0、
     `Network.loadingFailed` 0
5. 全ページスクリーンショットを目視し、desktopでは二表と二市の根拠が並列で読め、mobileでは
   ページ横スクロールなしで二市の列、7日、二つの根拠カードが縦方向に読めることを確認した。

合格生JSON・要約・スクリーンショットは
`evidence/20260810_supply_view1_browser_raw.json`、
`evidence/20260810_supply_view1_browser_summary.txt`、
`evidence/20260810_supply_view1_screenshot_1440.png`、
`evidence/20260810_supply_view1_screenshot_390x844.png`。

### 退行確認と現在地

- `python -B -m unittest tests.test_site -v`: 48件成功。
- `python -B -m unittest discover -s tests -v`: 80件成功。
- `spec_coverage.py`: README 43トークン、status 0トークン、index 18トークン、欠落0で全合格。
- 実装担当の自己検証合格後、Codexが実ファイル・差分・証拠・スクリーンショットを独立に
  読み戻し、`run_record.md` rev.36でSUPPLY-VIEW-1を受入済みとした。
- V4受入時点ではcommit、push、GitHub Pages更新、公開、地図、経路検索、船木鉄道取得、
  次機能へ進まなかった。この公開前状態は後掲の履歴であり、RELEASE-1でPages更新まで解消した。

### 受入結果と停止位置

- Codexはinline faviconの最小差分、CDP driver、生JSON、2枚のスクリーンショット、補正履歴、
  48/80テストと最終確認証拠を読み戻して受入済み。V4受入時点では次の一作業を未定義としたが、
  その後RELEASE-1を実行した。

## 2026-08-11 SUPPLY-VIEW-1 岩国市・光市のGTFS供給比較画面（V3時点の実装担当自己検証・履歴）

### なぜこの作業をしたか

検証済みJSONがあっても、交通担当者やGTFSを初めて見る人は、岩国市関連・光市関連の
フィードに何が収録され、具体的な同じ1週間に何便が予定されているかを画面から確認できなかった。
今回の目的は、出典、公式ページの日付、GTFS内部日付、確認日、計算日、対象範囲を混同せず、
2フィードの構造3指標と7実日付の予定運行便を切替操作なしで確認できるようにすることだった。

### やったこと

1. `src/build_site_data.py`へ、受入済み入力JSONのSHA256・構造・状態・共通比較週を検証してから、
   `docs/data/gtfs_supply_metrics.json`へバイト同一コピーする処理を追加した。
2. `docs/index.html`の公式GTFS確認状況直後、市町選択直前へ独立比較区画を追加した。
   構造3指標表、7実日付の日別表、状態語、フィード別`details`根拠欄をJSONから描画する。
3. 既存3 JSONの読込と新しい供給JSONの読込を別の`Promise.all`・別の失敗表示に分離した。
   JSON由来文字列は`textContent`等で出力し、`innerHTML`は使っていない。
4. `tests/test_site.py`へ、バイト一致、固定20値、状態整合、異常入力拒否、2表・根拠・JSONリンク、
   数値非ハードコード、失敗分離、安全DOM、レスポンシブ、README/状況ページ同期の11テストを追加した。
5. READMEと状況ページを、SUPPLY-VIEW-1の利用者成果、2 ZIPの検査済み状態、実測テスト件数へ同期した。

### 確認できた事実

- 入力と静的サイト用JSONは5904 bytes、SHA256
  `26167df77efce48e6dbcacde757a08ff40f7229fe99b9928f25b541f3766db9b`でバイト一致した
  （`evidence/20260810_supply_view1_build_raw.txt`）。
- 固定20値は岩国市関連が1/46/800・185/186/183/161/188/146/39、光市関連が
  1/7/172・55/55/55/55/55/41/41で、全て`measured`・`reason=null`のまま表示入力にした。
- 集中テスト48件、全unittest 80件が成功した
  （`evidence/20260810_supply_view1_focused_test_raw.txt`、
  `evidence/20260810_supply_view1_unittest_full_raw.txt`）。
- 旧版比較はREADME 43トークン、状況ページ0トークン、index 18トークンで欠落0・全て合格した
  （`evidence/20260810_supply_view1_spec_coverage_raw.txt`）。
- JavaScript構文検査は合格、`innerHTML` 0件、比較区画は市町選択より前、比較週の日付・実測値の
  HTML/JavaScriptへの二重転記なし
  （`evidence/20260810_supply_view1_static_check_raw.txt`）。
- `git diff --check`終了コード0、staged 0件、dirtyテキストの秘密情報らしき値0件。
  入力JSONと原本2 ZIPのSHA256も受入済み値から不変
  （`evidence/20260810_supply_view1_final_checks_raw.txt`、文書同期後の再確認は
  `evidence/20260810_supply_view1_postsync_checks_raw.txt`）。

### V3時点で詰まったこと（履歴）

- V3当時、ローカルHTTPはHTTP 200で到達できたが、writer環境のBrowser runtimeは
  `No browser is available`、利用可能一覧も`[]`だった。したがって1440px幅・390×844相当の
  実画面目視、ページ横スクロール0、コンソールエラー0は当時未確認で、成功とは記録しなかった
  （`evidence/20260810_supply_view1_browser_unavailable_raw.txt`）。
- SPEC.mdとの食い違いは見つけていない。このV3時点の残件は、上記V4節のEdge/CDP実測28/28で
  解消し、Codexが`run_record.md` rev.36でSUPPLY-VIEW-1を受入済みとした。

### V4での解消と現在地

- Edge/CDPで1440×1600・390×844、横overflow 0、エラー4系統0、20値一致を確認し、Codexの
  独立読戻しまで完了した。SUPPLY-VIEW-1に未解消条件はない。V4受入時点では次作業を未定義とし、
  その後RELEASE-1を実行した。
- V4受入時点ではcommit、push、GitHub Pages更新、公開、地図、経路検索、船木鉄道取得、次機能へ
  進まなかった。現在の公開状態は前掲RELEASE-1節を正とする。

## 2026-08-10 SUPPLY-METRIC-2-CORR-1 exact_times割り切れ要件とroute_id/service_id参照整合性の訂正

Codexの受入監査で、直前2件のSUPPLY-METRIC-2記録（本ファイルの「SUPPLY-METRIC-2 岩国市・光市の
輸送供給指標計算」「SUPPLY-METRIC-2 岩国市・光市GTFS指標の計算」の各「実装上の判断」節）に
書いた2つの解釈がSPEC.md §15.2・§15.4.3の要件を満たしていないと指摘された。1作業限定の訂正。
直前2件の記録は書き換えず残し、本節で訂正内容を追記する。

### やったこと

1. 具体的な指摘内容を確認した:
   - SPEC.md §15.4.3手順5「時刻範囲が正で割り切れることを検証し」を「割り切れなくてもよい」と
     誤読していた。3900秒/600秒（余り300）のexact_times=1をmeasured 7と算出する実装だったが、
     正しくはinvalid_inputにすべきだった
   - `trips.txt`の`route_id`/`service_id`の参照整合性検証が欠落しており、未知の`service_id`が
     measured 0（無視されるだけ）、未知の`route_id`がmeasured 1（そのままカウント）になる、
     `route_id`列が丸ごと無くてもmeasured 1になる、という3つの不具合があった
2. `src/calculate_gtfs_supply_metrics.py`の`_prepare_trip_count_context()`を修正した:
   - exact_times=1行に`(end_time - start_time) % headway_secs != 0`の検証を追加し、
     割り切れない場合はinvalid_inputにした。便数計算式も余り処理が不要になったため
     `(end_sec - start_sec) // headway_secs`へ単純化した
   - `routes: FileTable`引数を追加し、`routes.txt`のroute_id集合（存在・列・空欄・重複を検証）
     を確定してから、`trips.txt`の必須列を`(route_id, service_id, trip_id)`へ拡張し、
     各行のroute_id/service_idの空欄・参照先の妥当性を検証するようにした
     （`calendar_dates.txt`だけで定義されたservice_idは引き続き有効な参照先として扱う）
   - `calendar.txt`の`start_date > end_date`行をinvalid_inputにする検証を追加した
   - モジュールdocstringの誤った判断記述を削除し、正しい要件の説明に置き換えた
3. 修正前に、事前確認用の一時スクリプト（実行後にリポジトリから削除）で、岩国市・光市の
   実ZIPがどちらも新しい検証条件（route_id/service_id参照・空欄・calendar日付範囲・
   frequencies.txt不在）に抵触しないことを確認してから修正に着手した
4. `python -B src/calculate_gtfs_supply_metrics.py`を再実行し、`data/gtfs_supply_metrics.json`の
   SHA256が修正前と完全一致すること（実測値が不変であること）を確認した
5. `tests/test_gtfs_supply_metrics.py`を更新した。`3b_frequencies_exact_times_1_remainder_rounds_up`
   （誤った解釈のテスト）を`3b_frequencies_exact_times_1_non_divisible_invalid_input`へ
   置き換え、新規セクション「6. trips.txtのroute_id/service_id参照整合性」として
   `6a`〜`6j`の10件を追加した（合計29件→39件）
6. `python -m unittest discover -s tests -v`を実行し、69件成功（既存68件は退行なし）を確認した
7. `git diff --check`・`git diff --cached --name-only`（0件）・`git status --short --branch`を
   実行し、変更・新規ファイルが許可範囲内であることを確認した
8. 修正内容・実測値不変の確認・テスト変更を`evidence/20260810_supply_metric2_corr1_fix_verification_raw.txt`
   ・`evidence/20260810_supply_metric2_corr1_git_diff_check.txt`へ記録し、内容が古くなった
   `evidence/20260810_supply_metric2_focused_test_raw.txt`・
   `evidence/20260810_supply_metric2_unittest_full_raw.txt`（29件のsubTest一覧・旧3bの記述を
   含んでいた）を最新状態へ再生成した
9. `verification.md`にSUPPLY-METRIC-2-CORR-1の検証結果を追記した
10. 作業中に作成した事前確認用の一時スクリプトはすべて実行後にリポジトリから削除した

### 確認できた事実（数字を含める。根拠はevidence/のファイル名で示す）

- 修正後の`data/gtfs_supply_metrics.json`のSHA256は
  `26167df77efce48e6dbcacde757a08ff40f7229fe99b9928f25b541f3766db9b`で、修正前と完全一致。
  岩国市・光市とも1フィードあたり10値（構造指標3+実日付7）、2フィード合計20値
  （agency=1/1、route_id=46/7、boarding_location=800/172、
  7日分の便数は岩国市185/186/183/161/188/146/39、光市55/55/55/55/55/41/41）は変わっていない
- 事前確認スクリプトで、岩国市・光市とも(a)trips.txtの全route_idがroutes.txtに存在、
  (b)trips.txtの全service_idがcalendar.txt∪calendar_dates.txtに存在、
  (c)route_id/service_idの空欄行が無い、(d)calendar.txtにstart_date>end_dateの行が無い、
  (e)frequencies.txtが存在しない、ことを確認した
  （`evidence/20260810_supply_metric2_corr1_fix_verification_raw.txt`）
- `tests/test_gtfs_supply_metrics.py`のsubTestは29件から39件（`3b`を1件修正、`6a`〜`6j`の
  10件を新規追加）。`python -B -m unittest tests.test_gtfs_supply_metrics -v`は
  `Ran 1 test`・`OK`（内部の39件すべて成功。失敗があれば個別に報告される仕様のため、
  失敗0件が全件成功の証跡）
- `python -m unittest discover -s tests -v`: `Ran 69 tests`・`OK`（既存68件退行なし）。
  `evidence/20260810_supply_metric2_unittest_full_raw.txt`（再生成）
- `git diff --check`: 終了コード0、出力なし。`git diff --cached --name-only`: 0件。
  `evidence/20260810_supply_metric2_corr1_git_diff_check.txt`

### 詰まったこと、SPEC.mdと食い違ったこと

- SPEC.md本文との食い違いは無い。今回の訂正はSPEC.md §15.2・§15.4.3の要件を実装が
  満たしていなかった不具合の修正であり、SPEC.mdの記載自体は変更していない

### 次にやること

- Codexが`src/calculate_gtfs_supply_metrics.py`の実装、`tests/test_gtfs_supply_metrics.py`の
  39件、`evidence/20260810_supply_metric2_corr1_*`の証拠を読み戻して受入判定すること
- SUPPLY-METRIC-2の対象外（比較週の一般選定アルゴリズム、他17市町への汎用化、追加指標、
  地図・経路検索、船木鉄道取得、commit・push・公開）はいずれも指示があるまで着手しない

## 2026-08-11 ENTRY-PAGE-1 現在地（末尾正本）

詳細は上掲「ENTRY-PAGE-1 作品①の応募説明ページ」を参照。現在はローカル実装、集中54件・全86件、
SPEC旧版693トークン欠落0、ローカルEdge/CDP補正後30/30、2 PNG目視まで完了した。
残件は公開前最終検査、commit・push・Pages配備、公開`entry.html`の再測定である。
UDC概要フォーム、本応募、BODIK APPs登録は行っていない。

## 2026-08-11 ENTRY-PAGE-1 公開受入（末尾正本）

- 公開前最終検査は、集中54/54、全86/86、SPEC旧版693トークン欠落0、diff check、
  staged 0、秘密情報0、末尾空白0、変更範囲、JSON・2 ZIP不変の全項目が合格した。
- implementation commit `e63a7b8745aa494df9263d1e2f07ca289d28fbb8`を`origin/main`へpushした。
- GitHub Pages run `31462820456`はbuild・deployともsuccess。
- 公開`entry.html`はHTTP 200、14,892 bytes、ローカルとバイト一致、SHA256
  `3b3d9f6591cbc1fbf1c888a49e029e4ad830885671136984b43abe3a352c8d72`。
- 公開トップ、状況ページ、供給指標JSONもHTTP 200・ローカルとバイト一致。
- 公開URLの実Edge/CDPは1440×1600・390×844の両幅で30/30。overflow 0、
  console error、Runtime例外、Log error、Network.loadingFailedは全て0。
- Codexが公開2 PNGを読み、初見向けの問題説明、81字概要、使い方、審査3観点、限界、導線を確認した。

ENTRY-PAGE-1をCodex受入済みとし、ロードマップ07「応募用の公開物一式」は作品①について完了した。
次の一作業は未定義である。工程08のUDC概要フォーム送信またはBODIK APPs登録はG6の本人承認前に
開始しない。本応募、受賞、作品②は未達である。

## 2026-08-12 WORK1-FRESHNESS-1 ローカル実装・検証

### 実装したもの

- `data/source_freshness_manifest.json`に、受入済み4登録簿PDF・2 GTFS ZIPの公式URL、
  ローカルパス、bytes、SHA256、取得日、安全上限を固定した。
- `src/check_source_freshness.py`はローカル基準を先に検証し、HTTPSかつ許可2ホストだけを
  ストリーム取得する。本文を保存せず、`unchanged`・`changed`・`unavailable`・`oversize`・
  `invalid_baseline`を区別する。原本・派生データ・公開値への出力は拒否する。
- `.github/workflows/source-freshness.yml`は週1回と手動実行、`contents: read`だけとし、
  結果JSONをartifactへ保存する。自動commit、issue作成、Pages更新は含めていない。
- READMEと`docs/status.html`は固定確認日と最新Actions状態を分離し、固定HTMLを
  「現在も不変」という断定に使わない説明へ同期した。

### 初回実測と自動テスト

- 2026-08-11T22:47:51Zの実ネットワーク照合は、4 PDFが`unchanged`、2 GTFS ZIPが
  TLS接続不能で`unavailable`、他3状態は0、終了コード2。応答本文は証拠へ保存していない。
- WORK1集中16件と全102件のunittestが成功。旧SPEC 703トークンは欠落0。
- 秘密情報検査は変更・新規テキスト全件で一致0。`git diff --check`は終了コード0、staged 0件。
- 6原本、既存データ、公開JSON、供給指標、`docs/index.html`、`docs/entry.html`の計23ファイルは、
  現行SHA256とGit clean-filter後のHEAD blobを照合し23/23不変だった。

### 実ブラウザと現在地

- 新しい状況節はCodex in-app browserの1440×1600・390×844で必須語彙、固定確認日、
  102件、Actions導線を読戻し、両幅overflow 0、error log 0。2 PNGを目視した。
- 既存アプリのEdge/CDP退行検証は28/28。両幅overflow 0、console error、
  `Runtime.exceptionThrown`、Log error、`Network.loadingFailed`は全て0。
- F3・F4とF5のローカル独立読戻しは完了した。commit・push・GitHub上のworkflow初回実行・
  Pages反映は行っておらず、WORK1-FRESHNESS-1は公開適用承認ゲートで停止している。
- UDC概要フォーム、本応募、BODIK APPs登録、作品②、次の強化段階へは進んでいない。
  次段階はWORK1-FRESHNESS-1の公開受入後に一つだけ定義する。

## 2026-08-12 WORK1-FRESHNESS-1 GitHub公開・最終受入

- 本人がcommit、`main`へのpush、GitHub Actions適用、GitHub Pages更新、公開後検証を明示承認した。
- 実装22件を`1c11e61458440a6a495a9cf6b55d1ff58753c851`、公式actionsのv7更新を
  `4f681aafd2076537c48d2ecf4f18632b452657fb`としてcommit・pushした。
- 最終手動run `31547441902`は、4 PDFを`unchanged`、岩国・光GTFSを`tls_error`による
  `unavailable`、他3状態を0と判定し、終了コード2となった。step 5の結果artifact保存は成功した。
  これは取得不能を通知する仕様どおりの`failure`で、検査器故障ではない。
- `actions/checkout`、`actions/setup-python`、`actions/upload-artifact`をGitHub公式の現行v7へ更新し、
  2回目のrunでNode.js 20非推奨警告が消えたことを確認した。
- Pages build run `31546971782`は成功。公開`status.html`はHTTP 200、公開blobとimplementation
  commitのblob `e66f8983590164f887345d0c185c6a2a811a0574`が一致した。
- 公開URLを1440×1600・390×844で再測定し、必須文言とActions導線あり、古い承認待ち文言なし、
  両幅overflow 0、console error 0。公開スクリーンショット2件を目視した。
- 全102件成功、保護対象不変、原本・派生データ・公開値の自動更新なしを維持した。
  `evidence/20260812_work1_freshness_public_acceptance.json`を公開後受入の正本とする。
- 以上によりWORK1-FRESHNESS-1を最終受入済みとする。UDC概要フォーム、本応募、BODIK APPs登録、
  作品②の変更は行っていない。
- 次段階は`WORK1-AWARD-COMPARISON-1`だけを定義する。作品①の公開成果をUDC2026公式審査観点と
  証拠へ対応づけ、作品②でも共用できる尺度の比較用スコアカードにする。実装はまだ開始しない。

## 2026-08-12 WORK1-SCOPE-LOCK-1 ローカル実装・検証

- 本人指示により、作品②を作品①Chatから絶対に操作対象へ入れない境界段階を追加した。
  作品②の場所・repo名・ファイルは調べず、作品①だけをallowlistにするdeny-by-default方式とした。
- `AGENTS.md`は、親・兄弟ディレクトリの探索、別作品の読取り・変更・実行、別repoへの`git -C`、
  別作品を調べるエージェント委譲を禁止する。境界違反時は回避せず安全停止する。
- `work_scope.json`と`src/check_work_scope.py`は、実行repo、Git root、作品①origin、候補パスを照合し、
  repo外、親移動、別repo引数を内容を読まずに終了コード2で拒否する。
- `.github/workflows/work1-scope-lock.yml`はpush・PR・手動実行でread-only検査を行う。
  GitHub公式actions v7、`contents: read`だけで、commit・push・Pages更新処理を持たない。
- 境界集中11件と全113件のunittestが成功。ガードへ実変更7件、公開表示2件、記録6件を渡し、
  すべて作品①repo内として許可された。
- `WORK_SCOPE.md`と公開`docs/status.html`へ、作品①・不可視の作品②・人の比較ゲートを分離した
  全体DAGを追加した。ローカル1440×1600・390×844は両幅overflow 0、console error 0で目視済み。
- OS全体のACLは変更していない。Windows権限による物理隔離が必要なら別workspaceまたは別アカウントが
  必要だが、このChat・repo・CIの操作契約は作品①だけにfail-closedした。
- 正本は`evidence/20260812_work1_scope_lock_local.json`。公開後受入までは
  `WORK1-AWARD-COMPARISON-1`を開始しない。

### GitHub公開・最終受入

- implementation commit `ae86f0a3018c4187feefa80afcb26f382e043f69`を`main`へpushした。
- Work 1 scope lock run `31549042703`は、作品①checkout、origin allowlist、外部パス拒否テストを含む
  全stepが`success`。Pages run `31549042228`も`success`だった。
- 公開`status.html`はHTTP 200、公開blobとcommit blobは
  `f9299d00bb1628177034819b91832c35536ca5ee`で一致した。
- 公開1440×1600・390×844で作品①レーン、不可視の作品②レーン、人の比較ゲート、DAG導線を確認。
  両幅overflow 0、console error 0、既存freshness節あり。
- HEAD＝origin/main、cleanを読み戻し、WORK1-SCOPE-LOCK-1を最終受入済みとする。
  次段階は定義済みの`WORK1-AWARD-COMPARISON-1`だけで、まだ開始していない。

## 2026-08-12 WORK1-AWARD-COMPARISON-1 ローカル実装・検証

- UDC2026公式作品募集要領を一次情報で読み戻した。一次審査基準は実用度・完成度・挑戦度、
  数値配点は非公開。「道路・交通」と「教育・政治」は赤字の2026年度重点分野で、重点分野作品には
  作品審査の優遇措置がある。
- `SPEC.md` rev.5.8 §20へ、0〜5点・0.5刻み、3観点同率、公開証拠・確信度・不足証拠必須の
  共通採点規則を固定した。内部比較値を公式点、順位、受賞確率へ読み替えない。
- 作品①の公開証拠だけで、実用度3.5、完成度4.0、挑戦度3.0、内部比較指数70.0 / 100とした。
  作品②を探索・参照・採点・変更せず、他作品からの入力は0件。
- 2026年特別賞は、GTFS賞`eligible`、BODIK賞`condition_unmet`、DATAKIDS賞`not_claimed`、
  JACIC賞`not_listed_2026`と分離した。BODIK登録・UDC応募は行っていない。
- `data/award_scorecard_schema.json`と`data/work1_award_scorecard.json`を正本とし、
  `docs/data/`の公開用JSONとバイト一致させた。`docs/award-comparison.html`は点数を直書きせず、
  JSONから3観点、4賞、改善上位3件を安全なDOM APIで描画する。
- 改善上位3件は、オンライン利用者検証、GTFS確認・実測範囲の拡張、具体タスクに沿う確認メモ出力。
  この段階では改善機能を実装せず、比較可能な停止点を作る。
- 集中12件、全125件のunittestが成功。SPEC旧版748トークンは欠落0、`git diff --check`成功。
- 1440×1600・390×844の同一オリジンiframe実測は、両幅overflow 0、ページ内runtime error 0、
  3観点・4賞・改善3件・70.0を確認。公開ページを直接開いたdev errorも0だった。
- 原本6件、既存データ・公開JSON、`docs/index.html`の保護22件は22/22不変。
- ローカル判定はGitHub適用へ進めるGO。commit・push・Pages反映・公開URL読戻しは未実施で、
  独立監査、注力判断、外部提出も行っていない。

## 2026-08-12 WORK1-AWARD-COMPARISON-1 GitHub公開・最終受入

- 実装20件を`a96aa04c971049e06251bd5b8e4998e731c47ea7`として`main`へcommit・pushした。
- Work 1 scope lock run `31551751898`とPages run `31551751667`は、head SHA一致で`success`。
- 公開HTML、schema JSON、scorecard JSON、応募説明、検証状況の5件はHTTP 200。公開bytesの
  SHA-256はcommit済みファイルと5/5一致した。
- 公開スコアカードを新規ブラウザタブの1280×720で確認し、内部比較指数70.0、実用度3.5、
  完成度4.0、挑戦度3.0、公式点ではない旨、他作品入力0件を読み戻した。横overflow、ページ内
  runtime error、Browser dev errorはいずれも0。
- ローカル1440×1600・390×844の受入と公開bytes一致を合わせ、PC・スマートフォンへ同一成果物が
  配信されたことを固定した。正本は
  `evidence/20260812_work1_award_comparison_public_acceptance.json`。
- 原本6件、既存公開JSON・計算値、公開トップの値は変更していない。作品②、UDC応募、
  BODIK APPs登録、注力判断、独立監査は実施していない。
- WORK1-AWARD-COMPARISON-1を最終受入済みとする。次段階はread-onlyの
  `WORK1-AWARD-AUDIT-1`だけを定義し、開始直前で停止する。

## 2026-08-12 WORK1-TASK-MEMO-1 ローカル実装・検証

- `WORK1-AWARD-AUDIT-1`は固定HEAD・公開bytes・採点計算・証拠リンク・作品②入力0件を
  read-onlyで監査し、P0・P1なしのGOだった。比較可能は完成ではないという本人方針に従い、
  作品①単独の改善を再開した。
- `docs/municipality-memo.html`を追加し、既存公開4 JSONだけから19市町の登録供給、GTFS確認状態、
  測定済み指標、限界、次の確認事項を一枚に構成した。市町付きURL、トップとの往復、URLコピー、
  印刷・PDF保存に対応する。
- 登録0件とGTFS未確認を不存在へ読み替えず、測定値のない市町へ値を補完しない。原本、`data/`、
  `docs/data/`、処理・計算コード、スコアカードJSONは変更していない。作品②入力は0件。
- 集中14件、全139件のunittestが成功。SPEC rev.5.8→rev.5.9は809トークン欠落0、
  `git diff --check`成功、秘密情報らしき値0、禁止範囲の変更0。
- 実ブラウザで下関市、宇部市、岩国市の3分岐を確認。1440×1600・390×844とも横overflow 0、
  runtime error 0。印刷操作と印刷CSSの操作部非表示・メモ本文・リンク保持を確認した。
- 正本は`evidence/20260812_work1_task_memo_local_acceptance.json`、ブラウザ要約は
  `evidence/20260812_work1_task_memo_browser_summary.txt`。判定はGitHub適用へ進めるローカルGO。
  commit・push・Pages・公開URL読戻しは未完了で、独立監査はまだ開始しない。

### GitHub公開・最終受入

- 実装14件を`8bf5a1f32c3744918d2db8d35acd8d100d6b98d1`として`main`へcommit・pushした。
- Work 1 scope lock run `31557300073`とPages run `31557299397`はhead SHA一致で`success`。
- 公開メモ、トップ、応募説明、状況ページはHTTP 200でcommit bytesと4/4一致。既存3 JSONの差異は
  PagesのCRLF→LF正規化だけで、改行正規化後bytesとJSON内容が一致した。指標JSONはbytes一致。
- 公開1440×1600の岩国市と390×844の宇部市を実測し、代表分岐、横overflow 0、ページ内runtime
  error 0、Browser dev error 0を確認。公開トップから岩国市付きメモへ往復できた。
- 正本は`evidence/20260812_work1_task_memo_public_acceptance.json`。WORK1-TASK-MEMO-1を
  最終受入済みとする。次段階はread-onlyの`WORK1-TASK-MEMO-AUDIT-1`だけで、まだ開始しない。

## 2026-08-12 WORK1-GTFS-COVERAGE-2 ローカルGO

- 未確認14市町の公式資料を再探索し、新規採用可能GTFS ZIPは0件と記録した。受入済み岩国市・
  光市ZIPは公式配布バイトとサイズ・SHA-256が一致し、原本や公開値の自動採用は行っていない。
- 光市GTFS「広域生活交通」の94乗降停留所IDを確認し、光市63・周南市31を特定した。周南市内
  31 ID・17停留所名を根拠に、周南市を光市フィードの関連市町へ訂正した。
- 公開メモは市町に関連するfeed IDで実測を参照するようにし、周南市では光市フィード全体値と
  明示した。周南市内だけの値は分割・推計せず、未確認市町にも値を補完しない。
- GTFS関連確認6 / 19市町、未確認13 / 19市町、実測2フィード。全148 unittest成功。
- PC 1440×1600、スマートフォン390×844、光市・周南市・下関市の代表分岐、印刷を確認し、
  横overflow、runtime error、Browser dev errorはいずれも0。ローカル判定はGO。
- 証拠は`evidence/20260812_work1_gtfs_coverage_*.json`。作品②、外部参加者への連絡、UDC応募、
  BODIK登録は0件。次は同段階のcommit・push・Actions・Pages・公開読戻しである。

### GitHub公開・最終受入

- 実装26ファイルを`99f255113496d1c3b540a577e37f79da52d4aec1`として`main`へcommit・pushした。
- Work 1 scope lock run `31586538279`とPages run `31586537631`は同じhead SHAで`success`。
- 公開メモ・状況ページ・JSON 3件はHTTP 200。HTML 2件と指標JSONはcommit bytes一致、残る
  JSON 2件もPagesのCRLF→LF正規化後に内容一致した。
- 公開PC 1440×1600の周南市、公開390×844の周南市・下関市、状況ページのPC・390pxを確認し、
  関連フィード全体値、未確認時の非推計、横overflow 0、runtime/dev error 0を受け入れた。
- 正本は`evidence/20260812_work1_gtfs_coverage_public_acceptance.json`。
  WORK1-GTFS-COVERAGE-2を最終受入済みとし、次はread-onlyの
  `WORK1-GTFS-COVERAGE-AUDIT-1`だけを定義して未着手で停止する。

## 2026-08-12 WORK1-AWARD-COMPARISON-2 ローカルGO

- 固定HEAD `15cfe4b4ef7418793346ef1040d3c3d6d0056f51`のGTFS範囲独立監査は、
  P0 0件、P1 0件、P2 1件でGO。P2だった「9 / 10・次は外部提出」を、公開基盤の上で
  比較・独立監査・改善を反復する継続モデルへ訂正した。
- 2026年公式要項の実用度、完成度、挑戦度、道路・交通の重点分野、GTFS賞条件を再確認。
  数値配点は非公開のままで、内部比較値を公式点・順位・受賞確率とは表示しない。
- 市町別確認メモの公開を根拠に`method_fit`を4.0から4.5へ更新した。GTFS関連確認6 / 19、
  未確認13 / 19、関連実測表示3市町、実測2フィードを反映したが、新規採用フィードと外部利用者
  証拠は増えていないため、実用度3.5、完成度4.0、挑戦度3.0、総合70.0は据え置いた。
- 改善上位3件を、別の人間承認ゲートであるオンライン利用者検証と、自力実行可能な
  類似サービス比較・再生成復旧訓練へ分離した。参加者への連絡は行っていない。
- 集中14件、全150件のunittest、`git diff --check`が成功。正本と公開スコアカードJSONは
  byte一致し、raw、受入済みZIP、既存実測値、公開トップ、応募説明の変更は0件。
- 比較ページ・状況ページを1440×1600と390×844で実ブラウザ確認し、横overflow、ページ内
  runtime error、Browser dev error / warningはいずれも0。ローカル判定はGitHub適用へ進めるGO。
- 正本は`evidence/20260812_work1_award_comparison2_official_recheck.json`と
  `evidence/20260812_work1_award_comparison2_local_acceptance.json`。作品②入力、外部連絡、
  UDC応募、BODIK登録は0件。次は同段階のcommit・push・Actions・Pages・公開読戻しである。

### GitHub公開・最終受入

- 実装commit `a963cee4bc7146c525af888f9dcd9c8f60de3a24`を`main`へpushした。
- Work 1 scope lock run `31592318490`とPages run `31592317621`は同じhead SHAで`success`。
- 公開比較HTML、状況HTML、スコアカードJSONはHTTP 200でcommit bytesと3/3一致した。
- 公開1440×1600・390×844で比較値70.0、方法適合性4.5、人間承認1件、自力候補2件、
  継続改善、次の独立監査を読み戻した。横overflow、runtime error、Browser dev error / warningは0。
- 正本は`evidence/20260812_work1_award_comparison2_public_acceptance.json`。
  WORK1-AWARD-COMPARISON-2を最終受入済みとし、次はread-onlyの
  `WORK1-AWARD-COMPARISON-AUDIT-2`だけを定義して未着手で停止する。

## WORK1-AWARD-COMPARISON-TRACEABILITY-1（2026-08-12 ローカルGO）

- 固定HEAD `8452d6cc8892b1f55fa5f652eb5d82cf4a3fcd59`の独立監査はP0 0件、P1 0件、
  P2 2件でGO。公式用語の揺れと状況ページの比較第2版参照不足だけを修正した。
- 現行比較記録は公式表記「実用度・完成度・挑戦度」に一致し、旧表記0件。
  `docs/status.html`は`SPEC.md §20・§23`を表示し、回帰検査で固定した。
- 比較集中14件・全150件のunittest、旧SPEC 865トークン欠落0、`git diff --check`、
  秘密情報0、許可外変更0、作品①スコープ検査が成功した。
- 4 PDF・2 GTFS ZIPはmanifestと6/6一致。スコアカード2 JSONは基準HEADとbytes一致し、
  SHA256 `0826a1851464cd7198f10f9eb4eddb0896c8af2c1a156e8a87cca49754d9d021`、総合70.0を維持した。
- ローカルPC 1440×1600・スマートフォン390×844で横overflow、Browser dev log、壊れた画像は0件。
  作品②入力、参加者連絡、UDC応募、BODIK登録は0件。次は同段階のcommit・push・Pages・公開読戻し。

### GitHub公開・最終受入

- 実装commit `7fdf04aa0ba2ddd75347402e4717188fc1c31ec5`を`main`へpushした。
- Work 1 scope lock run `31599775095`とPages run `31599774202`は同じhead SHAで`success`。
- 公開状況HTML・比較HTML・スコアカードJSONはHTTP 200でcommit bytesと3 / 3一致した。
- 公開1440×1600・390×844で公式3基準、総合70.0、`SPEC.md §20・§23`、現在段階、
  次のread-only監査を読み戻した。横overflow、Browser dev log、壊れた画像は0件。
- 正本は`evidence/20260812_work1_award_comparison_traceability_public_acceptance.json`。
  WORK1-AWARD-COMPARISON-TRACEABILITY-1を最終受入済みとし、次はread-onlyの
  `WORK1-AWARD-COMPARISON-TRACEABILITY-AUDIT-1`だけを定義して未着手で停止する。

## 2026-08-13 WORK1-RELEASE-ATTESTATION-1 実装・稼働確認開始

- 固定HEAD `a0fd712e5a00865430be9bdb2827aecd4fa8b54a`のread-only監査はGO、P0 0件、P1 0件、
  P2 1件。P2はrepo内の受入記録が最終commit自身と後続Actions run IDを記録できない自己参照。
- `SPEC.md` rev.5.13 §25に、Pages成功後の外部artifactで最終公開HEADを証明する段階を固定した。
- `.github/workflows/release-attestation.yml`は対象SHAを明示checkoutし、scope guardと全151テスト後、
  `src/build_release_attestation.py`で90日保存のJSON・Markdown artifactを生成する。
- JSONはattestation run、Pages run、対象SHA、公開3資産のcommit bytes、6原本、保護対象、
  実用度3.5・完成度4.0・挑戦度3.0・総合70.0、作品①境界を一括検証し、不一致時は`NO_GO`。
- 原本、ZIP、既存公開値、スコアカード、公開トップ、応募説明は不変。作品②入力、参加者連絡、
  UDC応募、BODIK登録は0件。commit、push、Pages、初回・最終artifactの読戻しまで同段階で続ける。
- 専用1件、集中15件、全151件、旧SPEC 879トークン欠落0、scope guard、6原本一致を受け入れた。
  ローカル正本は`evidence/20260813_work1_release_attestation_local_acceptance.json`。
- 実装commit `0c13efe`のscope lockとPagesはsuccessし、公開statusは同commit bytesへ更新された。
  初回attestation run `31645546295`はPages eventの旧SHA報告とLinux依存差を隠さず失敗した。
- workflowをWindows runner・`requirements.txt`導入・`main`明示checkoutへ修正する。Pages報告SHAは
  artifactへ別記し、対象SHAとの実配信結合は公開3資産のbytes一致でfail closed検証する。
- 修正commit `3cdccf6`のscope lock・Pagesはsuccess。attestation run `31645964105`は
  Windows checkoutのCRLF変換と非UTF-8標準出力を検出して停止した。checkout前にLF・Python UTF-8を
  固定し、既存JSON・CSV bytesと日本語テスト出力をローカル受入条件へ一致させる。

### 初回外部artifact GO

- commit `3dd769397316176d5e043f96a70d2208d9d2af66`のscope lock `31646185332`、Pages
  `31646184526`、release attestation `31646234472`は同一SHA・success。
- artifact `9160820965`をダウンロードし、decision GO、151 / 151テスト、公開3 / 3、原本6 / 6、
  保護対象3 / 3、errors 0、作品②入力・参加者連絡・応募・BODIK各0件を確認した。
- 正本は`evidence/20260813_work1_release_attestation_public_acceptance.json`。初回結果を最終commitへ
  反映し、その最終HEAD自身の新しいartifactを外部正本として再確認する。

## 2026-08-13 WORK1-RELEASE-EVIDENCE-HARDENING-1 ローカル実装

- 監査対象HEAD `ed1f0b4`はGO（P0 0件、P1 0件、P2 3件）。P2三点をこの段階の入力にした。
- 最終attestation JSONの可読LF版と完全再現用Base64正本、監査結果JSONを`docs/data/`へ固定した。
  Base64復号後は7,446 bytes、SHA256
  `6b47326a3066e7cf08231e901f2470d91086d34a7a2025baf47f00f09abf85ab`。
- attestationの公開bytes照合を3資産から6資産へ拡張し、Pages報告SHA不一致をNO_GO条件へ追加した。
- workflowが観測したHTTP GETと、作業者が宣言する作品①の境界を別オブジェクトへ分離した。
- 専用テスト1 / 1、全151 / 151テスト、`git diff --check`が成功した。旧SPECの検査対象904トークンは
  欠落0件。ローカル正本は
  `evidence/20260813_work1_release_evidence_hardening_local_acceptance.json`。
  push、Actions artifact、公開ブラウザ受入は続行する。

### 初回強化artifact GO

- commit `7b4527a8737166acebead845afc3a0549cc40227`のscope lock `31650244128`、
  Pages `31650243548`、release attestation `31650293402`は同一SHA・success。
- artifact `9162307877`をダウンロードし、GO、151 / 151、公開6 / 6、原本6 / 6、保護3 / 3、
  Pages報告SHA一致、観測GET 6件、境界各0、errors 0を確認した。
- 公開6資産はHTTP 200かつローカルbytesと一致。正本は
  `evidence/20260813_work1_release_evidence_hardening_public_acceptance.json`。
  この記録を反映した最終HEAD自身を次のartifactで再確認する。

### 最終受入の外部正本

- 初回GO記録を含む最終HEAD自身もscope lock、Pages、release attestationで再確認する。
- 最終run ID、artifact digest、公開ブラウザ結果は外部正本として読み戻し、自己参照を避けるため
  repoへ再書込みしない。次段階はread-onlyの
  `WORK1-RELEASE-EVIDENCE-HARDENING-AUDIT-1`だけとする。

## 2026-08-13 WORK1-RELEASE-EVIDENCE-PERMALINK-1 開始

- 固定HEAD `618d6367796b26a5b206aacea983b38834393e16`の強化後独立監査はGO、P0 0件、P1 0件、
  P2 1件。P2は最終artifactの90日失効と公開物から最終runを恒久的に発見できない点である。
- GitHub Release・tagは0件で衝突なし。`/releases/latest`と固定資産名を先にmainへ書き、
  その最終commit自身のCI完了後、同じSHAのannotated tagとReleaseへ証拠を固定する。
- 添付するのはartifact ZIP、attestation JSON・Markdown、来歴JSON、SHA256一覧の5資産。
  Release公開後はmainを再変更せず、公開URL・bytes・SHA256を外部から読戻す。
- Release/tagは検証証拠の公開でありUDC提出ではない。作品②、参加者連絡、応募・BODIK、
  原本、公開値、スコアカード、公開トップ、応募説明、workflow権限は変更しない。
- 次段階はread-onlyの`WORK1-RELEASE-EVIDENCE-PERMALINK-AUDIT-1`だけとし、まだ開始しない。

## 2026-08-13 WORK1-PUBLIC-INFORMATION-ARCHITECTURE-1 ローカル実装

- WORK1-RELEASE-EVIDENCE-PERMALINK-1は最終HEAD `1858190c47ad364a99fbf4fa32cfaf607279073d`と
  公開Releaseの5資産まで最終受入済み。
- 改善判断用の数値診断は`data/`の正本2件に保持し、Pages用HTML・JSON・schema 3件を削除した。
- 応募説明を「利用価値と現在の到達点」へ改め、自己採点ではなく利用価値・実装・新しい取組の
  事実を示す。状況・README・WORK_SCOPEも同じ公開情報設計へ同期した。
- attestation公開照合を6資産から4資産へ変更し、内部診断がPagesに再混入した場合は
  `internal_scorecard_published_to_pages`でNO_GOにする。
- 関連69 / 69、全151 / 151テスト、scope checker、`git diff --check`はsuccess。旧SPECの
  検査対象941トークンは欠落0件。CI、Pages、PC・スマートフォン公開受入は続行する。
- ローカル正本は`evidence/20260813_work1_public_information_architecture_local_acceptance.json`。
- 作品②入力、参加者連絡、UDC概要フォーム、本応募、BODIK登録は各0件。

### 初回公開受入GO

- commit `48f5f90e6edbc104927095f6c4b0f4f03fe697ad`のscope lock `31669272686`、Pages
  `31669272478`、release attestation `31669319617`は全success。
- artifact `9169120899`はGO、151 / 151、公開4 / 4、原本6 / 6、保護3 / 3、
  `pages_copy_absent=true`、errors 0。正本は
  `evidence/20260813_work1_public_information_architecture_public_acceptance.json`。
- PC 1440×1600・スマートフォン390×844は横overflow、壊れた画像、dev log各0。
  現行4 URLは200、削除3 URLは404。公開HTMLに旧スコアリンクは0件。
- この記録を含む最終HEAD自身を同じ3 workflowで外部再確認する。次段階はread-only監査だけ。

## 2026-08-19 WORK1-WHY-TO-PRECONSULTATION-FLOW-CORRECTION-1 ローカル受入

- 基準HEAD `7f06e3af77f4f128a914148a1a510d6915f99957`の変更なし利用導線監査は、
  P0 0 / P1 2 / P2 2、NO_GO。機械的な19市町の導線は成立したが、Whyの公開不在、古いGTFS説明、
  市町選択の発見性、市町別の不足情報整理に未完了があった。
- 確定Whyと現在の第一段階を公開4 HTMLへ明示し、トップの主要操作から市町選択、現在確認できる
  登録・公式交通データ、日付・対象範囲、分からないこと、行政・事業者への確認事項、同じ市町の
  協議前確認メモまでを一つの導線にした。
- 市町別GTFS確認日は選択行の`2026-08-13`へ連動。取得・安全確認・解析済みと、認証・一般非配布・
  取得不能を混同する古い一律説明を解消した。
- 専用6 / 6、関連契約81 / 81、全196 / 196、scope guard、`git diff --check`がsuccess。
  PC 1280×720・スマートフォン390×844で6状態組合せを確認し、横overflow・runtime error・
  developer log warning/errorは各0。
- 原本、`data/`、`docs/data/`、公開JSON・CSV、計算コード、内部診断、作品②入力は各0変更。
  外部連絡、利用者テスト依頼、応募・BODIK、新原本取得・採用、外部送信は各0実行。
- 正本は`evidence/20260819_work1_why_to_preconsultation_flow_correction_local_acceptance.json`。
  判定は`LOCAL_GO`。次は同一HEADのCI・Pages・公開byte・公開実画面受入。

## 2026-08-18 WORK1-AWARD-SCORECARD-RECALIBRATION-4 ローカル受入

- 直前の変更なし監査はGO、P0 0 / P1 0 / P2 1。P2は完了済みの独立再現訓練が
  改善候補に残っていた記録上の軽微な不整合である。
- Ubuntuで固定7原本から17生成物を完全byte一致で再作成し、欠損から復旧した実績、
  `pdfplumber==0.11.10`、190 / 190テストを内部診断へ追加した。
- `continuity`を4.0→4.5へ更新した。人間による第三者再現ではないため
  `reproducibility_verifiability`は4.5を維持した。
- 実用度3.5、完成度4.5、挑戦度3.5、総合比較値76.7、利用者価値2.0、関係アクター1.0は据え置いた。
- 完了済み再現訓練を改善候補から除外した。次の自走候補は操作アクセシビリティ監査とし、
  利用者検証は連絡の人間承認ゲート、残存公開GTFS供給測定は原本受入ゲートとして分離した。
- 内部正本は28,683 bytes、SHA256
  `284574b2fb11528c803493a510238a7e9dc8f9e45e877933ef07c99eafa7979c`。
- Pages用内部診断3資産は不在を維持し、原本・公開値・公開5資産・作品②は変更していない。
- ローカル正本は`evidence/20260818_work1_award_scorecard_recalibration_4_local_acceptance.json`。
  判定は`LOCAL_GO`。同一HEADの4 workflow、両artifact、公開境界、Drive保存の受入まで完了扱いにしない。

### 公開受入GO

- 実装commit `97d8dd0ad2ab289968b99cfacc037c6121c0208f`のreproduction
  `32082844455`、scope lock `32082844340`、Pages `32082843686`、release attestation
  `32082918200`は同一SHAで全success。
- reproduction artifact `9305663647`はGO。Ubuntu / Python 3.12.13、`pdfplumber` 0.11.10、
  原本7 / 7、再作成・正規化・完全byte一致17 / 17、欠損復旧、190 / 190、errors 0。
- release artifact `9305689516`もGO。190 / 190、原本7 / 7、公開5 / 5、内部正本28,683 bytes・
  固定SHA一致、3基準3.5 / 4.5 / 3.5、総合76.7、Pages copy不在、errors 0。
- 公開4 HTMLとJRバスJSONはHTTP 200・commit bytes一致。内部診断3 URLは404、内部マーカー0件。
- 公開正本は`evidence/20260818_work1_award_scorecard_recalibration_4_public_acceptance.json`。
  判定は`GO`。次段階は変更なしの`WORK1-AWARD-SCORECARD-RECALIBRATION-4-AUDIT-1`だけ、未開始。
- 本記録commit自身の4 workflow、最終bundle、指定Driveへのbundle・公開正本保存は外部読戻しし、
  commit後に生成されるIDを自己参照させないためrepoへ再書込みしない。

## 2026-08-18 WORK1-INDEPENDENT-REPRODUCTION-DRILL-HARDENING-CLOSURE-1 GO

- read-only監査はP0 0件、P1 1件、P2 0件でNO-GO。成果自体ではなく、`run_record.md`と
  `verification.md`がDrive保存待ちを示し、歴史的な公開受入JSONに`drive_save_pending=true`が
  残る一方、最終HEAD再受入とDrive保存は完了済みという正本の食い違いを検出した。
- 基準HEAD `2cbcda1422119ff774cd345f485efc0a27d3eb1f`のreproduction `32062573376`、
  scope lock `32062573317`、Pages `32062572842`、release attestation `32062648070`は
  同一SHAで全success。再現artifact `9298673019`はstrict required、`pdfplumber` 0.11.10、
  原本7 / 7、正規化・完全byte一致17 / 17、復旧success、190 / 190、errors 0でGO。
- release artifact `9298717667`もGO、190 / 190、原本7 / 7、公開5 / 5、errors 0。
  公開5資産はHTTP 200・commit bytes一致、内部診断3 URLは404、公開HTMLの内部マーカーは0件。
- 指定Driveフォルダには検証済み`yamaguchi-yusho-data-2cbcda1.bundle`（24,666,096 bytes、
  SHA256 `5b6aae145238827e7b646fc530cad560f3af94e9cc38e3b05c1eecd62bc220e5`）と公開受入JSONを
  保存・読戻し済み。歴史的JSONは改変せず、本closure証拠が保存完了を後置して正本を解消する。
- 本closure commit自身の4 workflowと最終closure bundle・JSONは、commit後に生成される外部IDを
  repoへ再書込みせず、同一SHA・決定的ファイル名・Drive読戻しを外部最終化証拠とする。
- 作品コード、workflow、依存、7原本、公開値、内部評価、作品②、参加者連絡、応募・BODIKの
  変更・実行は各0。判定は`GO`。
- 次段階は変更を伴わない
  `WORK1-INDEPENDENT-REPRODUCTION-DRILL-HARDENING-CLOSURE-AUDIT-1`だけとし、開始しない。

## 2026-08-18 WORK1-INDEPENDENT-REPRODUCTION-DRILL-HARDENING-1 ローカル受入

- 直前のread-only監査はP0 0件、P1 1件、P2 1件でNO-GO。Ubuntuでbyte 17 / 17の実績は
  あったがGO条件ではなく、直接依存の版固定とartifactへの実版記録もなかった。
- GitHub workflowをstrict modeにし、17件中1件でもbyte不一致ならNO-GO・非0終了とした。
  strict 16 / 17の拒否と17 / 17の許可を回帰テストで固定した。
- `pdfplumber==0.11.10`を固定し、再現artifactへ実際の導入版を記録する。
- Windows通常ドリルは原本7 / 7、再作成・内容一致17 / 17、byte 8 / 17、復旧success、
  snapshot全188 / 188、全acceptance checks true、errors 0でGO。
- Windows strict probeはbyte 8 / 17を検知し、exact byte check false、decision NO-GO、終了コード1。
  これはUbuntu CIで17 / 17未満を見逃さないことの期待どおりの失敗である。
- 専用10 / 10、全190 / 190、旧SPEC 1,096トークン欠落0、scope checker、
  `git diff --check`はsuccess。原本・公開値・公開HTML・内部評価の変更は0。
- ローカル正本は
  `evidence/20260818_work1_independent_reproduction_drill_hardening_local_acceptance.json`。
  判定は`LOCAL_GO`。公開同一HEAD受入とDrive正本保存が成功するまで段階完了とは扱わない。

### 公開受入GO

- 実装commit `8cc1dd1faa5ddce55a8df13e294625c577008d07`の専用reproduction
  `32061952130`、scope lock `32061952004`、Pages `32061951202`、release attestation
  `32062034517`は同一SHAで全success。
- reproduction artifact `9298453361`はstrict required、`pdfplumber` 0.11.10、原本7 / 7、
  再作成・正規化・完全byte一致各17 / 17、復旧success、全190 / 190、全checks true、errors 0でGO。
- release artifact `9298500411`もGO。190 / 190、原本7 / 7、公開5 / 5、保護内部評価の期待値・
  固定ハッシュ一致、Pages copy不在、errors 0。
- 公開4 HTMLとJRバス供給指標JSONはHTTP 200・commit bytes一致。内部診断3 URLは404、
  公開HTMLの内部点数・内部JSONマーカーは0件。
- 原本・GTFS ZIP・公開値・内部評価・作品②入力・参加者連絡・応募・BODIKは各0変更・0実行。
  公開正本は
  `evidence/20260818_work1_independent_reproduction_drill_hardening_public_acceptance.json`。
- 次は公開記録commit自身の同一HEAD 4 workflowを再確認し、bundleと公開正本を指定Google Driveへ
  保存・読戻す。そこまで成功した場合だけ段階を最終GOとする。

## 2026-08-17 WORK1-INDEPENDENT-REPRODUCTION-DRILL-1 ローカル受入

- 固定HEAD `69b51d0f305451de94d9d8f7c04ab755ea13176e`の再採点監査はread-onlyでGO、
  P0 0件、P1 0件。人間承認後、外部依存のない独立再現・復旧訓練を開始した。
- `git archive`の一時スナップショットだけで、manifest固定の4 PDF・3 GTFS ZIPを7 / 7照合し、
  5ビルダーが作る`data/`10件・`docs/data/`7件を削除状態から再生成した。
- Windowsローカルでは完全byte一致6 / 17、CRLFをLFへ正規化した内容一致17 / 17。
  差はOSのテキスト改行表現だけで、JSON値・CSVセル・行順・件数・数値の差は0。
- 一時スナップショット内の`docs/data/municipal_supply.json`を意図的に欠落させ、欠落検知後に
  `src/build_site_data.py`で復旧し、全17件が再び内容一致した。正本worktreeの削除・変更は0。
- 専用8 / 8、通常worktree全188 / 188、一時スナップショット全180 / 180、
  一時領域削除、scope checker、`git diff --check`はsuccess。
- Ubuntuのクリーンrunnerで同じドリルと全188件を実行し、専用artifactへJSON・Markdownを保存する
  read-only workflowを追加した。新規原本取得・自動採用・作品②入力・参加者連絡・応募・BODIKは各0。
- 初回Ubuntu run `32000044592`は原本7 / 7、内容17 / 17、byte 15 / 17、復旧successまで通過後、
  既存ZIP安全テストが`zipfile`のバックスラッシュ処理をWindows固有と仮定して1件失敗しNO-GO。
  artifact `9278075041`を読戻し、CSV 2件をLF固定し、テストを両OS許容へ修正した。安全判定は緩和しない。
- ローカル正本は
  `evidence/20260817_work1_independent_reproduction_drill_local_acceptance.json`。
  判定は`LOCAL_GO`。次は実装commitをmainへpushし、専用CI・scope lock・Pages・attestationを受け入れる。

### 公開受入GO

- 実装commit `67ae2331c2ba534b07792a051de274764847583b`とOS差修正commit
  `8bc365851400ff9d77337530efd50bbd0cacf92b`をmainへ適用した。
- 最終HEADの専用reproduction run `32000484380`、scope lock `32000484357`、Pages
  `32000484004`、release attestation `32000534195`は同一SHAで全success。
- reproduction artifact `9278207726`はGO。Ubuntu / Python 3.12で原本7 / 7、再作成17 / 17、
  正規化一致17 / 17、完全byte一致17 / 17、欠損検知・復旧success、全188 / 188、一時領域削除、errors 0。
- release artifact `9278233578`はGO、188 / 188、原本7 / 7、公開5 / 5、保護内部評価の期待値・
  固定ハッシュ一致、Pages copy不在、errors 0。
- 公開4 HTMLとJRバス供給指標JSONはHTTP 200かつcommit bytes一致。内部診断3 URLは404、
  公開HTMLの内部点数・内部JSONマーカーは0件。
- 原本、GTFS ZIP、公開値、内部評価、作品②入力、参加者連絡、応募・BODIKは各0変更・0実行。
  公開正本は`evidence/20260817_work1_independent_reproduction_drill_public_acceptance.json`。
- 判定は`GO`。次段階は変更なしの
  `WORK1-INDEPENDENT-REPRODUCTION-DRILL-AUDIT-1`だけとし、開始しない。この記録を含む最終HEAD自身を
  同じ4 workflowで外部再確認し、後続run IDはrepoへ再書込みしない。

## 2026-08-17 WORK1-AWARD-SCORECARD-RECALIBRATION-3 ローカル受入

- 固定HEAD `a573e78adc0019fb3d9d410813c306267c3a350a`のJRバス中国独立指標化監査はGO、
  P0 0件、P1 0件。原本・既存2フィード・公開配信にドリフトはなかった。
- JRバス中国の広域1フィードを既存2フィードへ混在させず独立公開した根拠だけを反映し、
  `output_comprehensiveness`を3.5→4.0へ更新した。第三者評価がない`method_fit`は4.5据え置き。
- 実用度3.5、完成度4.5を維持し、挑戦度3.0→3.5、総合比較値73.3→76.7となった。
  利用者価値2.0、関係アクター1.0は据え置いた。
- 完了済み`jrbus_supply_metrics_extension`を候補から除外した。利用者検証は人間承認ゲート、
  自走可能な最上位候補は`independent_reproduction_drill`、新規原本受入候補は別ゲートとした。
- 内部正本は27,275 bytes、SHA256
  `6a4c1e16e10835ced55243dcdb0c418045f0b5f44e78c1cd7d295aacfc931131`。
- 専用21 / 21、全180 / 180、旧SPEC 1,065トークン欠落0、scope checker、
  `git diff --check`、秘密情報定型検査0件はsuccess。
- Pages用内部診断3資産は不在。`raw/`、既存公開JSON、公開4 HTML、JRバス公開JSON、
  作品②入力、参加者連絡、応募・BODIK、原本自動採用、内部点数公開は各0変更・0実行。
- ローカル正本は
  `evidence/20260817_work1_award_scorecard_recalibration_3_local_acceptance.json`。
  判定は`LOCAL_GO`。次は実装commitをmainへpushし、同一HEADのCI・Pages・artifact・公開非露出を受け入れる。

### 公開受入GO

- 実装commit `adee2e563c6e366d2e72bcf834b2455a663f307a`のscope lock `31997422132`、
  Pages `31997421753`、release attestation `31997461330`は同一SHAで全success。
- artifact `9277289118`はGO、180 / 180、原本7 / 7、公開5 / 5、errors 0。
  保護した内部評価の期待値と固定ハッシュは一致し、Pages copyは不在だった。
- 公開4 HTMLとJRバス供給指標JSONはHTTP 200かつcommit bytes一致。内部診断HTML・JSON・schemaの
  3 URLは404を維持し、公開HTMLの内部点数・内部JSONマーカーは0件だった。
- 原本、GTFS ZIP、既存公開値、公開5資産、作品②入力、参加者連絡、応募・BODIK、
  原本自動採用、内部点数公開は各0変更・0実行。公開正本は
  `evidence/20260817_work1_award_scorecard_recalibration_3_public_acceptance.json`。
- 判定は`GO`。次段階は変更なしの
  `WORK1-AWARD-SCORECARD-RECALIBRATION-3-AUDIT-1`だけとし、開始しない。
  この記録を含む最終HEAD自身を同じ3 workflowで外部再確認する。

## 2026-08-15 WORK1-JRBUS-CHUGOKU-SUPPLY-METRIC-1 ローカル受入

- 受入済みJRバス中国GTFSを、既存2フィード比較から分離した広域フィード全体の独立指標として実装。
- 固定週2026-08-17〜23の構造指標は1 / 18 / 890、予定運行便は
  436 / 436 / 436 / 436 / 436 / 323 / 242。山口県関係4路線は関係根拠として原本照合。
- 出力2コピーは3,537 bytes、SHA256
  `eac7e55b0b548f2fe69cf8ce17a03d6d7f4516edf089da362ecbfcc371c6fc05`で完全一致。
  既存2フィードJSONは固定SHA256のまま不変。
- indexは別枠の構造3行・日別7行・関係4路線・原本・限界をJSONから表示。
  statusとentryも「市町別値ではない」ことを明記。
- 専用6 / 6、全180 / 180、旧SPEC 1,038トークン欠落0、scope checker、
  `git diff --check`はsuccess。
- PC 1440×1600・スマートフォン390×844とも、横overflow 0、runtime error 0、
  壊れた画像0。既存2フィード比較と市町ビューも同時に表示。
- 原本、自動採用、既存供給JSON、内部点数、作品②、参加者連絡、応募・BODIKは各0変更・0実行。
- ローカル正本:
  `evidence/20260815_work1_jrbus_supply_metrics_local_acceptance.json`。
  判定は`LOCAL_GO`。公開受入が成功するまで段階完了とは扱わない。

### 公開受入GO

- 実装commit `24f9daaa51c55c2ca7082902af33b2a4b574f12e`のscope lock `31992603588`、
  Pages `31992603178`、release attestation `31992641885`は同一SHAで全success。
- artifact `9275834888`はGO、180 / 180、原本7 / 7、公開5 / 5、errors 0。
  内部正本は26,259 bytes、SHA256
  `7dd51a118c5a32053c6c40d584f3c769bf8bfcea8bf24eda812bcf57ffeedea3`、
  3基準3.5 / 4.5 / 3.0、総合73.3、Pages copy不在で一致した。
- 公開4 HTMLと新しいJRバス供給指標JSONはHTTP 200・commit bytes一致。内部診断3 URLは404、
  公開HTMLの内部点数・内部JSONマーカーは0件。
- 原本、既存2フィード供給JSON、市町値推定、内部点数、作品②、参加者連絡、応募・BODIKは
  各0変更・0実行。公開正本は
  `evidence/20260817_work1_jrbus_supply_metrics_public_acceptance.json`。
- 判定は`GO`。次段階は変更なしの
  `WORK1-JRBUS-CHUGOKU-SUPPLY-METRIC-AUDIT-1`だけとし、開始しない。
  この記録を含む最終HEAD自身を同じ3 workflowで外部再確認する。

## 2026-08-14 WORK1-AWARD-SCORECARD-RECALIBRATION-2 ローカル受入

- 公式GTFS範囲拡張監査GO（P0 0 / P1 0 / P2 3）を入力に、内部診断を再評価した。
- `scope_reliability`は3.5→4.0、`output_comprehensiveness`は3.0→3.5。
- 実用度3.5、完成度4.5、挑戦度3.0、総合比較値73.3。利用者価値2.0と関係アクター1.0は据え置き。
- 完了済みの公式範囲拡張を改善候補から除外し、自走候補1位をJRバス中国GTFSの独立供給指標化とした。
- 内部正本は26,259 bytes、SHA256
  `7dd51a118c5a32053c6c40d584f3c769bf8bfcea8bf24eda812bcf57ffeedea3`。
- 専用21 / 21、全174 / 174、旧SPEC 1,023トークン欠落0、scope・差分検査はsuccess。
- 7原本、既存公開JSON・計算値、公開4 HTMLは不変。Pages用内部診断3資産は不在。
- 作品②入力、参加者連絡、応募・BODIK登録は各0。正本は
  `evidence/20260814_work1_award_scorecard_recalibration_2_local_acceptance.json`。
- 判定は`LOCAL_GO`。次はcommit・push・同一HEADの3 workflow・公開非露出受入。

### 公開受入GO

- 実装commit `4250c5d00dd6753712a61cda0f644ea382f760a3`のscope lock `31758902249`、
  Pages `31758901395`、attestation `31758940307`は同一SHAで全success。
- artifact `9203835239`はGO、174 / 174、原本7 / 7、公開4 / 4、内部診断26,259 bytes・
  SHA256一致、3基準3.5 / 4.5 / 3.0、総合73.3、Pages copy不在、errors 0。
- 公開4 HTMLはHTTP 200・commit bytes一致。内部診断3 URLは404、公開HTMLの内部点数・導線は0件。
- 正本は`evidence/20260814_work1_award_scorecard_recalibration_2_public_acceptance.json`。
- 次は記録commit自身の同一HEAD workflowを確認し、read-only独立監査の前で停止する。

## 2026-08-13 WORK1-OFFICIAL-GTFS-COVERAGE-EXTENSION-1 ローカル受入

- 固定HEAD `a00d2351d41a5f3c9f2db96405effc6cdd3b79f6`から、未確認13市町を公式一次資料で再調査した。
- ユーザー承認後、JRバス中国公式ZIP 1件を手動受入した。1,863,715 bytes、SHA256
  `9162224158a8a748d0365e850f2c0575c845a98063b7a469912c0a15b9201620`、ZIP安全性とCRCは合格。
- 公式フィード記録8件、19市町のアクセス状態は公開7、認証2、一般配布なし9、現在取得不能1。
  非公開・認証・取得不能の本体や値は補完していない。
- 原本変更検知を4 PDF・3 GTFS ZIPの7件へ拡張。実照合は5件`unchanged`、岩国・光の2 ZIPが
  `unavailable/tls_error`、`changed` 0。公開値の自動更新は0。
- 既存2フィードの輸送供給指標はバイト一致。JRバス中国の広域18路線を市町別供給比較へ混ぜていない。
- 専用5 / 5、全174 / 174、旧SPEC 1,000トークン欠落0、scope・差分・JSON検査はsuccess。
- ローカルPC 1440×1600・スマートフォン390×844で山口・宇部・下関・和木の4状態を確認。
  全ケースで状態一致、横overflow 0、壊れた画像0、console error 0。スマホの行動カードは1列。
- 初回公開後に検証状況ページの長いSHA256・証拠パスで横overflow 170pxを検出した。
  折返しCSSと静的回帰を追加し、390×844ローカル再確認は横overflow 0。
- 正本は`evidence/20260813_work1_official_gtfs_coverage_extension_local_acceptance.json`。
  次はcommit・push、同一SHAの3 workflow、公開PC・スマホ読戻し。監査はまだ開始しない。

### 公開受入GO

- 実装`3f4e508`後、公開読戻しで検証状況ページのスマホ横overflow 170pxを検出し、`bc10eb1`で修正。
- 最終HEAD `bc10eb1596f42ddaa224e8e36786a2d3357450e9`のscope lock `31690749743`、Pages
  `31690749021`、attestation `31690803185`は全success。
- artifact `9177321319`はGO、174 / 174、原本7 / 7、公開4 / 4、保護不一致0、errors 0。
- 公開HTML 4件・JSON 2件はHTTP 200かつcommit blob bytes一致。PC・スマホの4状態各4ケース、
  状況ページ、横overflow 0、壊れた画像0、console error 0。スマホ行動カードは1列。
- 正本は`evidence/20260813_work1_official_gtfs_coverage_extension_public_acceptance.json`。
  次はread-only独立監査だけとし、まだ開始しない。

## 2026-08-13 WORK1-AWARD-SCORECARD-RECALIBRATION-1 ローカル受入

- 類似サービス3件・一次資料6件と4状態別協議前ハンドオフを、公開しない内部診断へ再評価した。
- 新規性小項目は3.5→4.0。利用者実証、追加採用フィード、共同設計は未達のため、実用度3.5、
  完成度4.0、挑戦度3.0、総合70.0は据え置いた。
- 完了済みの類似比較を改善候補から除外した。利用者検証は人間承認ゲートとして残し、自走可能な
  最上位候補を独立再生成・復旧訓練、次候補を未確認13市町の公式GTFS追加確認とした。
- 内部正本は25,203 bytes、SHA256
  `b1f03e1f09d4a39fbe8c8afdf4253c837bfea9c09bf7f3be02bb51449c12215d`。
- 専用27 / 27、全169 / 169、旧SPEC 984トークン欠落0、scope checker、`git diff --check`、
  秘密情報定型検査0件。原本6 / 6、公開4 HTMLは基準HEADと一致する。
- Pages用内部診断3資産は不在で、公開HTMLへの点数・内部JSON導線は0件。
- 作品②入力、参加者連絡、応募・BODIK登録、公開値変更は各0件。
- ローカル正本は`evidence/20260813_work1_award_scorecard_recalibration_local_acceptance.json`。
  次はcommit・main push、同一SHAのscope lock・Pages・attestation、公開非露出の読戻し。

### 公開受入GO

- 実装commit `87521caef7c25e1f43361bae683cb1d049bd7fed`のscope lock `31681446757`、Pages
  `31681446291`、release attestation `31681506825`は同一SHAで全success。
- artifact `9173662821`はGO、169 / 169、公開4 / 4、原本6 / 6、保護3 / 3、errors 0。
  内部正本のbytes・SHA、Pages copy不在、作品①境界も一致した。
- 公開4 URLはHTTP 200かつcommit bytes一致。内部診断3 URLは404を維持し、公開HTMLの点数・
  内部JSON導線は0件。公開正本は
  `evidence/20260813_work1_award_scorecard_recalibration_public_acceptance.json`。
- この記録を含む最終HEAD自身を同じ3 workflowで再確認する。次段階はread-only監査だけ。

## 2026-08-13 WORK1-PRECONSULTATION-HANDOFF-1

### 仕様・実装・ローカル受入

- 固定HEAD `0aeff8fd58d2cd3d7ea49635f17c46037e6801b5`の類似サービス比較監査はGO、
  P0 0件、P1 0件、P2 3件。`SPEC.md` rev.5.18 §30と`run_record.md` rev.62へ次段階を固定した。
- 既存4 JSONだけから、19市町を登録あり・GTFS確認済み4、登録0・確認済み2、登録あり・
  未確認11、登録0・未確認2へ分類する。測定値有無は別属性として表示する。
- `docs/municipality-memo.html`へ「共有する事実」「次に確認する」「分析へ渡す条件」を追加した。
  0件・未確認・フィード全体指標の非主張と、外部サービスへ自動送信しない境界を明記した。
- GTFSデータリポジトリ、GTFS-GO、国土交通省LINKS Mobilysの安定した3 HTTPS導線を採用した。
  2026-08-13に3 / 3到達を再確認し、取得不安定だったAPI詳細URLへ依存しない。
- 専用6 / 6、全163 / 163、旧SPEC 963トークン欠落0、scope checker、`git diff --check`、
  秘密情報定型検査0件はsuccess。6原本、既存data・docs/data、公開トップ、応募説明、内部診断は不変。
- ローカル実ブラウザで下関市・宇部市・岩国市・防府市の4状態を確認し、全ケースで状態・測定値属性、
  3外部安全リンク、横overflow 0、runtime error 0。1280×720でハンドオフ節を目視し、印刷CSSと
  共有URL・既存詳細導線の回帰テストも成功した。
- ローカル正本は`evidence/20260813_work1_preconsultation_handoff_local_acceptance.json`。
  次は実装commitをmainへpushし、同一SHAのscope lock・Pages・attestationと公開PC・スマホを受け入れる。

### 公開受入GO

- 実装commit `5cb74d3b39e7750816c931bdf711e41c34eb581b`のscope lock `31677956643`、Pages
  `31677956172`、release attestation `31678001513`は同一SHAで全success。
- artifact `9172305641`はGO、163 / 163、公開4 / 4、原本6 / 6、保護3 / 3、
  `pages_copy_absent=true`、errors 0。JSON 8,974 bytes・SHA256
  `96be84edc277dda880fc0de1737c97aa6f7794191396cf423ac126061c0f631c`を読戻した。
- 公開中核4 URLはHTTP 200かつ同commit bytes一致。公開4状態を直接確認し、PC 1440×1600と
  スマートフォン390×844の4状態計8ケースも状態・測定値属性・安全リンクに一致した。
- PCは3列、スマートフォンは1列。全ケースで横overflow 0、対象runtime error 0、壊れた画像0。
  直接公開ページのdev logも0。共有URL、印刷、既存詳細導線を維持した。
- 内部点数、原本、GTFS ZIP、既存公開値、公開トップ、応募説明、作品②入力、参加者連絡、
  応募・BODIK、外部サービスへのデータ送信は各0変更・0実行。
- 公開正本は`evidence/20260813_work1_preconsultation_handoff_public_acceptance.json`。
  この記録を含む最終HEAD自身を同じ3 workflowで外部再確認する。次段階はread-only監査だけ。

## 2026-08-13 WORK1-SIMILAR-SERVICE-BENCHMARK-1 ローカル受入

- 固定HEAD `c8dc19d6b2a9303018e4eccf3426aa7c3c11a262`の公開情報設計監査はGO、
  P0 0件、P1 0件、P2 1件。P2は現HEADのCI artifactと永続Releaseの証拠寿命の差である。
- 国内3サービスを工程別に選定した。GTFSデータリポジトリは取得、GTFS-GOは可視化・頻度集計、
  LINKS Mobilysは計画分析・シナリオ検討を主役割として、運営・開発主体と国土交通省の一次資料
  6件を2026-08-13に確認した。
- 作品①は、登録供給・公式GTFS確認状況・日付・根拠・限界を19市町の同じ入口で揃え、
  共有・印刷できる協議前確認メモへ渡す前段の確認層として説明する。
- `docs/entry.html`へ3比較対象、主役割、役割差、6一次資料リンク、比較限界を追加した。
  `docs/status.html`では旧い「比較スコアカード公開済み」という誤表示を削除し、現在地と次の監査を同期した。
- 世界初、国内唯一、優劣、国内網羅、対象側の機能不存在、高度分析の代替、点数上昇は主張しない。
- 専用6 / 6、全157 / 157テスト、旧SPEC 950トークン欠落0、scope checker、
  `git diff --check`、秘密情報定型検査0件はsuccess。
- `raw/`、既存`data/`、既存`docs/data/`、公開トップ、内部診断2件は基準HEADと同一。
  作品②入力、参加者連絡、応募・BODIK、Release・tag削除は各0件。
- ローカル正本は`evidence/20260813_work1_similar_service_benchmark_local_acceptance.json`。
  次はcommit・main push、同一SHAのscope lock・Pages・attestation、公開PC・スマートフォン読戻し。

### 公開受入GO

- 実装commit `84125e2f12337c3f73ceda484676d7316780422c`のscope lock `31673053501`、Pages
  `31673053080`、release attestation `31673111345`は同一SHAで全success。
- artifact `9170461765`はGO、157 / 157、公開4 / 4、原本6 / 6、保護3 / 3、
  `pages_copy_absent=true`、errors 0。正本は
  `evidence/20260813_work1_similar_service_benchmark_public_acceptance.json`。
- 公開応募説明はPC 1440×1600・スマートフォン390×844で3比較対象、公式リンク6件、比較限界4項目を確認。
  状況ページも1440×1200・390×844で確認し、横overflow、壊れた画像、dev logは全て0。
- 公開中核4 URLは200かつcommit bytes一致。削除した内部診断HTML・JSON・schemaは404を維持した。
- 内部点数、原本、GTFS ZIP、公開値、作品②入力、参加者連絡、応募・BODIKは各0変更。
- この記録を含む最終HEAD自身を同じ3 workflowで外部再確認する。次段階はread-only監査だけ。
