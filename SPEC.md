# 作品① 実行仕様 — I-1 登録簿の機械可読化 / I-4 GTFS公式状況表示 / I-5 現況説明同期 / GTFS-2 公式ZIP取得・実測 / SUPPLY-METRIC-1 輸送供給指標定義 / SUPPLY-VIEW-1 輸送供給比較表示 / RELEASE-1 公開 / ENTRY-PAGE-1 応募説明ページ / WORK1-FRESHNESS-1 原本変更検知 / WORK1-AWARD-COMPARISON-1 比較準備 / WORK1-TASK-MEMO-1 交通協議前確認メモ / WORK1-GTFS-COVERAGE-2 公式範囲再確認 / WORK1-AWARD-COMPARISON-2 現行証拠再評価 / WORK1-AWARD-COMPARISON-TRACEABILITY-1 公式用語・根拠追跡

**rev.5.12 / 2026-08-12**（rev.1→rev.2 の改訂点は §10.1、rev.2→rev.3 は §10.2、rev.3→rev.4 は §10.3、rev.4→rev.4.1 は §10.4、rev.4.1→rev.4.2 は §10.5、rev.4.2→rev.4.3 は §10.6、rev.4.3→rev.4.4 は §10.7、rev.4.4→rev.4.5 は §10.8、rev.4.5→rev.4.6 は §10.9、rev.4.6→rev.4.7 は §10.10、rev.4.7→rev.4.8 は §10.11、rev.4.8→rev.4.9 は §10.12、rev.4.9→rev.4.10 は §10.13、rev.4.10→rev.5.0 は §10.14、rev.5.0→rev.5.1 は §10.15、rev.5.1→rev.5.2 は §10.16、rev.5.2→rev.5.3 は §10.17、rev.5.3→rev.5.4 は §10.18、rev.5.4→rev.5.5 は §10.19、rev.5.5→rev.5.6 は §10.20、rev.5.6→rev.5.7 は §10.21、rev.5.7→rev.5.8 は §10.22、rev.5.8→rev.5.9 は §10.23、rev.5.9→rev.5.10 は §10.24、rev.5.10→rev.5.11 は §10.25、rev.5.11→rev.5.12 は §10.26。消した記述はそこに全部書いてある）

UDC2026 作品① の実装仕様。**このファイルはリポジトリ直下に `SPEC.md` として置き、Claude Code に読ませる。**

§0〜§11は受け入れ済みI-1、§12は受け入れ済みI-4、§13は受け入れ済みI-5、
§14は受け入れ済みGTFS-2、§15は受け入れ済みSUPPLY-METRIC-1の仕様として保持し、
§16は受け入れ済みSUPPLY-VIEW-1、§17は受け入れ済みRELEASE-1、
§18は受け入れ済みENTRY-PAGE-1、§19は受け入れ済みWORK1-FRESHNESS-1、
§20は受け入れ済みWORK1-AWARD-COMPARISON-1、§21は受け入れ済みWORK1-TASK-MEMO-1、
§22はWORK1-GTFS-COVERAGE-2の実行仕様である。
プロジェクトのゴール、終了条件、確定方針は`run_record.md` §0.5 を正とし、
このファイルがそれを上書きしてはならない。

**書き手はこのファイルの発行元（チャット側）で、Claude Code は読むだけ。**
仕様に誤りを見つけた場合、Claude Code は勝手に書き換えず、`PROGRESS.md` に報告して止まること。

---

## 0. 目的

中国運輸局が PDF でのみ公表している「自家用有償旅客運送者の登録簿」を構造化し、
山口県の公共交通の「供給側データ」の欠落を埋める作品①に用いる。
作品①は GTFS-JP の整備状況・鮮度、交通空白、福祉輸送（UD タクシー・福祉有償運送）の供給を突き合わせる。
そのうち I-1 は、自家用有償旅客運送の福祉有償運送と交通空白地有償運送について、
市町別の供給量（団体数・車種別の車両台数）を機械可読なデータとして取り出す。

同種の機械可読データは全国に存在しない。これを作ること自体が成果物になる。

---

## 1. スコープ

**今回やる**: 山口県分 4 ファイル。
**今回やらない**: 鳥取・島根・広島への拡張（山口県が完成し、検証を通ってから着手）。岡山県は事務権限が県知事に移譲済みで別ソースのため対象外。

---

## 1.5 置き場所とリポジトリ構成

ローカルは以下に作る。作品②も同じ階層に並べる。

Windows: `C:\Users\<ユーザー名>\dev\udc2026\`
Mac / Linux: `~/dev/udc2026/`

**OneDrive や iCloud Drive の同期対象フォルダには置かないこと。**
Windows の「ドキュメント」「デスクトップ」は OneDrive にリダイレクトされていることが多く、
同期プロセスが `.git` 内のファイルをロックしてリポジトリを壊す。
ユーザー名に日本語が含まれる場合は `C:\dev\udc2026\` を作る。

```
dev/udc2026/
├─ .venv/                           ← 仮想環境はここに1つだけ。2リポジトリで共有する
├─ yamaguchi-yusho-data/            ← 作品①（この仕様書の対象）
└─ yamaguchi-highschool-pubcom/     ← 作品②（後日）
```

作品①リポジトリの中身:

```
yamaguchi-yusho-data/
├─ README.md              作品の説明。出典とライセンスを明記（§8）
├─ SPEC.md                この仕様書をここにコピーして置く
├─ run_record.md          goal-to-done の実行契約。§0.5 がプロジェクトの根拠
├─ CLAUDE.md              Claude Code への常設指示（下記）
├─ .gitignore
├─ requirements.txt       `pdfplumber` の1行だけ。**pandas は使わない**
├─ src/
│  ├─ fetch.py            一覧ページと PDF の取得
│  └─ parse.py            第2号様式のパーサ
├─ raw/
│  ├─ *.pdf               取得した PDF。出典保存のため git に含める
│  └─ text/               ページ単位の抽出テキスト（§4 の最後）
├─ data/                  operators.csv, vehicles.csv, 同 JSON
├─ tests/
│  └─ test_verify.py      合計欄との照合を自動テストとして書く
├─ verification.md        検証結果（§5 の実行結果）
└─ docs/                  GitHub Pages の公開ルート
   └─ index.html
```

GitHub の Settings → Pages → Source を「main ブランチの /docs フォルダ」にする。
ビルド不要。`docs/index.html` を置けばそのまま公開される。

`.gitignore` には最低限これを入れる:

```
.venv/
__pycache__/
.claude/*.lock
```

`.claude/` 自体は意図的にコミットするが、`.claude/scheduled_tasks.lock` はデスクトップアプリが
管理するロックファイルで、コミットしてはいけない。

`CLAUDE.md` には最低限これを書いておく:

```
このリポジトリは UDC2026 応募作品①。
ゴール、終了条件、確定方針は run_record.md §0.5 が正。
SPEC.md は I-1 の実装仕様。SPEC.md に書かれていない実装仕様を勝手に足さない。
代表者の氏名は抽出も出力もしない。
「動いた」で完了としない。SPEC.md §5 の完了条件を満たし、
verification.md に実行結果を書くまで未完了。
```

**CLAUDE.md はシステムプロンプトではなく、遵守は保証されない。**
「代表者氏名を出力しない」のような絶対条件は CLAUDE.md への記載だけに頼らず、
`tests/test_verify.py` の自動テストで担保する（§5-8）。

### 実行環境の制約（この環境では必須）

**素の `python` コマンドを使わないこと。**
0 バイトの Microsoft Store エイリアスに解決され、非対話シェルから呼ぶとハングする。

- `uv`（`C:\Users\user\.local\bin\uv.exe`）経由で呼ぶ、または
- `C:\Users\user\dev\udc2026\.venv\Scripts\python.exe` のフルパスで呼ぶ

`uv python list` も引数なしだと同じ理由でハングするので `--managed-python --only-installed` を付ける。

commit email は `git config --local` で noreply アドレスを設定する。`--global` は触らない。

### Claude Code に最初に投げる指示

```
SPEC.md を読んで、§2 の「最初にやること」から着手して。
一覧ページを cp932 の生バイトでデコードして、山口県の4行の対応が
SPEC.md §2 の表と一致するか確認して。
そこまで終わったら止まって、確認結果を報告して。
```

**最初から parse.py を書かせない。**一覧ページの照合で一度止める。

---

## 2. 入力

一覧ページ: `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`
**このページは Shift-JIS（cp932）である。**UTF-8 で読むと全て文字化けする。

**WebFetch や HTTP クライアントの要約機能を一次情報として使わないこと。**
過去にこのページの要約が、存在しない「貨物自動車運送事業者登録状況の表」を返し、
山口県の行として鳥取県のファイル名を提示した実績がある。
**生バイトを取得して自前でデコードし、目視できる形に落としてから照合する。** `fetch.py` もこの方針で書く。

山口県分の 4 ファイル（`https://wwwtb.mlit.go.jp/chugoku/content/` 配下）:

| 運送種別 | 実施主体 | ファイル |
|---|---|---|
| 交通空白地有償運送 | 市町村営 | `000359215.pdf` |
| 交通空白地有償運送 | NPO等 | `000268896.pdf` |
| 福祉有償運送 | 市町村営 | `000230003.pdf` |
| 福祉有償運送 | NPO等 | `000271730.pdf` |

**この対応表は一覧ページと照合済みで一致している。**（rev.1 では「推定・未確認」だった）
照合時に判明した一覧ページの癖:

- 行順は 広島 → 鳥取 → 島根 → 山口。**岡山県の行は無い**
- **実施主体（自治体／NPO）はヘッダ行の列ラベルには書かれていない。**
  各セルのリンクテキスト内の「（自治体）」「（ＮＰＯ）」にのみある。ここから判定する
- 島根県の列1は `<a>` が2つに割れている。リンク抽出時は重複除去する

`000271730.pdf`（福祉有償運送・NPO等）の実測値:

- 4ページ / 136,476 バイト / MD5 `c49543729e3122adbd935fa79260b3b7`
- テキスト埋め込みあり（`page.images` は空。スキャンではない）
- 1ページ1団体で計4件。**下関市だけではない**（下関市3件、周防大島町1件）
- 登録番号は 第1号・第3号・第4号・第6号。**2号と5号は欠番**

`000271730.pdf` は処理・完了条件1〜8の検証まで完了済み。

`000230003.pdf`（福祉有償運送・市町村営）の処理済み事項:

- 3ページ / 19,003 バイト / MD5 `0f50b2ed90744aca1be2829299b1b4a1`
- テキスト埋め込みあり（全ページ `page.images` は空。スキャンではない）
- 1ページ1団体で計3件。登録番号は第1号・第3号・第4号。**第2号は欠番**
- パーサへの追加と完了条件1〜8の検証まで完了済み。車両明細12行、合計6台、うち軽4台

`000359215.pdf`（交通空白地有償運送・市町村営）の処理済み事項:

- 20ページ / 393,922 バイト / MD5 `d1f464e5e8fd89f6501c7e037c369912` /
  SHA256 `3107e8555aefe86527d482422977c050b4514cb06b0f077187cdec4f38a2c66b`
- テキスト埋め込みあり、暗号化なし。全20ページをPNG化し、原本と目視照合済み
- ページ1〜19に12団体。ページ20は「輸送人員実績報告提出状況」の付録で、登録簿レコードではない
- 岩国市はページ3〜7、下関市は9〜10、周南市は14〜16にまたがる。**1ページ1団体ではない**
- 車両欄は全団体とも共通7列。12団体の合計106台、うち軽2台
- パーサへの追加と完了条件1〜8の検証まで完了済み。車両明細59行、合計106台、うち軽2台

`000268896.pdf`（交通空白地有償運送・NPO等）の確認済み事項:

- 4ページ / 149,313バイト / MD5 `83a60f022276cb26c6967bc85b947f67` /
  SHA256 `ece80864a99fc392dec0c279fe92f3609a43c11ce32178ced0cd60839c9b6177`
- テキスト埋め込みあり、暗号化なし。全4ページをPNG化し、原本と目視照合済み
- 全4ページが各1団体・1事務所。継続ページと付録はない
- 登録番号は `中山過第1号`、`中山過第2号`、`中山交第2号`、`中山交第4号`
- 車両欄の印字見出しは `普通自動車(軽)`。4団体の合計7台、うち軽2台
- 原本取得・全ページ構造確認・パーサ追加・4PDF統合検証まで完了。4団体、車両明細8行、
  7台、うち軽2台を出力し、完了条件1〜8を通過済み

山口県4PDFはすべて取得済み。Codexが指定する1本・1工程ずつ扱い、最後のパーサ増分も
完了条件1〜8を再実行して `verification.md` へ記録する。

### 利用規約（確認済み・2026-08-09）

国土交通省ウェブサイトで掲載・発信するコンテンツは、特記されていない限り著作権は国土交通省に帰属し、
権利表記がない限り**「公共データ利用規約（第1.0版）」PDL1.0 に準拠した利用条件**で利用できる。
条件は次の2つ。

1. 出典を記載する
2. 編集・加工して利用する場合は、出典とは別に編集・加工した旨を記載する。
   加工した情報を、あたかも国が作成したかのような態様で公表・利用しない

数値データ・簡単な表・グラフ等は著作権の保護対象ではないため本ルールの適用外、とも書かれている。
出典: `https://www.mlit.go.jp/link.html`

登録簿一覧は国土交通省ドメイン配下の中国運輸局公式ページ
`https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`であり、山口県分4PDFにも別の
権利表記・利用条件の明示はない。国土交通省の別ルール適用コンテンツ一覧
`https://www.mlit.go.jp/page/kanbo01_hy_003657.html`にも登録簿は列挙されていない。
したがって4PDFは上記の一般条件どおりPDL1.0準拠として扱う。確認記録・取得ハッシュ・
除外一覧との照合は`evidence/20260809_mlit_usage_terms.txt`を正とする。
**出典明記と加工明示は必ず行う**（§8）。

---

## 3. 出力

`data/` 配下に 2 テーブルを CSV と JSON で出す。1 団体 = 1 行の主テーブルと、車両の明細テーブルに分ける。

**`registration_no` 単独は主キーではない。** `000271730.pdf` と `000230003.pdf` に
`中山福第3号` が1件ずつ存在する。団体の主キー、および2テーブル間の結合キーは
`(source_pdf, registration_no)` の複合キーとする。登録番号だけで集計・辞書化してはならない。

### 3.1 `operators.csv`

| 列名 | 内容 | 例 |
|---|---|---|
| `registration_no` | 登録番号（半角に正規化） | 中山福第1号 |
| `registration_no_raw` | 登録番号（原文のまま） | 中山福第１号 |
| `authority_code` | 登録番号の付与主体部分。国権限では運輸局・運輸支局コード。旧市町村運営番号の `市` は含めない | 中山 |
| `service_type_code` | 原本の種別1文字。`福` / `交` / 旧登録の `過` | 福 |
| `serial_no` | 一連番号 | 1 |
| `pref` | 県 | 山口県 |
| `transport_type` | 運送の種別 | 福祉有償運送 |
| `operator_type` | 実施主体。§2のファイル対応に従い `市町村営` または `NPO等` | NPO等 |
| `org_name` | 名称 | 特定非営利活動法人 豆たん |
| `org_address` | 住所 | 下関市古屋町2丁目4-8 |
| `org_address_raw` | 住所（原文のまま） | 下関市古屋町２丁目４－８ |
| `service_area` | 運送の区域 | 下関市全域（離島を除く） |
| `service_area_raw` | 同（原文のまま） | |
| `service_area_municipalities` | 区域から抽出した市町名（複数可、`;` 区切り） | 下関市 |
| `registered_date` | 登録年月日（西暦 ISO） | |
| `renewed_date` | 更新登録年月日（西暦 ISO）。無ければ空 | 2024-04-24 |
| `valid_from` | 有効期間の開始（西暦 ISO） | 2024-04-24 |
| `valid_to` | 同 終了（西暦 ISO） | 2027-04-23 |
| `scope_i_physical` … `scope_to_other` | 旅客範囲 イ〜ト の該当フラグ（7列、0/1） | |
| `office_name` | 事務所の名称（ページ上部の記載）。複数は原本の上から順に `;` 区切り | 豆たん介護センター |
| `office_location` | 事務所の位置（同上）。名称と同じ順・同じ件数で `;` 区切り | 下関市内日下1027-3 |
| `partner_operator_name` | 事業者協力型の協力事業者名（無ければ空） | |
| `partner_operator_address` | 同 住所 | |
| `vehicles_total` | 合計欄の括弧**外**の値（総台数） | 3 |
| `vehicles_total_kei` | 合計欄の括弧**内**の値（うち軽・内数） | 2 |
| `source_pdf` | 出典ファイル名 | 000271730.pdf |
| `source_page` | 登録番号がある開始ページの番号 | 1 |
| `flags` | 検証で立った異常フラグ（`;` 区切り。無ければ空）。値は `office_mismatch` / `office_notation_diff`（§3.6） | |

旅客範囲の記号対応（施行規則第49条第2号）:
イ 身体障害者 / ロ 精神障害者 / ハ 知的障害者 / ニ 要介護認定 / ホ 要支援認定 / ヘ 基本チェックリスト / ト その他

**交通空白地有償運送の旅客は「地域住民又は観光旅客その他の来訪者」であり、イ〜トの欄は使われない。**
交通空白地のファイルではフラグ7列はすべて 0 になるのが正常。

ページ上部に複数事務所がある場合、`office_name` と `office_location` はそれぞれ同じ順序で `;` 連結する。
2列の分割後件数は必ず一致させ、上から1始まりの順序を `vehicles.office_seq` と対応させる。
例: 4事務所なら両列とも4要素であり、車両欄側も `office_seq=1..4` になる。

### 3.2 `vehicles.csv`

| 列名 | 内容 |
|---|---|
| `registration_no` | operators への複合外部キーの一部（正規化済みの値）。`source_pdf` と組み合わせる |
| `office_seq` | 団体内の事務所連番。1始まり |
| `office_name` | 車両欄側に書かれた事務所の名称 |
| `office_location` | 同 位置 |
| `ownership` | `所有` / `持込` / **空**（区分の列が無いファイル） |
| `vehicle_type` | 分析用の正規区分。寝台車 / 車いす車 / 兼用車 / 回転シート車 / セダン等 / バス / 普通自動車 / 合計 |
| `vehicle_type_label` | 原本の列ヘッダ文字列そのまま |
| `count` | 括弧**外**の数字。その車種の総台数（軽を含む） |
| `count_kei` | 括弧**内**の数字。うち軽自動車（内数）。列に(軽)が無ければ空 |
| `source_pdf` | 出典ファイル名 |
| `source_page` | その車両行が記載されたページ番号 |

`vehicles.csv` の各行は `(source_pdf, registration_no)` で `operators.csv` の1団体に結合する。
事務所・所有区分・車種まで含む行の識別には、さらに `office_seq` / `ownership` / `vehicle_type` を使う。

`vehicle_type` が `合計` の行も**削除せず残す**。§5-2 の検算で再計算値と突き合わせるため。

交通空白地有償運送では、帳票の `セダン等` 列に入った値を
`vehicle_type=普通自動車`、`vehicle_type_label=セダン等(軽)` とする（§3.4(c)）。
福祉有償運送の同じ帳票列は `vehicle_type=セダン等` のままであり、運送種別をまたいで同じ意味だと扱わない。

**`ownership` が空になるのは正常な状態。**
`000230003.pdf`（福祉・市町村営）と交通空白地2PDFには所有区分の列そのものが無い。
`unknown` のような値で埋めず、空欄にする。ファイル種別から復元できる。

### 3.3 出さないもの

**`代表者の氏名` は抽出も出力もしない。**
登録簿には記載されているが、供給量の分析に不要であり、機械可読化して再配布する必要がない。パース段階で捨てる。

### 3.4 制度上の定義（データから推測してはいけない事項）

**ここに書いてあることをデータから推定し直さない。**制度定義は通達、旧コード・帳票構造は
原本全ページの確認記録を根拠とする。ここに無い制度事項で迷ったら、データを睨むのではなく通達を当たる。

根拠:
- 福祉有償運送の登録に関する処理方針について（令和2年11月27日 国自旅第317号、最終改正 令和6年9月30日 国自旅第197号）
  `https://www.mlit.go.jp/jidosha/content/001766049.pdf`
- 交通空白地有償運送の登録に関する処理方針について（令和2年11月27日 国自旅第316号、最終改正 令和6年9月30日 国自旅第199号）
  `https://www.mlit.go.jp/jidosha/content/001766048.pdf`

#### (a) 括弧付きの数字は軽自動車の**内数**

両通達 2.(2)⑦ に同一文言がある。

> 自動車の台数を記載（軽自動車がある場合には、その数を内数として括弧書きで記載。
> また、自動運行旅客運送（特定自動運行旅客運送を除く。）又は特定自動運行旅客運送の用に供する
> 自動車がある場合には、その数を内数として該当欄に記載）するものとする。

- 括弧外 = その車種の総台数（軽を含む）
- 括弧内 = そのうち軽自動車の台数
- 空の括弧 `( )` / 括弧なし / 素の `0` = 軽 0 台

したがって `count_kei ≤ count` は**様式上必ず成立する**。破れたら原本の異常ではなく**抽出誤り**。

**括弧書きになるのは軽自動車だけではない。**自動運行・特定自動運行の車両数も内数で記載される。
山口県4ファイルでは自動運行・特定自動運行の内数を示す列見出し・値は未観測。
それでも**「括弧＝必ず軽」と決め打つコードを書かない。**
括弧の意味は列ヘッダから判定する。

#### (b) 登録番号は構造を持つ

事務・権限が国にある場合（山口県分はこれ）:
`〔運輸局の頭1文字〕〔運輸支局の頭1文字〕〔種別1文字〕第〔一連番号〕号`

事務・権限が地方公共団体にある場合:
`〔地方公共団体の名称〕〔種別1文字〕第〔一連番号〕号`

現行通達の種別は **福＝福祉有償運送 / 交＝交通空白地有償運送**。
例外として神戸運輸監理部兵庫陸運部の管轄は頭2文字「神兵」、沖縄総合事務局は「沖」1文字のみ。
出典: 両通達 別記1。

両通達の附則は、廃止前の「市町村運営有償運送の登録に係る処理方針」に基づいて付与された
登録番号を、現行方針の登録番号とみなすと定めている。このため、現行の付与規則だけでは分解できない
**旧体系の登録番号も有効な番号として残る**。

`000230003.pdf` では `中山市福第１号`、`中山福第３号`、`中山市福第４号` を実測した。
両通達の附則と旧市町村運営番号の公表例に基づき、本仕様では `中山市福` の `市` を
旧市町村運営番号の標識として扱い、地方公共団体への権限移譲を示す `authority_code` には含めない。
この場合は `authority_code=中山`、`service_type_code=福` とする。

`000268896.pdf` では、交通空白地有償運送の旧登録2件に `中山過第１号`・`中山過第２号`、
新しい2件に `中山交第２号`・`中山交第４号` を実測した。`過` を `交` へ置換する根拠は
原本にも現行通達にもないため、旧体系の原文コードとして残す。この場合は
`authority_code=中山`、`service_type_code=過` とし、ファイル種別との整合では交通空白地の
許容コードを `交` または `過` とする。

実装は次を守る:
- `registration_no_raw` は原文をそのまま保持する
- `registration_no` は全角数字・空白だけを正規化し、旧体系の `市` を削除しない
- 国権限の旧市町村運営番号は
  `〔運輸局の頭1文字〕〔運輸支局の頭1文字〕市〔種別1文字〕第〔一連番号〕号` として分解する
- 汎用の `(.+?)([福交過])` だけで分解し、`authority_code` を `中山市` とする実装は禁止する

この規則に従えば `中山福第１号` は 中国運輸局／山口運輸支局／福祉有償運送 の第1号と読める。
**原本PDF内に注記があるわけではなく、様式定義からの導出である。**

実装上の効果: **ファイル種別と登録番号の種別コードの一致を検証に使える**（§5-3）。
また**欠番があるので、一連番号から件数を推測してはいけない。**

#### (c) 車種区分は運送の種別ごとに違う

福祉有償運送（317号 2.(2)⑦）: 寝台車 / 車いす車 / 兼用車 / 回転シート車 / セダン等 の5区分
交通空白地有償運送（316号 2.(2)⑦）: バス（乗車定員11人以上）/ 普通自動車（同11人未満）の2区分

`000271730.pdf` と `000359215.pdf` で実測した車両欄は、どちらも**7列**だった:
寝台車(軽) / 車いす車(軽) / 兼用車(軽) / 回転シート車(軽) / セダン等(軽) / バス / 合計(軽)。
**バス列にのみ (軽) が無い**（バスは定員11人以上のため軽自動車が存在しない）。
よって括弧付きの数字は1行あたり最大6個。

`000359215.pdf` は交通空白地であるにもかかわらず共通7列を使い、実値は `セダン等` と `バス` の列だけにある。
一方、316号 2.(2)⑦が交通空白地について定める種類は「普通自動車／バス」の2区分である。
したがって本仕様では、**交通空白地の帳票上の `セダン等` を制度上の `普通自動車` に対応させる。**
分析用の `vehicle_type` は `普通自動車`、原本の `vehicle_type_label` は軽内数表示を含む
`セダン等(軽)` として両方を残す。
これは公式通達の2区分と実物帳票の値位置を組み合わせた対応であり、福祉有償運送の `セダン等` を
`普通自動車` に改名する規則ではない。

`000268896.pdf` の車両欄は、値を持つ見出しが `バス` / `普通自動車(軽)` / `合計(軽)` で、
間の未使用列は見出し自体が空欄だった。分析用 `vehicle_type` は `普通自動車`、原本の
`vehicle_type_label` は軽内数表示と原本の括弧形を含む `普通自動車(軽）` とする。
空見出し列は値が空または空括弧だけなら
未使用列として無視し、値があるのに見出しが空なら停止する。**列ヘッダを読んで動的にマップし、
7列固定のコードを書かない。**

#### (d) 有効期間

更新登録で付与される期間（両通達 5.(3)）:

| 条件 | 期間 |
|---|---|
| 改善命令・事故・業務停止命令のいずれも無い | 3年 |
| かつ事業者協力型である | 5年 |
| いずれかに該当する | 2年 |

**新規登録時の期間は未確認。**期間長の検証は警告に留め、外れても止めない（§5-4）。

#### (e) 公表は制度上恒常的に行われる

運輸支局長等は登録簿を簿冊に調製して備え置き、公衆の縦覧に供するとともに、
インターネットの利用その他の方法により**公表しなければならない**（両通達 2.(4)③ ほか）。
新規・更新・変更・軽微変更のいずれについても同旨。単発の掲載ではないため、
データセットを継続更新する前提を置いてよい。

### 3.5 実物1件の対応例（これが基準）

`000271730.pdf` の1団体目。**パーサはまずこの1件を再現できることを目標にする。**
rev.1 で「推定」としていた2箇所は**両方とも確定した**（(a) は §3.4(a) の通達、(b) は座標抽出で確認済み）。

原本に書かれている内容:

```
登録番号            中山福第１号
名称                特定非営利活動法人 豆たん
代表者の氏名        （記載あり。抽出しない）
住所                下関市古屋町２丁目４－８
路線又は運送の区域  下関市全域（離島を除く）
運送の種別          福祉有償運送
更新登録年月日      令和6年4月24日 ～ 令和9年4月23日
運送する旅客の範囲  イ ニ ホ ヘ
事務所の名称・位置  豆たん介護センター ／ 下関市内日下1027-3
```

同じブロックの車両欄を `extract_text()` にかけると、こう出てくる:

```
2 1 3
( ) (1) ( ) ( ) (1) (2)
( ) ( ) ( ) ( ) ( ) ( )
```

**これがこの作業の核心。**車両欄は7列の表だが、**空欄セルはテキスト抽出で消える**ため、
残った `2 1 3` がどの列の数字か判別できない。だから座標ベースの抽出が必須になる。
素朴なテキスト抽出では絶対に正しく読めない。

座標で切ると、こう対応する:

| 列 | 所有・括弧外 | 所有・括弧内(軽) |
|---|---|---|
| 寝台車 | — | ( ) |
| 車いす車 | 2 | (1) |
| 兼用車 | — | ( ) |
| 回転シート車 | — | ( ) |
| セダン等 | 1 | (1) |
| バス | — | （列に(軽)なし） |
| 合計 | 3 | (2) |

3行目がすべて空括弧なのは「持込」の行で、持込は 0 台。

期待される出力（`operators.csv` の1行、抜粋）:

| 列 | 値 |
|---|---|
| registration_no | 中山福第1号 |
| authority_code / service_type_code / serial_no | 中山 / 福 / 1 |
| org_name | 特定非営利活動法人 豆たん |
| service_area / service_area_raw | 下関市全域(離島を除く) / 下関市全域（離島を除く） |
| service_area_municipalities | 下関市 |
| transport_type / operator_type | 福祉有償運送 / NPO等 |
| valid_from / valid_to | 2024-04-24 / 2027-04-23 |
| scope_i_physical / scope_ni_care / scope_ho_support / scope_he_checklist | 1 |
| scope_ro_mental / scope_ha_intellectual / scope_to_other | 0 |
| office_name / office_location | 豆たん介護センター / 下関市内日下１０２７－３ |
| vehicles_total / vehicles_total_kei | 3 / 2 |

期待される出力（`vehicles.csv`）:

| registration_no | office_seq | ownership | vehicle_type | count | count_kei |
|---|---|---|---|---|---|
| 中山福第1号 | 1 | 所有 | 車いす車 | 2 | 1 |
| 中山福第1号 | 1 | 所有 | セダン等 | 1 | 1 |
| 中山福第1号 | 1 | 所有 | 合計 | 3 | 2 |

**合計 3 台（うち軽 2 台）。5 台ではない。**持込は 0 台なので行を出さない。

`vehicles.office_location` は車両欄側の原文で「下関市内日下1027-3」（半角）。
`operators.office_location` はページ上部の原文で「下関市内日下１０２７－３」（全角）。
同じ事務所だが印字が違う。どちらも正規化せずそのまま出す（§4.3）。この差だけではフラグを立てない（§3.6）。

### 3.6 原本側のデータ異常は直さない

事務所の名称・位置はページ上部と車両欄の2箇所に印字され、食い違うことがある。
どちらかを選んで消さない。上部を `operators.office_name` / `office_location` に、
車両欄を `vehicles.office_name` / `office_location` に入れ、両方残す。

複数事務所は原本の上から順に対応させ、`office_seq` ごとに名称・位置を比較する。
事務所数が上部と車両欄で違う場合、または同じ `office_seq` の名称が不一致なら
`office_mismatch` とする。名称が一致し位置だけが不一致なら、その事務所について
`office_notation_diff` とする。`operators.flags` は団体内の全事務所で生じたフラグの重複を除き `;` 区切りで持つ。

`000359215.pdf` の複数事務所は、上部と車両欄で名称が違っても**原本の出現順**で対応させる。
名称の一致を探して並べ替えてはならない。両側の名称・位置は上書きせず残す。

同PDFについて、両表を `office_seq` 順に全26事務所比較した確認済みの差は次の8件。
全角半角と空白だけを吸収した後の結果であり、これ以外の18事務所は一致する。

| 登録番号 | `office_seq` | 観測した差 | 期待フラグ |
|---|---:|---|---|
| 中山市交第1号 | 1 | 上部「山口県熊毛郡上関町」／車両欄「上関町役場」 | `office_mismatch` |
| 中山市交第5号 | 2 | 周東総合支所の位置が「下久原1208番地1」／「下久原1209番地1」 | `office_notation_diff` |
| 中山市交第7号 | 1 | 菊川総合支所の課名・位置が異なる | `office_mismatch` |
| 中山市交第7号 | 2 | 豊田総合支所が「地域政策課」／「地域振興課」 | `office_mismatch` |
| 中山市交第7号 | 3 | 豊北総合支所が「地域政策課」／「地域振興課」 | `office_mismatch` |
| 中山市交第11号 | 1 | 上部「阿武町（総務課）」／車両欄「阿武町」 | `office_mismatch` |
| 中山市交第12号 | 6 | 菊川・富田線の位置が「周南市大字下上2146」／「周南市下上2146」 | `office_notation_diff` |
| 中山市交第13号 | 1 | 上部「下松市役所」／車両欄「下松市」 | `office_mismatch` |

内訳は `office_mismatch` 6事務所、`office_notation_diff` 2事務所、該当団体は6団体。

**比較の前処理:** 比較のときだけ両辺に適用する。出力する値は正規化しない（§4.3）。

1. 全角→半角
2. 空白（全角・半角）をすべて除去

これで解消する差はフラグを立てない。
実例: 中山福第1号の「下関市内日下１０２７－３」（上部）と「下関市内日下1027-3」（車両欄）は同一とみなす。
中山福第3号の「特定非営利活動法人 らいと」（上部）と「特定非営利活動法人らいと」（車両欄）も同一。

**フラグは2種類:** 前処理をしても一致しない場合、どの欄が食い違うかで分ける。

| 条件 | フラグ | 意味 |
|---|---|---|
| `office_name` が不一致 | `office_mismatch` | 別の組織が記載されている疑い。位置も違うことが多い |
| `office_name` は一致し `office_location` のみ不一致 | `office_notation_diff` | 同一事務所の住所表記の差とみられる |

**`000271730.pdf` の実例:**

| 登録番号 | ページ上部（名称／位置） | 車両欄（名称／位置） | フラグ |
|---|---|---|---|
| 中山福第６号 | 大島白壽會／山口県大島郡周防大島町大字西方1623番地の３ | 桑陽病院腎友会／防府市車塚町3番２０号 | `office_mismatch` |
| 中山福第４号 | まんてんの星／下関市菊川町大字田部５３６番地１ | まんてんの星／下関市菊川町大字田部５３６－１ | `office_notation_diff` |

**重要な注記:** `office_notation_diff` は「同じ場所である」と主張するものではない。名称が一致し位置の文字列だけが
違うという観測を記録するだけ。「番地」の有無やハイフン表記の差を吸収する住所正規化は行わない。
本当に別の場所である可能性を、正規化で消してしまわないため。

**出さないもの:** 両方が一致する場合はフラグを立てない。`operators.flags` が空であることが正常な状態。
いずれのフラグも `verification.md` に全件列挙する。

---

## 4. 実装方針

- **`pdfplumber` を使う。** `pypdf` / `PyPDF2` はレイアウトを壊すので不可。
- **pandas を使わない。**標準ライブラリの `csv` と `json` で書く。出力は数千行規模で pandas の利点がなく、
  依存込みで数十MBを占める。依存は `pdfplumber` だけに保つ。
- 対象 PDF はスキャンではなくテキストが埋め込まれている。OCR は不要。
- 様式は道路運送法施行規則の**第2号様式で固定**。1 団体 1 ブロックの繰り返し構造。
- 団体の区切りは**登録番号の出現**で判定する。登録番号があるページから、次の登録番号があるページの
  直前までを1団体に束ねる。`000359215.pdf` では3団体が複数ページにまたがることを確認済み。
- 登録番号を持たない末尾ページを無条件で直前団体へ結合しない。`000359215.pdf` のページ20は
  「輸送人員実績報告提出状況」の付録なので登録簿レコードから除外し、生テキストだけを§4.4に従って保存する。
- 素朴な `extract_text()` では表のセルが混ざる。`extract_words()` で座標を取り、
  ラベル語（「登録番号」「名 称」「運送の区域」など）の座標を基準に、
  その右側・下側にある語を拾う方式で組む。
- 「事務所の名称及び位置」は1行とは限らない。上部表と車両欄の横罫線から事務所行を分割し、
  上から順に `office_seq` を付ける。複数行を1つの名称・位置として結合しない。
- `operators.source_page` は登録番号のある開始ページ、`vehicles.source_page` は車両行の実在ページとする。
- `000359215.pdf` と `000268896.pdf` には所有／持込の区分列がないため `ownership` は空にする。
- 各事務所行の合計欄は `vehicle_type=合計` として残す。全事務所の下に団体集計が別記されている場合、
  その団体集計を `vehicles.csv` へ重複出力せず、事務所行の和との照合に使う。
  団体集計がない周南市・美祢市は、事務所行の和から `operators.vehicles_total` / `vehicles_total_kei` を導出する。
- 光市のページ11は運送種別の欄が2群ある。○印と値がある交通空白地側だけを読み、空の福祉側と空行を事務所に数えない。
- 美祢市は登録者の `org_name=美祢市`、`operator_type=市町村営` とし、民間タクシー会社2社は
  原本どおり事務所名・位置として保持する。会社を登録者へ置き換えず、制度上の役割を推測で付与しない。
- `000268896.pdf` は各ページ1団体だが、既存の座標パーサへ戻さず罫線表パーサを使う。
  登録番号の `過`、空の車種見出し、複数行事務所名を同じ表構造の差として扱う。
- 同PDFの車両欄では、事務所セルの先頭1行を名称と決め打ちしない。上部表の事務所名称を
  全半角・空白を吸収して車両欄結合文字列の接頭辞と照合し、一致した長さまでを名称、残りを位置とする。
  全4ページで上部の名称+位置と車両欄の結合文字列が一致し、期待する事務所差分は0件。
- 同PDFのページ4は、原本画像と語座標で合計2・軽1が印字されているが、右罫線の形状により
  `table.extract()` の合計セルだけ空になる。合計列ヘッダのx範囲から `extract_words()` の数字を
  座標フォールバックで取得し、普通自動車の明細2・軽1と一致することを検証する。
  座標値も取れない場合だけ明細から導出し、警告ではなくテスト失敗にする。合計0として通さない。
- 同PDFページ2の運送区域は市名を省略して `向津具地区` と地区内地名だけを印字する。
  国土地理院住所検索で `油谷向津具上`・`油谷向津具下` と長門市役所が同じ自治体コード
  `35211` であることを確認済みなので、この登録に限り `service_area_municipalities=長門市` とする。
  一般的な団体住所・事務所住所フォールバックには広げない。

### 4.1 列境界はページごとに導出する

**テンプレートを1つ作って全団体に当てる方式は使えない。**（rev.1 の誤り）
`000271730.pdf` の車両欄の列境界は p1=294.0 / p2=285.0 / p3=286.0 / p4=286.3 と
ページごとに異なっていた。

手順:

1. `page.edges` から `orientation == "v"` の縦罫線を取る
2. x 座標の差が 2pt 未満のものは同一の罫線として1本に畳む
3. 畳んだ結果を列境界として、そのページの表を切る
4. 列ヘッダのテキストを読んで `vehicle_type` にマップする（§3.4(c)）

**ページごとにこの導出を行い、前ページの境界を使い回さない。**
団体ごとにアドホックな分岐を書き始めたら、それは列境界の導出を怠っているサイン。

### 4.2 括弧の扱い

括弧は**全角と半角が混在**する。p4 では全角括弧が縦に分離して出現し、半角括弧と混ざった。
「持込」行に `( )` ではなく素の `0` が入るページもある。

したがって文字列マッチではなく、**セル内の数字トークンを座標で拾い、括弧文字の有無で
内数か総数かを判定**する。空括弧・括弧なし・素の `0` はいずれも 0 として扱う（§3.4(a)）。

### 4.3 正規化

原本は全角・半角が混在している。登録番号だけでも
第１号（全角）／第3号（半角）／第４号／第６号 とばらついていた。
事務所所在地も上部が全角「１０２７－３」、車両欄が半角「1027-3」だった。

| 対象 | 方針 |
|---|---|
| `registration_no` | 半角に正規化。原文は `registration_no_raw` に残す |
| 数値列すべて | 半角に正規化して int |
| 日付列すべて | 和暦を西暦に変換し ISO 8601 |
| `org_address` / `service_area` | 半角に正規化。原文は `_raw` 列に残す |
| `org_name` / `office_name` / `office_location` / `vehicle_type_label` | **原文保持。正規化しない** |

和暦変換は令和・平成・昭和に対応させる（令和 = 2018 + 年、平成 = 1988 + 年、昭和 = 1925 + 年）。

### 4.4 生テキストを残す

`raw/text/{pdfname}_p{n}.txt` に、ページ単位の抽出テキストを保存する。
ただし代表者氏名の値だけは座標で特定して `[氏名-非出力]` に置換し、永続化しない。
「代表者の氏名」のラベルと、それ以外のページ内容は残す。文字列リテラルとして実名をコード・テスト・証拠へ書かない。
出典原本である `raw/*.pdf` は改変しない。
CSV の任意のセルについて、どのページの何を読んだのかを後から追えるようにするため。
`verification.md` の突合表はここを参照して書く。

---

## 5. 完了条件（これを満たすまで I-1 は未完了）

各PDFを追加するたびに1〜8を再実行する。現在の受け入れ対象は処理済みの `000271730.pdf`・
`000230003.pdf`・`000359215.pdf` と、追加する `000268896.pdf` の4本すべて。
証拠として提示できるものが揃って初めて山口県4PDFの I-1 全体を完了とする。
「動いた」「たぶん合っている」は完了ではない。

1. **団体数の一致** — 人間が目視で数えた団体数がファイル別に
   `000271730.pdf=4件`、`000230003.pdf=3件`、`000359215.pdf=12件`、
   `000268896.pdf=4件`、合計23件であり、`operators.csv` の行数と一致する。
   `000359215.pdf` ページ20の付録は団体に数えない。
   `vehicles.csv` は既存82行＋新規8行＝90行、団体の実車両合計は既存129台＋新規7台＝136台、
   うち軽は既存18台＋新規2台＝20台になる。軽は内数なので合計へ加算しない。
2. **車両合計の照合（自動テスト）** — `(source_pdf, registration_no)` で識別した各団体について、
   `vehicles.csv` の `合計` 以外の行の
   `count` 合計が `operators.vehicles_total` と一致する。`count_kei` についても同様に
   `vehicles_total_kei` と一致する。**不一致が1件でもあれば未完了。**
   この検算は PDF 側が答えを持っているので、必ず `tests/test_verify.py` に書く。
3. **`count_kei` ≤ `count` が全行で成立**（§3.4(a) より様式上の不変条件）。
   破れたら抽出誤り。**自動テストに入れる。**
4. **登録番号の整合** — `service_type_code` がファイル種別と一致する
   （福祉2ファイルは全件 `福`、`000359215.pdf` は全件 `交`、`000268896.pdf` は
   `過` が第1・2号、`交` が第2・4号）。`authority_code` が全件 `中山`。
   `(source_pdf, registration_no)` が全団体で一意であることを自動テストに入れる。
   `registration_no` 単独の重複は正常で、`中山福第3号` が2ファイルに1件ずつあることを記録する。
   `000359215.pdf` では旧体系 `中山市交` が1・3・5・6・7・8・10・11・12・13号、
   現行体系 `中山交` が1・3号であり、旧体系の `市` を正規化時に消していないことも確認する。
5. **全件突合** — 全23団体について `source_pdf` / `registration_no` / `org_name` / `service_area` / `valid_to` /
   事務所と車種別台数を原本と照合し、一致表を `verification.md` に出力する。
   `000271730.pdf` の欠番2号・5号、`000230003.pdf` の欠番2号をファイル別に記録する。
   `000359215.pdf` は上記2つの番号体系を分けて実在番号を記録し、複数ページ3団体の開始・終了ページと
   ページ20除外、交通空白地の `vehicle_type=普通自動車` / `vehicle_type_label=セダン等(軽)` の対応も突合する。
   `000268896.pdf` は4登録すべての開始・終了ページが同一で、`vehicle_type` /
   `vehicle_type_label` が `普通自動車` / `普通自動車(軽）`、普通自動車4行+合計4行、7台・軽2台、
   ページ2の `service_area_municipalities=長門市` を突合する。
6. **日付の健全性** — `valid_from < valid_to` が全件で成立。年が2000年代前半や2100年代になっていない。
   有効期間の長さが2年/3年/5年のいずれか。**外れても止めず、警告として記録する**（§3.4(d)）。
7. **事務所情報の不一致の列挙** — operators 側と vehicles 側の事務所名称・位置が食い違う団体を、
   `(source_pdf, registration_no, office_seq)` を添えてフラグの種類別に全件列挙する（§3.6）。
   `000271730.pdf` では中山福第６号が `office_mismatch`、中山福第４号が `office_notation_diff`、
   `000230003.pdf` では中山市福第1号の事務所1が `office_mismatch` になる。
   `000359215.pdf` は§3.6の確認済み8事務所をフラグ種別・`office_seq` とも一致させる。
   団体単位の `operators.flags` は、中山市交第1・7・11・13号が `office_mismatch`、
   中山市交第5・12号が `office_notation_diff`、他6団体は空になる。
   中山市福第4号の4事務所、ならびに交通空白地の岩国市6・下関市4・
   周南市6・美祢市2事務所がそれぞれ `office_seq=1..n` で欠落せず、
   不要なフラグが立たないことも確認する。原本の不一致を消さずに残すこと。
   `000268896.pdf` は全4団体が各1事務所で、ページ4の2行名称を正しく分割した後の
   事務所差分は0件、団体フラグは全件空になる。
   全角半角と空白の差だけでフラグが立っていたら誤検出。前処理を見直す
   （中山福第1号の位置、中山福第3号の名称がその対象）。
8. **代表者氏名が派生テキストとデータ出力に含まれていない**ことを確認する。
   4本の各原本PDF・23登録から氏名値をテスト実行中のメモリにだけ取得し、`data/*.csv` / `data/*.json`、
   `raw/text/*.txt`、テキスト形式の `evidence/` に含まれないことを自動テストに入れる。
   `raw/text` では氏名値が `[氏名-非出力]` に置換されていることも確認する。
   出典原本 `raw/*.pdf` は検査対象外で、改変しない。実名をテストコードや検証記録へ書き残さない。

上記1〜8の実行結果（数字と出力）を `verification.md` に残す。

---

## 6. 完了後にすぐ効く派生物

I-1 が通ったら、次の2つは追加コストがほぼゼロで作れる。中間報告で見せる素材になる。

- **有効期間切れが近い団体の一覧** — `valid_to` が6か月以内の団体。供給が消えるリスクの可視化。
- **市町別の集計** — `service_area_municipalities` で寄せて、団体数と、運送種別別の
  車いす車・寝台車・普通自動車・バスの台数を市町別に出す。**軽は内数なので二重計上しないこと**（`count` だけを足す）。

これが出た時点で「山口県の福祉有償運送と交通空白地有償運送の供給が、市町別に台数で見える」と言える。
5分の中間報告はこれ一枚で成立する。

---

## 7. 取得済み4ファイルへの展開

4PDFすべてが原本取得・全ページ構造確認・パーサ追加・完了条件1〜8を通過済み。

各ファイルの確認済みまたは既知の差分:

| ファイル | 確認済みまたは想定される差分 |
|---|---|
| `000230003.pdf`（福祉・市町村営） | **処理済み:** 3ページ、登録番号は1・3・4号。1・4号は旧体系の `中山市福`。所有区分の列が無いため `ownership` は空。第4号は1団体4事務所 |
| `000359215.pdf`（交通空白地・市町村営） | **処理済み:** 20ページ中1〜19ページに12団体、ページ20は付録。3団体が改ページ。所有区分なし。帳票の `セダン等` は分析上 `普通自動車` に対応。車両明細59行、106台、うち軽2台。§3.4(c), §4, §5 |
| `000268896.pdf`（交通空白地・NPO等） | **処理済み:** 4ページ・4団体・各1事務所。所有区分なし。`過` / `交` の2コード、見出し `普通自動車`、空見出し列、ページ4の合計セル座標フォールバック、複数行事務所名、ページ2の向津具地区→長門市対応。車両明細8行、7台、うち軽2台。§3.4(b)(c), §4, §5 |

今後、原本更新で列ヘッダ・罫線・登録番号が本版と違ったら、推定で一般化せず
`PROGRESS.md` に報告して止まる。

---

## 8. 出典表記とライセンス

`README.md` と `docs/index.html` の両方に、次を必ず記載する（§2 の利用規約より）。

- 出典: 「自家用有償旅客運送者登録簿」（国土交通省中国運輸局）
  `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`
- 加工した旨: 上記を加工して作成
- 取得日
- 国土交通省が作成したものではない旨が読み手に伝わる書き方にする

派生データセット自体のライセンスは CC BY 4.0 とし、原データが PDL1.0 準拠である旨を併記する。
`raw/*.pdf`は原データの現物であり、本プロジェクトのCC BY 4.0で再許諾したとは表示しない。

---

## 9. 未確認事項

**ここに残っているものを、確認せずに埋めない。**

| # | 内容 | 影響 |
|---|---|---|
| U2 | 複数事務所が同一ページ内にある例は `000230003.pdf` 第4号で確認済み。事務所一覧が改ページする例は未確認 | 団体の区切り方。§4 |
| U3 | 新規登録時の有効期間 | §5-6 を警告に留めている理由 |
| U7 | 美祢市の民間タクシー会社2社が制度上どの役割で事務所欄に記載されているか | 作品説明。抽出は原文保持で進めるため実装の停止条件ではない |

---

## 10. 改訂履歴

### 10.1 rev.1 → rev.2

| # | rev.1 | rev.2 | 根拠 |
|---|---|---|---|
| 1 | §4「テンプレートを1つ作れば全団体に効く」 | **誤り。削除。** 列境界はページごとに縦罫線から導出（§4.1） | p1=294.0/p2=285.0/p3=286.0/p4=286.3 と実測で不一致 |
| 2 | `vehicles.ownership` を必須（所有/持込） | 空欄可。`000230003.pdf` には所有区分の列が無い | 実物確認 |
| 3 | 括弧＝軽の内数は「読み取りからの推定。目視照合で確認せよ」 | **確定**（§3.4(a)）。`count_kei ≤ count` を不変条件として自動テスト化 | 国自旅第317号/第316号 2.(2)⑦ |
| 4 | `vehicles` は `is_kei` 0/1 で行を分ける | `count`（総数）と `count_kei`（うち軽）の2列に変更 | 原本の値をそのまま持てる。分解して持つと不変条件の検査が書けない |
| 5 | `office_name`/`office_location` を1組だけ持つ | 上部と車両欄の両方を保持し、不一致をフラグ化（§3.6） | p4 に別組織・別市町が入る原本異常 |
| 6 | 全角/半角の正規化対象が未定義 | 列ごとの方針を表で明示（§4.3） | 登録番号・所在地で混在を実測 |
| 7 | 登録番号を不透明な文字列として扱う | 構造を分解して持ち、種別コードを検証に使う（§3.4(b), §5-4） | 両通達 別記1 |
| 8 | 車種を福祉の5区分＋バスで固定 | 種別ごとに違うことを明記。列ヘッダから動的にマップ（§3.4(c)） | 国自旅第316号 2.(2)⑦ |
| 9 | 4ファイルの種別対応は「推定・未確認」 | **照合済みで一致**（§2）。一覧ページの癖も記載 | 一覧ページの生デコードと突合 |
| 10 | 「最初にやること: 利用規約を確認」 | **確認済み**（§2）。PDL1.0 準拠。中国運輸局への適用のみ U6 に残す | mlit.go.jp/link.html |
| 11 | `000271730.pdf` は「下関市の…記録」 | **誤り。**下関市3件＋周防大島町1件。登録番号は2号・5号が欠番 | 実物確認 |
| 12 | 旅客範囲7列の扱いが福祉前提 | 交通空白地では全件0が正常と明記（§3.1） | 国自旅第316号 2.(2)⑧ |
| 13 | `.gitignore` の規定なし | `.claude/*.lock` を必須化（§1.5） | ロックファイルの混入 |
| 14 | 生テキストの保存規定なし | `raw/text/` を必須化（§4.4） | 突合の追跡可能性 |
| 15 | 実行環境の制約の記載なし | `python` を直接呼ばない旨を明記（§1.5） | Store エイリアスでハング |
| 16 | 出典表記・ライセンスの規定なし | §8 を新設 | 利用規約の条件 |
| 17 | `§3.4 実物1件の対応例（これが基準）` | **§3.5 に繰り下げ。**制度上の定義を対応例の前に置いたため。rev.1 は §3.4 の次に §3.3 が来る節番号の乱れがあり、これも直した | 節の依存順 |
| 18 | 完了条件は5項目 | **8項目に増やした**（§5）。追加したのは `count_kei ≤ count` の不変条件、登録番号の種別コード整合、事務所名不一致の全件列挙。これに伴い構成図の `├─ verification.md        検証結果（§5 の 1〜5 の実行結果）` を「§5 の実行結果」に改めた | 証拠32/33、§3.6 |
| 19 | 構成図の `├─ raw/                   取得した PDF。出典保存のため git に含める` | `raw/*.pdf`（PDF・git 管理）と `raw/text/`（ページ単位の抽出テキスト）に分割 | §4.4 |
| 20 | 初回指示が `まず中国運輸局の利用規約を確認して控えを取り、` `次に一覧ページを cp932 でデコードして、山口県の行と列の対応が` `SPEC.md の表と一致するか確認して。` | **規約確認は完了したので指示から外し**、一覧ページの cp932 照合から始める内容に差し替えた（§1.5） | §2 の確認結果 |

---

### 10.2 rev.2 → rev.3

000271730.pdf の実装（コミット 1df4042）で判明したことを反映した改訂。実装は rev.2 のまま完了条件8項目を通過しており、rev.3 は出力を変える改訂ではなく、rev.2 の誤りの訂正と、保留にしていた判断の規定である。

| # | rev.2 | rev.3 | 根拠 |
|---|---|---|---|
| 1 | ヘッダは rev.2 で、rev.1 からの改訂先を §9 と記載 | ヘッダを rev.3 とし、rev.1→rev.2 は §10.1、rev.2→rev.3 は §10.2 と明記 | 改訂履歴の参照先を現行の節番号に合わせるため |
| 2 | §3.6 は名称不一致の `office_mismatch` だけを規定 | 名称・位置の両方を上部と車両欄に残し、比較時だけ全角→半角・空白除去を行う。名称不一致は `office_mismatch`、名称一致で位置のみ不一致は `office_notation_diff` とする | 000271730.pdf で、実質同じ表記差と、名称一致・位置文字列不一致の両方を確認したため |
| 3 | `operators.flags` の値域が未記載 | `office_mismatch` / `office_notation_diff` を値として明記 | §3.6 の2種類のフラグと列定義を一致させるため |
| 4 | §3.5 の旧文言「`service_area / service_area_municipalities`」は正規化値と原文を区別せず、上部の `office_location` も半角で記載 | `service_area / service_area_raw` と `service_area_municipalities` の2行に分け、上部の `office_location` を原文どおり全角で記載 | §4.3 の正規化・原文保持方針と実装結果に合わせるため |
| 5 | §3.5 に上部と車両欄の所在地表記差の説明なし | 上部は全角、車両欄は半角の原文を保持し、この差だけではフラグを立てないと追記 | 中山福第1号で印字差を確認したため |
| 6 | 完了条件7は事務所名の不一致だけを列挙 | 事務所名称・位置の不一致をフラグ別に全件列挙し、000271730.pdf の期待結果4件を規定 | 誤検出を防ぎつつ2種類の不一致を検証するため |
| 7 | §10 の見出しは「10. rev.1 からの改訂点」で、rev.1 / rev.2 表に小見出しなし | 見出しを「10. 改訂履歴」に変更し、表の直前に「10.1 rev.1 → rev.2」を追加 | rev.3 の改訂履歴を §10.2 として併記するため |

---

### 10.3 rev.3 → rev.4

`run_record.md` §0.5 の実行契約と `000230003.pdf` の実物確認を反映した改訂。
パーサ実装前に、作品①における I-1 の役割と旧体系登録番号の分解規則を確定する。

| # | rev.3 | rev.4 | 根拠 |
|---|---|---|---|
| 1 | §0 の目的を「市町別の福祉輸送の供給量」に限定 | 作品①全体は GTFS-JP、交通空白、福祉輸送を突き合わせ、I-1 は福祉有償運送と交通空白地有償運送を担うと明記 | `run_record.md` §0.5 の作品①コンセプト |
| 2 | §1.5 で `run_record.md` をリポジトリ外とし、SPECを唯一の仕様とする文例を記載 | 実在するリポジトリ内配置に合わせ、ゴール・終了条件・確定方針は `run_record.md` §0.5、SPECは I-1 実装仕様と分離 | 実行契約と実ファイル配置 |
| 3 | 登録番号は現行通達の付与構造だけを規定 | 現行通達附則が有効とみなす旧市町村運営番号を追加し、`中山市福` でも `authority_code=中山`、`service_type_code=福` と規定 | 両通達附則、`000230003.pdf` の1ページ・3ページ |
| 4 | `000230003.pdf` のページ数・MD5・列構成を U1 に残し、§7 は想定だけを記載 | 3ページ、登録番号1・3・4号、所有区分列なしを確認済みとして §7 に反映し、U1 から除外 | `evidence/20260809_000230003_inspection.txt`、同 page1 PNG |

項目2で削除した旧コードブロック行（網羅チェック用の現物記録）:

```
SPEC.md が唯一の仕様。SPEC.md に書かれていない仕様を勝手に足さない。
├─ run_record.md                    ← 会話で更新される全体の記録。ここに置く（リポジトリの外）
```

---

### 10.4 rev.4 → rev.4.1

rev.4 で §7 と U1 を更新した際に残った §2 の古い着手指示を訂正し、実装前の入力状態を一意にした。

| # | rev.4 | rev.4.1 | 根拠 |
|---|---|---|---|
| 1 | §2 は `000271730.pdf` を「最初の1本」とし、他3ファイルを未取得と記載 | `000271730.pdf` は処理済み、`000230003.pdf` は取得・構造確認済みでパーサ未着手、未取得は残り2ファイルと記載 | Git履歴、`evidence/20260809_000230003_inspection.txt` |
| 2 | `operator_type` の例は `NPO等` だけで値域が不明 | §2の対応表に従い `市町村営` / `NPO等` の2値と明記 | 4入力ファイルの実施主体区分 |

---

### 10.5 rev.4.1 → rev.4.2

`000230003.pdf` 全3ページの実装前目視で、1団体4事務所の原本構造を確認したため、
事務所を欠落させない保持方法と比較単位を規定した。

| # | rev.4.1 | rev.4.2 | 根拠 |
|---|---|---|---|
| 1 | `operators.office_name` / `office_location` は単数だけを想定 | 複数は原本順に `;` 連結し、名称と位置の要素数・順序を一致させる | `000230003.pdf` 第4号は上部・車両欄とも4事務所 |
| 2 | `vehicles.office_seq` は定義済みだが複数事務所への付番手順が未記載 | 上から1始まりで付番し、上部表と車両欄を同じ `office_seq` で対応させる | 同第4号の4行構造 |
| 3 | 事務所の名称・位置を団体単位で比較 | `office_seq` ごとに比較し、件数差または名称差を `office_mismatch`、位置だけの差を `office_notation_diff` とする | 複数事務所で不一致箇所を失わないため |
| 4 | U2 は複数事務所自体を未確認と読めた | 同一ページ内の複数事務所は確認済み、改ページだけを未確認として分離 | 実物3ページ目の目視 |

---

### 10.6 rev.4.2 → rev.4.3

2つの福祉PDFを統合すると同じ `registration_no` が別団体に存在するため、複合キーと
2PDF増分の完了条件を規定した。

| # | rev.4.2 | rev.4.3 | 根拠 |
|---|---|---|---|
| 1 | `registration_no` を vehicles から operators への単独外部キーとして記載 | 団体・結合キーを `(source_pdf, registration_no)` とし、登録番号だけで集計・辞書化することを禁止 | 2PDFに `中山福第3号` が1件ずつ実在 |
| 2 | 完了条件1〜8は `000271730.pdf` 1本・4団体だけが対象 | 現在の増分を2PDF・7団体とし、ファイル別件数、車両23行・23台・軽16台を規定 | 両PDF全ページの目視と既存CSV集計 |
| 3 | 全件突合・欠番・事務所フラグは最初の4団体だけを規定 | 7団体へ拡張し、ファイル別欠番、第3号の正常な重複、市町村営第1号の不一致、第4号の4事務所を追加 | `000230003.pdf` 全3ページの実物確認 |

---

### 10.7 rev.4.3 → rev.4.4

代表者氏名を出力しない運用と、追跡用ページテキストの保存を両立するため、派生テキストの
限定的な伏字化と検査範囲を規定した。

| # | rev.4.3 | rev.4.4 | 根拠 |
|---|---|---|---|
| 1 | §4.4 はページ抽出テキストをそのまま保存 | 原本PDFは改変せず、`raw/text` では代表者氏名の値だけを `[氏名-非出力]` に置換 | CLAUDE.md の氏名非出力規約と追跡可能性の両立 |
| 2 | 完了条件8は氏名が「出力」に無いことだけを規定 | `data/`、`raw/text/`、テキスト証拠を動的に検査し、実名をコードへ書かないことを明記 | 既存テストは `data/` だけを検査し、旧 `raw/text` には氏名値が残っていたため |

---

### 10.8 rev.4.4 → rev.4.5

`000359215.pdf` 全20ページの取得・構造確認と、国自旅第316号の車種区分を反映し、
3PDF増分の実装方法と完了条件を確定した。

| # | rev.4.4 | rev.4.5 | 根拠 |
|---|---|---|---|
| 1 | §2 は `000230003.pdf` をパーサ未着手、`000359215.pdf` と `000268896.pdf` を未取得と記載 | 前者を処理済み、`000359215.pdf` を取得・全ページ構造確認済み、未取得を `000268896.pdf` だけに更新 | Git履歴、`evidence/20260809_000359215_inspection.txt`、原本20ページ |
| 2 | §3.4(c) と §9 の `U5` は、交通空白地の「普通自動車」が帳票のどの列に入るか未確認 | 交通空白地の帳票上 `セダン等` を分析用 `普通自動車` に対応させ、原本ラベルも別列に保持 | 国自旅第316号 2.(2)⑦の2区分、`000359215.pdf` 全車両欄の値位置 |
| 3 | §4 は複数ページ団体を未観測とし、末尾付録・団体集計行・2群レイアウトの扱いを未規定 | 登録番号アンカーのページ束ね、ページ20除外、団体集計の非重複、光市の交通空白地群選択を規定 | 岩国市p3〜7、下関市p9〜10、周南市p14〜16、光市p11、付録p20 |
| 4 | 下関市の上部・車両欄の事務所差、美祢市の民間事務所の扱いが未規定 | 原本順 `office_seq` で両側を保持し、下関市1〜3を `office_mismatch`。美祢市は登録者と事務所を分け、役割を推測しない | 原本p9〜10、p18、既存§3.6の非上書き原則 |
| 5 | 完了条件1〜8は2PDF・7団体・車両23行・23台・軽16台 | 3PDF・19団体・車両82行・129台・軽18台へ拡張し、改ページ・付録除外・車種対応も全件突合対象に追加 | 全ページ目視、ファイル別手集計 |
| 6 | `## 7. 残り3ファイルへの展開` は処理前の題名・着手条件 | `## 7. 取得済み3ファイルと残り1ファイルへの展開` に更新 | 現在の取得・処理状況 |
| 7 | §6 は派生集計を福祉有償運送だけで説明 | 福祉有償運送と交通空白地有償運送を運送種別別に集計 | `run_record.md` §0.5 の作品①コンセプト |

---

### 10.9 rev.4.5 → rev.4.6

パーサ着手前に `000359215.pdf` のページ上部と車両欄を全26事務所について機械比較し、
差がある事務所を原本画像で再確認した。rev.4.5の期待フラグは下関市だけを列挙しており不完全だったため訂正する。

| # | rev.4.5 | rev.4.6 | 根拠 |
|---|---|---|---|
| 1 | `000359215.pdf` の期待フラグは中山市交第7号の `office_seq=1..3` だけで、`office_seq=4` と他団体はフラグなし | 差がある8事務所を§3.6へ全件列挙し、団体単位の期待値を完了条件7へ反映 | 原本の上部表・車両欄表の全26事務所比較、全ページPNG目視 |
| 2 | 下関市だけに原本順の対応規則を記載 | 複数事務所を持つ全団体に原本順の対応規則を適用 | 名称差がある場合に誤って別行へ再対応させないため |

---

### 10.10 rev.4.6 → rev.4.7

全26事務所から差がある8事務所を除いた一致事務所数の算術誤記だけを訂正する。

| # | rev.4.6 | rev.4.7 | 根拠 |
|---|---|---|---|
| 1 | §3.6で、差がある8事務所以外の一致数を19事務所と記載 | 18事務所へ訂正 | 26事務所 − 差分8事務所 = 18事務所 |

---

### 10.11 rev.4.7 → rev.4.8

最後の山口県原本 `000268896.pdf` を取得し、全4ページを目視・罫線表・語座標で検査した。
パーサ着手前に、旧登録番号、空見出し列、罫線欠落、複数行事務所名、市名省略区域を仕様化する。

| # | rev.4.7 | rev.4.8 | 根拠 |
|---|---|---|---|
| 1 | §2は `000359215.pdf` をパーサ未着手、`000268896.pdf` を「未取得・列構成未確認」と記載 | `000359215.pdf` を処理済みへ更新し、`000268896.pdf` の4ページ・4登録・7台・軽2台・ハッシュを確認済みとして追記 | Git履歴、`evidence/20260809_000268896_inspection.txt`、全4ページPNG |
| 2 | 現行種別を `福` / `交` とし、登録番号分解は `(.+?)([福交])` だけを想定 | 交通空白地の旧登録2件に実在する `過` を原文保持し、`service_type_code=過`、交通空白地の許容コードを `交` / `過` とする | 原本ページ1・2の登録番号欄 |
| 3 | `000268896.pdf` の列構成は未確認で、交通空白地は共通表の `セダン等` を `普通自動車` へ対応 | 同PDFは原本見出し自体が `普通自動車`。空見出しの未使用列を無視し、値があれば停止する | 全4ページ車両欄、罫線表抽出 |
| 4 | 車両欄の事務所セルは先頭1行を名称として処理 | 上部名称を正規化した接頭辞で名称/位置を分け、ページ4の名称2行折返しによる偽差分を防ぐ | 4ページ全件で上部名称+位置と車両欄結合文字列が一致 |
| 5 | 合計セルは `table.extract()` の値を使用 | ページ4は印字合計2・軽1が語座標にあるのに表抽出だけ落ちるため、合計列の座標フォールバックと明細照合を必須化 | 400dpi目視、合計2のx0=513.48、明細2のx0=324.84 |
| 6 | 市名の無い運送区域の個別規則なし | ページ2の `向津具地区` だけを `service_area_municipalities=長門市` とする | 国土地理院住所検索で油谷向津具上・下と長門市役所が自治体コード`35211`で一致 |
| 7 | 完了条件1〜8は3PDF・19団体・82行・129台・軽18台 | 4PDF・23団体・90行・136台・軽20台へ拡張し、4登録・事務所差分0・代表者氏名23件を全件検証 | 原本全ページ目視、ファイル別手集計 |
| 8 | `## 7. 取得済み3ファイルと残り1ファイルへの展開`、U1はページ数・MD5・列構成、U4は自動運行内数の出現を未確認 | `## 7. 取得済み4ファイルへの展開`へ更新。U1を解消し、自動運行内数は4PDF全てで未観測と確定してU4を解消 | 原本取得、ハッシュ、全ページPNG・見出し目視 |

---

### 10.12 rev.4.8 → rev.4.9

パーサ着手前に現行 `vehicles.json` と4本目の車種見出しを読み戻し、原文保持列から
軽内数表示を落とす記述を訂正した。分析用の正規区分は変更しない。

| # | rev.4.8 | rev.4.9 | 根拠 |
|---|---|---|---|
| 1 | `vehicle_type_label=セダン等`、4本目は `vehicle_type_label` も `普通自動車` と記載 | `000359215.pdf` は `セダン等(軽)`、`000268896.pdf` は原本どおり `普通自動車(軽）` とし、`vehicle_type` だけを `普通自動車` に正規化 | `vehicle_type_label` はSPEC §3.2・§4.3で原文保持列。原本ヘッダと現行生成データの再照合 |

---

### 10.13 rev.4.9 → rev.4.10

4PDF統合実装後の処理状況を反映し、国土交通省の公式利用条件と別ルール一覧を照合してU6を解消した。

| # | rev.4.9 | rev.4.10 | 根拠 |
|---|---|---|---|
| 1 | `000268896.pdf`は「パーサへの追加と出力検証は未着手」「構造確認済み」「7台・軽2台が期待値」 | パーサ追加・4PDF統合検証・完了条件1〜8通過済みに更新 | コミット`71a5361`、`verification.md`の30テスト・独立集計 |
| 2 | §2見出しは「利用規約（確認済み・2026-08-07）」で、U6として「`wwwtb.mlit.go.jp/chugoku`に`www.mlit.go.jp`と同じ利用規約が適用されるか」を未確認とした | 確認日を2026-08-09へ更新。国土交通省ウェブサイトで掲載・発信する情報を対象とする一般条件、別条件の明示なし、別ルール一覧への登録簿の列挙なしを確認し、4PDFをPDL1.0準拠として扱う | `https://www.mlit.go.jp/link.html`、`https://www.mlit.go.jp/page/kanbo01_hy_003657.html`、`evidence/20260809_mlit_usage_terms.txt` |
| 3 | 派生データをCC BY 4.0、原データをPDL1.0準拠とだけ記載 | `raw/*.pdf`は原データの現物で、プロジェクトのCC BY 4.0による再許諾ではないと明記 | 原データと派生データの利用条件を混同させないため |

---

### 10.14 rev.4.10 → rev.5.0

I-1の仕様と完了条件は変更せず、受け入れ済みGTFS-1棚卸しを入力とする次の一作業I-4を追加した。

| # | rev.4.10 | rev.5.0 | 根拠 |
|---|---|---|---|
| 1 | ファイル全体をI-1の実装仕様として記載 | §0〜§11をI-1として保持し、§12にI-4の独立した実装範囲・スキーマ・完了条件を追加 | `run_record.md` §2決定19の「詳細仕様はSPEC.mdを唯一の正とする」を維持するため |
| 2 | GTFS-JPの整備状況・鮮度は作品①の目的にあるが、判定語・日付の扱い・表示要件は未定義 | 公式資料での確認状態、公式データ基準日、公式有効期間の3軸を分離し、単一スコアを禁止 | `evidence/20260809_gtfs_yamaguchi_inventory.txt` とGTFS-1のCodex受入結果 |
| 3 | GTFSの実装はI-2・I-3の対象外 | I-4だけを次の一作業とし、GTFS本体取得・解析、地図、経路検索、既存登録簿データの再処理は引き続き対象外 | 一度に一つの作業とし、未確認を推測で埋めないため |

### 10.15 rev.5.0 → rev.5.1

I-4の仕様と完了条件は変更せず、受け入れ後に古くなった現況説明だけを同期するI-5を追加した。

| # | rev.5.0 | rev.5.1 | 根拠 |
|---|---|---|---|
| 1 | §12を「次の一作業I-4」と記載 | §12を受け入れ済みとして保持し、§13にI-5の独立した完了条件を追加 | 2026-08-09のCodex独立検証でI-4のデータ・55テスト・PC/スマホ表示を受け入れたため |
| 2 | `README.md`と`docs/status.html`のI-4後の現況は対象外 | I-4後も残る「30テスト」「41テスト」「I-1・I-2受入」「I-3後に再評価」を除き、I-1〜I-4受入済みへ同期 | ユーザーが目的・現在地・残作業を誤認しないようにするため |
| 3 | 次の作品機能は未定 | I-5は説明同期だけとし、GTFS本体取得、地図、経路検索、作品②、公開を同時に行わない | 一度に一つの作業を維持するため |

### 10.16 rev.5.1 → rev.5.2

I-5の受入とチェックポイントコミット後、内部運行期間表示を直接仕様化せず、
その入力となる公式GTFS ZIPの取得・実測をGTFS-2として分離した。

| # | rev.5.1 | rev.5.2 | 根拠 |
|---|---|---|---|
| 1 | §13は次の一作業I-5の実装仕様 | §13を受け入れ済みとして保持し、§14にGTFS-2を追加 | I-5・I-5-CORR-1はCodex受入済みで、`74423df`にチェックポイント化したため |
| 2 | GTFS本体の取得・解析はI-5対象外で、次の作品機能は未定 | 認証不要の岩国市・光市2 ZIPだけを取得・安全検査・実測し、内部運行期間表示の仕様入力を確定する | `data/gtfs_feeds.csv`と`evidence/20260809_gtfs_source_zip_head_check.txt`ではHEADだけが確認済みで、本体のファイル構成・件数・日付は未確認のため |
| 3 | 公式ページ記載の基準日・有効期間だけを表示 | `feed_info.txt`、`calendar.txt`、`calendar_dates.txt`の候補値を別々に実測し、この段階では単一の内部運行期間へ統合しない | 実物未確認のまま算出規則と表示値を確定すると、推測を確認済みとして扱うため |
| 4 | 船木鉄道は認証済み本体未取得 | GTFS-2でも対象外とし、アクセストークンの取得・使用・保存を行わない | `access_status=authentication_required_not_retrieved`であり、認証を行う権限を確認していないため |

---

### 10.17 rev.5.2 → rev.5.3

GTFS-2のCodex受入後、利用者へ見せる数字の意味を実装より先に固定するため、
SUPPLY-METRIC-1を追加した。この改訂は指標定義だけで、集計コードや公開画面は変更しない。

| # | rev.5.2 | rev.5.3 | 根拠 |
|---|---|---|---|
| 1 | §14を次の一作業GTFS-2として実行 | §14を受け入れ済みとして保持し、§15を現在の一作業SUPPLY-METRIC-1とする | GTFS-2は公式2 ZIP、検査コード、68テスト、実測証拠をCodexが受け入れたため |
| 2 | コアファイルの行数と一意ID数を実測するが、利用者向けの供給指標名は未定 | `route_id`、`stop_id`、`trip_id`の数を現実の路線・物理停留所・交通全体と同一視しない表示名と単位を定義 | GTFSではIDがデータ作成者の表現単位で、`stop_id`は複数の`location_type`を表し得るため |
| 3 | `calendar`系の日付候補を別々に記録し、単一期間へ統合しない | 比較週と各実日付を明示し、その日付で有効な`service_id`から予定便ID数を算出する規則を定義 | 「平日便数」だけでは日付例外や曜日差を隠すため |
| 4 | フィードと市町の対応は棚卸し結果として保持 | フィード総数値を複数市町へ転記せず、未確認・算出不能・0を別状態にする | GTFS未確認を交通不存在と誤認せず、自治体間比較の単位をそろえるため |
| 5 | 次段階は内部日付と収録内容の表示仕様化 | 次段階をSUPPLY-METRIC-2の計算実装候補に限定し、別途一作業として承認されるまで着手しない | 指標の意味、計算、限界を先に受け入れ、表示実装と同時着手しないため |

### 10.18 rev.5.3 → rev.5.4

SUPPLY-METRIC-2のCodex受入後、検証済み20値を利用者が同じ日付・同じ定義で確認できる
ローカル比較表示へ進めるため、SUPPLY-VIEW-1を追加した。公開はこの工程へ含めない。

| # | rev.5.3 | rev.5.4 | 根拠 |
|---|---|---|---|
| 1 | §15を現在の一作業SUPPLY-METRIC-1とする | §15を受け入れ済みとして保持し、§16を現在の一作業SUPPLY-VIEW-1とする | 指標定義、2 ZIPの計算、訂正、独立検算、69テストをCodexが受け入れたため |
| 2 | 将来の機械可読出力契約だけを定義 | SHA256で固定した`data/gtfs_supply_metrics.json`を表示の唯一の数値入力とする | HTMLへの数値転記と、計算値・表示値のずれを防ぐため |
| 3 | 比較可能な条件と禁止表現を定義 | 同じ比較週の2フィードを構造3値と7実日付で並べる画面契約を定義 | 市町切替を繰り返さず、日付差と範囲を保ったまま確認できる利用者成果へ進めるため |
| 4 | `metric_status`の意味をデータ契約として定義 | `measured`以外を0へ変換しない表示語彙と理由表示を必須化 | 未確認・算出不能・入力異常を交通供給0と誤認させないため |
| 5 | 公開画面は変更しない | ローカルHTML・静的サイト用JSON・テスト・証拠までを実装対象とし、commit・push・Pages更新は別ゲートとする | 現在のPagesは旧版が公開中であり、実装受入と外部反映を分離するため |

### 10.19 rev.5.4 → rev.5.5

SUPPLY-VIEW-1のローカル受入後も公開トップが旧「準備中」のままで、想定利用者が成果へ到達できない
状態を解消するため、個別承認済みのRELEASE-1を追加した。UDC本応募と受賞の状態は変更しない。

| # | rev.5.4 | rev.5.5 | 根拠 |
|---|---|---|---|
| 1 | SUPPLY-VIEW-1をローカル受入で停止 | RELEASE-1としてcommit・push・Pages更新・公開URL検証までを一作業にする | Goal-to-doneの完了条件を利用者が到達できる外部状態まで追うため |
| 2 | 外部公開済みと表示しない | 公開URL・commit・配備成功・公開ブラウザ証拠が揃う場合だけGitHub Pages公開中と表示してよい | 公開前の誤表示防止契約を、公開後の検証可能な事実表示へ更新するため |
| 3 | 公開ブラウザ証拠なし | 1440×1600・390×844の公開URL実測、生JSON、要約、PNGを必須化 | ローカル成功と公開成功を混同しないため |
| 4 | 応募・受賞と公開をまとめて未達表示 | 作品①の現行デモ公開と、T1〜T4・外部提出を分離して表示する | 公開しただけで応募・受賞を完了扱いしないため |

### 10.20 rev.5.5 → rev.5.6

RELEASE-1で公開したデモは数値と根拠を確認できるが、初見の審査員や交通担当者が、作品の課題、
利用方法、価値、審査根拠を一続きに理解できる入口がない。ロードマップ07を準備状態で終わらせず、
既存の公開デモ・コード・データを応募説明ページで結ぶENTRY-PAGE-1を追加する。

| # | rev.5.5 | rev.5.6 | 根拠 |
|---|---|---|---|
| 1 | 次工程を「応募用の公開物一式」とだけ記録 | 作品①の応募説明ページを公開URLで受け入れるENTRY-PAGE-1を一作業として定義 | 作業名ではなく、初見の読者が作品価値を理解できる成果で区切るため |
| 2 | 公開デモ、コード、データが別々に存在 | 課題、利用者、使い方、使用データ、審査3観点、限界、既存成果への導線を一ページにまとめる | UDC2026の実用度・完成度・挑戦度を第三者が追跡できるようにするため |
| 3 | RELEASE-1公開で工程06まで完了 | ENTRY-PAGE-1の公開受入でロードマップ07を完了とし、工程08の外部提出だけをG6に残す | 「公開物一式」を永久に準備中と表示しないため |
| 4 | Chat一覧から作業経緯を参照 | `run_record.md` §4にHEAD、今回の一作業、再開手順を固定する | Chat表示が一時的に消えてもリポジトリだけで安全に再開するため |

### 10.21 rev.5.6 → rev.5.7

ENTRY-PAGE-1で作品概要エントリーへ進める公開地盤はできたが、概要エントリーは受賞までの途中工程である。
早期公開の利点を、原本更新の観測期間と継続運用の証拠へ変えるため、受入済み原本を自動更新せずに
外部原本との差異だけを検出するWORK1-FRESHNESS-1を追加する。

| # | rev.5.6 | rev.5.7 | 根拠 |
|---|---|---|---|
| 1 | ロードマップ07完了後は工程08の外部提出だけ | 作品①を受賞に向けて段階制Goal-to-doneで継続し、最初の強化段階をWORK1-FRESHNESS-1とする | 概要エントリー可能は全体ゴールの完了ではなく、早期開始の観測期間を活用できるため |
| 2 | 原本の取得日・SHA256を固定表示 | 外部原本を再取得して`unchanged`・`changed`・`unavailable`等を区別する検査を追加 | 固定した検証日だけでは、公開後の原本更新を利用者が判別できないため |
| 3 | 原本差異の検査は手動証拠だけ | 定期CIと手動実行の両方で検査し、結果を上書き更新に使わない | 継続性を証拠化しつつ、未検証の新原本で受入済み値を変えないため |
| 4 | 次機能は未定義 | WORK1-FRESHNESS-1受入後に次の受賞寄与段階を一つだけ定義する | 全体を一つの終わらないGoalにせず、段階ごとに完了判定するため |

### 10.22 rev.5.7 → rev.5.8

WORK1-FRESHNESS-1とWORK1-SCOPE-LOCK-1を公開受入し、作品①を他作品へ接続せずに評価可能な境界が
整った。次の注力判断を印象ではなく同じ尺度で行えるよう、公式審査観点と公開証拠を対応づける
WORK1-AWARD-COMPARISON-1を追加する。

| # | rev.5.7 | rev.5.8 | 根拠 |
|---|---|---|---|
| 1 | 原本変更検知までを一作業とする | UDC2026公式の実用度・完成度・挑戦度へ作品①の公開証拠を対応づける | 受賞寄与と残課題を検証可能にして、次の注力判断へ渡すため |
| 2 | 作品①の審査3観点は応募説明ページの文章だけ | 0〜5点の共通尺度、確信度、公開証拠、不足証拠を機械可読JSONと公開ページで固定する | 他作品の中身を入力にせず、同一形式の出力だけを人が比較できるようにするため |
| 3 | 特別賞候補は過年度計画を含む | 2026年公式要項に掲載されたGTFS賞・BODIK賞・DATAKIDS賞だけを現行判定する | 年度違いの賞や条件を受賞戦略へ混ぜないため |
| 4 | 次の強化候補に順位がない | 証拠不足と期待改善幅から上位3件を示すが、この段階では機能追加・登録・応募を行わない | 注力判断前に作品①だけを過剰強化せず、比較可能な停止点を作るため |

### 10.23 rev.5.8 → rev.5.9

WORK1-AWARD-COMPARISON-1とread-only独立監査で、比較可能な現状と既知の不足を固定した。比較可能は
作品完成を意味せず、概要エントリーまで約3か月あるため、外部参加者を待たずに利用者タスクへ直結する
WORK1-TASK-MEMO-1を追加する。

| # | rev.5.8 | rev.5.9 | 根拠 |
|---|---|---|---|
| 1 | 市町別の値は画面内で閲覧する | 選択市町の登録供給、GTFS状況、日付、根拠、限界、次の確認事項を一つの確認メモにする | 交通協議や追加調査へ渡せる具体的な成果物にするため |
| 2 | 選択状態は公開トップのURLだけで共有する | 市町名を含む独立メモURL、URLコピー、印刷表示を提供する | オンライン共有と会議資料化を同じ公開成果から行えるようにするため |
| 3 | 比較スコアカードは改善候補を示すだけ | 外部依存のない改善候補`task_based_decision_output`を実装する | 実用度・完成度を先に高め、後の利用者検証でも同じタスクを使えるため |
| 4 | 比較可能な停止点で作品①を固定する | 作品①と作品②をそれぞれ高い水準へ進めてから比較し、作品②の内容は引き続き入力しない | 比較準備を完成と誤認せず、受賞までの時間を改善に使うため |

### 10.24 rev.5.9 → rev.5.10

WORK1-TASK-MEMO-1の公開受入とread-only独立監査をGOとした後、応募までの時間を使って
実用度をさらに高める。公式カタログ14市町分の再探索では新しい採用可能ZIPは見つからなかった一方、
受入済み光市GTFSの`広域生活交通`が周南市内にも停留所を持つことをGTFS本体と国土地理院APIで
確認できたため、原本を増やさず対象範囲の表現と実務メモを訂正する。

| # | rev.5.9 | rev.5.10 | 根拠 |
|---|---|---|---|
| 1 | 公式資料で確認できた市町は5市町 | 周南市を追加し6市町とするが、整備率・交通カバー率とは呼ばない | 光市公式GTFSの路線7に周南市コード35215の停留所を確認したため |
| 2 | 実測値は岩国市・光市だけに表示 | 2フィードの値は増やさず、関連フィードとして周南市からも同じ光市GTFSのフィード全体値を参照できる | 市町内だけの値へ分割・推計せず、既存の安全検査済み実測を正しい単位で再利用するため |
| 3 | GTFS確認日は2026-08-09 | 公式カタログと2 ZIPを2026-08-12に再確認し、取得結果を状態別に記録する | 早期公開後の継続確認を再現可能な履歴へするため |
| 4 | WORK1-TASK-MEMO-1で既存4 JSONを変更しない | WORK1-GTFS-COVERAGE-2だけでGTFS状況・関連表示・証拠を更新する | 受入済み履歴を保ち、別段階の訂正範囲を明確にするため |

### 10.25 rev.5.10 → rev.5.11

WORK1-GTFS-COVERAGE-AUDIT-1のGOとP2 1件を受け、現行公開証拠を同一尺度で再評価する
WORK1-AWARD-COMPARISON-2を追加した。P2の有限工程表示は継続改善モデルへ直し、方法の妥当性だけを
4.5へ更新し、実用度3.5、完成度4.0、挑戦度3.0、総合70.0は据え置いた。

### 10.26 rev.5.11 → rev.5.12

WORK1-AWARD-COMPARISON-AUDIT-2のGOとP2 2件を受け、現行記録の審査基準名を公式表記
「実用度・完成度・挑戦度」へ統一し、状況ページの根拠を§20・§23へ接続する
WORK1-AWARD-COMPARISON-TRACEABILITY-1を追加した。採点値、スコアカードbytes、原本、公開値は変えない。

---

## 11. この仕様書を改訂するときの手順

**改訂は必ず旧版の現物を手元に置いて行う。**無い場合は改訂に着手せず、旧版を要求する。
再構成で代替しない。rev.2 の初稿は rev.1 を持たずに書いたため、`operators.csv` の改名、
`service_area_municipalities`、旅客範囲の7フラグ列、§1.5、§5、§6、§7 を落とした。

改訂後は `tools/spec_coverage.py` を走らせ、**出力を添えて出す。**

```
python tools/spec_coverage.py <旧版> <新版>
```

旧版の見出し・バッククォート識別子・表の1列目・コードブロック行を総当たりで新版に照合し、
落ちたトークンが §10 の改訂点表に書かれているかまで見る。
**未説明の欠落が1件でもあれば不合格**（終了コード 1）。合格出力が無い改訂版は配布しない。

---

## 12. I-4 実行仕様 — 山口県19市町のGTFS/GTFS-JP公式状況表示

### 12.1 目的と利用者成果

I-4の目的は、GTFS-1で確認した公式一次資料の範囲と日付を、I-2の市町別登録供給ビューに
事実だけで追加することである。利用者は同じ市町を選んだまま、次を確認できる。

1. その市町を運行対象に含む公式GTFS/GTFS-JPフィードを確認できたか
2. 確認できたフィードの公開主体、対象の書かれ方、公式ページ、アクセス条件
3. 公式ページにデータ基準日があるか、その日付はいつか
4. 公式に有効期間があるか、確認日がその期間内か
5. 同じ市町の自家用有償旅客運送の登録供給

I-4は作品①の「供給側データの欠落」を見えるようにする一段であり、
GTFSの有無だけで公共交通の充足・不足や交通空白の原因を判定しない。

### 12.2 入力として確定している事実

正とする棚卸しは`evidence/20260809_gtfs_yamaguchi_inventory.txt`、個別根拠は
`evidence/20260809_gtfs_source_*.txt`である。確認日は2026-08-09。

- 公式一次資料で確認できたフィードは3件
- 公式フィードが運行対象の市町名を明記しているのは5市
  （岩国市、光市、宇部市、美祢市、山陽小野田市）
- 今回確認した公式資料の範囲でフィードを確認できなかったのは14市町
- 岩国市と光市は公式ページ記載のデータ基準日が2026-04-01。
  GTFS内部の有効期間はZIP未展開のため未確認
- 船木鉄道は公式有効期間が2025-11-17〜2026-11-16。
  認証キーを含むURL雛形は確認済みだが、認証済みファイル本体は未取得

3フィードと5市町は別の数である。船木鉄道1フィードを3フィードとして数えず、
5/19を「整備率」「網羅率」「交通カバー率」と呼ばない。5/19は
「今回確認した公式資料で、対象市町名を含むフィードを確認できた市町数」に限る。

### 12.3 3軸の判定規則

単一の鮮度点数、順位、合否、赤黄緑の総合評価は作らない。次の3軸を独立して表示する。

#### 軸A: 公式資料での確認状態

`availability_status`は次のいずれかとする。

| 値 | 意味 | 表示 |
|---|---|---|
| `confirmed` | 公式一次資料が、その市町を運行対象に含むGTFS/GTFS-JPフィードを明記 | 公式フィードを確認 |
| `not_confirmed_in_checked_sources` | GTFS-1で確認した公式資料の範囲では確認できない | 今回確認した公式資料の範囲では未確認 |
| `unassessed` | 対象範囲の調査自体が未完了 | 未調査 |

現在の19市町は`confirmed`が5、`not_confirmed_in_checked_sources`が14、
`unassessed`が0である。後二者を「GTFSなし」「未整備」「非対応」と書き換えない。

#### 軸B: 公式ページ記載のデータ基準日

`reference_date_status`は`confirmed`または`not_stated`とする。
日付がある場合だけ`official_reference_date`へISO 8601の`YYYY-MM-DD`で記録する。
これはカタログ最終更新日、HTTP Last-Modified、ファイル名内日付、
GTFS内部の`feed_start_date` / `feed_end_date`のいずれとも自動的に同一視しない。

#### 軸C: 公式有効期間

`validity_status_at_check`は次のいずれかとし、必ず`checked_at`と組で表示する。

| 値 | 意味 |
|---|---|
| `within_official_period` | 公式の開始日・終了日があり、確認日が期間内 |
| `outside_official_period` | 公式の開始日・終了日があり、確認日が期間外 |
| `not_confirmed` | 公式有効期間を確認していない |

現在は船木鉄道だけが`within_official_period`、岩国市・光市は`not_confirmed`である。
`not_confirmed`を期限切れと表示しない。表示後に日付が経過しても自動的に「期限切れ」へ変えず、
次回の公式資料確認とビルドで更新する。

### 12.4 出力ファイルとスキーマ

手入力の根拠は受け入れ済み棚卸しに限定し、次のCSVを作る。
同名JSONを生成して`docs/data/`へ公開用に複製する。

#### `data/gtfs_feeds.csv`（1行1フィード、3行）

| 列名 | 内容 |
|---|---|
| `feed_id` | リポジトリ内で一意な固定ID |
| `publisher` | 公式公開主体 |
| `dataset_name` | 公式データセット名 |
| `official_page_url` | 公式データセットページ |
| `download_url_or_template` | 公開URLまたは認証キー付きURL雛形 |
| `access_status` | `public_head_confirmed` / `authentication_required_not_retrieved` |
| `format_label` | 公式ページの形式表記 |
| `official_reference_date` | 軸Bの日付。無ければ空欄 |
| `reference_date_status` | 軸Bの状態 |
| `catalog_updated_date` | 公式カタログの更新日。無ければ空欄 |
| `official_valid_from` | 公式有効期間開始日。無ければ空欄 |
| `official_valid_to` | 公式有効期間終了日。無ければ空欄 |
| `validity_status_at_check` | 軸Cの状態 |
| `checked_at` | 公式資料の確認日 |
| `license_name` | 公式利用条件名 |
| `license_url` | 公式利用条件URL |
| `scope_note` | 対象路線・対象地域の公式記載と、範囲の未確認事項 |
| `source_evidence` | 根拠ファイル。複数は`;`区切り |

#### `data/municipality_gtfs.csv`（1行1市町、19行）

| 列名 | 内容 |
|---|---|
| `municipality_code` | 既存の公式19市町一覧の自治体コード |
| `municipality` | 市町名 |
| `availability_status` | 軸Aの状態 |
| `feed_ids` | 対応する`feed_id`。複数は`;`区切り、未確認なら空欄 |
| `scope_note` | 市域全体を網羅するとは限らないことを含む事実・限界 |
| `checked_at` | 公式資料の確認日 |
| `source_evidence` | 市町判定の根拠ファイル。複数は`;`区切り |

CSVはUTF-8、改行はLF、ヘッダ順は上表どおりとする。
`data/gtfs_feeds.json` / `data/municipality_gtfs.json`はCSVと値・行順が一致し、
`docs/data/gtfs_feeds.json` / `docs/data/municipality_gtfs.json`は対応する`data/`版と
バイト一致させる。

### 12.5 画面要件

旧`docs/index.html`の現物から改訂し、既存の市町セレクタと登録供給表示を残す。

1. 県全体の概要は「公式フィード3件」「公式資料で対象市町名を確認できた市町5/19」と表示する
2. 5/19の近くに、整備率・市域カバー率ではないことを常時表示する
3. 市町を選ぶと、登録供給と別カードで軸A・B・C、公開主体、対象範囲、公式ページを表示する
4. `confirmed`でも市域全体・全事業者・全路線の網羅を断定しない
5. `not_confirmed_in_checked_sources`は
   「今回確認した公式資料の範囲ではGTFS/GTFS-JPを確認できませんでした」と表示する
6. 岩国市・光市はデータ基準日2026-04-01と内部有効期間未確認を同時に表示する
7. 船木鉄道の対象3市は有効期間2025-11-17〜2026-11-16、確認日現在は期間内、
   認証済み本体未取得を表示する
8. 確認日2026-08-09、出典、利用条件、調査範囲の限界へ画面内から到達できる
9. 不明・未確認を赤色や警告アイコンだけで示さず、文字でも意味を表示する
10. 既存の登録供給の数値・注意書き・原本リンクを変更しない

### 12.6 完了条件

1. `data/gtfs_feeds.csv`は3行、`data/municipality_gtfs.csv`は公式順19行で、
   `feed_id`と`municipality_code`が各ファイル内で一意
2. 市町判定は`confirmed=5`、`not_confirmed_in_checked_sources=14`、`unassessed=0`
3. 3フィードと5市町の関係を重複なく保持し、船木鉄道は1フィードのまま3市へ関連付く
4. 岩国市・光市の基準日、船木鉄道の有効期間とアクセス条件が§12.2と一致
5. CSV・`data/` JSON・`docs/data/` JSONが相互に一致し、URL値が受け入れ済み棚卸しと一致し、
   全証拠ファイルが実在する。認証が必要なURLへ実トークンを付けたアクセスは行わない
6. 画面が§12.5を満たし、「GTFSなし」「未整備」「整備率」「網羅率」「交通カバー率」と断定しない
7. 既存41テストを退行させず、行数・状態別件数・日付・関係・禁止表現・公開JSON一致のテストを追加する
8. `python -m unittest discover -s tests -v`が全件成功する
9. デスクトップと390×844相当のモバイル幅で、市町選択、3軸、登録供給、出典を読み戻し、
   横方向のページはみ出しとコンソールエラーが0件
10. 旧`docs/index.html`の現物から改訂し、`tools/spec_coverage.py`の合格出力を提示する

### 12.7 I-4に含めないもの

- GTFS/GTFS-JP本体のダウンロード、認証、展開、パース、仕様検証
- 停留所・路線・時刻表・運賃・地図形状の表示
- 経路検索、GTFS-RT、リアルタイム処理、ユーザ投稿
- 市域全体・全事業者・全路線の網羅判定
- 鮮度の単一スコア、ランキング、閾値による良否判定
- 自家用有償旅客運送4PDFの再処理、登録供給の再集計
- 作品②、他県展開、外部公開、BODIK APPs登録、UDCへの提出

I-4は上記完了条件まで実行して停止し、Codexが実ファイル、差分、テスト、画面、
`tools/spec_coverage.py`の出力を読み戻すまで受け入れ済みにしない。

---

## 13. I-5 実行仕様 — READMEと状況ページの現況説明同期

### 13.1 目的と利用者成果

I-5の目的は、新機能を増やすことではなく、`README.md`と`docs/status.html`を
受け入れ済みI-1〜I-4の現在地へ同期することである。利用者は次を一度に確認できる。

1. 何のための作品で、現在どこまで利用できるか
2. 登録簿4PDFの機械可読化と市町別登録供給ビューが受け入れ済みであること
3. 公式GTFS/GTFS-JPの確認状況3フィード・対象名確認5/19市町を表示済みであること
4. 5/19は整備率・網羅率・交通カバー率ではなく、残り14市町も不存在断定ではないこと
5. T1〜T3の自力終了条件とT4の成果条件は未達で、ローカル実装完了と応募完了は別であること

### 13.2 入力として確定している事実

- I-1: 山口県分4PDF、23団体、`vehicles.csv` 90行、実車両136台、軽20台
- I-2: 公式順19市町、市町別登録供給、登録あり15/19市町
- I-3: `docs/status.html`から`docs/index.html`へ到達できる現況説明
- I-4: 公式フィード3件、公式資料で対象市町名を確認できた5/19市町、
  `confirmed=5`、`not_confirmed_in_checked_sources=14`
- GTFS-1-CORR-2: 和木町の正しいCKAN組織IDは`35321`、総務省6桁コードは`353213`。
  組織21件と全文検索25件を区別し、GTFS未確認の結論は維持
- I-5開始前の自動テストは55件成功
- T1、T2、T3、T4はいずれも未達。外部公開・応募・受賞を完了と表示しない

### 13.3 変更対象

Claude Codeが変更してよい既存ファイルは次だけとする。

- `README.md`
- `docs/status.html`
- `tests/test_site.py`
- `PROGRESS.md`
- `verification.md`

旧版現物、比較結果、ブラウザ検証記録・スクリーンショットは
`evidence/20260810_i5_*`として新規作成してよい。
`CLAUDE.md`、`SPEC.md`、`run_record.md`、`tools/spec_coverage.py`、
`docs/index.html`、`src/`、`data/`、`raw/`、既存`evidence/`は変更しない。

### 13.4 README要件

1. このリポジトリの役割を、登録簿の機械可読化だけでなく、
   市町別登録供給と公式GTFS/GTFS-JP確認状況を表示する静的ビューまで含む現況へ直す
2. 4PDF・23団体・90行・136台・軽20台を保持する
3. 公式フィード3件、対象市町名を確認できた5/19、未確認14/19を追加する
4. 5/19を整備率・網羅率・交通カバー率と呼ばず、未確認を不存在と書かない
5. `data/gtfs_feeds.*`、`data/municipality_gtfs.*`、`docs/index.html`を収録物・利用画面として案内する
6. 「30テスト成功」の固定値を除き、I-5完了時の全テスト成功を事実どおり記録する
7. このリポジトリやローカル実装をUDC応募・公開・受賞の完了と扱わない

### 13.5 状況ページ要件

1. 「I-1・I-2受入済み」「41テスト」「7工程中4工程」「I-3後に再評価」など、
   I-4前の進捗表示を除く
2. I-1〜I-4の受け入れ済み成果を、目的との関係が分かる短い説明で表示する
3. 登録簿4PDFの数値とI-4の3フィード・5/19・14/19を混同せず表示する
4. 5/19の意味と限界を常時読める文字で示す
5. T1〜T3は自力終了条件、T4は成果条件として分離し、いずれも未達と表示する
6. 締切を進捗の主表示にせず、作品目的・利用者成果・現在地・残作業を先に示す
7. `docs/index.html`、公開JSON、出典・利用条件へ実在するリンクで到達できる
8. 外部公開済み、応募済み、受賞済みとは表示しない

### 13.6 完了条件

1. 変更・新規ファイルが§13.3の範囲だけ
2. `README.md`と`docs/status.html`が§13.2〜§13.5の事実・表現要件を満たす
3. `README.md`から「30テスト」、`docs/status.html`から「41テスト」「I-3後に再評価」などの
   古い現況表示が無くなる
4. 3フィード、5/19、14/19の関係を自動テストし、禁止表現による不存在・率の断定を防ぐ
5. 既存55テストを退行させず、I-5用テストを追加する
6. `python -m unittest discover -s tests -v`が全件成功し、実測件数をREADME、状況ページ、
   `verification.md`、完了報告で一致させる
7. `docs/status.html`から`docs/index.html`と指定した公開JSONへリンク操作で到達できる
8. デスクトップと390×844相当のモバイル幅で、ページ横方向のはみ出しと
   コンソールエラーが0件
9. 旧`README.md`と旧`docs/status.html`の現物から改訂し、それぞれについて
   `tools/spec_coverage.py`の合格出力を提示する
10. `git diff --check`が終了コード0で、commit、push、GitHub Pages公開、外部提出を行わない

### 13.7 I-5に含めないもの

- `docs/index.html`の機能・表示変更
- GTFS/GTFS-JP本体の取得、認証、解析、地図・経路検索
- 登録簿4PDFの再処理・再集計
- 作品②、他県展開、BODIK APPs登録、UDC応募、外部公開
- 次の作品機能の選定・実装

I-5は現況説明の同期だけを完了条件まで実行して停止し、Codexが旧版現物、差分、テスト、
リンク操作、PC・スマホ表示、`tools/spec_coverage.py`の出力を読み戻すまで受け入れ済みにしない。

---

## 14. GTFS-2 実行仕様 — 岩国市・光市の公式GTFS ZIP取得・安全検査・実測

### 14.1 目的と作品ゴールとの関係

作品①で将来、県・市町の交通担当者が「公式ページ記載のデータ基準日」「GTFS内部の日付」
「検証日」「収録内容」を混同せず確認できるようにするには、GTFS本体の実測値が必要である。
現在はZIPへのHEADリクエストが成功したことしか確認しておらず、本体の構成・列・件数・日付は未確認である。

GTFS-2の目的は、認証不要の公式2 ZIPを取得して原本を固定し、安全に中身を読み、
次の画面機能を推測なしで仕様化するための候補値と異常を証拠化することである。
GTFS-2自体は公開画面を変更せず、自治体やフィードの良否を判定しない。

### 14.2 入力として確認済みの事実

| feed_id | 対象 | 現在のaccess_status | GTFS-2での扱い |
|---|---|---|---|
| `iwakuni-gtfsjp` | 岩国市（生活交通バス・由宇地区バス） | `public_head_confirmed` | 公式URLからGETして実測する |
| `hikari-gtfs` | 光市（広域生活交通、ひかりぐるりんバス、光市営バス） | `public_head_confirmed` | 公式URLからGETして実測する |
| `sentetsu-odpt-gtfsjp` | 船木鉄道 | `authentication_required_not_retrieved` | 対象外。認証しない |

- ダウンロードURLは`data/gtfs_feeds.csv`の`download_url_or_template`をfeed_idで引く
- 岩国市・光市は`evidence/20260809_gtfs_source_zip_head_check.txt`でHEADだけを確認済み。
  GET、本体保存、展開、パースは未実施
- 公式ページ記載のデータ基準日は両方とも2026-04-01だが、GTFS内部の日付と同一とはみなさない
- GTFSファイル要件の参照元はGTFS Schedule Reference
  `https://gtfs.org/documentation/schedule/reference/`（2026-08-10確認、同ページは2026-04-27改訂と表示）

### 14.3 変更できるファイル

Claude Code側で変更・新規作成できるのは次だけとする。

- `raw/gtfs/iwakuni_gtfsjp_20260401.zip`
- `raw/gtfs/hikari_gtfs_20260401.zip`
- `src/inspect_gtfs_archives.py`
- `tests/test_gtfs_inspection.py`
- `tests/test_site.py`（`TOTAL_TEST_COUNT`の67→68だけ）
- `README.md`（自動テスト件数の67→68だけ）
- `docs/status.html`（自動テスト件数の67→68だけ）
- `PROGRESS.md`
- `verification.md`
- `evidence/20260810_gtfs2_*`

`CLAUDE.md`、`SPEC.md`、`run_record.md`、`tools/spec_coverage.py`、`.claude/`、
既存の`raw/`・`evidence/`、`data/`、`docs/index.html`、既存の`src/`、
`tests/test_verify.py`は変更しない。
`README.md`、`docs/status.html`、`tests/test_site.py`は上記のテスト件数同期以外を変更しない。

### 14.4 取得規則

1. リポジトリ直下に移動し、開始時の`pwd`、`git status --short --branch`、HEADを記録する
2. `data/gtfs_feeds.csv`から対象2件のURLと`access_status=public_head_confirmed`を読み、
   認証情報・Cookie・既存セッションを付けずにHTTPS GETする
3. 各レスポンスについて取得日時、要求URL、最終URL、HTTP状態、バイト数、SHA256と、
   `Content-Type`、`Content-Length`、`Last-Modified`、`ETag`、`Content-Disposition`、
   `Date`、`Cache-Control`だけを`evidence/20260810_gtfs2_*`へ保存する。
   `Set-Cookie`、Cookie、Authorizationヘッダー値、JWT形式値、APIキーは保存しない
4. HTTP 200、ZIPマジックバイト、20 MiB以下を確認してから所定の`raw/gtfs/`へ保存する
5. URLが無効、認証を要求、HTML等の非ZIPを返す、上限を超える場合は、そのフィードを推測や代替元で補わず停止して報告する
6. 取得した生バイトを整形・再圧縮・ファイル名変更以外の加工で書き換えない

### 14.5 ZIP安全検査

`src/inspect_gtfs_archives.py`はファイルを作業ディレクトリへ展開せず、標準ライブラリ`zipfile`で読む。
次のいずれかに該当するZIPは内容集計へ進まず失敗として記録する。

- メンバー数が200件を超える
- 1メンバーの非圧縮サイズが100 MiB、全メンバー合計が500 MiBを超える
- 非空メンバーの非圧縮サイズ÷圧縮サイズが200を超える
- 絶対パス、ドライブ名、`..`、バックスラッシュ、NUL、ディレクトリ階層を含むメンバー名
- Unicode正規化・大文字小文字を無視したメンバー名の重複
- 暗号化メンバー、シンボリックリンク、通常ファイル以外
- CRC検査に失敗する

CSVはファイル単位で`utf-8-sig`により厳格に復号し、UTF-8として読めない場合に
Shift_JIS等へフォールバックしない。BOMの有無は証拠へ記録する。

### 14.6 実測する内容

各ZIPについて、次を決定論的に記録する。

1. 全メンバーのファイル名、圧縮サイズ、非圧縮サイズ、CRC
2. `agency.txt`、`stops.txt`、`routes.txt`、`trips.txt`、`stop_times.txt`の有無、
   ヘッダー、データ行数、主IDの空欄数・一意数・重複数
3. `stops.txt`は`location_type`の値別件数も記録し、単一の「停留所数」をまだ選ばない
4. `calendar.txt`は`service_id`、曜日フラグ、`start_date`、`end_date`を検査し、
   行数、service_id一意数、最小start_date、最大end_dateを候補値として記録する
5. `calendar_dates.txt`は行数、service_id一意数、`exception_type`別件数、
   追加日と削除日の最小・最大日を別々に記録する
6. `feed_info.txt`があれば行数、`feed_start_date`、`feed_end_date`、`feed_version`を記録する。
   無いこと自体は異常としない
7. `trips.txt`が参照するservice_idと、calendar系に存在するservice_idの差集合を両方向で記録する
8. GTFSの日付は8桁`YYYYMMDD`かつ実在日付として検証し、不正値を変換・補正しない

GTFS-2では`feed_info`の期間、`calendar`の範囲、`calendar_dates`の追加・削除日を
一つの「内部運行期間」へ統合しない。行数と一意ID数も一つの件数へ丸めない。
この工程は構造と候補値の実測であり、GTFS仕様への完全適合を認証するものではない。

### 14.7 自動テスト

新規テストはネットワークへ接続せず、一時ディレクトリ内の合成ZIPと保存済み公式ZIPで行う。
`tests/test_gtfs_inspection.py`には検出可能なテストメソッドを1件だけ追加し、
各条件は名前付き`subTest`で分離する。最終unittest件数は68件とする。

1. 正常な最小GTFS ZIPから、ヘッダー・行数・ID・日付候補を再現できる
2. パストラバーサル、絶対パス、階層、重複名、暗号化、シンボリックリンクを拒否する
3. メンバー数、個別・合計サイズ、圧縮比の境界値を検査する
4. UTF-8、UTF-8 BOMを区別して読み、非UTF-8を拒否する
5. 不正日付、必須列不足、空ID、重複IDを黙って補正・重複排除しない
6. `calendar.txt`のみ、`calendar_dates.txt`のみ、両方あり、両方なしを区別する
7. 岩国市・光市の保存済みZIPについてSHA256と実測結果を固定値で検査する
8. 既存67テストを退行させず、新規1テストを含む68件が成功する

### 14.8 完了条件

1. 対象が岩国市・光市の公式2 ZIPだけで、船木鉄道の認証・取得を行っていない
2. 2 ZIPの要求URL、最終URL、取得日時、HTTP状態、公開可能なヘッダー、サイズ、SHA256を保存した
3. 生ZIPを指定した`raw/gtfs/`へ保存し、取得直後と最終ファイルのSHA256が一致する
4. §14.5の安全検査を通過し、ファイルを作業ディレクトリへ展開していない
5. §14.6の実測値を2フィード分提示し、未存在・空欄・不正値を推測で埋めていない
6. 新規検査スクリプトとテストを実ファイルから読み戻せる
7. `python -m unittest discover -s tests -v`が68件成功し、既存67件を退行させていない
8. `README.md`、`docs/status.html`、`tests/test_site.py`のテスト件数が68で一致し、
   それ以外の内容を変更していない
9. `PROGRESS.md`、`verification.md`、`README.md`、`docs/status.html`、`tests/test_site.py`の
   編集前現物を`evidence/20260810_gtfs2_*_before.*`へ保存し、最終版との
   `tools/spec_coverage.py`合格出力5件を提示する
10. 全変更内のJWT形式値、Authorizationヘッダー値、実APIキー、秘密鍵が0件である
11. 変更・新規ファイルが§14.3だけで、既存ファイルの意図しない変更がない
12. `git diff --check`を実行する。生の取得証拠だけに由来する警告がある場合は、
    対象ファイルと件数を分離して報告し、コード・Markdown側が終了コード0であることを示す
13. commit、push、GitHub Pages公開、外部提出を行っていない

### 14.9 GTFS-2に含めないもの

- `data/gtfs_feeds.*`、`data/municipality_gtfs.*`、公開JSON、`docs/index.html`の更新
- 内部運行期間の最終算出規則、単一鮮度スコア、ランキング、合否、赤黄緑評価
- GTFS未確認や期間外を、交通空白・公共交通不存在・自治体の不備とみなすこと
- 地図、停留所位置表示、経路検索、GTFS-RT、利用者投稿
- 船木鉄道のアクセストークン取得・認証済みダウンロード
- 登録簿4PDFの再処理、作品②、他県展開、BODIK APPs登録、UDC応募、外部公開

### 14.10 停止条件と次段階

GTFS-2は2 ZIPの原本固定・安全検査・実測・証拠化だけを完了条件まで実行して停止する。
取得失敗、安全検査失敗、仕様矛盾が1件でもあれば、代替データや推測で埋めずCodexへ報告する。
Codexが実ZIP、ハッシュ、検査コード、テスト、実測証拠を読み戻して受け入れた後にだけ、
内部日付と収録内容を利用者へどう表示するかを次の一作業として仕様化する。

---

## 15. SUPPLY-METRIC-1 — 自治体別輸送供給指標の定義

### 15.1 解決する問題と利用者成果

公共交通に詳しくない人が、GTFSの`routes.txt`、`stops.txt`、`trips.txt`の行数を見ても、
その数字が現実の何を表すかは分からない。行数をそのまま「路線数」「停留所数」「便数」と呼ぶと、
データ作成者によるIDの分け方、運行日の違い、日付例外、複数市町を含むフィード範囲を隠してしまう。

SUPPLY-METRIC-1の目的は、県・市町の公共交通担当者、交通事業者、初めて見る一般利用者が、
次の問いに同じ定義で答えられるようにすることである。

1. 今回確認した公式資料の範囲で、その市町に関連する公式GTFSを確認できたか
2. 確認済みGTFSには、どの単位の路線情報・乗降場所情報が収録されているか
3. 明示した実日付ごとに、GTFS上で何件の運行便が予定されているか
4. その数字はどのZIP、どの日付、どの検証日に基づくか
5. 数字だけから交通の不存在、便利さ、品質を断定できない理由は何か

この工程は**指標名、単位、計算規則、比較条件、欠測時の扱いを定義するだけ**である。
集計コード、JSON/CSV、README、HTML、地図、経路検索、公開物は変更しない。

### 15.2 一次仕様と入力

GTFS項目の意味は、2026-08-10に確認した公式GTFS Schedule Referenceを基準とする。

- `https://gtfs.org/documentation/schedule/reference/`
- 公式ページ表示上の改訂日: 2026-04-27
- `routes.txt`の主キーは`route_id`
- `stops.txt`の`stop_id`は、`location_type`により乗降場所、駅、出入口、構内ノード等を表し得る
- `trips.txt`の各`trip_id`は`route_id`と`service_id`を参照する
- `calendar.txt`は曜日別の通常運行日、`calendar_dates.txt`は実日付の追加・削除を表す
- `frequencies.txt`は定間隔運行または同一間隔の時刻表を表し、単純な`trip_id`件数とは別に扱う

ローカル入力は、GTFS-2でCodex受入済みの次の現物だけを基準にする。

- `raw/gtfs/iwakuni_gtfsjp_20260401.zip`
- `raw/gtfs/hikari_gtfs_20260401.zip`
- `evidence/20260810_gtfs2_inspection_run_raw.txt`
- `verification.md`のGTFS-2受入表
- `data/gtfs_feeds.csv`と`data/municipality_gtfs.csv`の受入済み対応関係

原本ZIPは読み取り専用とし、SUPPLY-METRIC-1では再取得・展開・更新しない。

### 15.3 比較単位

指標は次の3単位を混ぜない。

| 単位 | 意味 | 禁止する扱い |
|---|---|---|
| 市町 | 総務省コードで識別する行政区域 | 関連GTFSが未確認の市町を交通手段0とみなさない |
| フィード | 1つの`feed_id`と、SHA256で固定した1つのGTFS ZIP | フィード全体の値を、関係する複数市町の各市町内値として複製しない |
| サービス日 | `YYYY-MM-DD`で明示したGTFS上の運行日 | 日付を示さず「平日」「休日」の代表値と呼ばない |

岩国市と`iwakuni-gtfsjp`、光市と`hikari-gtfs`は、受入済み棚卸しでは1市1フィードの対応である。
ただし、これはZIP内の全乗降場所が行政界内にあることを証明しない。
SUPPLY-METRIC-1の数値は「その市町に関連付けて確認したフィード全体の収録値」と表示し、
「市町内だけの値」や「市町の全公共交通」とは表示しない。

複数フィードが関係する市町ではフィード別に表示する。重複路線・共同運行・区域外停留所の判定規則を
別作業で受け入れるまで、市町合計を作らない。

### 15.4 確定する指標

#### 15.4.1 確認状態と日付

これらは供給量ではなく、数字を正しく読むための必須文脈である。

| 項目 | 表示名 | 元データ | 意味と制限 |
|---|---|---|---|
| `availability_status` | 公式GTFS確認状態 | `data/municipality_gtfs.csv` | `confirmed`は確認済み、`not_confirmed_in_checked_sources`は確認資料内で未確認。後者を不存在としない |
| `official_reference_date` | 公式ページ記載のデータ基準日 | `data/gtfs_feeds.csv` | 公式ページ側の日付。GTFS内部の開始日と同一視しない |
| `checked_at` | 公式資料の確認日 | `data/gtfs_feeds.csv` / `data/municipality_gtfs.csv` | Codex/Claudeが資料を確認した日。データ内容の基準日ではない |
| `source_zip_sha256` | 検査したGTFS原本 | GTFS-2取得証拠 | どのZIPから算出したかを固定する。値だけを原本から切り離さない |
| `feed_info`日付 | GTFSフィード日付範囲 | `feed_info.txt` | 完全で信頼できる時刻表情報を提供すると宣言した`feed_start_date`と`feed_end_date`を別々に示す。範囲内の毎日運行を意味しない |
| `calendar`日付 | 通常運行規則の範囲 | `calendar.txt` | service_idごとの`start_date`と`end_date`を表す |
| `calendar_dates`日付 | 例外の追加日・削除日 | `calendar_dates.txt` | `exception_type=1`と`2`を分けて示す |

`feed_info`、`calendar`、`calendar_dates`の日付を、一つの「内部運行期間」へ丸めない。
最小日と最大日の間に毎日運行があるとも解釈しない。

#### 15.4.2 GTFS収録構造の件数

| 指標ID | 一般向け表示名 | 計算 | 表示時に必ず併記する限界 |
|---|---|---|---|
| `gtfs_agency_record_count` | GTFS収録の交通ブランド情報件数 | `agency.txt`の検査済みデータ行数 | 法人格を持つ交通事業者数や登録簿の団体数ではない |
| `gtfs_route_id_count` | GTFS収録路線ID数 | 空欄・重複検査に合格した一意`route_id`数 | 現実の路線の分け方はデータ作成者により異なる |
| `gtfs_boarding_location_id_count` | GTFS収録乗降場所ID数 | `location_type`が空欄または`0`の一意`stop_id`数 | 方向別・のりば別に複数IDとなり得るため、物理的な停留所数ではない |

`trips.txt`の全`trip_id`数と`stop_times.txt`の全行数は診断値として保持できるが、
日付を反映しないため利用者向けの「便数」「供給量」としては表示しない。

#### 15.4.3 実日付別の予定運行便数

指標IDは`scheduled_trip_count_by_date`、表示名は「この日にGTFSで予定された運行便数」とする。
1便は、指定サービス日に有効である1つの`trip_id`による1回の運行として数える。

指定日`D`の計算は次の順で行う。

1. `calendar.txt`の各行について、`start_date <= D <= end_date`かつDの曜日フラグが`1`なら、
   その`service_id`を有効集合へ入れる
2. `calendar_dates.txt`のDの行を適用し、`exception_type=1`は追加、`2`は削除する
3. 有効集合に含まれる`service_id`を参照する`trips.txt`の一意`trip_id`を選ぶ
4. 選んだtripに`frequencies.txt`の行が無ければ、その`trip_id`を1便と数える
5. `exact_times=1`の行がある場合は、時刻範囲が正で割り切れることを検証し、
   `start_time + n * headway_secs < end_time`を満たす0以上の整数nの個数を数える。
   元のテンプレートtripを別に1便加算しない
6. 有効tripに`exact_times=0`または空欄が1件でもある場合、正確な発車回数は時刻表に固定されないため、
   その日の総数を部分値で出さず`not_exact_frequency_based`とする

`calendar.txt`が無く`calendar_dates.txt`だけがある場合は空集合から`exception_type=1`を追加する。
両方が無い、必須列が無い、日付・曜日値・例外値・参照関係が不正、同一主キーが重複する場合は
0へ補正せず`invalid_input`とする。

### 15.5 比較週の選び方

「平日」「土曜」「日曜」を日付なしの3値にはしない。比較対象フィードについて、次の規則で
月曜から日曜までの7実日付を1組選び、7日すべてを表示する。

1. 比較対象フィードの確認済み`official_reference_date`の最大値を基準日とする
2. 基準日以後で最初の月曜日を週開始候補とする
3. 月曜から日曜までの各日を、各フィードの`calendar`系規則で評価できることを確認する
4. `feed_info.txt`がある場合は7日すべてがその範囲内であることも確認する
5. 条件を満たさなければ次の月曜日へ進み、全対象フィードに共通する最初の1週を選ぶ
6. 各フィードの探索上限は、`feed_end_date`があればその日、無ければ`calendar.end_date`と
   `calendar_dates.date`から得られる最終日とする。上限を決められない場合は`not_calculable`とする
7. 全対象フィードの探索上限の最小値までに共通週が無ければ`not_comparable_no_common_week`とし、
   別々の週を同じ列で比較しない。確認済み`official_reference_date`が無いフィードも同じ列へ入れない

現在の岩国市・光市2フィードでは、公式基準日2026-04-01以後の最初の月曜で、双方の
`feed_info`範囲内にある**2026-04-06（月）〜2026-04-12（日）**をSUPPLY-METRIC-2の
最初の計算対象週とする。SUPPLY-METRIC-1では便数自体をまだ算出しない。

月曜から金曜の5値がすべて同じ場合だけ「この比較週の平日は毎日N便」と要約できる。
異なる場合は5日分を表示し、平均値や最大値だけへ丸めない。土曜・日曜も実日付を併記する。

### 15.6 0、未確認、算出不能の区別

各数値は`metric_status`を持ち、次の状態を区別する。

| `metric_status` | 意味 | 数値欄 |
|---|---|---|
| `measured` | 受入済みZIPと規定の計算で値を再現できた | 0以上の整数を表示できる |
| `not_confirmed` | 今回確認した公式資料で対象GTFSを確認できない | 空欄。0を表示しない |
| `not_calculable` | GTFSはあるが、必要なファイル・列・期間が不足する | 空欄。理由を表示する |
| `invalid_input` | 日付、主キー、参照、列値等の検証に失敗した | 空欄。異常を0へ補正しない |
| `not_comparable_scope` | 市町範囲や対象フィードの単位がそろわない | 個別値は保持できても合計・順位を出さない |
| `not_exact_frequency_based` | 定間隔運行で正確な発車回数が固定されない | 部分的な便数を総数として表示しない |
| `not_comparable_no_common_week` | 共通の比較週を選べない | 日付の違う値を同じ比較列に置かない |

`measured`の0は「そのGTFS・その指標・その実日付で該当レコードが0」という意味だけを持つ。
その市町に鉄道、タクシー、予約制交通、GTFS未収録のバス等が存在しないという意味にはしない。

### 15.7 自治体間比較で許可すること・禁止すること

比較できるのは、同じ`metric_version=SUPPLY-METRIC-1`、同じ指標ID、同じ7実日付、
同じ検証条件を満たし、フィード範囲が併記された値だけである。

許可する表示:

- 岩国市関連フィードと光市関連フィードの、フィード別収録値の並列表示
- 7実日付ごとの予定運行便数
- ZIPのSHA256、公式基準日、GTFS内部日付、確認日の併記
- `metric_status`と算出不能理由の表示

禁止する表示:

- 未確認市町を0件として順位付けすること
- 路線ID数、乗降場所ID数、予定運行便数を足して単一スコアにすること
- `confirmed`市町数を整備率、交通カバー率、GTFS普及率と呼ぶこと
- 登録簿の団体数・車両数とGTFSのID数・便数を加算すること
- 人口、面積、利用者数で正規化していない値を「十分」「不足」と評価すること
- 予定便数だけから運行の信頼性、使いやすさ、運賃、所要時間、バリアフリー対応を断定すること

### 15.8 将来の出力契約

SUPPLY-METRIC-2で機械可読出力を作る場合、最低限次を1レコードから追跡できる形で持つ。

- `metric_version`
- `municipality_code`、`municipality`
- `feed_id`、`source_zip_sha256`
- `scope_note`
- `official_reference_date`、`checked_at`
- `comparison_week_start`、`comparison_week_end`
- 指標ID、値、`metric_status`、算出不能理由
- `feed_info`、`calendar`、`calendar_dates`の日付根拠を分離した値

フィールド名・CSV/JSONの正規形・保存先はSUPPLY-METRIC-2で確定する。
SUPPLY-METRIC-1では新しいデータファイルを作らない。

### 15.9 SUPPLY-METRIC-1の完了条件

1. 目的、想定利用者、解決する誤解を、GTFSを知らない人にも読める言葉で説明した
2. 指標ごとに表示名、GTFS上の計算元、単位、制限を定義した
3. フィード値と市町内値を区別し、複数市町・複数フィードの合算を禁止した
4. `calendar`と`calendar_dates`を適用する実日付別便数の計算順を定義した
5. 比較週を決定論的に選び、岩国市・光市の最初の対象週を2026-04-06〜2026-04-12に固定した
6. 0、未確認、算出不能、不正入力、比較不能、定間隔運行を別状態にした
7. 指標から断定できない事項と禁止する表示を定義した
8. 公式GTFS Schedule ReferenceとGTFS-2受入済み現物に照らして再現可能性を監査した
9. 編集前`SPEC.md`との`tools/spec_coverage.py`が欠落・説明なし0で合格した
10. unittest 68件、`git diff --check`、staged 0件を確認した
11. `run_record.md` §0.5を変更していない
12. 集計コード、テスト、データ、raw ZIP、README、HTML、公開状態を変更していない

### 15.10 停止条件と次段階

SUPPLY-METRIC-1は指標定義のCodex受入までで停止する。Claude Codeは起動しない。

次の一作業候補はSUPPLY-METRIC-2とし、対象は岩国市・光市の受入済み2 ZIPから、
§15.4〜§15.6の値を決定論的に計算し、テストと証拠を作ることに限定する。
`raw/gtfs/`は変更せず、HTML、地図、経路検索、船木鉄道取得、commit、push、公開を含めない。

SUPPLY-METRIC-2は別途、目的、変更可能ファイル、完了条件、Claude利用方法を一作業として
Codexが提示し、ユーザーが開始を承認するまで着手しない。

---

## 16. SUPPLY-VIEW-1 実行仕様 — 岩国市・光市のGTFS輸送供給比較表示

### 16.1 目的と利用者成果

山口県や市町の公共交通担当者、交通事業者、GTFSを初めて見る人が、現在の画面で
「公式GTFSを確認できたか」までは分かっても、そこにどの程度の路線情報・乗降場所情報があり、
具体的な1週間に何便が予定されているかは確認できない。

SUPPLY-VIEW-1の目的は、受入済みの岩国市関連フィードと光市関連フィードについて、
同じ2026-04-06〜2026-04-12の7実日付、同じ`metric_version=SUPPLY-METRIC-1`、同じ表示単位で
検証済みの値を並べ、数字の根拠と限界までスマホで読めるローカル画面を完成させることである。

利用者は市町選択を往復せず、次を一つの比較区画で確認できる。

1. 各フィードに収録された交通ブランド情報、路線情報ID、乗降場所IDの件数
2. 2026-04-06〜2026-04-12の各実日付にGTFSで予定された運行便数
3. 値が`measured`か、表示できない状態か、その理由
4. どの市町に関連付けたどのフィード全体の値で、市町内だけの値ではないこと
5. 公式ページ記載日、GTFS内部日付、確認日、計算日、原本ZIP名・SHA256

### 16.2 入力として固定する現物

表示値の唯一の数値入力は、SUPPLY-METRIC-2でCodex受入済みの
`data/gtfs_supply_metrics.json`とする。SHA256は
`26167df77efce48e6dbcacde757a08ff40f7229fe99b9928f25b541f3766db9b`である。

- 2レコード: `iwakuni-gtfsjp`、`hikari-gtfs`
- 各レコード10値: 構造指標3値、2026-04-06〜2026-04-12の実日付7値
- 岩国市関連: 1、46、800、便数185/186/183/161/188/146/39
- 光市関連: 1、7、172、便数55/55/55/55/55/41/41
- 全20値の`metric_status`は`measured`、`reason`は`null`
- 原本ZIPのSHA256、公式基準日、確認日、計算日、比較週、`date_basis`、`scope_note`を保持する

出典リンク、公開主体、データセット名称、利用条件は、既存の`docs/data/gtfs_feeds.json`を
`feed_id`で結合して表示する。結合できないフィードを推測で補わず、比較区画だけをエラー状態にする。

`data/gtfs_supply_metrics.json`、`src/calculate_gtfs_supply_metrics.py`、原本ZIP、既存の
SUPPLY-METRIC-2証拠は読み取り専用とし、表示都合で値・状態・文言を改変しない。

### 16.3 画面内の位置と情報構造

`docs/index.html`の「公式GTFS/GTFS-JPの確認状況（山口県全体）」の直後、既存の
「確認する市町を選ぶ」の直前に、独立した`section`として追加する。

- 見出し: 「岩国市・光市のGTFS供給を同じ日付で確認」
- 冒頭説明: GTFSは予定運行を表すデータで、この数字は確認したフィード全体の収録値である
- 構造表: 行を交通ブランド情報件数、路線情報ID数、乗降場所ID数、列を岩国市関連・光市関連とする
- 日別表: 行を曜日付き7実日付、列を岩国市関連・光市関連の予定運行便数とする
- 根拠欄: フィードごとに`details`を設け、範囲、日付、ZIP、SHA256、内部日付、出典、利用条件を示す
- 静的サイト用JSONへの実在リンクを出典欄と`noscript`案内へ追加する

構造表・日別表には`caption`、列見出し・行見出しの`scope`を設定する。390px幅でもページ全体の
横スクロールを発生させず、2市の値と日付を同じ視野で読める表幅・折返しにする。

### 16.4 一般向け表示語彙

指標IDをそのまま主見出しにせず、次の表示名を使う。

| 指標ID | 表示名 | 単位 | 必ず併記する意味 |
|---|---|---|---|
| `gtfs_agency_record_count` | GTFS収録の交通ブランド情報 | 件 | 法人格を持つ事業者数ではない |
| `gtfs_route_id_count` | GTFS収録の路線情報ID | ID | 現実の路線数、系統数、道路上の線数ではない |
| `gtfs_boarding_location_id_count` | GTFS収録の乗降場所ID | ID | 物理的な停留所数ではない |
| `scheduled_trip_count_by_date` | この日にGTFSで予定された運行便 | 便 | 実運行、利用者数、便利さ、定時性を表さない |

「路線数」「停留所数」「市町の便数」「交通量」「供給が十分・不足」「順位」「スコア」と表示しない。
月曜〜金曜を平均値・最大値・単一の「平日便数」へ丸めず、7日すべてを実日付付きで表示する。

### 16.5 `metric_status`の表示規則

画面は値オブジェクトごとの`metric_status`を解釈し、次の固定語彙を使う。

| `metric_status` | 表示 | 数値の扱い |
|---|---|---|
| `measured` | 測定済み | 0以上の`value`と単位を表示する |
| `not_confirmed` | 今回未確認 | 数値を空欄にし、0を表示しない |
| `not_calculable` | 算出不能 | 数値を空欄にし、`reason`を表示する |
| `invalid_input` | 入力異常 | 数値を空欄にし、`reason`を表示する |
| `not_comparable_scope` | 範囲が異なり比較対象外 | 数値を比較列へ置かず、理由を表示する |
| `not_exact_frequency_based` | 正確な発車回数を固定できない | 部分値や推定値を表示しない |
| `not_comparable_no_common_week` | 共通の比較週なし | 異なる日付の値を同じ比較表へ置かない |

未知の状態、`measured`なのに整数値が無い、非`measured`なのに数値がある、2フィードの
`metric_version`・比較週・日付キーが一致しない場合は表示用に補正せず、比較区画をエラー表示にする。
既存の登録供給ビューとGTFS確認状態は引き続き利用できなければならない。

### 16.6 変更可能ファイルと書き手

Claude Code側で変更・新規作成できるのは次だけとする。

- `src/build_site_data.py`（受入済みJSONのバイト同一コピー追加）
- `docs/data/gtfs_supply_metrics.json`（`data/`側とのバイト同一コピー）
- `docs/index.html`（比較区画、データ読込、出典リンク、レスポンシブ表示）
- `tests/test_site.py`（データ・HTML・回帰テストと最終テスト件数）
- `README.md`（利用可能な画面の説明と実測テスト件数の同期）
- `docs/status.html`（現在地・利用可能な画面・実測テスト件数の同期）
- `PROGRESS.md`
- `verification.md`
- `evidence/20260810_supply_view1_*`

`CLAUDE.md`、`SPEC.md`、`run_record.md`、`tools/spec_coverage.py`、`.claude/`、`data/`、
`raw/`、既存の`evidence/`、`src/calculate_gtfs_supply_metrics.py`、
`tests/test_gtfs_supply_metrics.py`、`docs/data/`の既存ファイルは変更しない。

開始時に既存dirty差分を保存し、上記既存5ファイル（`src/build_site_data.py`、`docs/index.html`、
`tests/test_site.py`、`README.md`、`docs/status.html`）の作業前SHA256を証拠へ記録する。

### 16.7 実装規則

1. `src/build_site_data.py`は`data/gtfs_supply_metrics.json`を内容変換せず
   `docs/data/gtfs_supply_metrics.json`へバイト同一コピーする
2. 既存3 JSONの読込失敗と供給指標JSONの読込失敗を分離し、新しいJSONの失敗で既存画面を壊さない
3. JSON由来文字列は`textContent`または同等の安全なDOM APIで出力し、`innerHTML`へ入れない
4. `feed_id`で既存フィード情報と結合し、表示順は岩国市関連、光市関連へ固定する
5. 数値をHTMLやJavaScript定数へ二重転記せず、実行時に静的サイト用JSONから読む
6. 完全なZIP SHA256は省略せず表示し、長い文字列は390px幅で折り返す
7. 色だけに状態を依存せず、見出し、表、状態語、理由をテキストで伝える
8. 供給値を登録簿の団体数・車両数へ加算せず、県合計、順位、良否、単一スコアを作らない
9. フッターの「GTFS-JPとの統合は今後」という旧状態を、今回利用可能になった範囲へ同期する
10. ネットワーク取得、外部ライブラリ追加、地図、経路検索、船木鉄道認証を行わない

### 16.8 自動テストと証拠

最低限、次を名前付きテストとして追加または既存テストへ統合する。

1. `data/gtfs_supply_metrics.json`と`docs/data/gtfs_supply_metrics.json`のバイト一致
2. 2レコード、同一`metric_version`、同一比較週、3構造指標、7日付、全20値の固定値一致
3. `metric_status`と`value`/`reason`の整合、不明状態を許さないこと
4. HTMLに比較区画、2表、根拠欄、状態表示、JSONリンク、エラー表示が存在すること
5. HTMLに実測値をハードコードせず、`data/gtfs_supply_metrics.json`から読むこと
6. 新しいデータ読込失敗が既存登録供給・GTFS確認状態の描画を妨げないこと
7. 禁止表示、平均化、ランキング、登録簿値との加算が無いこと
8. `innerHTML`を使わず、既存の代表者氏名非出力・秘密情報非収録テストが退行しないこと
9. 全unittestが成功し、実測テスト件数を`tests/test_site.py`、README、状況ページ、
   `verification.md`、完了報告で一致させること

次の生証拠を`evidence/20260810_supply_view1_*`へ保存する。

- 作業前の変更範囲と既存5ファイルSHA256
- ビルド実行と静的サイト用JSONのバイト一致
- 集中テストと全unittest
- `tools/spec_coverage.py`による旧版対新版のREADME、`docs/status.html`、`docs/index.html`の合格出力
- 1440px幅と390×844相当での表示確認、ページ横スクロール0、コンソールエラー0
- `git diff --check`、staged 0件、秘密情報らしき値0件、最終変更ファイル一覧

### 16.9 完了条件

1. §16.1の利用者成果を`docs/index.html`の一つの比較区画で達成した
2. 静的サイト用JSONが受入済みJSONとバイト一致し、20値・状態・日付・原本根拠を追跡できる
3. 岩国市関連と光市関連を、同じ構造指標と同じ7実日付で切替操作なしに確認できる
4. 非`measured`状態を0にせず、異常な入力や比較条件不一致をエラーとして表示できる
5. 範囲と限界をGTFS未経験者が読める語彙で併記し、禁止表現を使用していない
6. 既存の登録供給ビュー、19市町選択、GTFS確認状態、出典リンクが退行していない
7. 1440px幅と390×844相当で主要情報・根拠へ到達でき、ページ横スクロール・コンソールエラーが0件
8. 自動テスト、旧版網羅検査、差分検査、秘密情報検査がすべて合格した
9. 変更・新規ファイルが§16.6だけで、原本ZIP・計算コード・受入済みJSONを変更していない
10. Codexが実ファイル、静的サイト用JSON、表示値、ブラウザ操作、テスト、証拠を独立に読み戻した
11. commit、push、GitHub Pages更新、外部提出を行っていない

### 16.10 停止条件、Claude依存、次段階

SUPPLY-VIEW-1はローカル比較表示のCodex受入までを一作業とし、仕様だけで完了扱いしない。
Claude Codeの利用時間制限中は、Codexが§16の確定、実行指示、受入観点の準備まで進め、
Claude側所有ファイルの実装開始は利用可能になるまで依存待ちとする。

Claudeを直接起動する場合は、モデル、目的、変更範囲、想定時間を示した新しい個別承認を要する。
過去のSUPPLY-METRIC-2用承認を流用しない。既定はCodexが指示文を提示し、ユーザーがClaudeへ渡す。

実装・検証・Codex受入後も自動的にcommit・push・公開へ進まない。現在公開中の旧GitHub Pagesを
更新する作業はSUPPLY-VIEW-1から分離し、外部差分、公開内容、ロールバック方法を示して本人の
個別承認を得るまで停止する。

---

## 17. RELEASE-1 実行仕様 — 受入済み成果のGitHub Pages公開

### 17.1 目的と利用者成果

ローカルで受け入れた比較画面を、県・市町の交通担当者や事業者が公開URLから実際に利用できる
状態へ移す。公開トップが旧「準備中」のままならRELEASE-1は未完了である。

### 17.2 範囲と除外

含むものは、受入済み差分のcommit、`main`へのpush、GitHub Pages配備、公開URLと公開JSONの
読み戻し、1440×1600・390×844の実Edge検証、公開状態の文書・証拠同期である。

UDC概要エントリー、本応募、BODIK APPs等への外部提出、地図、経路検索、船木鉄道取得、
新しい供給指標、他市町へのGTFS汎用化は含めない。

### 17.3 公開状態の表示契約

1. 公開URL、公開対象commit、Pages配備成功、公開URLのブラウザ証拠が揃った場合だけ、
   `README.md`と`docs/status.html`に「GitHub Pagesで公開中」と表示してよい
2. 現行デモの公開を、T1〜T4、UDC応募、外部提出、受賞の達成として表示しない
3. 「応募済み」「外部提出済み」「受賞済み」「受賞しました」は引き続き禁止する
4. 公開URLと公開データへの実在リンクを示し、第三者が同じ内容を読み戻せるようにする

### 17.4 変更・証拠範囲

- 仕様・受入: `SPEC.md`、`run_record.md`
- 現在地・契約テスト: `README.md`、`docs/status.html`、`tests/test_site.py`
- 実行記録: `PROGRESS.md`、`verification.md`
- 公開検証: `evidence/20260811_release1_*`
- 公開URLを検証できるようにする最小変更: `evidence/20260810_supply_view1_cdp_driver.mjs`

受入済み数値JSON、GTFS ZIP、計算・検査コード、`docs/index.html`の表示内容は変更しない。

### 17.5 完了条件

1. 受入済み66パスのrelease commitが`origin/main`に存在する
2. GitHub Pagesのbuild・deployが成功し、公開トップがHTTP 200で旧「準備中」を含まない
3. 公開`gtfs_supply_metrics.json`がローカル受入済みJSONとバイト一致する
4. 公開URLの1440×1600・390×844実測で全28条件が合格する
5. 両幅で横overflow 0、console error、`Runtime.exceptionThrown`、Log error、
   `Network.loadingFailed`がすべて0である
6. スクリーンショットをCodexが読み、2市、7実日付、根拠、対象範囲の限界を確認する
7. 全unittest、旧版網羅、差分、秘密情報、JSON・ZIP不変検査が合格する
8. 公開状態をREADME、状況ページ、進捗、検証、受入記録へ同期し、`origin/main`とHEADを一致させる
9. UDCへの外部提出、受賞表明、次機能を行っていない

## 18. ENTRY-PAGE-1 実行仕様 — 作品①の応募説明ページ公開

### 18.1 目的と利用者成果

現在の公開デモは、登録簿とGTFSの数値・日付・根拠を確認できる。一方、初めて見る人には、
なぜ二つの資料を一緒に見る必要があるのか、誰の判断に役立つのか、どこまで分かり、何は
分からないのかが一ページで伝わらない。

ENTRY-PAGE-1では、県・市町の公共交通担当者、交通事業者、UDC審査員が、作品の課題から
検証可能な成果までを5分以内に把握し、公開デモ・コード・データへ移動できる応募説明ページを
公開する。既存成果を説明で束ねる作業であり、新しい交通機能の追加ではない。

### 18.2 応募説明の固定内容

- 作品名: **山口県 市町別の登録供給ビュー**
- 作品タイプ: **アプリケーション**
- 作品テーマ: **道路・交通**
- 100字概要（81字）:
  **山口県の公共交通担当者・事業者向けに、分散した登録簿と公式GTFSを市町別に整理し、輸送供給・日付・根拠・データの限界を同じ画面で確認できる静的Webアプリです。**

ページは少なくとも次を、交通データを知らない人にも分かる日本語で示す。

1. 利用者が判断前に知りたいのは、地域に登録された移動サービスと、GTFSに予定された運行供給であること
2. 登録簿とGTFSは公開場所・単位・日付の意味が異なり、そのままでは混同しやすいという課題
3. 市町選択、岩国市・光市関連フィードの同日比較、原本・日付・限界の確認という3段階の使い方
4. 山口県公表の自家用有償旅客運送登録簿、山口県オープンデータカタログ等の公式GTFS、
   公式19市町一覧を使用していること
5. UDC2026の一次審査基準である実用度・完成度・挑戦度に対し、確認可能な実装・証拠を対応付けること
6. 交通の便利さ、実際の運行、利用者数、全事業者の網羅、GTFS未確認地域の不存在を断定しないこと
7. 公開デモ、状況ページ、GitHubリポジトリ、主要な公開JSONへの実在リンク
8. UDC応募、BODIK登録、外部提出、受賞が未実施であること

個人のメールアドレス、氏名、障害情報、非公開の職歴情報、アクセストークンは掲載しない。

### 18.3 範囲と除外

含むものは、`docs/entry.html`、`docs/index.html`・`docs/status.html`・`README.md`からの導線、
`tests/test_site.py`の契約テスト、進捗・検証・受入記録、commit、`main`へのpush、GitHub Pages配備、
公開URLのPC・スマホ実測である。

受入済みデータ・JSON・GTFS ZIP・計算コード・指標値は変更しない。地図、経路検索、リアルタイム、
船木鉄道GTFS本体取得、作品②、UDC概要フォーム送信、作品本応募、BODIK APPs登録は含めない。
外部提出は`run_record.md` §0.5のG6として別に止める。

### 18.4 表示・リンク契約

1. `docs/entry.html`はJavaScriptなしでも本文とリンクを読める静的HTMLとする
2. 390px幅で文書全体の横overflowを発生させず、1440px幅でも本文の行長を読みやすく保つ
3. 既存の公開トップと状況ページから応募説明ページへ移動できる
4. 応募説明ページから公開デモ、供給比較、状況ページ、コード、主要JSONへ移動できる
5. 外部リンクはHTTPSとし、新しいタブで開く場合は`rel="noopener"`を付ける
6. 「応募済み」「外部提出済み」「受賞済み」「交通が充足」「GTFSが存在しない」と断定しない

### 18.5 完了条件

1. §18.2の固定内容と8項目を`docs/entry.html`で確認できる
2. 100字概要が100字以内で、作品名・タイプ・テーマ・使用データが公式フォーム項目へ転記可能である
3. 公開トップ、状況ページ、応募説明ページ間の相互導線と、デモ・コード・主要JSONのリンクが実在する
4. 追加契約テストを含む全unittest、旧版網羅、`git diff --check`、秘密情報検査が合格する
5. 受入済みJSONとGTFS ZIPのSHA256がRELEASE-1受入時から不変である
6. 1440×1600・390×844の実ブラウザでHTTP 200、横overflow 0、console error、
   `Runtime.exceptionThrown`、Log error、`Network.loadingFailed`が全て0である
7. commitが`origin/main`へpushされ、GitHub Pagesのbuild・deploy成功後、公開`entry.html`を読み戻す
8. README、状況ページ、PROGRESS、verification、run_recordを「応募用公開物一式は作品①について受入済み」へ同期する
9. UDC概要フォーム、作品本応募、BODIK APPsへの外部提出を行っていない
10. Chat一覧が利用できなくても、`run_record.md` §4の再開点から現在地と次の承認ゲートを復元できる

---

## 19. WORK1-FRESHNESS-1 実行仕様 — 受入済み原本の変更検知と継続確認

### 19.1 段階ゴールと利用者成果

作品①の公開値は、2026年8月に取得・検証した4登録簿PDFと2 GTFS ZIPを基準にしている。
公開後に公式原本が更新されても、現在の画面だけでは、受入済み値が引き続き同じ原本に基づくのか、
再検証が必要になったのかを継続的に判別できない。

WORK1-FRESHNESS-1では、公開中の値を自動更新せず、受入済み原本と現在の公式配布ファイルを比較して、
不変・変更・到達不能を区別する。県・市町の交通担当者、交通事業者、UDC審査員が、固定した取得日だけで
なく、原本変更を継続確認する仕組みと検査履歴へ到達できる状態までを、この段階の成果とする。

### 19.2 監視対象

認証不要で取得済みの次の6原本だけを必須対象とする。

| 種別 | ローカル原本 | 公式配布URL | 基準SHA256 |
|---|---|---|---|
| 登録簿PDF | `raw/000230003.pdf` | `https://wwwtb.mlit.go.jp/chugoku/content/000230003.pdf` | `d215cada64ce8d174ec22cb521379734da185b7616f29b27166dfccf701af1ab` |
| 登録簿PDF | `raw/000268896.pdf` | `https://wwwtb.mlit.go.jp/chugoku/content/000268896.pdf` | `ece80864a99fc392dec0c279fe92f3609a43c11ce32178ced0cd60839c9b6177` |
| 登録簿PDF | `raw/000271730.pdf` | `https://wwwtb.mlit.go.jp/chugoku/content/000271730.pdf` | `86b3e32f3d3017cba613d644102f262a0a0cb2761925718ce9eea12e71a9add1` |
| 登録簿PDF | `raw/000359215.pdf` | `https://wwwtb.mlit.go.jp/chugoku/content/000359215.pdf` | `3107e8555aefe86527d482422977c050b4514cb06b0f077187cdec4f38a2c66b` |
| GTFS ZIP | `raw/gtfs/iwakuni_gtfsjp_20260401.zip` | `https://yamaguchi-opendata.jp/ckan/dataset/2dbaeb43-5134-4880-90a3-62870504f1d3/resource/bac76226-a946-466f-a94c-d61dcb6ab0dc/download/gtfs-jp2026-03-27_1458_.zip` | `d236a58ff4a0edb4812a8bed543d4897670441164a1019e88d5e35ded5052de2` |
| GTFS ZIP | `raw/gtfs/hikari_gtfs_20260401.zip` | `https://yamaguchi-opendata.jp/ckan/dataset/db885818-b1bd-4848-986f-45119e8acb31/resource/c804039c-7d37-4e45-9288-f09fc1bbd249/download/hikari_gtfs_20260401_.zip` | `f3403ebaf481805fff0e2316be3a986732f443a06a64eab5b579ea17191adde7` |

船木鉄道は認証が必要で原本未取得のため対象外とする。未確認14市町を不存在へ変換しない。
公式ページの掲載日、GTFS内部有効期間、検査実行日時は別項目のまま保持し、単一の鮮度点数へ統合しない。

### 19.3 変更可能ファイルと書き手

チャット側だけが変更するもの:

- `SPEC.md`
- `run_record.md`

実装側が変更できるもの:

- `data/source_freshness_manifest.json`
- `src/check_source_freshness.py`
- `tests/test_source_freshness.py`
- `.github/workflows/source-freshness.yml`
- `docs/status.html`
- `README.md`
- `PROGRESS.md`
- `verification.md`
- `evidence/20260811_work1_freshness_*`

受入済み`raw/`、`data/operators.*`、`data/vehicles.*`、`data/gtfs_supply_metrics.json`、
`docs/data/`、GTFS計算・検査コード、公開トップ・応募説明ページは変更しない。

### 19.4 マニフェストと判定契約

`data/source_freshness_manifest.json`は1原本1要素とし、少なくとも次を持つ。

- `source_id`、`source_type`
- `official_url`、`local_path`
- `baseline_sha256`、`baseline_bytes`、`baseline_acquired_at`
- `max_download_bytes`

検査はHTTPSの許可済みホストだけへGETし、応答を一時メモリまたは一時ファイルでストリーム処理する。
`raw/`と受入済みデータへ書き込まない。認証情報、Cookie、個人情報を使用・保存しない。

原本単位の状態は次に限定する。

- `unchanged`: 取得成功かつSHA256が基準値と一致
- `changed`: 取得成功かつSHA256が基準値と不一致
- `unavailable`: DNS・TLS・タイムアウト・HTTPエラー等で取得不能
- `oversize`: 宣言上限を超え、安全に取得を継続できない
- `invalid_baseline`: マニフェストまたはローカル基準原本が契約に不一致

全件`unchanged`だけを終了コード0とする。`changed`を1、外部取得問題を2、基準異常を3として区別する。
変更時は新原本を採用せず、別段階で安全検査・構造検査・再計算・表示差分を受け入れるまで現行値を保持する。

### 19.5 継続実行と公開表示

GitHub Actionsは週1回と手動実行を提供し、権限を`contents: read`に限定する。結果JSONを実行artifactへ
保存し、原本変更・取得不能・基準異常ではworkflowを失敗させる。commit、issue作成、Pages更新、
原本・派生データの自動更新は行わない。

`docs/status.html`とREADMEは、検査の目的、対象6原本、状態語彙、最終受入時の実測結果、Actions履歴への
導線を示す。固定HTMLを「現在も不変」と断定せず、最新状態はActions履歴で確認するよう明記する。

### 19.6 自動テストと証拠

ネットワークを使わないテストで、少なくとも次を検証する。

1. マニフェスト6件がローカル6原本とファイルサイズ・SHA256まで一致する
2. URLがHTTPSかつ許可ホストで、パストラバーサルと重複IDがない
3. モック応答で`unchanged`、`changed`、`unavailable`、`oversize`を区別する
4. 変更・取得不能時も`raw/`と受入済みデータを変更しない
5. 終了コード0・1・2・3の優先順位が判定契約と一致する
6. workflowが週1回・手動実行・`contents: read`だけで、自動commit・issue・Pages更新を含まない
7. 状況ページとREADMEが固定確認日と最新Actions状態を混同しない

実ネットワーク検査の生JSON、要約、6原本の作業前後SHA256、全検証出力を
`evidence/20260811_work1_freshness_*`へ保存する。レスポンス本文と認証情報は証拠へ保存しない。

### 19.7 段階完了条件

1. §19.2の6原本を、受入済み値の自動更新なしで再検査できる
2. 初回実ネットワーク検査で6件の個別状態と終了コードを証拠化する
3. 定期・手動workflowが最小権限で実在し、実行履歴へ公開状況ページから到達できる
4. 追加テストを含む全unittest、旧版網羅、`git diff --check`、秘密情報検査が合格する
5. 受入済み6原本・公開JSON・既存計算値のSHA256が作業前後で不変である
6. 1440×1600・390×844の実ブラウザで既存表示と新しい継続確認説明を読み取れ、横overflowと4種のエラーが0である
7. Codexが実ファイル、全差分、検査結果、workflow、公開表示を独立に読み戻す
8. commit・push・GitHub上のworkflow初回実行・Pages反映は、適用前に対象と影響を示した承認ゲートを通す
9. UDC概要フォーム、本応募、BODIK APPs登録、作品②、次の作品強化段階へ進んでいない
10. 受入後に次の受賞寄与段階を一つだけ定義して停止する

### 19.8 停止条件

- 公式配布に認証、利用条件交渉、課金、アクセス回避が必要になった
- 6原本のいずれかが`changed`または`invalid_baseline`となり、現在の公開値の根拠が変わった
- 取得先が許可ホスト外またはHTTPS以外へ遷移した
- 実装が原本・派生値を自動更新しようとした
- 既存の公開値、日付の区別、非断定注記に退行が生じた

## 20. WORK1-AWARD-COMPARISON-1 実行仕様 — UDC2026受賞準備スコアカード

### 20.1 段階ゴールと利用者成果

作品①の公開成果だけを、UDC2026公式の一次審査基準と2026年特別賞条件へ対応づける。
比較のために他作品のパス、URL、ファイル、Git履歴、公開物を参照せず、作品①と同じ形式を別Chatでも
独立に使えるようにする。利用者は、点数だけでなく、点数を支える公開証拠、確信度、不足証拠、
次に効く改善上位3件を一ページで確認できる。

### 20.2 公式根拠と分類

公式根拠は2026-06-20更新のUDC2026作品募集要領
`https://urbandata-challenge.jp/udc2026_entry`とし、2026-08-12に読み戻す。

- 作品タイプ: `アプリケーション`
- 作品テーマ: `道路・交通`
- 重点分野: `true`。公式ページの赤字表示と「重点分野の作品に対する作品審査の優遇措置」を根拠とする
- 一次審査基準: `実用度`、`完成度`、`挑戦度`
- 公式数値配点: 非公開。内部比較点を公式点、受賞確率、順位予測として表示しない
- 現行特別賞: `GTFS賞`、`BODIK賞`、`データキッズ（DATAKIDS）賞`

過年度計画にあったJACIC賞は2026年公式要項に掲載がないため現行候補へ含めない。

### 20.3 共通採点規則

内部比較は各観点0.0〜5.0点、0.5点刻みとし、次の共通アンカーを使う。

| 点 | 共通アンカー |
|---:|---|
| 0 | 目的または成果が確認できず、評価根拠がない |
| 1 | 意図・構想だけで、利用可能な成果や実施証拠がほぼない |
| 2 | 部分的な試作・成果はあるが、主要目的との接続または検証が弱い |
| 3 | 主要用途で利用できるが、範囲・外部検証・継続性等に重要な不足がある |
| 4 | 主要用途を公開物で達成し、第三者が根拠を追跡できる。限定的な不足が残る |
| 5 | 目的、実装、外部利用・効果、網羅性、継続性が強い証拠でそろい、重大な不足がない |

各観点は次の下位項目を同じ重みで平均し、最も近い0.5点へ丸める。

- 実用度: `problem_value_clarity`、`method_fit`、`user_value_evidence`
- 完成度: `public_implementation`、`reproducibility_verifiability`、`scope_reliability`
- 挑戦度: `novelty`、`output_comprehensiveness`、`continuity`、`actor_diversity`

総合比較指数は3観点を同じ重みで平均し、`score / 5 × 100`で0〜100へ換算する。
公式配点が公開されていないため、`official_weight`は常に`null`、内部の`comparison_weight`は各観点
`1/3`とする。総合指数は比較用の診断値であり、UDC事務局の採点や受賞確率ではない。

### 20.4 証拠と確信度

各下位項目と各観点は、少なくとも次を持つ。

- `score`、`rationale`
- `evidence`。第三者が到達できるHTTPS URLと、そのURLが支える主張を分離する
- `missing_evidence`。未確認事項を不存在や失敗へ変換しない
- `confidence`: `high`、`medium`、`low`
- `as_of`: 根拠を読み戻した日

`high`は公開成果と自動検証等の直接証拠がそろう場合、`medium`は公開成果はあるが外部利用・効果等を
作品側の説明に依存する場合、`low`は意図・推測・未公開情報が中心の場合とする。証拠URLがない点数、
限界を消した主張、他作品から取得した情報を使う点数は無効とする。

### 20.5 特別賞の判定

特別賞は一次審査3観点と加算せず、`eligible`、`condition_unmet`、`not_claimed`、`not_listed_2026`で
別表示する。

- GTFS賞: 作品①は公式GTFS / GTFS-JPを使用した公開成果があるため`eligible`
- BODIK賞: BODIK APPsへの本年度登録を行っていないため`condition_unmet`。登録と応募文での明記は別の承認ゲート
- DATAKIDS賞: 子どものデータ利活用・学習に資する証拠が作品①にないため`not_claimed`
- JACIC賞: 2026年公式要項に掲載がないため`not_listed_2026`

### 20.6 成果物と変更範囲

次を作成・更新できる。

- `data/award_scorecard_schema.json`
- `data/work1_award_scorecard.json`
- `docs/data/award_scorecard_schema.json`
- `docs/data/work1_award_scorecard.json`
- `docs/award-comparison.html`
- `tests/test_award_comparison.py`
- `docs/entry.html`、`docs/status.html`、`README.md`、`WORK_SCOPE.md`
- `SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`
- `evidence/20260812_work1_award_comparison_*`

受入済み`raw/`、既存`data/*.csv`・`data/*.json`（上記2新規を除く）、`docs/index.html`、既存の
`docs/data/*`（上記2新規を除く）、処理・計算コード、既存テスト、scope lockの境界ファイルは変更しない。

### 20.7 公開ページ契約

`docs/award-comparison.html`はJSONを唯一の点数入力とし、次を初見の読者がPC・スマートフォンで読める。

1. この点数が公式採点・順位・受賞確率ではないこと
2. 作品タイプ、テーマ、重点分野と公式要項へのリンク
3. 実用度・完成度・挑戦度の点数、確信度、根拠、公開証拠、不足証拠
4. 総合比較指数と同一尺度の説明
5. 特別賞4候補の現行状態と未充足条件
6. 期待改善幅、労力、外部依存を含む改善上位3件
7. 作品①以外を入力にしておらず、比較・注力判断は人が行うこと
8. 公開デモ、応募説明、状況ページ、コード、機械可読JSONへの導線

### 20.8 自動テストと証拠

ネットワークを使わないテストで、少なくとも次を検証する。

1. schemaと作品①JSONが構造、enum、点数刻み、平均・丸め、総合指数で一致する
2. 3観点・10下位項目すべてに根拠、公開URL、確信度、不足証拠、確認日がある
3. 公式配点を`null`とし、比較重みを3観点同率にする
4. 特別賞状態と2026公式条件が§20.5に一致する
5. `data/`と`docs/data/`の2 JSONがそれぞれバイト一致する
6. 公開ページが点数をハードコードせず、安全なDOM APIでJSONから表示する
7. 公開ページ、応募説明、状況ページ、README、WORK_SCOPE.mdに相互導線と非断定注記がある
8. 作品①以外のパス・URL・ファイル・得点を成果物へ含めない
9. 既存全unittest、旧版網羅、`git diff --check`、秘密情報検査、保護対象SHA256が合格する

PC 1440×1600とスマートフォン390×844で、必須情報、横overflow 0、console error 0を確認し、
生結果、要約、スクリーンショットを証拠化する。

### 20.9 段階完了条件

1. §20.2の公式根拠・分類・重点分野・特別賞条件が一次情報で確認済み
2. §20.3〜§20.5の共通形式を、他作品の内容なしで再利用できる
3. 作品①だけの点数・確信度・証拠・不足証拠と改善上位3件が公開される
4. GTFS賞の該当根拠、BODIK賞の未充足条件、DATAKIDS賞の非主張、JACIC賞の2026非掲載が区別される
5. 全テスト、旧版網羅、差分、秘密情報、保護対象不変、PC・スマホ受入が合格する
6. commit・`main`へのpush・GitHub Pages反映と公開URL読戻しが完了する
7. 固定HEAD、公開URL、再現手順、残課題を記録し、比較準備版を凍結する
8. 作品②の探索・参照・採点・変更、注力作品の決定、UDC応募、BODIK登録、機能追加を行っていない
9. 独立監査の対象HEADと開始条件だけを定義し、監査は次段階として停止する

### 20.10 停止条件

- 作品①以外のパス、URL、ファイル、Git履歴、公開物を入力にする必要が生じた

## 22. WORK1-GTFS-COVERAGE-2 公式範囲再確認

### 22.1 段階ゴールと利用者成果

2026-08-12時点の山口県オープンデータカタログ、ODPT、既知の市町・事業者公式ページを再確認し、
既存のGTFS確認表を一次資料の現在状態へ近づける。新しい公式ZIPが安全に採用できる場合だけ別原本として
受け入れるが、本段階で新規採用候補は0件だった。代わりに、受入済み光市GTFSの路線7
`広域生活交通`に周南市内停留所が含まれる事実を、GTFS本体と国土地理院の逆ジオコーダーで検証し、
周南市の交通協議前確認メモから同じ関連フィードを確認できるようにする。

利用者は、周南市について「周南市が公開主体」または「市内交通を網羅」と誤認せず、光市が公式配布する
フィードに周南市内停留所が収録されていること、そのフィード全体の実測値、公式ページ、確認日、限界を
一つの公開メモで追跡できる。

### 22.2 一次資料と判定単位

- 山口県オープンデータカタログのCKAN APIを全件検索し、GTFS、標準的なバス情報フォーマット、
  バス、および未確認14市町の交通関連パッケージを確認する
- ODPTカタログで山口県関連の静的GTFSを確認する
- 既知の市町・事業者公式ページは、到達可否とGTFS配布への明示的導線だけを確認する
- 受入済み岩国市・光市ZIPは公式配布URLの現在バイトをSHA-256で照合する
- 光市ZIPの路線7に属する乗降停留所をGTFSから抽出し、緯度経度を国土地理院APIで市町コードへ対応させる

外部確認の結果は`unchanged`、`changed`、`unavailable`、`not_found_in_checked_sources`を分離する。
`unavailable`はファイル変更を意味せず、HTML 404はGTFS不存在を意味しない。市町表の
`not_confirmed_in_checked_sources`も不存在・未整備へ読み替えない。

### 22.3 受入主張と表示範囲

1. 2026-08-12の公式カタログ再探索で、新規採用可能な公式GTFS ZIPは0件だった
2. 岩国市・光市の受入済みZIPは公式配布バイトとのSHA-256が一致し`unchanged`だった
3. 光市GTFSの路線7は94乗降停留所IDを持ち、国土地理院逆ジオコード結果は光市63、周南市31だった
4. 周南市側31停留所IDは17の停留所名に対応する。代表例は高水駅、ゆめプラザ熊毛、熊毛中前、三丘温泉である
5. 市町表は周南市を`confirmed`かつ`feed_ids=["hikari-gtfs"]`とし、全体を6確認・13未確認へ更新する
6. 6/19は公式資料で関連フィードを確認できた市町数であり、GTFS整備率、路線網羅率、交通カバー率ではない
7. 実測レコードは岩国・光の2フィードのまま増やさない。周南市では`feed_ids`で光市フィードを関連付け、
   フィード全体値として表示する。周南市内だけの便数・停留所数・事業者数へ分割または推計しない
8. 光市・周南市とも、当該フィードが市内の全事業者・全路線・全公共交通を網羅するとは表示しない
9. `checked_at`は外部資料を再確認した2026-08-12とし、既存のデータ基準日・内部有効期間・実測日と混同しない

### 22.4 証拠契約

- `evidence/20260812_work1_gtfs_coverage_discovery.json`: 公式カタログ検索、公式ページ到達、
  状態別件数、新規採用0件、採否理由
- `evidence/20260812_work1_gtfs_coverage_hikari_route7.json`: 受入済みZIPのハッシュ、路線、
  停留所ID・名称の集計、市町コード別集計、代表逆ジオコード結果
- `evidence/20260812_work1_gtfs_coverage_live_checks.json`: 公式ZIP再取得のHTTP状態、サイズ、SHA-256、
  既存freshness検査との結果差と原本非更新
- `evidence/20260812_work1_gtfs_coverage_*`: テスト、差分、ブラウザ、GitHub、Pages、受入記録

証拠JSONは取得時刻、URL、HTTP状態、検索語または識別子、判定を保持し、秘密情報や認証値を含めない。
ライブAPIへ依存する検証は証拠化時に実行し、通常のunittestは受入済みZIPと固定証拠だけで再現する。

### 22.5 変更範囲

変更・追加できるのは次だけとする。

- `data/gtfs_feeds.csv`、`data/gtfs_feeds.json`、`data/municipality_gtfs.csv`、`data/municipality_gtfs.json`
- `data/gtfs_supply_metrics.json`
- `docs/data/gtfs_feeds.json`、`docs/data/municipality_gtfs.json`、`docs/data/gtfs_supply_metrics.json`
- `src/build_gtfs_status.py`、`src/calculate_gtfs_supply_metrics.py`、`src/build_site_data.py`
- `docs/municipality-memo.html`、`docs/index.html`、`docs/entry.html`、`docs/status.html`
- `tests/test_site.py`、`tests/test_task_memo.py`、`tests/test_gtfs_supply_metrics.py`、
  `tests/test_gtfs_coverage.py`
- `README.md`、`WORK_SCOPE.md`、`SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`
- `evidence/20260812_work1_gtfs_coverage_*`

`raw/`の4 PDFと`raw/gtfs/`の2 ZIP、登録供給JSON、比較スコアカードJSON、scope lock境界、workflowは
変更しない。新しいZIP、認証付きフィード、手動作成の推計値を採用しない。公開トップと応募説明ページは
現在値の同期・導線修正だけを許可し、新機能や採点変更を同時に行わない。

### 22.6 自動テストと実画面受入

ネットワークを使わないテストで、少なくとも次を検証する。

1. 受入済み光市ZIPのサイズ・SHA-256と安全なZIP構造が固定値に一致する
2. 路線7、94停留所ID、証拠に記録した周南市17停留所名、代表座標がGTFS本体に存在する
3. 19市町が一対一で、周南市が光市フィードへ関連し、確認6・未確認13となる
4. `data/`と`docs/data/`の3 JSONがそれぞれ同一バイトである
5. 実測レコードは2件のままで、周南市のメモがfeed ID関連付けにより光市フィード全体値を表示する
6. 周南市内だけの数値、整備率、網羅率、交通カバー率、GTFS不存在を断定する表現がない
7. 既存全unittest、rev.5.9旧版網羅、`git diff --check`、秘密情報、保護対象不変が合格する

実画面は少なくとも周南市、光市、未確認市町の3分岐を、PC 1440×1600、スマートフォン390×844、
印刷媒体で確認する。周南市で関連フィード、2フィード全体値の一方、根拠、確認日、限界が読めること、
横overflow 0、ページ内runtime error 0、公開トップとの往復を受け入れる。

### 22.7 段階完了条件

1. 公式カタログ14市町分、ODPT、既知公式ページの再探索結果を状態別に証拠化する
2. 新規採用0件と、受入済み2 ZIPの`unchanged`を第三者がURL・サイズ・SHA-256から再現できる
3. 光市GTFS路線7の周南市内収録範囲を、GTFS本体・国土地理院結果・オフラインテストで検証できる
4. 市町表6/19、未確認13/19、関連実測表示3市町・実測2フィードの単位がデータと画面で一致する
5. 原本、登録供給値、既存実測値、比較スコアカード、scope lock、作品②を変更していない
6. 全テスト、旧版網羅、差分、秘密情報、PC・スマホ・印刷の検証が合格する
7. commit、`main`へのpush、scope lock、GitHub Pages、公開URL読戻しが完了する
8. 固定HEAD、公開URL、再現手順、既知の取得不能・未確認13市町を記録する
9. 外部利用者への連絡、UDC概要フォーム、本応募、BODIK登録、作品②、再採点を行っていない
10. 次の受賞寄与段階を一つだけ定義し、独立監査開始直前で停止する

### 22.8 停止条件

- scope guardがrepo、origin、候補パスを拒否した
- 公式配布バイトが受入済みSHA-256と異なり、安全検査・構造検査・再計算の別受入が必要になった
- 認証情報、利用条件が確認できない新規ZIP、または原本の置換が必要になった
- 国土地理院結果とGTFS座標が矛盾し、周南市への関連付けを再現できなかった
- 周南市内だけの供給値、整備率、順位、網羅率を生成しないと表示できない設計になった
- 作品①以外のパス、URL、ファイル、Git履歴、公開物を入力にする必要が生じた
- 公式要項の根拠または2026年の賞条件を一次情報で確認できない
- 公開証拠のない点数、限界を消した主張、公式配点と誤認させる表示になる
- scope guardがrepo、origin、候補パスのいずれかを拒否した
- 既存の原本、公開値、計算値、公開アプリの機能へ変更が必要になった

---

## 21. WORK1-TASK-MEMO-1 実行仕様 — 交通協議前の市町別確認メモ

### 21.1 段階ゴールと利用者成果

県・市町の交通担当者または交通事業者が、公開トップで選んだ市町について、会議前の確認事項を
一つの公開メモとして読み、URL共有または印刷できるようにする。メモは判断そのものを代行せず、
登録簿で確認できた供給、公式GTFS / GTFS-JPの確認状況、根拠日、限界、次に確認すべき事項を
分離する。19市町すべてを同じ契約で扱い、0件・未確認を不存在へ読み替えない。

### 21.2 入力と真実の所在

入力は受入済み公開JSONの次の4件だけとし、値を複製した新しい派生JSONは作らない。

- `docs/data/municipal_supply.json`: 登録供給、運送種別内訳、団体、原本URL、登録有効期限
- `docs/data/municipality_gtfs.json`: 19市町の`confirmed` / `not_confirmed_in_checked_sources`、確認日、限界
- `docs/data/gtfs_feeds.json`: 公式データセット、公式ページ、基準日、アクセス状態、フィード範囲
- `docs/data/gtfs_supply_metrics.json`: 岩国市・光市関連2フィードだけの実測値と比較週

原本6件、`data/`と既存`docs/data/`、計算コード、既存の公開値は変更しない。メモ表示は入力JSONを
読み直して生成し、登録簿・GTFS・表示日の概念を混同しない。

### 21.3 公開URLと画面契約

公開URLは`docs/municipality-memo.html?municipality=<市町名>`とする。クエリ値は入力JSONに存在する
19市町との完全一致だけを受け入れ、不正・未知の値はデータ先頭の市町へ安全に戻す。公開トップの
市町別結果から現在の市町を含むメモへ移動でき、メモ側でも市町を変更できる。

画面は初見の読者がPC・スマートフォン・印刷で次を確認できる。

1. 市町名、用途、登録簿集計確認日、GTFS確認日
2. 登録団体数、登録車両数、軽自動車内数、運送種別内訳
3. 登録団体ごとの登録番号、団体名、運送種別、有効期限、車両数、原本ページへのHTTPS導線
4. GTFS状態を`公式資料で確認`または`今回確認した公式資料では未確認`として表示
5. GTFSの確認範囲、公式データセット、アクセス状態、基準日を登録簿と分離
6. 岩国市・光市だけは関連フィードの実測値・比較週を、フィード全体の値である限界とともに表示
7. 登録簿0件、GTFS未確認、GTFS実測なしを、それぞれ不存在・未整備・失敗と断定しない注記
8. 次の確認事項を、現在の状態から決まる具体的なチェックリストとして表示
9. 選択市町を含むURLのコピー、公開トップへ戻る導線、ブラウザ印刷ボタン
10. JavaScriptまたはJSON読込失敗時のエラー表示と、公開JSONへの代替導線

### 21.4 次の確認事項の決定規則

チェックリストは入力状態だけから決定し、行政判断や交通評価を生成しない。

- 全市町: 原本ページで登録番号・有効期限・運送区域を再確認する
- 登録団体0件: 他制度・民間交通・非公開情報を含む供給不存在を意味しないと明記し、追加資料を確認する
- 登録団体あり: 有効期限と複数市町重複を確認し、市町値を県計へ合算しない
- GTFS `confirmed`: 公式ページで配布状態と対象範囲を再確認する
- GTFS `not_confirmed_in_checked_sources`: 公式カタログと事業者ページを再確認し、不存在を断定しない
- 実測あり: 比較週・基準日・フィード全体という単位を確認してから協議資料へ使う
- 実測なし: 認証・取得・安全検査を別段階で受け入れるまで数値を補完しない

### 21.5 禁止表現と非機能要件

- 交通が便利・不便、足りる・不足、全事業者・全路線を網羅、GTFSなし、未整備、順位・優劣を断定しない
- 予定運行を実績、利用者数、リアルタイム運行、経路検索、行政の公式見解として表示しない
- URLクエリ、JSON文字列を`innerHTML`、`document.write`、`eval`へ渡さず、DOM APIと`textContent`を使う
- 外部リンクは`https:`だけを許可し、`target="_blank"`時は`rel="noopener noreferrer"`を付ける
- `@media print`で操作部品・ナビゲーションを隠し、本文、出典、限界、チェックリストを印刷対象にする
- 390×844で横overflowを発生させず、キーボード操作、見出し順、状態通知を維持する
- URLコピー失敗はメモ表示を壊さず、住所欄のURLを手動コピーできるようにする

### 21.6 変更範囲

変更・追加できるのは次だけとする。

- `docs/municipality-memo.html`
- `docs/index.html`、`docs/entry.html`、`docs/status.html`
- `tests/test_task_memo.py`
- `README.md`、`WORK_SCOPE.md`
- `SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`
- `evidence/20260812_work1_task_memo_*`

`raw/`、既存`data/`、既存`docs/data/`、処理・計算コード、既存テスト、scope lock境界ファイル、
比較スコアカードJSONは変更しない。スコアカードは2026-08-12時点の比較スナップショットとして保持し、
再採点は別段階で行う。

### 21.7 自動テストと実画面受入

ネットワークを使わない集中テストで、少なくとも次を検証する。

1. 4 JSONの構造と19市町の一対一対応を入力として検証する
2. 市町クエリの完全一致、未知値の安全な既定値、URL更新を検証する
3. §21.3の表示枠、4 JSON読込、トップからの市町付き導線、印刷CSSを検証する
4. 数値・市町名をHTMLへ直書きせず、JSONから安全なDOM APIで表示する
5. `confirmed` / `not_confirmed_in_checked_sources`、0件、実測有無の分岐と§21.4の文言を検証する
6. 禁止断定、HTTP外部リンク、`innerHTML`、`document.write`、`eval`がないことを検証する
7. JSON読込・URLコピーの失敗がメモ本文と公開JSON導線を壊さないことを検証する
8. 既存全unittest、rev.5.8旧版網羅、`git diff --check`、秘密情報、保護対象不変を検証する

実画面は少なくとも下関市（登録あり・GTFS未確認）、宇部市（登録0件・GTFS確認・実測なし）、
岩国市（登録あり・GTFS確認・実測あり）を確認する。PC 1440×1600、スマートフォン390×844、
印刷媒体で必須項目、分岐、リンク、横overflow 0、ページ内runtime error 0を受け入れる。

### 21.8 段階完了条件

1. 19市町の確認メモを既存4 JSONだけから生成できる
2. 公開トップとメモを市町付きURLで往復でき、URLコピーと印刷が機能する
3. 登録供給、GTFS状態、実測有無の3代表分岐が§21.3〜§21.5を満たす
4. 原本・既存JSON・計算値・比較スコアカードを変更していない
5. 全テスト、旧版網羅、差分、秘密情報、PC・スマホ・印刷の検証が合格する
6. commit、`main`へのpush、scope lock、GitHub Pages、公開URL読戻しが完了する
7. 固定HEAD、公開URL、再現手順、残課題を記録する
8. 作品②、外部利用者への連絡、UDC応募、BODIK登録、再採点を行っていない
9. 次の受賞寄与段階を一つだけ定義し、独立監査開始直前で停止する

### 21.9 停止条件

- scope guardがrepo、origin、候補パスを拒否した
- 既存公開値の修正、新しい原本取得、認証情報、外部連絡が必要になった
- 市町値の0またはGTFS未確認を不存在・不足・未整備へ読み替える必要が生じた
- 作品①以外のパス、URL、ファイル、Git履歴、公開物を入力にする必要が生じた

---

## 23. WORK1-AWARD-COMPARISON-2 実行仕様 — 現行公開証拠による再評価

### 23.1 段階ゴール

受け入れ済みの交通協議前確認メモ、GTFS公式範囲再確認、固定HEAD
`15cfe4b4ef7418793346ef1040d3c3d6d0056f51`に対する独立監査GOを、作品①だけの公開証拠として
比較スコアカードへ反映する。点数上昇を目的にせず、増えた証拠、変わらない限界、外部の人間承認が
必要な課題、自力で進められる課題を区別し、次の受賞寄与段階を選べる状態にする。

### 23.2 公式基準と内部比較値

- 2026年公式要項の一次情報を実行日に再確認し、実用度、完成度、挑戦度の定義、道路・交通の
  重点分野、GTFS賞条件を根拠として保持する。
- 公式要項が数値配点を公開していないため、0〜5の下位評価と0〜100の総合値は§20と同じ
  等重みの内部比較値とする。公式点、順位、受賞確率とは表示しない。
- 新しい公開証拠が既存の弱点を直接解消した場合だけ当該下位評価を変更する。関係市町数の増加を
  新規GTFSフィード、交通の良否、利用者価値の実証へ読み替えない。

### 23.3 再評価規則

1. `problem_value_clarity`は、用途説明が増えても関連利用者による価値検証がないため据え置く。
2. `method_fit`は、市町選択から共有・印刷可能な確認メモへ至る実装が公開されたため4.0から4.5へ更新する。
3. `public_implementation`は、公開メモ追加を認めるが既に高水準のため4.5で据え置く。
4. GTFS関連確認は5/19から6/19、未確認は14/19から13/19へ更新する。関連実測表示3市町、
   実測2フィードという単位を必ず併記し、`scope_reliability`と`output_comprehensiveness`は据え置く。
5. 関係範囲の再現性は増えたが、新規採用フィード0件、外部利用者評価0件、主体多様性の不足が残るため、
   3基準の表示値を実用度3.5、完成度4.0、挑戦度3.0、総合70.0で据え置く。
6. 根拠URL、取得不能、未確認、推計していない値を削らず、確信度と残課題を証拠の強さに合わせる。

### 23.4 改善候補の分離

公開する上位改善候補は次の3件とする。

1. 関連利用者3名以上のオンラインタスク評価。受賞寄与は大きいが、参加者への連絡と同意取得は
   別の人間承認ゲートであり、この段階では実行しない。
2. 国内の類似サービス・作品との体系的比較。公式・公開資料だけで差分と非主張を証拠化できるため、
   作品①だけで自力実行可能な次段階候補とする。
3. 第三者手順を想定したゼロからの再生成・復旧訓練。再現手順、生成物比較、失敗時復旧を
   公開証拠化できるため、自力実行可能な次段階候補とする。

### 23.5 公開成果物と継続改善ロードマップ

- `data/work1_award_scorecard.json`と`docs/data/work1_award_scorecard.json`を同一内容で更新する。
- `docs/award-comparison.html`に前回比較から増えた証拠、据え置き理由、外部承認ゲートと自力実行候補を表示する。
- `docs/status.html`の有限な「9 / 10・次は外部提出」を廃止し、受け入れ済み公開基盤の上で
  比較、監査、改善を反復する継続モデルにする。外部提出は残り1工程ではなく独立承認ゲートとして表示する。
- `README.md`、`WORK_SCOPE.md`、`PROGRESS.md`、`verification.md`、`run_record.md`を同じ現在地へ同期する。
- 原本、受入済みGTFS ZIP、既存供給値・実測値、公開トップ、応募説明ページは変更しない。

### 23.6 変更範囲

変更・追加できるのは次だけとする。

- `data/work1_award_scorecard.json`
- `docs/data/work1_award_scorecard.json`
- `docs/award-comparison.html`、`docs/status.html`
- `tests/test_award_comparison.py`
- `README.md`、`WORK_SCOPE.md`
- `SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`
- `evidence/20260812_work1_award_comparison2_*`

### 23.7 検証と段階完了条件

1. スキーマ検証、data/docs同一性、下位評価からの再計算が合格し、総合70.0の据え置きを再現できる。
2. 6/19、13/19、関連実測表示3市町、実測2フィード、外部利用者評価0件が画面・JSON・説明で一致する。
3. 前回から増えた証拠、点数を上げない理由、人間承認ゲート、自力実行可能な2候補を公開画面で区別できる。
4. 状況ページに有限な完了数や「外部提出が唯一の残工程」という誤解がなく、継続改善と独立監査を示す。
5. 全テスト、旧版網羅、`git diff --check`、秘密情報、保護対象不変、PC 1440px、スマートフォン390px、
   runtime error、横overflowを検証する。
6. commit、`main`へのpush、scope lock、GitHub Pages、公開URL読戻しが完了する。
7. 作品②入力、外部参加者への連絡、UDC応募、BODIK登録、公式点・順位・受賞確率の主張は0件である。
8. 次の一作業をread-onlyの`WORK1-AWARD-COMPARISON-AUDIT-2`だけに定義し、開始直前で停止する。

### 23.8 停止条件

- 公式基準に未公表の配点を補う、または公開証拠のない得点上昇が必要になった
- 関係市町を新規フィード、交通品質、整備率、網羅率へ読み替える必要が生じた
- 参加者への連絡、同意取得、外部アカウント操作、応募・登録が必要になった
- 原本、ZIP、既存公開値、公開トップ、応募説明ページ、作品①のscope lock境界を変更する必要が生じた
- 作品①以外のパス、URL、ファイル、Git履歴、公開物を入力にする必要が生じた

## 24. WORK1-AWARD-COMPARISON-TRACEABILITY-1 実行仕様 — 公式用語・根拠追跡

### 24.1 段階ゴール

`8452d6cc8892b1f55fa5f652eb5d82cf4a3fcd59`に対する
`WORK1-AWARD-COMPARISON-AUDIT-2`のGO（P0 0件、P1 0件、P2 2件）を受け、作品①の現行比較記録で
使う審査基準名をUDC2026公式表記へ統一し、公開状況ページから比較第1版§20と第2版§23の両方を
追跡できるようにする。受賞準備の数値評価は変更しない。

### 24.2 修正するP2

1. `SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`、公式再確認証拠で残った
   非公式な旧表記を、公式表記「実用度」「挑戦度」へ訂正する。
2. `docs/status.html`の比較根拠を`SPEC.md §20・§23`とし、現行比較の仕様へ直接到達できるようにする。

### 24.3 不変条件

- `data/work1_award_scorecard.json`と`docs/data/work1_award_scorecard.json`のbytes、SHA256、
  実用度3.5、完成度4.0、挑戦度3.0、総合70.0を変えない。
- 4登録簿PDF、2 GTFS ZIP、既存公開JSON、計算値、公開トップ、応募説明ページを変えない。
- 作品②のパス・ファイル・履歴を参照せず、入力件数を0件に保つ。
- 外部参加者への連絡、UDC応募、BODIK登録は独立した人間承認ゲートのまま実行しない。

### 24.4 許可する変更

- `SPEC.md`、`run_record.md`、`PROGRESS.md`、`verification.md`、`WORK_SCOPE.md`、`README.md`
- `docs/status.html`、`tests/test_award_comparison.py`
- `evidence/20260812_work1_award_comparison2_official_recheck.json`
- `evidence/20260812_work1_award_comparison_traceability_*`

### 24.5 検証と段階完了条件

1. 現行比較記録の審査基準名が「実用度・完成度・挑戦度」に一致し、旧表記が0件である。
2. 状況ページが`SPEC.md §20・§23`を根拠として表示する。
3. スコアカード2 JSONのbytesとSHA256が基準HEADと一致し、総合70.0を再現できる。
4. 全150テスト、`git diff --check`、旧版網羅、秘密情報、作品①スコープ、保護対象不変が合格する。
5. PC 1440×1600とスマートフォン390×844で横overflow、runtime error、dev errorが0件である。
6. commit、`main` push、scope lock、Pages、公開URLのcommit bytes一致まで完了する。
7. 次の一作業をread-onlyの`WORK1-AWARD-COMPARISON-TRACEABILITY-AUDIT-1`だけに定義し、開始しない。

### 24.6 停止条件

- 比較スコア、スコアカードJSON、原本、ZIP、既存公開値を変更する必要が生じた
- 作品②、外部参加者への連絡、応募・登録を入力または操作にする必要が生じた
- 公式未公表の配点、順位、受賞確率を主張する必要が生じた
