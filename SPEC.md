# I-1 実行仕様 — 自家用有償旅客運送 登録簿の機械可読化

UDC2026 作品① の最初の実装作業。**このファイルはリポジトリ直下に `SPEC.md` として置き、Claude Code に読ませる。**

このファイルは単体で完結している。他のファイルを参照しなくても、ここに書かれていることだけで作業できる。

**書き手はこのファイルの発行元（チャット側）で、Claude Code は読むだけ。**
仕様に誤りを見つけた場合、Claude Code は勝手に書き換えず、`PROGRESS.md` に報告して止まること。

---

## 0. 目的

中国運輸局が PDF でのみ公表している「自家用有償旅客運送者の登録簿」を構造化し、
市町別の福祉輸送の供給量（団体数・車種別の車両台数）を機械可読なデータとして取り出す。

同種の機械可読データは全国に存在しない。これを作ること自体が成果物になる。

---

## 1. スコープ

**今回やる**: 山口県分 4 ファイル。
**今回やらない**: 鳥取・島根・広島への拡張（山口県が完成し、検証を通ってから着手）。岡山県は事務権限が県知事に移譲済みで別ソースのため対象外。

---

## 1.5 置き場所とリポジトリ構成

ローカルは以下に作る。作品②も同じ階層に並べる。

Windows: `C:\Users\<ユーザー名>\dev\udc2026\`
Mac / Linux: `~/dev/udc2026/`

**OneDrive や iCloud Drive の同期対象フォルダには置かないこと。**
Windows の「ドキュメント」「デスクトップ」は OneDrive にリダイレクトされていることが多く、
同期プロセスが `.git` 内のファイルをロックしてリポジトリを壊す。
ユーザー名に日本語が含まれる場合は `C:\dev\udc2026\` を作る。

```
dev/udc2026/
├─ .venv/                           ← 仮想環境はここに1つだけ。2リポジトリで共有する
├─ run_record.md                    ← 会話で更新される全体の記録。ここに置く
├─ yamaguchi-yusho-data/            ← 作品①（この仕様書の対象）
└─ yamaguchi-highschool-pubcom/     ← 作品②（後日）
```

作品①リポジトリの中身:

```
yamaguchi-yusho-data/
├─ README.md              作品の説明。出典とライセンスを明記
├─ SPEC.md                この仕様書をここにコピーして置く
├─ CLAUDE.md              Claude Code への常設指示（下記）
├─ requirements.txt       `pdfplumber` の1行だけ。**pandas は使わない**
├─ src/
│  ├─ fetch.py            一覧ページと PDF の取得
│  └─ parse.py            第2号様式のパーサ
├─ raw/                   取得した PDF。出典保存のため git に含める
├─ data/                  operators.csv, vehicles.csv, 同 JSON
├─ tests/
│  └─ test_verify.py      合計欄との照合を自動テストとして書く
├─ verification.md        検証結果（§5 の 1〜5 の実行結果）
└─ docs/                  GitHub Pages の公開ルート
   └─ index.html
```

GitHub の Settings → Pages → Source を「main ブランチの /docs フォルダ」にする。
ビルド不要。`docs/index.html` を置けばそのまま公開される。

`CLAUDE.md` には最低限これを書いておく:

```
このリポジトリは UDC2026 応募作品①。
SPEC.md が唯一の仕様。SPEC.md に書かれていない仕様を勝手に足さない。
代表者の氏名は抽出も出力もしない。
「動いた」で完了としない。SPEC.md §5 の完了条件を満たし、
verification.md に実行結果を書くまで未完了。
```

### Claude Code に最初に投げる指示

```
SPEC.md を読んで、§2 の「最初にやること」から着手して。
まず中国運輸局の利用規約を確認して控えを取り、
次に一覧ページを cp932 でデコードして、山口県の行と列の対応が
SPEC.md の表と一致するか確認して。
そこまで終わったら止まって、確認結果を報告して。
```

**最初から parse.py を書かせない。**規約確認と一覧ページの照合で一度止める。
ここで表の対応が違っていたら、その先の作業は全部やり直しになる。

---

## 2. 入力

一覧ページ: `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`
**このページは Shift-JIS（cp932）である。**UTF-8 で読むと全て文字化けする。

山口県分の 4 ファイル（`https://wwwtb.mlit.go.jp/chugoku/content/` 配下）:

| 運送種別 | 実施主体 | ファイル |
|---|---|---|
| 交通空白地有償運送 | 市町村営 | `000359215.pdf` |
| 交通空白地有償運送 | NPO等 | `000268896.pdf` |
| 福祉有償運送 | 市町村営 | `000230003.pdf` |
| 福祉有償運送 | NPO等 | `000271730.pdf` |

`000271730.pdf` は内容確認済み。下関市の福祉有償運送を行う NPO 法人・社会福祉法人の記録が入っている。**これを最初の1本として着手すること。**

他3ファイルの種別対応は一覧ページの表の並びから推定したもので、未確認。
**着手前に一覧ページを cp932 でデコードして、行ラベル（県名）と列ラベル（種別・主体）を必ず突き合わせること。**

**最初にやること**: 中国運輸局サイトの利用規約ページを開き、二次利用の条件（出典表示の要否、再配布の可否）を確認して控えを取る。国交省サイトは政府標準利用規約準拠であることが多いが、確認せずに再配布しない。

---

## 3. 出力

`data/` 配下に 2 テーブルを CSV と JSON で出す。1 団体 = 1 行の主テーブルと、車両の明細テーブルに分ける。

### 3.1 `operators.csv`

| 列名 | 内容 | 例 |
|---|---|---|
| `registration_no` | 登録番号 | 中山福第1号 |
| `pref` | 県 | 山口県 |
| `transport_type` | 運送の種別 | 福祉有償運送 |
| `operator_type` | 実施主体 | NPO等 |
| `org_name` | 名称 | 特定非営利活動法人 豆たん |
| `org_address` | 住所 | 下関市古屋町２丁目４－８ |
| `service_area` | 運送の区域（原文のまま） | 下関市全域（離島を除く） |
| `service_area_municipalities` | 区域から抽出した市町名（複数可、`;` 区切り） | 下関市 |
| `registered_date` | 登録年月日（西暦 ISO） | |
| `valid_from` | 更新登録の有効期間の開始（西暦 ISO） | 2024-04-24 |
| `valid_to` | 同 終了（西暦 ISO） | 2027-04-23 |
| `scope_i_physical` … `scope_to_other` | 旅客範囲 イ〜ト の該当フラグ（7列、0/1） | |
| `office_name` | 事務所の名称 | |
| `office_location` | 事務所の位置 | |
| `partner_operator_name` | 事業者協力型の協力事業者名（無ければ空） | |
| `partner_operator_address` | 同 住所 | |
| `vehicles_total` | PDF の「合計」欄の値 | 3 |
| `source_pdf` | 出典ファイル名 | 000271730.pdf |
| `source_page` | 出典ページ番号 | |

旅客範囲の記号対応（PDF 内に凡例あり）:
イ 身体障害者 / ロ 精神障害者 / ハ 知的障害者 / ニ 要介護認定 / ホ 要支援認定 / ヘ 基本チェックリスト / ト その他

### 3.2 `vehicles.csv`

| 列名 | 内容 |
|---|---|
| `registration_no` | operators への外部キー |
| `vehicle_type` | 寝台車 / 車いす車 / 兼用車 / 回転シート車 / セダン等 / バス |
| `ownership` | 所有 / 持込 |
| `is_kei` | 軽自動車なら 1、それ以外 0 |
| `count` | 台数 |

PDF では台数が `2 1 3` のような素の数字列と `( ) (1) ( ) ( ) (1) (2)` のような括弧付きの列で並ぶ。**括弧付きが軽自動車の内数**である（様式の構造上そうなる）。ただしこれは読み取りからの推定なので、**最初の1ファイルで必ず原本と目視照合して確認すること。**推定が外れていたら、この仕様のこの行を書き換えてから先に進む。

### 3.4 実物1件の対応例（これが基準）

`000271730.pdf` の1団体目を実際に読み取ったもの。**パーサはまずこの1件を再現できることを目標にする。**

原本に書かれている内容:

```
登録番号            中山福第１号
名称                特定非営利活動法人 豆たん
代表者の氏名        （記載あり。抽出しない）
住所                下関市古屋町２丁目４－８
路線又は運送の区域  下関市全域（離島を除く）
運送の種別          福祉有償運送
更新登録年月日      令和6年4月24日 ～ 令和9年4月23日
運送する旅客の範囲  イ ニ ホ ヘ
事務所の名称・位置  豆たん介護センター ／ 下関市内日下1027-3
```

同じブロックの車両欄を `extract_text()` にかけると、こう出てくる:

```
2 1 3
( ) (1) ( ) ( ) (1) (2)
( ) ( ) ( ) ( ) ( ) ( )
```

**これがこの作業の核心。**車両欄は「寝台車／車いす車／兼用車／回転シート車／セダン等／バス／合計」の
7列の表だが、**空欄セルはテキスト抽出で消える**ため、残った `2 1 3` がどの列の数字か判別できない。
だから座標ベースの抽出が必須になる。素朴なテキスト抽出では絶対に正しく読めない。

期待される出力（`operators.csv` の1行）:

| 列 | 値 |
|---|---|
| registration_no | 中山福第1号 |
| org_name | 特定非営利活動法人 豆たん |
| org_address | 下関市古屋町２丁目４－８ |
| service_area | 下関市全域（離島を除く） |
| service_area_municipalities | 下関市 |
| transport_type | 福祉有償運送 |
| operator_type | NPO等 |
| valid_from | 2024-04-24 |
| valid_to | 2027-04-23 |
| scope_i_physical / scope_ni_care / scope_ho_support / scope_he_checklist | 1 |
| scope_ro_mental / scope_ha_intellectual / scope_to_other | 0 |
| office_name | 豆たん介護センター |
| office_location | 下関市内日下1027-3 |
| vehicles_total | 3 |

期待される出力（`vehicles.csv`、上記の解釈が正しい場合）:

| registration_no | vehicle_type | ownership | is_kei | count |
|---|---|---|---|---|
| 中山福第1号 | 車いす車 | 所有 | 0 | 1 |
| 中山福第1号 | 車いす車 | 所有 | 1 | 1 |
| 中山福第1号 | セダン等 | 所有 | 1 | 1 |

合計 3 台。`operators.vehicles_total` の 3 と一致する。持込は 0 台。

> **この1件の読み取りには推定が2箇所含まれる。**
> (a) 括弧付きの数字が軽自動車の内数であること、(b) 旅客範囲の記号がイ・ニ・ホ・ヘであること。
> どちらも原本の該当ページを開いて目視で確認し、違っていたらこの節を書き換えてから先に進むこと。
> 推定のまま他の3ファイルに展開しない。

### 3.3 出さないもの

**`代表者の氏名` は抽出も出力もしない。**
登録簿には記載されているが、供給量の分析に不要であり、機械可読化して再配布する必要がない。パース段階で捨てる。

---

## 4. 実装方針

- **`pdfplumber` を使う。** `pypdf` / `PyPDF2` はレイアウトを壊すので不可。
- **pandas を使わない。**標準ライブラリの `csv` と `json` で書く。出力は数千行規模で pandas の利点がなく、
  依存込みで数十MBを占める。依存は `pdfplumber` だけに保つ。
- 対象 PDF はスキャンではなくテキストが埋め込まれている。OCR は不要。
- 様式は道路運送法施行規則第51条の5 の**第2号様式で固定**。1 団体 1 ブロックの繰り返し構造。
  テンプレートを 1 つ作れば全団体に効く。**団体ごとにアドホックな分岐を書き始めたら、それは設計を間違えたサイン。**
- 素朴な `extract_text()` では表のセルが混ざる。`extract_words()` で座標を取り、
  ラベル語（「登録番号」「名 称」「運送の区域」など）の座標を基準に、
  その右側・下側にある語を拾う方式で組む。
- 和暦（令和6年4月24日、平成20年6月23日）を西暦 ISO に変換する。令和・平成・昭和に対応させる。
- 全角数字・全角ハイフンを正規化する。ただし `org_address` と `service_area` は**原文を保持した列も残す**（`_raw` サフィックス）。

---

## 5. 完了条件（これを満たすまで I-1 は未完了）

証拠として提示できるものが揃って初めて完了とする。「動いた」「たぶん合っている」は完了ではない。

1. **団体数の一致** — `000271730.pdf` を人間が目視で数えた団体数と、`operators.csv` の行数が一致する。
2. **車両合計の照合（自動チェック）** — 各団体について `vehicles.csv` の `count` 合計が `operators.vehicles_total`（PDF の「合計」欄）と一致する。**不一致が 1 件でもあれば未完了。**この検算は PDF 側が答えを持っているので、必ず自動テストとして書く。
3. **全件突合** — `000271730.pdf` の全団体について、`registration_no` / `org_name` / `service_area` / `valid_to` の 4 項目を原本と照合し、一致表を出力する。
4. **日付の健全性** — `valid_from < valid_to` が全件で成立。年が 2000 年代前半や 2100 年代になっていない。
5. **代表者氏名が出力に含まれていない**ことを grep で確認する。

上記 1〜5 の実行結果（数字と出力）を `verification.md` に残す。

---

## 6. 完了後にすぐ効く派生物

I-1 が通ったら、次の 2 つは追加コストがほぼゼロで作れる。中間報告で見せる素材になる。

- **有効期間切れが近い団体の一覧** — `valid_to` が 6 か月以内の団体。供給が消えるリスクの可視化。
- **市町別の集計** — `service_area_municipalities` で寄せて、団体数と車いす車・寝台車の台数を市町別に出す。

これが出た時点で「山口県の福祉輸送の供給が、市町別に台数で見える」と言える。5 分の中間報告はこれ一枚で成立する。

---

## 7. 残り 3 ファイルへの展開

`000271730.pdf`（福祉有償運送・NPO等）が完了条件を全て通ってから着手する。
市町村営のファイルは様式は同じだが記載の粒度が違う可能性がある。**最初の 1 件を目視で確認してからパーサを回すこと。**
