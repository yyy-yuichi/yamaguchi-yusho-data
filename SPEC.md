# I-1 実行仕様 — 自家用有償旅客運送 登録簿の機械可読化

**rev.4.1 / 2026-08-09**（rev.1→rev.2 の改訂点は §10.1、rev.2→rev.3 は §10.2、rev.3→rev.4 は §10.3、rev.4→rev.4.1 は §10.4。消した記述はそこに全部書いてある）

UDC2026 作品① の最初の実装作業。**このファイルはリポジトリ直下に `SPEC.md` として置き、Claude Code に読ませる。**

このファイルは I-1 の実装仕様として単体で完結している。プロジェクトのゴール、終了条件、確定方針は
`run_record.md` §0.5 を正とし、このファイルがそれを上書きしてはならない。

**書き手はこのファイルの発行元（チャット側）で、Claude Code は読むだけ。**
仕様に誤りを見つけた場合、Claude Code は勝手に書き換えず、`PROGRESS.md` に報告して止まること。

---

## 0. 目的

中国運輸局が PDF でのみ公表している「自家用有償旅客運送者の登録簿」を構造化し、
山口県の公共交通の「供給側データ」の欠落を埋める作品①に用いる。
作品①は GTFS-JP の整備状況・鮮度、交通空白、福祉輸送（UD タクシー・福祉有償運送）の供給を突き合わせる。
そのうち I-1 は、自家用有償旅客運送の福祉有償運送と交通空白地有償運送について、
市町別の供給量（団体数・車種別の車両台数）を機械可読なデータとして取り出す。

同種の機械可読データは全国に存在しない。これを作ること自体が成果物になる。

---

## 1. スコープ

**今回やる**: 山口県分 4 ファイル。
**今回やらない**: 鳥取・島根・広島への拡張（山口県が完成し、検証を通ってから着手）。岡山県は事務権限が県知事に移譲済みで別ソースのため対象外。

---

## 1.5 置き場所とリポジトリ構成

ローカルは以下に作る。作品②も同じ階層に並べる。

Windows: `C:\Users\<ユーザー名>\dev\udc2026\`
Mac / Linux: `~/dev/udc2026/`

**OneDrive や iCloud Drive の同期対象フォルダには置かないこと。**
Windows の「ドキュメント」「デスクトップ」は OneDrive にリダイレクトされていることが多く、
同期プロセスが `.git` 内のファイルをロックしてリポジトリを壊す。
ユーザー名に日本語が含まれる場合は `C:\dev\udc2026\` を作る。

```
dev/udc2026/
├─ .venv/                           ← 仮想環境はここに1つだけ。2リポジトリで共有する
├─ yamaguchi-yusho-data/            ← 作品①（この仕様書の対象）
└─ yamaguchi-highschool-pubcom/     ← 作品②（後日）
```

作品①リポジトリの中身:

```
yamaguchi-yusho-data/
├─ README.md              作品の説明。出典とライセンスを明記（§8）
├─ SPEC.md                この仕様書をここにコピーして置く
├─ run_record.md          goal-to-done の実行契約。§0.5 がプロジェクトの根拠
├─ CLAUDE.md              Claude Code への常設指示（下記）
├─ .gitignore
├─ requirements.txt       `pdfplumber` の1行だけ。**pandas は使わない**
├─ src/
│  ├─ fetch.py            一覧ページと PDF の取得
│  └─ parse.py            第2号様式のパーサ
├─ raw/
│  ├─ *.pdf               取得した PDF。出典保存のため git に含める
│  └─ text/               ページ単位の抽出テキスト（§4 の最後）
├─ data/                  operators.csv, vehicles.csv, 同 JSON
├─ tests/
│  └─ test_verify.py      合計欄との照合を自動テストとして書く
├─ verification.md        検証結果（§5 の実行結果）
└─ docs/                  GitHub Pages の公開ルート
   └─ index.html
```

GitHub の Settings → Pages → Source を「main ブランチの /docs フォルダ」にする。
ビルド不要。`docs/index.html` を置けばそのまま公開される。

`.gitignore` には最低限これを入れる:

```
.venv/
__pycache__/
.claude/*.lock
```

`.claude/` 自体は意図的にコミットするが、`.claude/scheduled_tasks.lock` はデスクトップアプリが
管理するロックファイルで、コミットしてはいけない。

`CLAUDE.md` には最低限これを書いておく:

```
このリポジトリは UDC2026 応募作品①。
ゴール、終了条件、確定方針は run_record.md §0.5 が正。
SPEC.md は I-1 の実装仕様。SPEC.md に書かれていない実装仕様を勝手に足さない。
代表者の氏名は抽出も出力もしない。
「動いた」で完了としない。SPEC.md §5 の完了条件を満たし、
verification.md に実行結果を書くまで未完了。
```

**CLAUDE.md はシステムプロンプトではなく、遵守は保証されない。**
「代表者氏名を出力しない」のような絶対条件は CLAUDE.md への記載だけに頼らず、
`tests/test_verify.py` の自動テストで担保する（§5-8）。

### 実行環境の制約（この環境では必須）

**素の `python` コマンドを使わないこと。**
0 バイトの Microsoft Store エイリアスに解決され、非対話シェルから呼ぶとハングする。

- `uv`（`C:\Users\user\.local\bin\uv.exe`）経由で呼ぶ、または
- `C:\Users\user\dev\udc2026\.venv\Scripts\python.exe` のフルパスで呼ぶ

`uv python list` も引数なしだと同じ理由でハングするので `--managed-python --only-installed` を付ける。

commit email は `git config --local` で noreply アドレスを設定する。`--global` は触らない。

### Claude Code に最初に投げる指示

```
SPEC.md を読んで、§2 の「最初にやること」から着手して。
一覧ページを cp932 の生バイトでデコードして、山口県の4行の対応が
SPEC.md §2 の表と一致するか確認して。
そこまで終わったら止まって、確認結果を報告して。
```

**最初から parse.py を書かせない。**一覧ページの照合で一度止める。

---

## 2. 入力

一覧ページ: `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`
**このページは Shift-JIS（cp932）である。**UTF-8 で読むと全て文字化けする。

**WebFetch や HTTP クライアントの要約機能を一次情報として使わないこと。**
過去にこのページの要約が、存在しない「貨物自動車運送事業者登録状況の表」を返し、
山口県の行として鳥取県のファイル名を提示した実績がある。
**生バイトを取得して自前でデコードし、目視できる形に落としてから照合する。** `fetch.py` もこの方針で書く。

山口県分の 4 ファイル（`https://wwwtb.mlit.go.jp/chugoku/content/` 配下）:

| 運送種別 | 実施主体 | ファイル |
|---|---|---|
| 交通空白地有償運送 | 市町村営 | `000359215.pdf` |
| 交通空白地有償運送 | NPO等 | `000268896.pdf` |
| 福祉有償運送 | 市町村営 | `000230003.pdf` |
| 福祉有償運送 | NPO等 | `000271730.pdf` |

**この対応表は一覧ページと照合済みで一致している。**（rev.1 では「推定・未確認」だった）
照合時に判明した一覧ページの癖:

- 行順は 広島 → 鳥取 → 島根 → 山口。**岡山県の行は無い**
- **実施主体（自治体／NPO）はヘッダ行の列ラベルには書かれていない。**
  各セルのリンクテキスト内の「（自治体）」「（ＮＰＯ）」にのみある。ここから判定する
- 島根県の列1は `<a>` が2つに割れている。リンク抽出時は重複除去する

`000271730.pdf`（福祉有償運送・NPO等）の実測値:

- 4ページ / 136,476 バイト / MD5 `c49543729e3122adbd935fa79260b3b7`
- テキスト埋め込みあり（`page.images` は空。スキャンではない）
- 1ページ1団体で計4件。**下関市だけではない**（下関市3件、周防大島町1件）
- 登録番号は 第1号・第3号・第4号・第6号。**2号と5号は欠番**

`000271730.pdf` は処理・完了条件1〜8の検証まで完了済み。

`000230003.pdf`（福祉有償運送・市町村営）の確認済み事項:

- 3ページ / 19,003 バイト / MD5 `0f50b2ed90744aca1be2829299b1b4a1`
- テキスト埋め込みあり（全ページ `page.images` は空。スキャンではない）
- 1ページ1団体で計3件。登録番号は第1号・第3号・第4号。**第2号は欠番**
- 原本取得と先頭ページの構造確認は完了済み。パーサへの追加と出力検証は未着手

未取得・列構成未確認は `000359215.pdf` と `000268896.pdf` の2ファイル。
残りはCodexが指定する1本・1工程ずつ扱い、取得時に `verification.md` へ記録する。

### 利用規約（確認済み・2026-08-07）

国土交通省ウェブサイトのコンテンツは、特記されていない限り著作権は国土交通省に帰属し、
権利表記がない限り**「公共データ利用規約（第1.0版）」PDL1.0 に準拠した利用条件**で利用できる。
条件は次の2つ。

1. 出典を記載する
2. 編集・加工して利用する場合は、出典とは別に編集・加工した旨を記載する。
   加工した情報を、あたかも国が作成したかのような態様で公表・利用しない

数値データ・簡単な表・グラフ等は著作権の保護対象ではないため本ルールの適用外、とも書かれている。
出典: `https://www.mlit.go.jp/link.html`

**未確認**: 上記は `www.mlit.go.jp` の規約であり、登録簿PDFが載っている
`wwwtb.mlit.go.jp/chugoku`（中国運輸局）に同じ規約が適用されるかは確認していない。
どちらの読みでも足りるよう、**出典明記と加工明示は必ず行う**（§8）。
**この件でサイト内を探し回らないこと。**目的から離れたら3手で打ち切る。

---

## 3. 出力

`data/` 配下に 2 テーブルを CSV と JSON で出す。1 団体 = 1 行の主テーブルと、車両の明細テーブルに分ける。

### 3.1 `operators.csv`

| 列名 | 内容 | 例 |
|---|---|---|
| `registration_no` | 登録番号（半角に正規化） | 中山福第1号 |
| `registration_no_raw` | 登録番号（原文のまま） | 中山福第１号 |
| `authority_code` | 登録番号の付与主体部分。国権限では運輸局・運輸支局コード。旧市町村運営番号の `市` は含めない | 中山 |
| `service_type_code` | 種別1文字。`福` または `交` | 福 |
| `serial_no` | 一連番号 | 1 |
| `pref` | 県 | 山口県 |
| `transport_type` | 運送の種別 | 福祉有償運送 |
| `operator_type` | 実施主体。§2のファイル対応に従い `市町村営` または `NPO等` | NPO等 |
| `org_name` | 名称 | 特定非営利活動法人 豆たん |
| `org_address` | 住所 | 下関市古屋町2丁目4-8 |
| `org_address_raw` | 住所（原文のまま） | 下関市古屋町２丁目４－８ |
| `service_area` | 運送の区域 | 下関市全域（離島を除く） |
| `service_area_raw` | 同（原文のまま） | |
| `service_area_municipalities` | 区域から抽出した市町名（複数可、`;` 区切り） | 下関市 |
| `registered_date` | 登録年月日（西暦 ISO） | |
| `renewed_date` | 更新登録年月日（西暦 ISO）。無ければ空 | 2024-04-24 |
| `valid_from` | 有効期間の開始（西暦 ISO） | 2024-04-24 |
| `valid_to` | 同 終了（西暦 ISO） | 2027-04-23 |
| `scope_i_physical` … `scope_to_other` | 旅客範囲 イ〜ト の該当フラグ（7列、0/1） | |
| `office_name` | 事務所の名称（ページ上部の記載） | 豆たん介護センター |
| `office_location` | 事務所の位置（同上） | 下関市内日下1027-3 |
| `partner_operator_name` | 事業者協力型の協力事業者名（無ければ空） | |
| `partner_operator_address` | 同 住所 | |
| `vehicles_total` | 合計欄の括弧**外**の値（総台数） | 3 |
| `vehicles_total_kei` | 合計欄の括弧**内**の値（うち軽・内数） | 2 |
| `source_pdf` | 出典ファイル名 | 000271730.pdf |
| `source_page` | 出典ページ番号 | 1 |
| `flags` | 検証で立った異常フラグ（`;` 区切り。無ければ空）。値は `office_mismatch` / `office_notation_diff`（§3.6） | |

旅客範囲の記号対応（施行規則第49条第2号）:
イ 身体障害者 / ロ 精神障害者 / ハ 知的障害者 / ニ 要介護認定 / ホ 要支援認定 / ヘ 基本チェックリスト / ト その他

**交通空白地有償運送の旅客は「地域住民又は観光旅客その他の来訪者」であり、イ〜トの欄は使われない。**
交通空白地のファイルではフラグ7列はすべて 0 になるのが正常。

### 3.2 `vehicles.csv`

| 列名 | 内容 |
|---|---|
| `registration_no` | operators への外部キー（正規化済みの値） |
| `office_seq` | 団体内の事務所連番。1始まり |
| `office_name` | 車両欄側に書かれた事務所の名称 |
| `office_location` | 同 位置 |
| `ownership` | `所有` / `持込` / **空**（区分の列が無いファイル） |
| `vehicle_type` | 寝台車 / 車いす車 / 兼用車 / 回転シート車 / セダン等 / バス / 普通自動車 / 合計 |
| `vehicle_type_label` | 原本の列ヘッダ文字列そのまま |
| `count` | 括弧**外**の数字。その車種の総台数（軽を含む） |
| `count_kei` | 括弧**内**の数字。うち軽自動車（内数）。列に(軽)が無ければ空 |
| `source_pdf` | 出典ファイル名 |
| `source_page` | 出典ページ番号 |

`vehicle_type` が `合計` の行も**削除せず残す**。§5-2 の検算で再計算値と突き合わせるため。

**`ownership` が空になるのは正常な状態。**
`000230003.pdf`（福祉・市町村営）には所有区分の列そのものが無い。
`unknown` のような値で埋めず、空欄にする。ファイル種別から復元できる。

### 3.3 出さないもの

**`代表者の氏名` は抽出も出力もしない。**
登録簿には記載されているが、供給量の分析に不要であり、機械可読化して再配布する必要がない。パース段階で捨てる。

### 3.4 制度上の定義（データから推測してはいけない事項）

**ここに書いてあることをデータから推定し直さない。**すべて通達に根拠がある。
ここに無い事項で迷ったら、データを睨むのではなく通達を当たる。

根拠:
- 福祉有償運送の登録に関する処理方針について（令和2年11月27日 国自旅第317号、最終改正 令和6年9月30日 国自旅第197号）
  `https://www.mlit.go.jp/jidosha/content/001766049.pdf`
- 交通空白地有償運送の登録に関する処理方針について（令和2年11月27日 国自旅第316号、最終改正 令和6年9月30日 国自旅第199号）
  `https://www.mlit.go.jp/jidosha/content/001766048.pdf`

#### (a) 括弧付きの数字は軽自動車の**内数**

両通達 2.(2)⑦ に同一文言がある。

> 自動車の台数を記載（軽自動車がある場合には、その数を内数として括弧書きで記載。
> また、自動運行旅客運送（特定自動運行旅客運送を除く。）又は特定自動運行旅客運送の用に供する
> 自動車がある場合には、その数を内数として該当欄に記載）するものとする。

- 括弧外 = その車種の総台数（軽を含む）
- 括弧内 = そのうち軽自動車の台数
- 空の括弧 `( )` / 括弧なし / 素の `0` = 軽 0 台

したがって `count_kei ≤ count` は**様式上必ず成立する**。破れたら原本の異常ではなく**抽出誤り**。

**括弧書きになるのは軽自動車だけではない。**自動運行・特定自動運行の車両数も内数で記載される。
山口県4ファイルに出現するかは未確認。**「括弧＝必ず軽」と決め打つコードを書かない。**
括弧の意味は列ヘッダから判定する。

#### (b) 登録番号は構造を持つ

事務・権限が国にある場合（山口県分はこれ）:
`〔運輸局の頭1文字〕〔運輸支局の頭1文字〕〔種別1文字〕第〔一連番号〕号`

事務・権限が地方公共団体にある場合:
`〔地方公共団体の名称〕〔種別1文字〕第〔一連番号〕号`

種別は **福＝福祉有償運送 / 交＝交通空白地有償運送**。
例外として神戸運輸監理部兵庫陸運部の管轄は頭2文字「神兵」、沖縄総合事務局は「沖」1文字のみ。
出典: 両通達 別記1。

両通達の附則は、廃止前の「市町村運営有償運送の登録に係る処理方針」に基づいて付与された
登録番号を、現行方針の登録番号とみなすと定めている。このため、現行の付与規則だけでは分解できない
**旧体系の登録番号も有効な番号として残る**。

`000230003.pdf` では `中山市福第１号`、`中山福第３号`、`中山市福第４号` を実測した。
両通達の附則と旧市町村運営番号の公表例に基づき、本仕様では `中山市福` の `市` を
旧市町村運営番号の標識として扱い、地方公共団体への権限移譲を示す `authority_code` には含めない。
この場合は `authority_code=中山`、`service_type_code=福` とする。

実装は次を守る:
- `registration_no_raw` は原文をそのまま保持する
- `registration_no` は全角数字・空白だけを正規化し、旧体系の `市` を削除しない
- 国権限の旧市町村運営番号は
  `〔運輸局の頭1文字〕〔運輸支局の頭1文字〕市〔種別1文字〕第〔一連番号〕号` として分解する
- 汎用の `(.+?)([福交])` だけで分解し、`authority_code` を `中山市` とする実装は禁止する

この規則に従えば `中山福第１号` は 中国運輸局／山口運輸支局／福祉有償運送 の第1号と読める。
**原本PDF内に注記があるわけではなく、様式定義からの導出である。**

実装上の効果: **ファイル種別と登録番号の種別コードの一致を検証に使える**（§5-3）。
また**欠番があるので、一連番号から件数を推測してはいけない。**

#### (c) 車種区分は運送の種別ごとに違う

福祉有償運送（317号 2.(2)⑦）: 寝台車 / 車いす車 / 兼用車 / 回転シート車 / セダン等 の5区分
交通空白地有償運送（316号 2.(2)⑦）: バス（乗車定員11人以上）/ 普通自動車（同11人未満）の2区分

`000271730.pdf` で実測した車両欄は**7列**だった:
寝台車(軽) / 車いす車(軽) / 兼用車(軽) / 回転シート車(軽) / セダン等(軽) / バス / 合計(軽)。
**バス列にのみ (軽) が無い**（バスは定員11人以上のため軽自動車が存在しない）。
よって括弧付きの数字は1行あたり最大6個。

**これは福祉・NPO等の1ファイルのみの観測。交通空白地2ファイルの列構成は未確認。**
上のとおり交通空白地の区分は「バス／普通自動車」であり、福祉の5区分が並ぶ保証はない。
**列ヘッダを読んで動的にマップすること。7列を前提にしたコードを書かない。**

#### (d) 有効期間

更新登録で付与される期間（両通達 5.(3)）:

| 条件 | 期間 |
|---|---|
| 改善命令・事故・業務停止命令のいずれも無い | 3年 |
| かつ事業者協力型である | 5年 |
| いずれかに該当する | 2年 |

**新規登録時の期間は未確認。**期間長の検証は警告に留め、外れても止めない（§5-4）。

#### (e) 公表は制度上恒常的に行われる

運輸支局長等は登録簿を簿冊に調製して備え置き、公衆の縦覧に供するとともに、
インターネットの利用その他の方法により**公表しなければならない**（両通達 2.(4)③ ほか）。
新規・更新・変更・軽微変更のいずれについても同旨。単発の掲載ではないため、
データセットを継続更新する前提を置いてよい。

### 3.5 実物1件の対応例（これが基準）

`000271730.pdf` の1団体目。**パーサはまずこの1件を再現できることを目標にする。**
rev.1 で「推定」としていた2箇所は**両方とも確定した**（(a) は §3.4(a) の通達、(b) は座標抽出で確認済み）。

原本に書かれている内容:

```
登録番号            中山福第１号
名称                特定非営利活動法人 豆たん
代表者の氏名        （記載あり。抽出しない）
住所                下関市古屋町２丁目４－８
路線又は運送の区域  下関市全域（離島を除く）
運送の種別          福祉有償運送
更新登録年月日      令和6年4月24日 ～ 令和9年4月23日
運送する旅客の範囲  イ ニ ホ ヘ
事務所の名称・位置  豆たん介護センター ／ 下関市内日下1027-3
```

同じブロックの車両欄を `extract_text()` にかけると、こう出てくる:

```
2 1 3
( ) (1) ( ) ( ) (1) (2)
( ) ( ) ( ) ( ) ( ) ( )
```

**これがこの作業の核心。**車両欄は7列の表だが、**空欄セルはテキスト抽出で消える**ため、
残った `2 1 3` がどの列の数字か判別できない。だから座標ベースの抽出が必須になる。
素朴なテキスト抽出では絶対に正しく読めない。

座標で切ると、こう対応する:

| 列 | 所有・括弧外 | 所有・括弧内(軽) |
|---|---|---|
| 寝台車 | — | ( ) |
| 車いす車 | 2 | (1) |
| 兼用車 | — | ( ) |
| 回転シート車 | — | ( ) |
| セダン等 | 1 | (1) |
| バス | — | （列に(軽)なし） |
| 合計 | 3 | (2) |

3行目がすべて空括弧なのは「持込」の行で、持込は 0 台。

期待される出力（`operators.csv` の1行、抜粋）:

| 列 | 値 |
|---|---|
| registration_no | 中山福第1号 |
| authority_code / service_type_code / serial_no | 中山 / 福 / 1 |
| org_name | 特定非営利活動法人 豆たん |
| service_area / service_area_raw | 下関市全域(離島を除く) / 下関市全域（離島を除く） |
| service_area_municipalities | 下関市 |
| transport_type / operator_type | 福祉有償運送 / NPO等 |
| valid_from / valid_to | 2024-04-24 / 2027-04-23 |
| scope_i_physical / scope_ni_care / scope_ho_support / scope_he_checklist | 1 |
| scope_ro_mental / scope_ha_intellectual / scope_to_other | 0 |
| office_name / office_location | 豆たん介護センター / 下関市内日下１０２７－３ |
| vehicles_total / vehicles_total_kei | 3 / 2 |

期待される出力（`vehicles.csv`）:

| registration_no | office_seq | ownership | vehicle_type | count | count_kei |
|---|---|---|---|---|---|
| 中山福第1号 | 1 | 所有 | 車いす車 | 2 | 1 |
| 中山福第1号 | 1 | 所有 | セダン等 | 1 | 1 |
| 中山福第1号 | 1 | 所有 | 合計 | 3 | 2 |

**合計 3 台（うち軽 2 台）。5 台ではない。**持込は 0 台なので行を出さない。

`vehicles.office_location` は車両欄側の原文で「下関市内日下1027-3」（半角）。
`operators.office_location` はページ上部の原文で「下関市内日下１０２７－３」（全角）。
同じ事務所だが印字が違う。どちらも正規化せずそのまま出す（§4.3）。この差だけではフラグを立てない（§3.6）。

### 3.6 原本側のデータ異常は直さない

事務所の名称・位置はページ上部と車両欄の2箇所に印字され、食い違うことがある。
どちらかを選んで消さない。上部を `operators.office_name` / `office_location` に、
車両欄を `vehicles.office_name` / `office_location` に入れ、両方残す。

**比較の前処理:** 比較のときだけ両辺に適用する。出力する値は正規化しない（§4.3）。

1. 全角→半角
2. 空白（全角・半角）をすべて除去

これで解消する差はフラグを立てない。
実例: 中山福第1号の「下関市内日下１０２７－３」（上部）と「下関市内日下1027-3」（車両欄）は同一とみなす。
中山福第3号の「特定非営利活動法人 らいと」（上部）と「特定非営利活動法人らいと」（車両欄）も同一。

**フラグは2種類:** 前処理をしても一致しない場合、どの欄が食い違うかで分ける。

| 条件 | フラグ | 意味 |
|---|---|---|
| `office_name` が不一致 | `office_mismatch` | 別の組織が記載されている疑い。位置も違うことが多い |
| `office_name` は一致し `office_location` のみ不一致 | `office_notation_diff` | 同一事務所の住所表記の差とみられる |

**`000271730.pdf` の実例:**

| 登録番号 | ページ上部（名称／位置） | 車両欄（名称／位置） | フラグ |
|---|---|---|---|
| 中山福第６号 | 大島白壽會／山口県大島郡周防大島町大字西方1623番地の３ | 桑陽病院腎友会／防府市車塚町3番２０号 | `office_mismatch` |
| 中山福第４号 | まんてんの星／下関市菊川町大字田部５３６番地１ | まんてんの星／下関市菊川町大字田部５３６－１ | `office_notation_diff` |

**重要な注記:** `office_notation_diff` は「同じ場所である」と主張するものではない。名称が一致し位置の文字列だけが
違うという観測を記録するだけ。「番地」の有無やハイフン表記の差を吸収する住所正規化は行わない。
本当に別の場所である可能性を、正規化で消してしまわないため。

**出さないもの:** 両方が一致する場合はフラグを立てない。`operators.flags` が空であることが正常な状態。
いずれのフラグも `verification.md` に全件列挙する。

---

## 4. 実装方針

- **`pdfplumber` を使う。** `pypdf` / `PyPDF2` はレイアウトを壊すので不可。
- **pandas を使わない。**標準ライブラリの `csv` と `json` で書く。出力は数千行規模で pandas の利点がなく、
  依存込みで数十MBを占める。依存は `pdfplumber` だけに保つ。
- 対象 PDF はスキャンではなくテキストが埋め込まれている。OCR は不要。
- 様式は道路運送法施行規則の**第2号様式で固定**。1 団体 1 ブロックの繰り返し構造。
- 団体の区切りは**登録番号の出現**で判定する。ページ数で数えない
  （観測上は1ページ1団体だが、複数事務所を持つ団体が改ページするかは未確認）。
- 素朴な `extract_text()` では表のセルが混ざる。`extract_words()` で座標を取り、
  ラベル語（「登録番号」「名 称」「運送の区域」など）の座標を基準に、
  その右側・下側にある語を拾う方式で組む。

### 4.1 列境界はページごとに導出する

**テンプレートを1つ作って全団体に当てる方式は使えない。**（rev.1 の誤り）
`000271730.pdf` の車両欄の列境界は p1=294.0 / p2=285.0 / p3=286.0 / p4=286.3 と
ページごとに異なっていた。

手順:

1. `page.edges` から `orientation == "v"` の縦罫線を取る
2. x 座標の差が 2pt 未満のものは同一の罫線として1本に畳む
3. 畳んだ結果を列境界として、そのページの表を切る
4. 列ヘッダのテキストを読んで `vehicle_type` にマップする（§3.4(c)）

**ページごとにこの導出を行い、前ページの境界を使い回さない。**
団体ごとにアドホックな分岐を書き始めたら、それは列境界の導出を怠っているサイン。

### 4.2 括弧の扱い

括弧は**全角と半角が混在**する。p4 では全角括弧が縦に分離して出現し、半角括弧と混ざった。
「持込」行に `( )` ではなく素の `0` が入るページもある。

したがって文字列マッチではなく、**セル内の数字トークンを座標で拾い、括弧文字の有無で
内数か総数かを判定**する。空括弧・括弧なし・素の `0` はいずれも 0 として扱う（§3.4(a)）。

### 4.3 正規化

原本は全角・半角が混在している。登録番号だけでも
第１号（全角）／第3号（半角）／第４号／第６号 とばらついていた。
事務所所在地も上部が全角「１０２７－３」、車両欄が半角「1027-3」だった。

| 対象 | 方針 |
|---|---|
| `registration_no` | 半角に正規化。原文は `registration_no_raw` に残す |
| 数値列すべて | 半角に正規化して int |
| 日付列すべて | 和暦を西暦に変換し ISO 8601 |
| `org_address` / `service_area` | 半角に正規化。原文は `_raw` 列に残す |
| `org_name` / `office_name` / `office_location` / `vehicle_type_label` | **原文保持。正規化しない** |

和暦変換は令和・平成・昭和に対応させる（令和 = 2018 + 年、平成 = 1988 + 年、昭和 = 1925 + 年）。

### 4.4 生テキストを残す

`raw/text/{pdfname}_p{n}.txt` に、ページ単位の抽出テキストをそのまま保存する。
CSV の任意のセルについて、どのページの何を読んだのかを後から追えるようにするため。
`verification.md` の突合表はここを参照して書く。

---

## 5. 完了条件（これを満たすまで I-1 は未完了）

対象は `000271730.pdf` の1本。証拠として提示できるものが揃って初めて完了とする。
「動いた」「たぶん合っている」は完了ではない。

1. **団体数の一致** — 人間が目視で数えた団体数（4件）と `operators.csv` の行数が一致する。
2. **車両合計の照合（自動テスト）** — 各団体について、`vehicles.csv` の `合計` 以外の行の
   `count` 合計が `operators.vehicles_total` と一致する。`count_kei` についても同様に
   `vehicles_total_kei` と一致する。**不一致が1件でもあれば未完了。**
   この検算は PDF 側が答えを持っているので、必ず `tests/test_verify.py` に書く。
3. **`count_kei` ≤ `count` が全行で成立**（§3.4(a) より様式上の不変条件）。
   破れたら抽出誤り。**自動テストに入れる。**
4. **登録番号の整合** — `service_type_code` がファイル種別と一致する
   （福祉ファイルなら全件 `福`）。`authority_code` が全件同一。
5. **全件突合** — 全4団体について `registration_no` / `org_name` / `service_area` / `valid_to` /
   車種別台数 を原本と照合し、一致表を `verification.md` に出力する。
   欠番（2号・5号）が存在することも記録する。
6. **日付の健全性** — `valid_from < valid_to` が全件で成立。年が2000年代前半や2100年代になっていない。
   有効期間の長さが2年/3年/5年のいずれか。**外れても止めず、警告として記録する**（§3.4(d)）。
7. **事務所情報の不一致の列挙** — operators 側と vehicles 側の事務所名称・位置が食い違う団体を、
   フラグの種類別に全件列挙する（§3.6）。`000271730.pdf` では中山福第６号が `office_mismatch`、
   中山福第４号が `office_notation_diff` になり、他の2件はフラグなしが正解。消さずに残っていることを確認する。
   全角半角と空白の差だけでフラグが立っていたら誤検出。前処理を見直す
   （中山福第1号の位置、中山福第3号の名称がその対象）。
8. **代表者氏名が出力に含まれていない**ことを grep で確認する。**自動テストにも入れる。**

上記1〜8の実行結果（数字と出力）を `verification.md` に残す。

---

## 6. 完了後にすぐ効く派生物

I-1 が通ったら、次の2つは追加コストがほぼゼロで作れる。中間報告で見せる素材になる。

- **有効期間切れが近い団体の一覧** — `valid_to` が6か月以内の団体。供給が消えるリスクの可視化。
- **市町別の集計** — `service_area_municipalities` で寄せて、団体数と
  車いす車・寝台車の台数を市町別に出す。**軽は内数なので二重計上しないこと**（`count` だけを足す）。

これが出た時点で「山口県の福祉輸送の供給が、市町別に台数で見える」と言える。
5分の中間報告はこれ一枚で成立する。

---

## 7. 残り3ファイルへの展開

`000271730.pdf`（福祉有償運送・NPO等）が完了条件を全て通ってから着手する。

各ファイルの確認済みまたは既知の差分:

| ファイル | 確認済みまたは想定される差分 |
|---|---|
| `000230003.pdf`（福祉・市町村営） | **確認済み:** 3ページ、登録番号は1・3・4号。1・4号は旧体系の `中山市福`。所有区分の列が無いため `ownership` は空になる |
| `000359215.pdf`（交通空白地・市町村営） | **車種区分が違う可能性**（バス／普通自動車）。§3.4(c) |
| `000268896.pdf`（交通空白地・NPO等） | 同上。旅客範囲7列はすべて 0 |

**いずれも最初の1件を目視で確認してからパーサを回すこと。**
列ヘッダが想定と違ったら、パーサを直す前に `PROGRESS.md` に報告して止まる。

---

## 8. 出典表記とライセンス

`README.md` と `docs/index.html` の両方に、次を必ず記載する（§2 の利用規約より）。

- 出典: 「自家用有償旅客運送者登録簿」（国土交通省中国運輸局）
  `https://wwwtb.mlit.go.jp/chugoku/00001_00903.html`
- 加工した旨: 上記を加工して作成
- 取得日
- 国土交通省が作成したものではない旨が読み手に伝わる書き方にする

派生データセット自体のライセンスは CC BY 4.0 とし、原データが PDL1.0 準拠である旨を併記する。

---

## 9. 未確認事項

**ここに残っているものを、確認せずに埋めない。**

| # | 内容 | 影響 |
|---|---|---|
| U1 | `000359215` / `000268896` のページ数・MD5・列構成 | §3.4(c), §7 |
| U2 | 複数事務所を持つ団体が改ページするか | 団体の区切り方。§4 |
| U3 | 新規登録時の有効期間 | §5-6 を警告に留めている理由 |
| U4 | 自動運行・特定自動運行の内数が山口県4ファイルに出現するか | 括弧の意味。§3.4(a) |
| U5 | 交通空白地の「普通自動車」が登録簿のどの列に入るか | §3.4(c) |
| U6 | `wwwtb.mlit.go.jp/chugoku` に `www.mlit.go.jp` と同じ利用規約が適用されるか | §2, §8 |

---

## 10. 改訂履歴

### 10.1 rev.1 → rev.2

| # | rev.1 | rev.2 | 根拠 |
|---|---|---|---|
| 1 | §4「テンプレートを1つ作れば全団体に効く」 | **誤り。削除。** 列境界はページごとに縦罫線から導出（§4.1） | p1=294.0/p2=285.0/p3=286.0/p4=286.3 と実測で不一致 |
| 2 | `vehicles.ownership` を必須（所有/持込） | 空欄可。`000230003.pdf` には所有区分の列が無い | 実物確認 |
| 3 | 括弧＝軽の内数は「読み取りからの推定。目視照合で確認せよ」 | **確定**（§3.4(a)）。`count_kei ≤ count` を不変条件として自動テスト化 | 国自旅第317号/第316号 2.(2)⑦ |
| 4 | `vehicles` は `is_kei` 0/1 で行を分ける | `count`（総数）と `count_kei`（うち軽）の2列に変更 | 原本の値をそのまま持てる。分解して持つと不変条件の検査が書けない |
| 5 | `office_name`/`office_location` を1組だけ持つ | 上部と車両欄の両方を保持し、不一致をフラグ化（§3.6） | p4 に別組織・別市町が入る原本異常 |
| 6 | 全角/半角の正規化対象が未定義 | 列ごとの方針を表で明示（§4.3） | 登録番号・所在地で混在を実測 |
| 7 | 登録番号を不透明な文字列として扱う | 構造を分解して持ち、種別コードを検証に使う（§3.4(b), §5-4） | 両通達 別記1 |
| 8 | 車種を福祉の5区分＋バスで固定 | 種別ごとに違うことを明記。列ヘッダから動的にマップ（§3.4(c)） | 国自旅第316号 2.(2)⑦ |
| 9 | 4ファイルの種別対応は「推定・未確認」 | **照合済みで一致**（§2）。一覧ページの癖も記載 | 一覧ページの生デコードと突合 |
| 10 | 「最初にやること: 利用規約を確認」 | **確認済み**（§2）。PDL1.0 準拠。中国運輸局への適用のみ U6 に残す | mlit.go.jp/link.html |
| 11 | `000271730.pdf` は「下関市の…記録」 | **誤り。**下関市3件＋周防大島町1件。登録番号は2号・5号が欠番 | 実物確認 |
| 12 | 旅客範囲7列の扱いが福祉前提 | 交通空白地では全件0が正常と明記（§3.1） | 国自旅第316号 2.(2)⑧ |
| 13 | `.gitignore` の規定なし | `.claude/*.lock` を必須化（§1.5） | ロックファイルの混入 |
| 14 | 生テキストの保存規定なし | `raw/text/` を必須化（§4.4） | 突合の追跡可能性 |
| 15 | 実行環境の制約の記載なし | `python` を直接呼ばない旨を明記（§1.5） | Store エイリアスでハング |
| 16 | 出典表記・ライセンスの規定なし | §8 を新設 | 利用規約の条件 |
| 17 | `§3.4 実物1件の対応例（これが基準）` | **§3.5 に繰り下げ。**制度上の定義を対応例の前に置いたため。rev.1 は §3.4 の次に §3.3 が来る節番号の乱れがあり、これも直した | 節の依存順 |
| 18 | 完了条件は5項目 | **8項目に増やした**（§5）。追加したのは `count_kei ≤ count` の不変条件、登録番号の種別コード整合、事務所名不一致の全件列挙。これに伴い構成図の `├─ verification.md        検証結果（§5 の 1〜5 の実行結果）` を「§5 の実行結果」に改めた | 証拠32/33、§3.6 |
| 19 | 構成図の `├─ raw/                   取得した PDF。出典保存のため git に含める` | `raw/*.pdf`（PDF・git 管理）と `raw/text/`（ページ単位の抽出テキスト）に分割 | §4.4 |
| 20 | 初回指示が `まず中国運輸局の利用規約を確認して控えを取り、` `次に一覧ページを cp932 でデコードして、山口県の行と列の対応が` `SPEC.md の表と一致するか確認して。` | **規約確認は完了したので指示から外し**、一覧ページの cp932 照合から始める内容に差し替えた（§1.5） | §2 の確認結果 |

---

### 10.2 rev.2 → rev.3

000271730.pdf の実装（コミット 1df4042）で判明したことを反映した改訂。実装は rev.2 のまま完了条件8項目を通過しており、rev.3 は出力を変える改訂ではなく、rev.2 の誤りの訂正と、保留にしていた判断の規定である。

| # | rev.2 | rev.3 | 根拠 |
|---|---|---|---|
| 1 | ヘッダは rev.2 で、rev.1 からの改訂先を §9 と記載 | ヘッダを rev.3 とし、rev.1→rev.2 は §10.1、rev.2→rev.3 は §10.2 と明記 | 改訂履歴の参照先を現行の節番号に合わせるため |
| 2 | §3.6 は名称不一致の `office_mismatch` だけを規定 | 名称・位置の両方を上部と車両欄に残し、比較時だけ全角→半角・空白除去を行う。名称不一致は `office_mismatch`、名称一致で位置のみ不一致は `office_notation_diff` とする | 000271730.pdf で、実質同じ表記差と、名称一致・位置文字列不一致の両方を確認したため |
| 3 | `operators.flags` の値域が未記載 | `office_mismatch` / `office_notation_diff` を値として明記 | §3.6 の2種類のフラグと列定義を一致させるため |
| 4 | §3.5 の旧文言「`service_area / service_area_municipalities`」は正規化値と原文を区別せず、上部の `office_location` も半角で記載 | `service_area / service_area_raw` と `service_area_municipalities` の2行に分け、上部の `office_location` を原文どおり全角で記載 | §4.3 の正規化・原文保持方針と実装結果に合わせるため |
| 5 | §3.5 に上部と車両欄の所在地表記差の説明なし | 上部は全角、車両欄は半角の原文を保持し、この差だけではフラグを立てないと追記 | 中山福第1号で印字差を確認したため |
| 6 | 完了条件7は事務所名の不一致だけを列挙 | 事務所名称・位置の不一致をフラグ別に全件列挙し、000271730.pdf の期待結果4件を規定 | 誤検出を防ぎつつ2種類の不一致を検証するため |
| 7 | §10 の見出しは「10. rev.1 からの改訂点」で、rev.1 / rev.2 表に小見出しなし | 見出しを「10. 改訂履歴」に変更し、表の直前に「10.1 rev.1 → rev.2」を追加 | rev.3 の改訂履歴を §10.2 として併記するため |

---

### 10.3 rev.3 → rev.4

`run_record.md` §0.5 の実行契約と `000230003.pdf` の実物確認を反映した改訂。
パーサ実装前に、作品①における I-1 の役割と旧体系登録番号の分解規則を確定する。

| # | rev.3 | rev.4 | 根拠 |
|---|---|---|---|
| 1 | §0 の目的を「市町別の福祉輸送の供給量」に限定 | 作品①全体は GTFS-JP、交通空白、福祉輸送を突き合わせ、I-1 は福祉有償運送と交通空白地有償運送を担うと明記 | `run_record.md` §0.5 の作品①コンセプト |
| 2 | §1.5 で `run_record.md` をリポジトリ外とし、SPECを唯一の仕様とする文例を記載 | 実在するリポジトリ内配置に合わせ、ゴール・終了条件・確定方針は `run_record.md` §0.5、SPECは I-1 実装仕様と分離 | 実行契約と実ファイル配置 |
| 3 | 登録番号は現行通達の付与構造だけを規定 | 現行通達附則が有効とみなす旧市町村運営番号を追加し、`中山市福` でも `authority_code=中山`、`service_type_code=福` と規定 | 両通達附則、`000230003.pdf` の1ページ・3ページ |
| 4 | `000230003.pdf` のページ数・MD5・列構成を U1 に残し、§7 は想定だけを記載 | 3ページ、登録番号1・3・4号、所有区分列なしを確認済みとして §7 に反映し、U1 から除外 | `evidence/20260809_000230003_inspection.txt`、同 page1 PNG |

項目2で削除した旧コードブロック行（網羅チェック用の現物記録）:

```
SPEC.md が唯一の仕様。SPEC.md に書かれていない仕様を勝手に足さない。
├─ run_record.md                    ← 会話で更新される全体の記録。ここに置く（リポジトリの外）
```

---

### 10.4 rev.4 → rev.4.1

rev.4 で §7 と U1 を更新した際に残った §2 の古い着手指示を訂正し、実装前の入力状態を一意にした。

| # | rev.4 | rev.4.1 | 根拠 |
|---|---|---|---|
| 1 | §2 は `000271730.pdf` を「最初の1本」とし、他3ファイルを未取得と記載 | `000271730.pdf` は処理済み、`000230003.pdf` は取得・構造確認済みでパーサ未着手、未取得は残り2ファイルと記載 | Git履歴、`evidence/20260809_000230003_inspection.txt` |
| 2 | `operator_type` の例は `NPO等` だけで値域が不明 | §2の対応表に従い `市町村営` / `NPO等` の2値と明記 | 4入力ファイルの実施主体区分 |

---

## 11. この仕様書を改訂するときの手順

**改訂は必ず旧版の現物を手元に置いて行う。**無い場合は改訂に着手せず、旧版を要求する。
再構成で代替しない。rev.2 の初稿は rev.1 を持たずに書いたため、`operators.csv` の改名、
`service_area_municipalities`、旅客範囲の7フラグ列、§1.5、§5、§6、§7 を落とした。

改訂後は `tools/spec_coverage.py` を走らせ、**出力を添えて出す。**

```
python tools/spec_coverage.py <旧版> <新版>
```

旧版の見出し・バッククォート識別子・表の1列目・コードブロック行を総当たりで新版に照合し、
落ちたトークンが §10 の改訂点表に書かれているかまで見る。
**未説明の欠落が1件でもあれば不合格**（終了コード 1）。合格出力が無い改訂版は配布しない。
